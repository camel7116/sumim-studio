# SUMIM Studio 자사 홈페이지 — 인수인계 문서

> **클로드에게**: 이 프로젝트를 이어받을 때 이 문서를 먼저 끝까지 읽어주세요.
> 여기에는 코드만으로는 알 수 없는 **확정된 결정, 반려된 시도, 남은 작업**이 정리되어 있습니다.
> 특히 "반려 목록"은 이미 시도했다가 되돌린 것들이라 **다시 제안하지 마세요.**

---

## 0. 이 폴더에 뭐가 들었나

| 항목 | 설명 |
|---|---|
| `sumim-studio/` | **실제 소스 코드** (Next.js 16 + TypeScript + Tailwind v4) |
| `_archive/particle-shell-2026-08-21/` | 🚨 **파티클 셸 아카이브 (2026-08-21 정리 패스)** — 파티클·DNA·건물·물감 번짐 부품, 셸 분기 스냅샷, 셸 전용 CSS, 메인에서 빠져 있던 섹션 6종, `/onepage` 라우트, §4-B 원문이 전부 여기 있다. `src` 밖이라 빌드에 안 들어간다. **되살리는 절차는 그 폴더의 `RESTORE.md`** |
| `SUMIM_DESIGN_BIBLE_PHASE1.md` | 디자인 원칙 문서. 코드 주석의 `문서 §7.5` 같은 표기가 이 문서를 가리킴 |
| `스밈스튜디오 자사 홈페이지 시안 01.html` | 현재 완성 상태를 열어볼 수 있는 단일 파일(수정 불가, 확인용) |
| `미래사옥.jpeg` | 히어로 배경 원본 (AI 생성 이미지). 셸 모드에선 안 쓰임 |
| `스밈스튜디오 원페이지 시안.html` | 2026-08-19 원페이지 단일 파일(17MB). **전역 캔버스 시절 산출물이라 현재 소스와 다름** — 참고용 |
| `스밈스튜디오 시안 2026-08-24 v7.html` | 🆕 **최신 팀 공유용 단일 파일 — 동작본 (12.26MB, 2026-08-24)**. **히어로 최종형** — 한 줄 `SUMIM`(다섯 글자 전부 흰색) · 글자 안 **보라 파도 3겹**(`letterWave:"fill"`, SVG 회색조 + CTA 그라데이션 `background-blend-mode:color` 라 팔레트 추종) · 진입 `split`(I 벽 슬라이드 → 좌우 지그재그 덮개) · 선언문 **한 줄**("매출이 나는 홈페이지를 만듭니다.")을 SUMIM 폭에 맞춰 크게(끝 Δ ≤ 1px) + 메타 + CTA 2개. 추출 `scratchpad/export-2026-08-24-v7.mjs`. **file:// 실측 — 콘솔 에러 0 · 외부 요청 0 · 가로 스크롤 0 · 문서 높이 9,083px**. 2026-08-24 skrxk0117@gmail.com 으로 첨부 발송(MIME 16.78MB) |
| `스밈스튜디오 시안 2026-08-23 v6.html` | **(이전 판) 팀 공유용 단일 파일 — 동작본 (11.53MB, 2026-08-23 새벽)**. v5 대비 히어로 진입을 **그라데이션 와이프**(`enter: "wipe"` — 배경색 덮개 1장이 왼→오로 1.6s ease-in-out, 앞단 페더 0.63em=잉크폭 20%, smoothstep 마스크로 마하 밴드 제거, `wipeKeepAccent:false`)로 교체 — 사용자 "퍼져 나가는 모양이 너무 어색". `spread`·`wall` 은 스위치 보존. 추출 `scratchpad/export-2026-08-23-v6.mjs`. **file:// 실측 — 콘솔 에러 0 · 외부 요청 0 · 가로 스크롤 0 · 문서 높이 11,009px · !/i 중심 Δ 0.7px**. 메일 발송 여부는 사용자 결정 대기(v4 만 발송됨) |
| `스밈스튜디오 시안 2026-08-23 v5.html` | **(이전 판) 팀 공유용 단일 파일 — 동작본 (11.58MB, 2026-08-23 새벽)**. v4 대비 **히어로 3칸 박스 구조** — 두 줄 같은 높이·같은 폭: 좌측 박스 `SUM`=`STUD`(자간 −0.064em + scaleX 0.835 + weight 860), 우측 박스 `M`=`O`(weight 560 + scaleX 1.186), !·i 기둥 잉크 중앙 정렬. 추출 `scratchpad/export-2026-08-23-v5.mjs`(v4 배너만 갱신). **file:// 실측 — 콘솔 에러 0 · 외부 요청 0 · 가로 스크롤 0 · 문서 높이 11,009px · !/i 중심 Δ 0.7px · 덮개 좌 936/우 469px 두 줄 동일**. ⚠️ 추출 시 **3101 에 구 서버가 남아 있으면 CSS 가 인라인되지 않는다**(EADDRINUSE 로 새 서버가 못 뜨고 구 서버가 옛 해시를 서빙) — `netstat -ano | grep 3101` 로 PID 를 죽이고 HTML 에 새 클래스가 있는지 확인한 뒤 추출할 것(TaskStop 은 자식 node 를 못 죽인다). 메일 발송 여부는 사용자 결정 대기 |
| `스밈스튜디오 시안 2026-08-22 v4.html` | **(이전 판) 팀 공유용 단일 파일 — 동작본 (11.58MB, 2026-08-22 밤)**. v3 대비 ① **히어로 2줄 워드마크 `SUM!M` / `STUDiO`**(!·i 세로 정렬, 왼쪽 끝 맞춤, `+` 마커 삭제, 선언문·메타 제외) ② **지그재그 퍼짐 진입**(!·i 경계에서 배경색 덮개가 걷힘 + 느린 씨앗 2곳) ③ 모바일 1~2차 전부(브릿지 폰 2대 V·서비스 좌우 2단·리뷰 상단·예산 셀렉트·하단 전화+카톡 바). 추출 절차·스크립트는 v3 과 동일하고 배너만 갱신(`scratchpad/export-2026-08-22-v4.mjs`), 빌드 `NEXT_DIST_DIR=.next-prod npx next build` → `next start -p 3101`. **file:// 실측 — 콘솔 에러 0 · 외부 요청 0 · 가로 스크롤 0 · 문서 높이 11,009px · !/i 중심 Δ 0.8px · 덮개 4개 pointer-events none**. 2026-08-22 22:2x skrxk0117@gmail.com 으로 첨부 발송 |
| `스밈스튜디오 시안 2026-08-21 v3.html` | **(이전 판) 팀 공유용 단일 파일 — 동작본 (11.53MB, 2026-08-21 밤)**. v2 대비 ① 후기 섹션 **검증 스탯 2개를 헤딩 옆 가로 배치**(상단 블록 590→244px · 섹션 1,116→770px) ② 브릿지 폰 **∧ → V**(앞 `-4.5deg` / 뒤 `+4.5deg`, 그룹 폭 430→405px 로 가림 **7.1%**) ③ **색상 조합 재큐레이션** — 색 8종→**5종**(테라레드 × 크림 · 차콜 × 베리 신규) · 톤 6종→**4종** ④ **온설 포트폴리오 최신화**(임베드·캡처 3종을 2026-08-16 시안으로). 추출 절차·스크립트는 v2 와 동일하고 배너만 갱신(`scratchpad/export-2026-08-21-v3.mjs`), 서버는 `NEXT_DIST_DIR=.next-prod npx next build` → `next start -p 3101`. **file:// 실측 — 콘솔 에러 0 · 외부 요청 0 · 문서 높이 11,164px · 전폭 마키 0~1536px(카드 10) · 브릿지 폰 앞 `sinZ −0.0785` / 뒤 `+0.0785` · 가림 15.1px(7.1%) · 서비스 축 아이콘 4 · 후기 스탯 2개 같은 행(top 동일) 헤딩 오른쪽 · colorLab 색 5 × 톤 4, 차콜 × 베리 선택 시 `--color-cta` `#ba1650` 로 실제 변경** |
| `스밈스튜디오 시안 2026-08-21 v2.html` | **(이전 판) 팀 공유용 단일 파일 — 동작본 (12.97MB, 2026-08-21 오후)**. v1 대비 ① 약속+서비스 병합(좌 약속 / 우 서비스, 좌우 끝선 정렬) ② 포지션+스탯+후기 병합(라임 한 단계 진하게 `#cbd696`) ③ **카드 박스 라운드 전부 제거**(안쪽 pill·체크 아이콘은 유지) ④ Process 밴드 검정 + 좌측 스티키 패널에 [상담 예약하기] 고정 ⑤ 하단 상담 바 오른쪽 버튼 = **카카오톡 채널**(옐로 고정, 새 탭) ⑥ FAQ 아이콘 45° 회전 수정. 추출 절차·스크립트는 아래 v1 과 동일하고 배너만 갱신(`scratchpad/export-2026-08-21-v2.mjs`), 서버는 `NEXT_DIST_DIR=.next-prod npx next build` → `next start -p 3101`. **file:// 실측 — 콘솔 에러 0 · 외부 요청 0 · 문서 높이 11,205px · 카드 radius 0px · 카톡 버튼 href `https://pf.kakao.com/_nkKSX/friend`(`_blank`) · 병합 섹션 좌우 끝선 차이 0px · colorLab 동작** |
| `스밈스튜디오 시안 2026-08-21 (SUMIM).html` | **(이전 판) 팀 공유용 단일 파일 — 동작본 (12.97MB, 2026-08-21)**. §5-B 의 **SUM!M 워드마크 히어로 + 팀 팔레트(검정·라임·바이올렛)** 까지 전부 들어간 모습이다. 히어로 글자별 진입(0.5s 실측 `S:98% U:93% M:77% !:37% M:−20%` → 1.5s 전부 100%)·브릿지 폰 마우스 3D 틸트·후기 마키·카운트업·목업 호버·FAQ·네비 밴드 반전이 실제로 동작한다. 🚨 **좌하단 [색상 조합] 패널이 이 파일 안에서도 동작한다** — 팀이 색 8종 × 톤 6종을 직접 눌러 보라고 `colorLab` 을 켠 채 추출했다(실측: 테라코타 샌드 선택 시 void `rgb(43,26,18)` · mist `rgb(243,233,221)` · cta `#c0572d` 로 실제 변경, 초기화 시 복귀). 선택은 그 파일의 localStorage 에만 남고 실제 사이트와 무관하다. 브릿지 폰 화면은 **가상 브랜드 데모 시안**이다(실제 포트폴리오 아님). 추출 절차는 (밴드).html 과 동일하고 스크립트만 `scratchpad/export-2026-08-21.mjs`(= `export-banded.mjs` 사본, 배너만 갱신). 빌드·서버는 `NEXT_DIST_DIR=.next-prod npx next build` → `next start -p **3101**`(dev :3000 · `.next` 불가침). **file:// 실측 — 콘솔 에러 0 · 외부 요청 0**(`performance` 리소스 중 data:/file: 아닌 것 없음) |
| `스밈스튜디오 시안 2026-08-20 (밴드).html` | **현재 디자인(리디자인 banded) 팀 공유용 단일 파일 — 동작본 (12.7MB, 2026-08-20)**. §5-B 1·2차가 전부 들어간 모습이다. 회전 단어·후기 마키·카운트업·목업 호버·FAQ 아코디언·네비 밴드 반전이 실제로 동작한다(파티클·DNA·건물은 이 디자인에서 의도적으로 꺼져 있다). 추출은 아래 (동작).html 과 같은 접근 B2, 스크립트만 `scratchpad/export-banded.mjs`(= export-live.mjs 사본). **추가 우회 1곳** — CSS `<link>` 를 `<style>` 로 인라인해도 **RSC 페이로드에 같은 경로가 HL 힌트 1 + React float 지시자 2로 남아** 하이드레이션 때 React 가 그 href 로 `<link>` 를 head 에 다시 꽂는다(file:// 404, 콘솔 에러 1건). 남은 경로 문자열을 **빈 `data:text/css,`** 로 바꿔 링크가 조용히 성공하게 했다(같은 CSS 를 data URI 로 또 넣으면 base64 팽창으로 +5MB). 실측 콘솔 에러 0 · 외부 요청 0 |
| `스밈스튜디오 시안 2026-08-19 (동작).html` | **팀 공유용 메인 페이지 단일 파일 — 동작본 (12.7MB, 2026-08-19)**. 파티클·Q&A 라인 모프·DNA·마키·카운트업·목업 호버가 **실제로 동작**한다. Next 프로덕션 번들을 통째로 인라인(접근 B2). 재추출: 아래 정적본과 같은 빌드·서버 후 `node scratchpad/export-live.mjs "<출력.html>" "<sumim-studio/.next-prod/static/chunks>"`. Turbopack 우회 두 곳 — ① 청크 등록 `push(["object"==typeof document?document.currentScript:void 0, …` 의 첫 인자를 `"static/chunks/<파일>.js"` 문자열로 교체(registerChunk 는 문자열이면 그대로 청크 경로로 쓴다) ② Next 의 `getAssetPrefix()` 가 `new URL(currentScript.src)` 를 하는데 인라인은 src 가 비어 TypeError → 빈 문자열 반환으로 치환. 지연 청크 5개까지 전부 선등록해야 파티클 컴포넌트가 산다 |
| `스밈스튜디오 시안 2026-08-19.html` | **팀 공유용 메인 페이지 단일 파일 (6.7MB, 2026-08-19)**. 더블클릭으로 열리는 자급자족 정적 스냅샷 — 캔버스 7개는 그린 그림을 그대로 구웠고 스크립트는 전부 뺐다(파티클 움직임·마키·카운트업·네비 밴드 전환 없음, CSS 호버는 동작). 1920×1080 기준으로 추출. 재추출: `cd sumim-studio && NEXT_DIST_DIR=.next-prod npx next build && NEXT_DIST_DIR=.next-prod npx next start -p 3100` 후 `node scratchpad/export-site.mjs "<출력.html>"`. **Next 번들을 통째로 인라인하는 방식은 불가** — Turbopack `registerChunk` 가 `document.currentScript.src` 로 청크를 식별해 file:// 에서 죽는다(실측 예외 15개, 캔버스 7개 미초기화) |

---

## 1. 프로젝트 정체

부산의 **SUMIM Studio(스밈 스튜디오)** 자사 홈페이지.
사용자는 **홈페이지 제작 + 블로그 마케팅 대행사**를 운영하며, 이 사이트가 자사 쇼케이스이자 영업 도구입니다.

- **타깃 고객**: 지역 사업자(사장님들). 40대 이상도 많음 → 쉬운 언어, 큰 글씨, 모바일 우선
- **최우선 메시지**: **"고객의 매출 성장"**. 예쁜 홈페이지가 아니라 매출로 이어지는 구조를 판다
- **핵심 차별점**: 브랜드 전략 + 홈페이지 + 네이버 플레이스 + 블로그를 하나로 연결

---

## 2. 실행 방법

```bash
cd sumim-studio
npm install
npm run dev          # http://localhost:3000
```

- **Node 20 이상** 필요 (개발 환경은 v22.14.0)
- 빌드: `npm run build` / 검사: `npx tsc --noEmit`, `npx eslint src`
- **dev 서버를 켠 채 빌드하려면** `NEXT_DIST_DIR=.next-prod npm run build` 로 산출물을 분리하세요. 기본 `.next`에 prod 빌드를 섞으면 dev 서버가 깨진 페이지를 서빙합니다.

### ⚠️ 검증할 때 반드시 주의
- 스크롤 애니메이션(리빌, Process 단계 강조)은 **브라우저 탭이 화면에 보일 때만** 동작합니다. 탭이 숨겨진 상태로 자동 검사하면 "고장난 것처럼" 보이니, 헤드리스 브라우저 스크린샷으로 검증하세요.
- 문의 폼 제출 테스트는 **실제 브라우저**로 하세요. 자동화 브라우저는 봇 차단에 걸립니다.
- 인앱 브라우저 패널이 좁으면(≈400px) 파티클이 꺼진 상태로 보입니다 — 1024px 이상으로 넓히거나 헤드리스 Edge(CDP 9222)로 캡처하세요. 헤드리스 탭을 40개 넘게 열면 스크롤이 먹통이 되니 주기적으로 재시작.

---

## 3. 사이트 구조

**메인 = 원페이지** (2026-08-21 사용자 확정 "자사 홈피는 임팩트 있게 원페이지로"). 섹션 배열은 `app/page.tsx` **한 곳**에 있습니다 — 2026-08-21 정리 패스에서 모드 분기(`visualMode`, `ShellSections`)를 없앴습니다.

| # | 섹션 | 밴드 | 컴포넌트 |
|---|---|---|---|
| 1 | Hero (**초대형 한 줄 워드마크 `SUMIM` — 3칸 박스 `[SUM][I][M]`**, 2026-08-24 · 글자는 **전부 흰 잉크**(`I` 포함) · 글자 **안쪽에 CTA 보라 파도 3겹** · 바로 아래 **워드마크 폭에 맞춘 큰 선언문 h1** + 메타 + CTA 2개) | 검정 | `hero.tsx` |
| 2 | Selected Work (**화면 전폭 2×2 격자** — 🆕 2026-08-26 밤 사용자 "한 줄에 2개씩" → 데스크톱도 `grid-cols-2`(`lg:grid-cols-4` 제거, 1901 뷰포트에서 칸 950px) · 같은 날 저녁 사용자 "목업은 이전으로 되돌리자" → **`QUAD_FRAME` 이 `"device"` 로 복귀**(2026-08-25 구성: 모니터 크롬 + 우측 하단 폰 PNG + 칸 안 아래 상시 설명글) · **4칸 전부 실제 프로젝트**(문결·온설·지안·**경희정원한의원**, "준비 중" 자리표시는 4건이 되며 자동으로 밀려남) · 클릭 → 모달. `"none"` 으로 올리면 2026-08-26 오후의 프레임 제거·호버 정보 안(이미지 4:3 꽉 채움 + 호버 오버레이)이 돌아오고, `WORK_LAYOUT` 을 `"cards3"` 로 하면 3열 기울기 카드 복귀) | 흰 | `selected-work.tsx` · `project-quad-card.tsx` |
| — | (협력 로고 — `partners` 가 null 이라 렌더 안 됨) | 라임 | `partner-strip.tsx` |
| 3 | 브릿지 "말로 설명하지 않습니다" + 폰 2대 | 검정 | `statement-band.tsx` |
| 4 | **약속(OUR PROMISE) + Services 4종** (2026-08-21 병합 · 상단 좌 헤딩 / 우 약속 텍스트, 하단 서비스 4열 카드) | 흰 | `service-system.tsx` |
| 5 | **포지션 선언 + 검증 스탯 + 후기 마키** (2026-08-21 병합) | 라임(한 단계 진하게) | `testimonial-marquee.tsx` |
| 6 | Q&A(문제) 카톡 패널 | 흰 | `client-problems.tsx` |
| 7 | Process | 검정 | `process.tsx` |
| 8 | FAQ | 흰 | `faq-section.tsx` |
| 9 | Final CTA · 푸터 | 검정 | `final-cta.tsx` |

배경은 **검정 `#000000` / 흰색 / 라임 `#EAF6AD` 밴드 교차**입니다(팀 팔레트 — `bandedPalette` 를 `"navy"` 로 바꾸면 딥 네이비·연회청·주황 세트로 되돌아갑니다). 섹션 순서를 바꾸면 **인접 밴드가 같은 색이 되지 않는지** 확인하세요(유일한 예외는 CTA→푸터). 5번(리뷰)은 앞뒤가 흰 밴드라 **라임이 그 사이를 끊는 자리**입니다. 7번 Process 는 2026-08-21 사용자 지시로 브릿지와 같은 **검정**이 됐습니다(앞뒤가 Q&A·FAQ 흰 밴드라 교차 유지). **카드 박스는 전부 직각**입니다(같은 날 "박스들 라운드 전부 제거" — 안쪽 pill·체크 아이콘은 유지, 남은 라운드는 팀 확인 대기).

**히어로 세로 규칙 (2026-08-22 갱신)**: 히어로에는 이제 **2줄 워드마크만** 있습니다 — "+" 마커는 삭제됐고 선언문 3줄·메타는 히어로에서 빠졌습니다(문구는 `site.ts` 에 보존, 배치 추후 결정). 🆕 **2026-08-24 부터 한 줄 `SUMIM`** 입니다(§5-B "한 줄 SUMIM + 글자 파도" 절) — 크기는 `min(30.8vw, max(40px, calc((100svh − var(--hero-reserve)) / 1.060017)))` 이고 **세 뷰포트 전부 폭이 크기를 잡습니다**(잉크 = 보이는 폭의 95.8~96.0%). 🆕 **2026-08-24 저녁**부터 워드마크 아래에 **잉크 폭이 정확히 같은 큰 선언문 h1** 이 붙어(§5-B "히어로 최종 색·선언문" 절) 세로 분모가 `0.82 → 1.060017`(= 0.82 + 문장 배율 0.240017), `--hero-reserve` 가 **230px / 300px** 이 됐습니다. 아래는 두 줄 시절 기록입니다. 워드마크는 노트북 크롬 뷰포트(1536×745) **첫 화면을 꽉 채웁니다**(두 줄 상자 595px / 섹션 가용 601px = **99%**). 크기는 `min(29.9vw, max(40px, calc((100svh − 150px) / 1.6287)))` — **폭·높이 두 기준 다 실측값**이라 임의로 키우지 마세요(§5-B "히어로 2줄 워드마크 + 스프레드 진입" 절과 그 안의 **"3칸 박스 구조(2026-08-23)"** 소절). 🚨 **한 줄 = `[좌측 박스][액센트 기둥][우측 박스]` 3칸**이고 두 줄이 **같은 칸 골격**을 씁니다 — 두 액센트의 잉크 중심 x, 두 줄의 잉크 왼끝·오른끝이 전부 같아야 합니다(실측 |Δ| ≤ 1px). 🆕 **2026-08-23 아래 줄 액센트는 소문자 "i" 가 아니라 위 줄 "!" 를 세로로 뒤집은 것**입니다(§5-B "보강 ⑤" 소절 — 스위치 `lines[1].accentFlipOf`, `null` 이면 "i" 복귀).

**서브 페이지**: `/work`, `/work/[slug]`, `/services`, `/services/[slug]`, `/about`, `/contact`, `/privacy`, `/thanks` — **라우트는 살아 있고 네비에서만 빠졌습니다**(회사가 커지면 되살립니다). `/privacy` 는 폼 개인정보 동의에 필요한 **법적 링크라 메인 폼·푸터에서 계속 링크**합니다.
- ⚠️ **네비에 About 항목이 없습니다** — 메인에 대응 섹션이 없어 앵커를 만들 수 없습니다(팀 소개 섹션이 생기면 `site.ts` `navigation.menu` 에 `{ label: "About", href: "#about" }` 로 넣으면 됩니다).
- ⚠️ **`/work` 카드 클릭은 아무 일도 하지 않습니다** — 카드가 원페이지 모달 트리거인데 그 페이지에는 `ProjectModal` 이 없습니다. 서브 페이지를 되살릴 때 함께 손봐야 합니다(정리 패스 이전부터 같은 상태).
- 🚨 `/onepage` 라우트는 **삭제**했습니다(셸 시절 스냅 시안). 원본은 `_archive/particle-shell-2026-08-21/app/onepage-page.tsx`.

**메인에 없는 섹션** — Proof Strip · Brand Statement · SUMIM Difference · Evidence · Team 은 2026-08-03 사용자 요청으로 빠진 뒤 계속 안 쓰여서 **아카이브로 옮기고 `src` 에서 삭제**했습니다(`_archive/particle-shell-2026-08-21/components/sections/`). Service System 은 6차에 메인으로 돌아왔습니다.

### 모바일 1차 (2026-08-22)

> 그동안 **"모바일 전면 미착수"** 였습니다. PC 가 완성된 상태를 기준으로(육아 코치 원칙 "PC 기반으로 모바일") **390×844 를 기준 뷰포트**로 1차를 넣었습니다. 점검 보조 폭은 430·768 입니다.

#### 🚨 먼저 알아둘 것 — 이미 되어 있던 것
진단 캡처를 먼저 찍어 보니 **깨진 곳이 생각보다 적었습니다**. Tailwind 기본이 모바일 우선이라 대부분의 섹션이 이미 1열로 흘렀고, **가로 스크롤도 처음부터 0** 이었습니다(넘치는 요소는 전부 `overflow:hidden` 인 마키 내부). 그래서 이번 패스는 **레이아웃 재작성이 아니라 "모바일에서만 어긋나던 5곳"** 을 고친 것입니다.

| 섹션 | 결정 |
|---|---|
| **히어로** | 🚨 **2026-08-22 저녁 2줄 워드마크로 전면 교체**(§5-B 해당 절) — 아래 내용은 그 전 기록입니다. 워드마크는 **폭에 묶여 있어**(`min(29.9vw, 62vh)` 중 vw 가 이김) 390px 에서 잉크폭이 **뷰포트의 95.9%** — 지시한 "~94%"를 이미 만족합니다. 다만 글자 높이가 96px 대라 세 블록이 화면 한가운데 작게 뭉쳐 보여, **블록 사이 간격만** `mt-6 → mt-9`(lg 이상은 6 유지)로 벌렸습니다. 첫 화면(844px) 안에 전부 들어옵니다. 🚨 **워드마크를 더 키우는 길은 없습니다** — 두 줄로 접는 안은 브랜드 표기가 달라져 손대지 않았습니다 |
| **Work** | 손대지 않음. 이미 1열이고 목업·폰 돌출이 뷰포트 안입니다. **호버 오버레이·로고는 그대로 뒀습니다** — 터치에서는 `:hover` 가 발동하지 않아 죽은 UI 가 보이지 않고, 탭은 모달로 갑니다. 지우면 데스크톱 동작까지 없애야 해서 이득이 없습니다(판단) |
| **브릿지** | 🚨 **정정(2026-08-22) — 모바일도 폰 2대 V자**입니다. 1차의 "앞 폰 한 대만"은 **오판이었습니다**(사용자 "휴대폰이 아직도 하나야"). 데스크톱 축소판으로 텍스트 아래 가운데, 섹션 높이 386 → **866px**. 자세한 값은 아래 §5-B "브릿지 폰 — 모바일도 2대" 절 |
| **병합(프로미스+서비스)** | 상단(헤딩·약속 리스트)은 손대지 않음. **서비스 카드 안만 좌우 2단**으로 바뀌었습니다(2026-08-22, 아래 §5-B "서비스 카드 좌우 2단" 절) — 카드 355 → **234px** |
| **리뷰** | 상단 블록이 **헤딩 좌 / 스탯 우** 2단이 됐습니다(2026-08-22, 아래 §5-B 절). 마키 카드 `min(84vw,440px)` = **327px** 는 그대로입니다 |
| **Q&A · Process · FAQ · CTA 폼 · 푸터** | 손대지 않음. **Process 는 이미 모바일에서 일반 흐름**입니다(스티키 좌패널의 큰 숫자 블록이 `hidden lg:block` 이라 모바일에서는 헤더 아래 CTA + 세로 타임라인으로 흐릅니다 — 실측 확인). 폼 입력칸은 패널 폭을 꽉 채우고(300px) 높이 53px, **글자 16px 이상이라 iOS 포커스 확대가 없습니다** |
| **colorLab** | 🆕 **모바일 숨김**(`lg` 미만 `display:none`). 패널이 320px 고정 + 최대 70vh 라 390px 화면을 사실상 덮고, 버튼 자체가 **모든 섹션에서 본문 위에 겹쳐** 앉아 있었습니다(진단 캡처). 팀 실험용 데스크톱 도구라 모바일 방문자에게 필요도 없습니다. 렌더를 막지 않고 CSS 로만 숨겨 창을 넓히면 바로 살아납니다 |

#### 🆕 모바일 하단 고정 상담 바 — [빠른 상담] + [카카오톡 상담]
`mobile-sticky-cta.tsx` 를 데스크톱 상담 바와 **같은 채널 2개**로 바꿨습니다(팀원 클로드 권고).
- 이전에는 "프로젝트 문의하기" 한 개가 폼(`#contact`)으로 갔습니다 — 모바일에서는 폼보다 전화·카톡이 먼저 눌리고(사장님 타깃), 무엇보다 **데스크톱만 두 채널이고 모바일만 다른 동선**이라 어긋났습니다.
- 라벨·폴백은 데스크톱과 **같은 `stickyConsultBar`** 를 읽습니다(문구를 컴포넌트에 적지 않음). 카카오 버튼은 `.btn-kakao` 라 팔레트·colorLab 과 무관하게 **브랜드 옐로 고정**입니다.
- 🚨 **티오 문구·뱃지는 넣지 않았습니다** — 390px 에서 버튼 2개와 같이 두면 바 높이가 두 배가 되어 콘텐츠를 가립니다(문서 §12.2). 그 수치는 리뷰 섹션 스탯이 이미 전합니다.
- **등장 임계값을 고정 600px → 뷰포트 1개 높이**로 바꿨습니다. 390×844 에서 600px 은 히어로가 아직 절반 남았을 때 바가 올라와 첫인상을 덮었습니다(데스크톱 상담 바와 같은 규칙이 됐습니다).
- 실측(390×844): 히어로 `보임 false`(top 844) → 중간 `보임 true`(top **771** · 높이 73px) → `#contact` 진입 시 다시 숨김. 버튼 href = `tel:01040061002` / `https://pf.kakao.com/_nkKSX/friend`.

#### 🆕 프로젝트 모달 — 모바일은 세로 스택 + **모바일 캡처 기본**
- 위 이미지 **55vh 고정** / 아래 정보(자체 스크롤). 둘 다 `flex-1`(50:50)이면 정보가 화면 절반을 먹어 캡처가 손바닥만 해집니다. 실측 패널 358×726 → 이미지 **464** / 정보 **262px**.
- 🚨 **`lg` 미만에서 열면 탭이 "모바일"로 시작**합니다 — 390px 폭에 1440px 데스크톱 풀페이지를 넣으면 글자가 3px 대라 아무것도 안 읽힙니다. 데스크톱 뷰는 토글로 볼 수 있습니다. 실측 `선택탭: ["모바일"]`.

#### 네비 모바일 메뉴 — 정리 패스 이후에도 정상
실측: 햄버거(`aria-label="메뉴 열기"`) → 전체 화면 패널 **390×776** · `body overflow: hidden` 잠금 · 항목 **5개**(Work·Services·Process·Contact + 프로젝트 문의) — **About 제거가 반영**돼 있습니다. Process 클릭 → `scrollY 8420`, `#process` 상단이 **80px**(고정 네비 높이)에 정확히 붙고 **메뉴가 자동으로 닫힙니다**.

#### 검증 (헤드리스 `mobile:true` · DSF2)
| 폭 | 문서 높이 | 가로 스크롤 | 콘솔 |
|---|---|---|---|
| **390×844**(기준) | 14,434px → **13,741px**(2026-08-22 공간 효율화 −745 · 브릿지 폰 2대 복원 +52) | **0**(`scrollWidth 390 = clientWidth`) | 0 |
| 430×932 | 14,260px → **13,566px** | **0** | 0 |
| 768×1024 | 12,234px → **11,885px** | **0** | 0 |
| 1536×745(데스크톱 회귀) | **11,009px — 모바일 작업 전과 동일** · colorLab `block` · 브릿지 폰 2대 · 모바일 폰 `display:none` · 상담 바 `flex` | 0 | 0 |

- **터치**: 마키를 손가락으로 끌면 실제로 따라옵니다(`translateX −44.6 → −140.3 → −168.4`, pointer 이벤트라 별도 처리 불필요). ESC/닫기 후 `body overflow` 복구 확인.
- 캡처: `mob-390-full.jpg`(전체 세로) · `mob-390-s00~s08*.png`(섹션 9장) · `mob-430-full.jpg` · `mob-768-full.jpg` · `mob-menu-open.png` · `mob-sticky-bar.png` · `mob-modal.png` · `mob-form.png` · 데스크톱 회귀 `desk-1536-after-mobile.jpg`.
- `tsc`·`eslint` 통과.

#### 🚨 모바일 남은 과제 (2차 이후)
1. ~~**히어로가 세로로 헐겁습니다**~~ → **2026-08-22 2줄 워드마크로 전환**(팀·사용자 확정). 다만 모바일은 여전히 **폭이 크기를 잡아** 844px 화면에서 잉크 세로가 166px(19.7%)뿐이라 위아래가 빕니다 — 모바일 전용 크기 규칙은 아직 없습니다.
2. **문서 세로 14,434px** — 데스크톱(11,009)보다 31% 깁니다. 섹션별 `py` 를 모바일에서 한 단계 줄이는 패스가 필요합니다(이번엔 리듬을 건드리지 않았습니다).
3. **가로 모드(landscape)** 미점검 — `min-h-[100svh]` 히어로가 가로에서 어떻게 접히는지 안 봤습니다.
4. **Work 목업 호버 레이어**가 터치에서 영영 안 보입니다 — 모바일 전용으로 "탭하면 스크롤" 같은 대체 동작을 줄지 판단 필요(지금은 탭 = 모달).
5. **폰트 크기·터치 타깃 전수 점검** 미실시(폼 입력·상담 바만 확인). 특히 FAQ 아코디언 헤더·마키 카드 안 링크.
6. 실기기 확인(iOS Safari 주소창 접힘 · `env(safe-area-inset-bottom)`) — 헤드리스로는 못 봅니다.

### 콘텐츠는 전부 한 곳에서 관리
텍스트를 고칠 땐 컴포넌트가 아니라 아래 파일을 수정하세요.
- `src/content/site.ts` — 히어로 선언문, 문제 섹션, 약속·스탯·후기, 연락처, 회사 정보
- `src/content/process.ts` — 프로세스 8단계
- `src/content/projects.ts` — 포트폴리오
- `src/content/faq.ts`, `src/content/services.ts`

### 남은 스위치 (2026-08-21 정리 패스 이후 이것뿐입니다)
| 스위치 | 위치 | 값 | 무엇을 바꾸나 |
|---|---|---|---|
| `bandedPalette` | `site.ts` | `"team"` | 팀 팔레트(검정·라임·바이올렛) ↔ `"navy"`(딥 네이비·연회청·주황) |
| `colorLab` | `site.ts` | `true` | 좌하단 "색상 조합" 실험 버튼·패널 (배포 후에도 유지 — 사용자 결정) |
| `bridgePhoneScreens` | `site.ts` | 데모 2종 | 브릿지 폰 화면. `null` 이면 실제 포트폴리오(`mobileImage ?? fullImage`)로 복귀 |
| `availability` | `site.ts` | 8월·2건 | 티오 수치 **단일 소스**(병합 섹션 스탯 + 하단 상담 바가 같이 읽음) |
| `reviewBand.enabled` · `bridgeStatement.enabled` · `trustProof.promises[].enabled` | `site.ts` | — | 해당 섹션/카드 노출 |

삭제된 스위치(`visualMode` · `particleShell` · `sectionAccentBlobs` · `serviceChannelIcons` · `finalCtaParticles` · `workDeviceMockup` · `qnaChatPanel`)는 전부 **아카이브 site.ts** 에 남아 있습니다.

---

## 4. 확정된 디자인 (임의로 바꾸지 마세요)

- **컬러(현행 = 팀 팔레트)**: 밴드 검정 `#000000` / 흰 `#ffffff` / 라임 `#EAF6AD`, 액센트 **바이올렛 `#6338EE`**(호버 `#7a52f2`). 토큰은 `globals.css` 의 `html[data-visual="banded"][data-palette="team"]` 블록.
  - 이전 세트(주황 `#EA580C` / 딥 네이비 `#141d33` / 연회청 `#f0f3f8`)는 **지우지 않고** 기본 블록에 남아 있어 `bandedPalette = "navy"` 로 즉시 복귀합니다.
  - 유채색은 **액센트 하나**만 씁니다 — CTA 버튼·강조 구절·수치 라벨·eyebrow 틱·워드마크 "!" 뿐입니다.
- **CTA 호버 = 살짝 밝아지며 떠오름**. 흰색 반전·어두워지는 호버는 둘 다 반려됨
- **네비 메뉴는 영어** (Work·Services·Process·Contact) + 액센트 "프로젝트 문의" 버튼. 전부 **섹션 앵커**입니다
- **Hero**: 초대형 워드마크 `SUM!M` + 우측 하단 선언문 3줄. **사진·CTA 버튼·스크롤 큐 없음**
  - 크기·광학 보정·글자별 진입은 전부 실측값입니다 (§5-B 히어로 절 — 글자를 바꾸면 다시 재야 합니다)
- **섹션 라벨**은 **액센트 틱 + 대문자 라벨**(18×3px 바 + 12.5px 굵은 글자)
- **하단 고정 상담 바**가 전화·문의를 맡습니다 (1024px 이상). 우하단 플로팅 3단은 삭제됨 — ▲TOP 재도입 여부는 팀 확인 대기
- **장식 배경 없음** — 파티클·DNA·건물·물감 번짐·격자는 전부 아카이브(§4-B). 브릿지 폰 목업만 남습니다
- **모션**은 전부 `prefers-reduced-motion` 대응 필수. LCP 영역은 JS 없이 CSS로만

---

## 4-B. 파티클 셸 → **아카이브로 이관됨 (2026-08-21)**

> 사용자 지시 **"파티클 사용했던 코드들은 따로 분리해 저장해놓고, 지금 하고 있는 코드만 정리"** 로
> 파티클 셸(shell 모드)은 **코드·CSS·설계 기록이 전부** `스밈 스튜디오/_archive/particle-shell-2026-08-21/` 로 갔습니다.
> - 복원 절차·배선: 그 폴더의 **`RESTORE.md`**
> - 이 절의 원문(섹션별 형태 표·실측값 전부): 같은 폴더의 **`SHELL_NOTES.md`**
> - 파티클 원조 데모는 원래 자리 그대로: `홈페이지 제작/코드/파티클배경데모/particle-demo.html`
>
> 🚨 **§4-B 에 있던 "반려 목록"은 여전히 유효**하므로 §7 로 승격했습니다(다시 제안 금지).

---

## 5. 연락·폼 (동작 중)

- 이메일 **sumimstudio@naver.com**, 전화 **010-4006-1002** → `site.ts`의 `email`, `phone`
- ✅ **카카오톡 채널 `https://pf.kakao.com/_nkKSX/friend`** (2026-08-21 확정) → `site.ts` 의 `chatbotUrl`. **하단 고정 상담 바의 채움 버튼**과 **Final CTA 카톡 버튼** 둘 다 이 URL 을 새 탭으로 엽니다(§5-B 카카오톡 절)
- **문의 폼 → 실제 메일 발송 연결 완료**
  - Web3Forms 사용. 키는 `.env.local`의 `NEXT_PUBLIC_WEB3FORMS_ACCESS_KEY`
  - ⚠️ 무료 플랜이 서버 호출을 막아서 **클라이언트에서 직접 전송**합니다 (`components/ui/contact-form.tsx`의 `handleSubmit`)
  - 서버 액션 `app/actions/contact.ts`는 미사용 상태로 보존 중 (다른 서비스로 옮길 때 재사용)
  - **배포 시 호스팅 환경변수에 키를 다시 등록**해야 메일이 갑니다

---

## 5-B. 리디자인 결정 (2026-08-19 저녁, 팀 피드백)

- 팀 평가: 현 다크+파티클 셸은 **콘셉 자체·완성도·메시지 전달 모두 부정적** ("어설프게 디자인 적용됐다", "무슨 회사인지 안 읽힌다")
- ~~**확정 방침 (한병선)**: **퍼널 구조(섹션 흐름·순서)는 그대로 유지**하고, **비주얼 디자인은 완전히 새로 들어간다**~~
  → 🚨 **2026-08-20 6차 사용자 지시로 대체됨** — "메이커리 홈페이지 분석해서 **구조를 전체적으로 바꿔보자**". banded 는 이제 섹션 순서 자체가 바뀌었습니다(§5-B 6차). shell 은 예전 퍼널 보존.
- **배경 방침(사용자 확정)**: 화이트 / 밝은 회색 / 검정 **무채색 밴드가 섹션별로 교차** — 현재도 밴드 시스템은 있으나 다크 셸이 전체를 어둡게 눌러 "다 어둡다"고 읽힘. 새 디자인은 밝은 밴드가 실제로 밝아야 함
- 한병선: "우리가 만든 홈페이지 배경에 그때 봤던 디자인이 어설프게 얹힌 느낌" / 김미라: 데모(폼폼이) 홈페이지 디자인 자체를 자사 홈피로 쓰고 싶었음(배경만 흰·회·검으로)
- **진행 방식(사용자)**: 레퍼런스 2~3곳을 골라 **섹션별 분석 → 스밈 퍼널에 매핑 적용**
- 방향 세부는 레퍼런스 비교 후 결정 — 페이블 세션에서 국내 에이전시·해외 에이전시·학원 3갈래 레퍼런스 조사 진행 중 (2026-08-19)
- §4-B의 파티클 셸 관련 확정들은 **이 리디자인이 시작되면 대부분 대체될 예정** — 단 반려 목록(§4-B·§7)과 콘텐츠 규칙(수치 지어내기 금지 등)은 계속 유효

### 1차 구현 완료 (2026-08-20)

퍼널(섹션 순서)은 그대로 두고 비주얼만 갈아 끼웠습니다. **기존 셸 코드는 하나도 지우지 않았습니다.**

#### 스위치 — `site.ts` 의 `visualMode`
```ts
export const visualMode: VisualMode = "banded";   // "shell" 로 바꾸면 §4-B가 그대로 복원
export const particleShell = SHELL_BY_MODE[visualMode];  // shell 에서만 true
```
- `particleShell` 을 import 하던 기존 파일(`particle-backdrop` · `city-skyline` · `dna-helix` · `hero` · `layout` · `page`)은 **한 줄도 고치지 않고** banded 에서 자동으로 꺼집니다.
- `<html data-visual="banded">` 가 붙고, `globals.css` 파일 **끝**의 `html[data-visual="banded"]` 블록이 색을 맡습니다. 위쪽 `html[data-shell="dark"]` 블록은 **손대지 않았습니다** — 토큰 정의가 두 블록에 중복돼 있는 건 의도된 것(셸 회귀 위험 0).
- 셸 회귀 실측(2026-08-20): `"shell"` 로 되돌리면 `data-shell="dark"` · 캔버스 7개 · 블롭 3개 · work-grid · cta-scrim · 원래 밴드 배정 · 원래 히어로 문구가 전부 그대로 복귀 (`shell-revert.png`).

#### banded 규칙
- **배경**: 흰 `#ffffff` / 밝은 회색 `#f1f2f4` / 검정 `#0b0d12` 무채색 밴드 교차. 셸과 달리 **섹션이 자기 배경을 직접 칠합니다**(::before 층계 없음 → transform/filter 금지 제약도 없음).
- **유채색은 주황 `#EA580C` 하나**뿐이고 CTA·강조 구절·수치 라벨·회전 단어에서만 "구두점"으로 씁니다.
- **장식은 전부 꺼집니다**: 파티클 캔버스·DNA 리본·CTA 건물은 `particleShell` 파생으로, 물감 번짐은 `SectionBlobs` 안에서, work 배경 격자는 `selected-work.tsx` 에서, CTA 스크림은 `final-cta.tsx` 에서 각각 렌더 자체를 하지 않습니다. `text-halo` 와 `#work` 후광 CSS는 `html[data-shell="dark"]` 스코프라 자동 무효입니다.
- **네비 밴드 반전**: 훅(`data-nav-band`)은 셸과 같고 방향만 반대 — banded 는 기본 토큰이 밝아서 **검정 밴드 위에서만** 바(`rgba(11,13,18,.85)`)와 글자를 뒤집습니다. 모바일 메뉴 패널(`#mobile-menu`)은 흰 면이라 그 안에서만 밝은 토큰으로 되돌립니다.

#### 밴드 배정 (퍼널 순서대로 — 인접 동일색 없음, 실측 확인)
| 섹션 | banded | shell(기존) |
|---|---|---|
| Hero | **검정** | 검정 |
| Q&A(문제) | 흰 | 흰 |
| TrustProof(WHY SUMIM) | **회색** | 검정 |
| Selected Work | **흰** | 회색 |
| Process | **회색** | 검정 |
| FAQ | 흰 | 흰 |
| Final CTA · 푸터 | 검정 | 검정 |

밴드 값은 컴포넌트에서 `band={visualMode === "banded" ? … : …}` 로 분기합니다 (trust-proof · selected-work · process).

#### 섹션별 변경
- **Hero (검정) — 새 선언 히어로**: eyebrow(주황) → 1줄차 작게·회색 "예쁜 홈페이지는 이제 누구나 만듭니다." → 2줄차 크게·흰색 "**[회전단어]**이/가 나는 홈페이지를 만듭니다." → 기존 CTA 2개. **좌측 정렬**(중앙정렬은 §7 반려. "셸 켜진 동안은 중앙"이라는 2026-08-18 예외는 셸 모드에만 걸린 것). 카피는 `site.ts` 의 **`hero.bandedHero`** 신설 필드이고 기존 `hero.heading`/`description` 은 셸용으로 보존.
  - **회전 단어** `components/ui/rotating-word.tsx`: 매출 → 문의 → 예약 → 단골, **2.5초** 간격. 후보를 같은 그리드 셀에 겹쳐 쌓아 **폭이 가장 긴 단어에 고정**되므로 뒷 문장이 좌우로 흔들리지 않습니다. 페이드+살짝 위로 슬라이드, **들어오는 쪽만 0.18s 지연**(동시에 페이드하면 두 단어가 겹쳐 보입니다 — 1920 캡처에서 실측). `prefers-reduced-motion` 이면 타이머 자체를 걸지 않고 "매출이"에서 정지(실측 확인). 스크린리더에는 첫 단어만 읽힙니다.
  - ⚠️ **조사는 계산합니다** — 받침 있으면 "이", 없으면 "가"(매출**이** / 문의**가** / 예약**이** / 단골**이**). 기획 문구는 "가"로 고정돼 있었지만 그러면 네 단어 중 셋이 비문이 됩니다. 한 글자로 고정하려면 `rotating-word.tsx` 의 `josa()` 가 상수를 반환하게만 바꾸면 됩니다.
- **TrustProof (회색) — 약속 카드 추가**: 기존 2단(헤딩 7 / 스탯 5)과 후기 마키는 그대로 두고 **그 사이에 약속 카드 그리드**를 넣었습니다(메이커리 패턴). 카드 내용은 `site.ts` 의 **`trustProof.promises`**.
- **Final CTA (검정) — 빠른 상담 2버튼**: 왼쪽 컬럼 설명 아래에 "예약 없이 바로" 라벨 + **전화하기**(`tel:` site.phone, 부제 "지금 바로 연결") / **카카오톡 상담**(우하단 플로팅 카톡과 같은 href, 부제 "편한 시간에 답장") 2버튼(수능선배 패턴). 폼 위에는 안내 문구 `finalCta.formNote`. 배경 장식·스크림 없음, 폼 패널은 `#15181e` + `rgba(255,255,255,.1)` 헤어라인, 입력창은 한 단계 밝은 `#1b1e25`.
  - 한글 라벨에 `.eyebrow`(자간 0.14em + uppercase)를 쓰면 글자가 흩어져서 "예약 없이 바로" 는 `text-label font-bold` 로 갔습니다.

#### 🚨 팀 확정이 필요한 것 (지금은 화면에 안 나감)
`site.ts` `trustProof.promises` 에 `enabled: false` + `// TODO` 로 남겨 뒀습니다. 확정되면 **`enabled` 만 true 로** 바꾸면 바로 노출됩니다.
1. **"무제한 수정"** — 수정 횟수 정책 (process.ts 06 의 `note: null` 과 같은 건)
2. **"월 작업량 한정"** — 월 몇 건인지 수치 없음. 숫자를 지어내지 않습니다
3. **폼 응답 시간 약속** ("24시간 내" 등) — `finalCta.formNote` 에 TODO 주석. 지금 문구는 시간 약속 없이 "담당자가 확인 후 직접 연락드립니다"
4. ~~**카카오톡 채널 URL**~~ — **2026-08-21 확정** `https://pf.kakao.com/_nkKSX/friend`. CTA 카톡 버튼과 하단 상담 바가 이 URL 을 새 탭으로 엽니다

#### 1차에서 남긴 것 → **2차에서 처리됨** (아래 절)
- Q&A~FAQ 비주얼 다듬기 · Process 인디고 → 주황 · 섹션 라벨 통일

### 2차 구현 완료 (2026-08-20) — 나머지 섹션 다듬기

1차와 같은 규칙(shell 보존 · 검증된 것만 · 캡처 검증)으로 Q&A·Work·Process·FAQ 와 공통 타이포를 정리했습니다.

#### 공통
- **섹션 라벨 통일 — 주황 틱 eyebrow**: `components/ui/section-header.tsx` 에 **`EyebrowLabel`** 을 새로 두고 `SectionHeader` 와 TrustProof 가 함께 씁니다.
  - banded: 라벨 왼쪽에 **18×3px 주황 바** + uppercase **12.5px** 굵은 글자(자간 .14em). 유채색을 쓰는 자리를 3px 바 하나로 줄인 형태.
  - shell: 2026-08-08 확정 **투명 pill 배지** 그대로.
  - ⚠️ banded 에서는 `eyebrow-pill` 클래스를 **붙이지 않습니다** — globals 의 `#work .eyebrow-pill` 이 배경을 채워 버립니다(그건 shell 의 회색 밴드+격자 대비용).
  - 가운데 정렬 헤더는 `SectionHeader` 에 새로 생긴 **`align="center"`** 로 틱까지 가운데 모읍니다(틱 eyebrow 가 flex 라 부모의 `text-center` 를 안 따릅니다).
- **타이포 리듬**: 섹션 헤딩 크기는 이미 전 섹션 `text-h1`(clamp 32→48)로 같았고 **굵기만** 어긋나 있었습니다(기본 600, `#work h2` 만 700). banded 에서 `html[data-visual="banded"] [data-band] h2 { font-weight:700; letter-spacing:-.02em }` 로 **전부 700** 통일. 색은 밴드 토큰이 이미 맡습니다(검정 밴드=흰 / 밝은 밴드=잉크).
- **히어로 상단 여백**: 1536×745에서 eyebrow 시작이 세로 **36.1% → 34.0%**. 섹션이 `flex items-center` 라 아래 패딩을 주면 내용이 그 **절반**만큼 올라갑니다(`pb-8` = 32px → 16px 상승). CTA 하단 540px · 스크롤 큐 673px 로 첫 화면 규칙 유지.

#### Q&A (흰 밴드)
- 카톡 패널 **구조는 그대로**. 흰 밴드 위에서 `#f1f2f4` 패널은 배경과 6/255 차이밖에 안 나 패널로 안 읽혔습니다 → banded 에서만 면 `#f5f6f8` · 테두리 `#e3e5e9` · `box-shadow: 0 8px 32px rgba(20,23,38,.06)`. 말풍선 그림자·간격은 손대지 않았습니다.
- 카피 추가 없음. **추후 팀 검토 항목**: 허영 지표(방문자 수 등) 대비 카피를 Q&A 에 넣을지 — 지금은 근거가 없어 기록만 합니다.

#### Selected Work (흰 밴드)
- `projects.ts` 에 **`perspective?: string`** 신설 — "작업물이 아니라 생각을 판다"(스러운스튜디오)를 카드 한 줄로. **메타 블록 최상단**(프로젝트명 위)에 주황 13px/500 한 줄, compact·일반 둘 다. **banded 전용**(shell 카드 모습 보존), 값이 없으면 줄 자체를 렌더하지 않습니다.
- 🚨 **아래 세 문장은 초안 — 팀 검토 필요.** 전부 `projects.ts` 에 이미 기록된 summary/overview/problem 에서만 뽑았고 새 사실을 만들지 않았습니다.

  | 프로젝트 | 한 줄 관점(초안) | 근거 |
  |---|---|---|
  | 문결 필라테스 | 움직임의 결이 첫 화면에서 느껴지도록 | 브랜드 메시지 "움직임의 결을 찾아드립니다" |
  | 온설 | 빚는 사람의 이야기가 먼저 읽히도록 | problem "아버지와 딸이 함께 빚는 술이라는 브랜드 이야기가 전달되지 않았다" |
  | 지안영어 | 내신과 정시가 왜 다른지부터 읽히도록 | 핵심 메시지 "내신과 정시는 전혀 다르게" + problem "수업 철학이 한눈에 읽혀야" |

  (페이블 초안은 온설 "고요한 브랜드의 결을 그대로 화면에" / 지안 "고민하지 않게, 바로 상담하도록" 이었습니다. 앞은 기록된 문제와, 뒤는 로고스 레퍼런스의 상담 유도 문장이라 지안의 과제와 어긋나 위처럼 조정했습니다 — 되돌리려면 `projects.ts` 의 `perspective` 한 줄씩.)
- 목업·호버 스크롤·3열은 그대로입니다.

#### Process (회색 밴드)
- 스티키 구조 유지. banded 에서 강조색 **인디고 → 주황**: 현재 단계 큰 숫자·진행 바·타임라인 마커. **비활성 단계는 전 구간 무채색**(`text-ink-muted`) — shell 은 lg 미만에서 인디고였는데 banded 는 그것도 눌렀습니다. shell 은 인디고 그대로(실측 `rgb(70,85,138)`).
- 분기는 `process-flow.tsx` 상단 상수 `ACCENT_TEXT` / `ACCENT_BG` 두 개.
- **TODO(팀 확정)**: 자체 방법론 네이밍. 후보 **"매출 구조 설계 프로세스"** — `process.ts` 상단 주석에 기록만 해 뒀고 화면에는 올리지 않았습니다.

#### FAQ (흰 밴드)
- **레이아웃: 중앙 720px 단일 컬럼으로 결정.** 왼쪽 4/12 컬럼은 원래 3D 압출 "FAQ" 글자 자리였는데, banded 에서 글자를 끄고 나니 헤딩 아래 500px 넘는 빈 공간만 남았습니다. 대안이던 "잉크 8% 큰 FAQ 글자"를 **선택하지 않은 이유**: ① 방금 전 섹션에서 전부 걷어낸 **장식을 다시 들이는 것**이고 ② eyebrow "FAQ" + 헤딩 "자주 묻는 질문" 과 합쳐 같은 말이 세 번 납니다. 컬럼을 없애고 헤더를 문항 위 가운데로 올리니 앞뒤 섹션(Q&A·Work 중앙 / Trust·Process 좌측 2단)과 리듬도 번갈아 맞습니다. shell 은 기존 4/8 2단 유지.
- **아코디언**: 문항 사이 헤어라인 유지 + 열린 항목 질문 앞 **주황 틱**(닫히면 폭 0 이라 글자가 안 흔들림, 섹션 라벨의 3px 바와 같은 언어) + 아이콘은 **한 종류(+)** 로 통일해 열릴 때 45° 회전(×)만 하고 **열린 것만 주황**. 호버 색 인디고 → 주황.
- **`faq.ts` 문항 2개 추가** (8 → 10건. 두 모드 공통 — 콘텐츠라 banded 로 가르지 않았습니다):
  1. **"기획안이나 자료가 없어도 되나요?"** — 근거: `contactPage.guide` 2번째 줄 "자료가 준비되지 않아도 상담을 시작할 수 있습니다"(메인 CTA·/contact 에 이미 나가는 문구). 기존 "준비해야 할 자료는 무엇인가요?" 바로 아래 두어 짝으로 읽히게 했습니다.
  2. **"담당자가 중간에 바뀌지 않나요?"** — 근거: `process.ts` 의 `processSection.description` "스밈의 모든 프로젝트는 대표가 처음부터 끝까지 함께합니다"(이미 화면에 나가는 문구) + `site.ts` `team.members` 가 대표 1인. ⚠️ **팀이 늘면 이 답변을 함께 갱신해야 합니다.**
- **추가하지 않은 문항** — `faq.ts` 상단에 TODO 주석으로만 남겼습니다: **비용 기준 · 표준 제작 기간 · 원본 파일/디자인 소유권 제공 여부**. 근거가 없어 문항 자체를 만들지 않았습니다.

#### 2차 이후 남은 것
- ~~**모바일 전면 미착수**~~ → **2026-08-22 모바일 1차 완료**(§3 "모바일 1차" 절). 남은 과제도 그 절 끝에 있습니다
- 섹션 헤딩→설명 사이 여백이 아직 섹션마다 조금씩 다릅니다(`SectionHeader` mt-6 / TrustProof mt-5 / Q&A mt-5). 크기·굵기는 통일했으나 여백까지 묶으려면 컴포넌트 정리가 한 번 더 필요합니다
- 허영 지표 대비 카피(Q&A) — 팀 검토 항목
- 방법론 네이밍 · FAQ 비용/기간/소유권 문항 — 위 TODO

### 3차 구현 완료 (2026-08-20) — 수능선배 스타일

사용자 피드백: **"수능선배(suneungsunbae.com) 느낌으로 수정 보완하자. 지금 우리 파티클 넣기 전 홈페이지랑 다를 게 없어."**
수능선배 실측 팔레트(딥 네이비 rgb(20,34,61)/rgb(51,61,75) · 연회청 rgb(240,243,248) · 흰 카드+그림자 · 하단 고정 상담 바) 중 **장치 4가지**를 스밈 퍼널에 이식했습니다. 포인트 색은 여전히 **주황 `#EA580C` 하나**(수능선배의 블루는 쓰지 않습니다). **전부 banded 전용 — shell 은 한 줄도 건드리지 않았습니다**(실측 회귀 확인, `shell-revert-3.png`).

#### ① 검정 → 딥 네이비 / 밝은 회색 → 연회청
`globals.css` 의 `html[data-visual="banded"]` 블록에서 **값만** 바꿨습니다.

| 자리 | 2차 | **3차** |
|---|---|---|
| void 밴드 (히어로·브릿지·CTA·푸터) | `#0b0d12` | **`#141d33`** |
| mist 밴드 (Trust·Process) | `#f1f2f4` | **`#f0f3f8`** |
| paper 밴드 | `#ffffff` | `#ffffff` (유지) |
| 네비 dark 바 | `rgba(11,13,18,.85)` | `rgba(20,29,51,.85)` |
| void `--color-surface` / `-subtle` | `#1b1e25` / `#23272f` | `#1d2740` / `#26314c` |
| void `--color-ink-secondary` / `-muted` | `#a2a7b0` / `#7d828c` | `#a8b1c4` / `#808a9e` |
| CTA 폼 패널 | `#15181e` | `#1a2338` |
| 밝은 밴드 잉크·헤어라인 | `#14161a` / `#e3e5e9` … | `#141a26` / `#e2e6ee` … (네이비를 아주 옅게 섞은 쿨 그레이) |
| Q&A 대화창 패널 | `#f5f6f8` / `#e3e5e9` | `#f4f7fb` / `#e2e7f0` |

실측 캡처 색: void `rgb(20,29,51)` · mist `rgb(240,243,248)` · paper `rgb(255,255,255)`. 검정 밴드 위 흰 글자 대비는 그대로이고, 보조 글자(`--color-ink-secondary`)는 네이비 위 대비 ≈6:1 로 잡았습니다.

#### ② 하단 고정 상담 바 (신규 `components/ui/sticky-consult-bar.tsx`)
흰 바 + 상단 그림자에 **[8월 예약] 배지 + "8월 신규 프로젝트 티오가 2건 남았습니다"(2건만 주황) + [빠른 상담](보더, `tel:`) + [프로젝트 문의하기](주황, `#contact`)**.

- 🚨 **수치 단일 소스**: `site.ts` 에 **`availability = { month: 8, remainingSlots: 2 }`** 를 새로 두고 **WHY SUMIM 스탯("8월 남은 티오 2건")과 이 바가 같은 값을 읽습니다.** 바 문구(`stickyConsultBar`)도 전부 여기서 파생 — 컴포넌트에 숫자를 적지 않았습니다. 월/티오가 바뀌면 `availability` 만 고치면 두 곳이 같이 바뀝니다.
- ⚠️ **"얼마 남지 않았어요" 류 긴박·과장 문구는 넣지 않았습니다** (사용자 지시). "2건" 사실만 적습니다.
- 노출: **스크롤 > 뷰포트 1개** → 등장 / **`#contact` 가 화면에 들어오면 숨김** / 폼 입력 중 숨김 / **1024px 미만 미노출**(그 아래는 기존 `MobileStickyCta` 담당) / 슬라이드 **300ms**, `prefers-reduced-motion` 이면 전환 없음 / `/contact`·`/thanks` 는 렌더 안 함.
- 보일 때만 `--consult-bar-h` 로 body 하단 여백을 채워 푸터 마지막 줄이 가리지 않습니다(1024px 이상에서만).
- 좌우 정렬은 `Container`(최대 1280px · lg 패딩 40px)와 같은 값 — 실측 배지 좌변이 섹션 본문과 같은 168px(1536폭).
- ~~**카카오톡 버튼은 넣지 않았습니다**~~ → **2026-08-21 해소**: 채널 URL 이 확정돼 **채움 버튼 자체가 카카오톡 상담으로 바뀌었습니다**(아래 카카오톡 절). 당시 사정은 — `site.chatbotUrl` 이 null 이라 카톡 링크가 `#contact` 로 떨어져 옆의 "프로젝트 문의하기"와 **같은 곳을 가리키는 버튼 두 개**가 됩니다. 폭은 1536에서도 여유가 있으니(버튼 2개 뒤 공백 ≈500px) **채널 URL이 확정되면 "빠른 상담" 왼쪽에 한 칸 추가**하면 됩니다.
- **우하단 플로팅 3단은 banded 에서 렌더하지 않습니다**(`scroll-progress.tsx` 상단 `visualMode !== "banded"` 조건). 전화·문의가 바로 옮겨갔기 때문인데, ⚠️ **▲TOP 버튼도 같이 사라집니다**(상단 진행 바는 그대로). 되살리려면 그 조건 한 줄만 빼면 됩니다 — **사용자 확인 필요**.

#### ③ 히어로 실사 복귀 (banded)
1·2차의 네이비 단색 선언 히어로 → **사옥 사진 풀블리드 + 하단 네이비 그라데이션 + 좌하단 텍스트**(수능선배 구성).

- 구성(위→아래): 작은 위치 한 줄(**기존 `hero.eyebrow` 재사용**, 주황) → 작게·흰 70% lead → **큰 선언 헤딩 + 회전 단어**(기존 `bandedHero` 카피·회전 그대로) → CTA 2개(사진 위라 보더 버튼은 §4 확정대로 `ghost`).
- 섹션이 `items-end` 로 바뀌고 Container 는 `pt-14 pb-20 lg:pb-24`. **1536×745 실측**: eyebrow top 386.8 · h1 430~561 · **CTA 하단 649**(첫 화면 안) · AI 고지 709~729. 1920×1080 도 동일 구성(CTA 하단 984).
- 그라데이션은 `bottom-0 h-[72%]`, `#141d33 0% → .95@38% → .82@62% → .45@85% → 0@100%`. **1차 시도(h-62%, 얕은 스톱)는 eyebrow 가 벽돌 위에 얹혀 주황이 묻혔습니다** — 문구 블록 최상단 알파가 ≈0.74 가 되도록 다시 잡은 값입니다. 위쪽에는 고정 네비 흰 글자용 옅은 스크림(상단 26%, 0.55→0)만 둡니다.
- **우하단 AI 생성 고지 문구 유지**(§4 필수). 네이비 위라 색만 `#7C8288 → #8b93a5`(기존 검정 스크림 히어로는 그대로).
- **스크롤 큐는 이 히어로에서만 생략** — 좌하단 문구 블록과 가운데 큐가 같은 높이에서 겹칩니다.
- 스위치: `hero.buildingImage.enabled = false` → **1·2차의 네이비 단색 선언 히어로로 그대로 폴백**(코드 분기 보존).

#### ④ 확신 화법 브릿지 밴드 (신규 `components/sections/statement-band.tsx`)
Work(흰)와 Process(연회청) **사이**에 텍스트만 있는 얕은 네이비 밴드(`py-24/28/32`, 실측 높이 386px). 큰 헤딩 2줄뿐이고 장식·버튼·수치 없음.

- 문구는 `site.ts` 의 **`bridgeStatement`** — 🚨 **초안(팀 검토 대기)**: `"말로 설명하지 않습니다.\n만든 것으로 보여드립니다."` `enabled: false` 로 하면 밴드가 통째로 빠지고 Work(흰)→Process(연회청)로 이어져 **교차 규칙은 그대로 유지**됩니다.
- 자리 선정 이유: 결과물을 보여준 직후(Work) 태도를 못 박고 과정 설명(Process)으로 넘어가는 순서이고, 흰↔회청 사이에 네이비가 들어가 **밴드 리듬이 오히려 또렷해집니다**(아래 표).

#### ⑤ 카드 언어 통일 — `.card-surface`
후기 마키 카드·약속 카드가 같은 클래스를 씁니다(banded 스코프라 shell 카드는 기존 그대로).

- **흰 면 `#ffffff` · 라운드 16px · `0 6px 24px rgba(20,29,51,.06)` · 1px 헤어라인**. 실측 두 카드 모두 `border-radius 16px` / `rgba(20,29,51,0.06) 0 6px 24px`.
- 후기 카드 하단 **업종 라벨은 주황 테두리 배지**(`.trade-badge` — 999px pill, 테두리 `rgba(234,88,12,.4)`, 글자 `#ea580c`, 11.5px). **내용은 "업종 공개 예정" placeholder 그대로**이고 후기 문구도 손대지 않았습니다. 같은 배지 스타일을 상담 바의 "8월 예약"이 재사용합니다.

#### 밴드 시퀀스 (3차 실측, 인접 동일색 없음)
| # | 섹션 | 밴드 | 실측 색 |
|---|---|---|---|
| 1 | Hero (사옥 사진 + 네이비 그라데이션) | void | `rgb(20,29,51)` |
| 2 | Q&A(문제) | paper | `rgb(255,255,255)` |
| 3 | TrustProof(WHY SUMIM) | mist | `rgb(240,243,248)` |
| 4 | Selected Work | paper | `rgb(255,255,255)` |
| 5 | **브릿지 밴드(신규)** | **void** | `rgb(20,29,51)` |
| 6 | Process | mist | `rgb(240,243,248)` |
| 7 | FAQ | paper | `rgb(255,255,255)` |
| 8 | Final CTA | void | `rgb(20,29,51)` |
| 9 | 푸터 | void | `rgb(20,29,51)` |

(8·9는 원래부터 이어지는 한 덩어리라 예외 — CTA 아래에 푸터가 같은 색으로 붙는 구성은 1차부터 유지입니다.)

#### 검증
`npx tsc --noEmit` · `npx eslint src` 통과. 1536×745·1920×1080 **콘솔 에러 0**.
캡처: `banded3-hero/bridge/trust/cta/sticky-mid/full-*.png`, 셸 회귀 `shell-revert-3.png`.
스티키 바 3상태 실측(1536): 히어로 `visibility:hidden`(top 745) / 중간 `visible`(top 672) / CTA `hidden`. 셸 회귀: `data-shell="dark"` · 캔버스 7 · 블롭 3 · work-grid · 플로팅 3단 복귀 · 상담 바·브릿지 밴드 없음 · 원래 히어로 문구/밴드 배정 복원.

#### 3차 이후 남은 것 (사용자·팀 확인 대기)
1. **브릿지 밴드 문구** — 초안 상태. 확정 문구를 `site.ts` `bridgeStatement.text` 에 넣으면 됩니다
2. **▲TOP 버튼** — banded 에서 플로팅 3단을 끄며 같이 사라졌습니다. 살릴지 확인 필요
3. ~~**카카오톡 버튼을 상담 바에 넣을지**~~ — **2026-08-21 완료**(채움 버튼을 카톡으로 교체)
4. **모바일** — 상담 바는 1024px 이상 전용. 모바일 전면은 여전히 미착수

### 4차 구현 완료 (2026-08-20) — 브릿지 밴드 폰 목업

사용자: **"'말로 설명하지 않습니다'(브릿지 밴드) 오른쪽에 고정 백그라운드로, 무료 아이폰·갤럭시 PNG 구해서 두 대가 입체적으로 있는 그림 적용해보자."**
3차의 브릿지 밴드를 **좌 텍스트 / 우 폰 두 대** 2단으로 확장했습니다. 왼쪽이 "말", 오른쪽 폰 화면이 **실제 만든 것** — 문구의 증거가 같은 화면에 섭니다. banded 전용이고 shell 은 그대로입니다(`shell-revert-4.png`).

#### 폰 프레임 = **자작 CSS/SVG** (외부 PNG 안 씀) 🚨 판단 근거
- **외부 에셋을 내려받지 않았습니다.** 파일 다운로드는 사용자 본인의 승인이 필요한 행위라 에이전트가 임의로 할 수 없고(세션 규칙), 무료 목업 PNG 는 "상업적 사용 가능"이라도 출처마다 조건이 갈립니다. **애플·삼성 공식 렌더는 저작권 대상**이라 애초에 후보가 아닙니다.
- 그래서 지시서의 **폴백(b) — 자작 프레임**으로 갔습니다. **라이선스 리스크 0**이고 사이트 카드 언어(라운드·헤어라인·그림자)와 그대로 맞물립니다. 기존 `device-mockup.tsx` 폰 프레임을 확장한 형태입니다.
- 두 대를 구분하는 방식: `PhoneFrame` 의 **`variant`** 한 개.
  - `"island"`(아이폰형) — 상단 가운데 **다이내믹 아일랜드** 알약 + 프레임 `#232c44` / 보더 흰 15%
  - `"hole"`(갤럭시형) — 상단 가운데 **펀치홀** 원 + 프레임 `#33405e` / 보더 흰 20% (한 톤 밝음)
  - 공통: 라운드 30px 프레임 / 24px 화면, 유리 대각 하이라이트 한 겹, `0 28px 60px -18px rgba(0,0,0,.65)` 드롭섀도
- **정품 PNG 로 바꾸고 싶다면**: Pixabay License 나 CC0 로 **명시된** 투명 배경 프레임을 사용자가 직접 받아 `public/images/devices/` 에 넣고 `PhoneFrame` 의 프레임 div 를 `<Image>` 한 장으로 갈아 끼우면 됩니다(화면 이미지는 그 아래 절대배치). 지금 구조가 그 교체를 전제로 나뉘어 있습니다.

#### 화면 콘텐츠 = 실제 포트폴리오
`projects.ts` 의 **`fullImage`** 앞 두 건을 그대로 씁니다(앞=문결 `moongyul-full.jpg` / 뒤=온설 `onseol-full.jpg`, `object-cover object-top`). 새 이미지를 만들지 않았고 **포트폴리오가 바뀌면 이 폰 화면도 같이 바뀝니다**. `fullImage` 가 2건 미만이면 폰 비주얼 자체를 렌더하지 않습니다.

#### 입체 배치 (실측 1536×745)
- 그룹 상자 350×500px, 부모에 `[perspective:1400px]`. ⚠️ **`preserve-3d` 안 씀** (2026-08-18 렉 전례, §4-B) — 각 폰을 평탄 회전시키고 크기·밝기로 앞뒤를 만듭니다.
- 앞 폰(아이폰형): 왼쪽 아래, 높이 94%(**227.8×477.7px**), `rotateY(-10deg) rotateZ(2deg)`, 밝기 그대로, `z-10`
- 뒤 폰(갤럭시형): 오른쪽 위, 높이 84%(**201.2×424.2px**), `rotateY(8deg) rotateZ(-1.5deg)`, `brightness(.85)`
- **겹침 72.6px = 앞 폰 폭의 32%** (지시 30% 목표)

#### "고정 백그라운드" = transform 패럴랙스
`background-attachment: fixed` 는 이 구조(폰이 배경이 아니라 DOM 레이어)에서 쓸 수 없어 **transform 패럴랙스로 대체**했습니다 — 효과는 같은 계열(스크롤보다 느리게 따라옴).
- 섹션이 화면을 지나는 진행도(0~1)를 **±28px**(총 이동 56px)로 매핑. 실측 1536: 진입 **+23.6px** → 이탈 **−20.8px**, 1920: +23.1 → −19.8.
- rAF 스로틀 + passive 리스너, **IntersectionObserver 로 섹션이 화면 밖이면 리스너를 떼고 정지**.
- `prefers-reduced-motion` 이면 아무것도 걸지 않습니다(정적).
- 컴포넌트가 `"use client"` 가 됐습니다(패럴랙스 때문). 밴드 배경·문구는 그대로입니다.

#### 레이아웃
`lg` 이상에서 `grid-cols-12` — 텍스트 7 / 폰 5(≈40%), `lg:min-h-[70vh]` + `items-center`, 섹션에 `overflow-hidden`(회전한 폰이 밴드 밖으로 새지 않게).
**1024px 미만은 폰을 렌더하지 않습니다** — 3차와 같은 텍스트만 밴드로 돌아갑니다(모바일 미착수).

#### 검증
`npx tsc --noEmit` · `npx eslint src` 통과 · 1536×745·1920×1080 **콘솔 에러 0**.
캡처: `bridge-phones-{1536x745,1920x1080}.png`(가운데), `bridge-phones-enter-*.png` / `bridge-phones-exit-*.png`(패럴랙스 2지점), 셸 회귀 `shell-revert-4.png`.

#### 4차 이후 남은 것
- ~~폰 화면이 데스크톱 풀페이지 캡처를 세로 크롭한 것~~ → **5차에서 실제 모바일 뷰로 교체됨**
- 정품 프레임 PNG 로 교체할지(위 자작 프레임 유지 vs 라이선스 확인된 에셋)
- 아래 "방향 갱신"의 메이커리 방향과 이 폰 비주얼(이미지 소재)의 관계 정리 필요

### 5차 구현 완료 (2026-08-20) — 폰 화면을 진짜 모바일 뷰로

사용자 승인: 브릿지 밴드 폰 화면을 **실제 모바일 뷰 캡처**로 교체.

#### 새 에셋 — `*-mobile.jpg` 3종
| 파일 | 크기 | 원본 |
|---|---|---|
| `public/images/projects/moongyul-mobile.jpg` | 780×4200 · 402KB | `/embeds/moongyul.html` |
| `public/images/projects/onseol-mobile.jpg` | 780×4200 · 1093KB | `/embeds/onseol.html` |
| `public/images/projects/jian-mobile.jpg` | 780×4200 · 450KB | `/embeds/jian.html` |

**캡처 조건 (재현용)** — 스크립트 scratchpad `mobile-shots.mjs`:
- 헤드리스 Chrome `Emulation.setDeviceMetricsOverride` **390×844 · deviceScaleFactor 2 · `mobile: true`** + 모바일 UA(iPhone Safari). 실제 모바일 레이아웃(햄버거 메뉴·1열 카드)이 나옵니다.
- 원본은 **로컬 서빙**(`http://localhost:3000/embeds/<slug>.html`) — `public/embeds/` 에 셋 다 있어 프로젝트 원본 폴더를 뒤질 필요가 없었습니다.
- **리빌 강제**: `document.fonts.ready` 대기 → 300px 씩 문서 끝까지 스크롤(IntersectionObserver 전부 발동) → 상단 복귀 → `captureBeyondViewport`. **지안 전례**(리빌 미발동으로 성적 카드가 빈 채로 찍힘)를 피하기 위한 같은 절차입니다.
- **상단 2100 CSS px**(= 780×4200 실픽셀, JPEG 80) — 폰 프레임 비율(9:19.5)보다 길게 찍어 `object-top` 크롭과 호버 스크롤 여유를 둡니다.
- **육안 확인 완료**: 세 장 모두 빈 섹션 없음(문결 Three Promises 카드 / 온설 브랜드 철학·사진 / 지안 성적 카드 3장 전부 채워짐). 캡처 중 콘솔 에러 0.

#### `projects.ts` — `mobileImage` 신설
```ts
mobileImage?: { src: string; width: number; height: number };
```
문결·온설·지안 3건 등록. **필드 주석에 위 캡처 조건을 적어 뒀습니다**(재캡처 시 같은 조건이어야 프레임 안에서 어긋나지 않습니다).

#### 우선순위 규칙 — **`mobileImage ?? fullImage`**
1. **브릿지 밴드**(`statement-band.tsx`): 폰 두 대 화면. 실측 앞=`moongyul-mobile.jpg` / 뒤=`onseol-mobile.jpg`
2. **포트폴리오 목업**(`project-card.tsx` → `device-mockup.tsx` 의 `phoneSrc`): 목업 폰도 진짜 모바일 뷰가 됩니다. 모니터의 호버 스크롤 레이어(`scrollSrc`)는 **데스크톱 `fullImage` 그대로** — 모니터에 모바일 화면이 흐르면 안 됩니다.
   - ⚠️ 이 항목은 **두 모드 공통**입니다(banded 전용 아님). 비주얼 모드가 아니라 "폰에는 폰 화면"이라는 콘텐츠 규칙이라 shell 에도 그대로 적용했습니다.
   - **호버 스크롤 동작 확인**: 호버 3초 시점 모니터 `object-position 50% 33.3%` / 폰 `50% 27.3%` — 기존 9초·11초 속도 그대로이고 50% 오버레이·로고도 유지됩니다(`mockup-hover-scroll-1536x745.png`).

#### 검증
`npx tsc --noEmit` · `npx eslint src` 통과 · 콘솔 에러 0.
캡처: `bridge-phones-v2-1536x745.png`(브릿지) · `mockup-mobile-view-1536x745.png`(목업 카드 crop) · `mockup-hover-scroll-1536x745.png`(호버 3초) · 셸 회귀 `shell-revert-5.png`.

#### 5차 이후 남은 것
- **4번째 포트폴리오(placeholder)** 에는 `mobileImage` 가 없어 폰 화면이 커버로 폴백합니다 — 실제 프로젝트가 들어올 때 같은 절차로 캡처
- `onseol-mobile.jpg` 가 **1.07MB** 로 셋 중 가장 큽니다(사진 비중). Next Image 가 서빙 시 최적화하므로 화면 성능 영향은 없지만, 저장소가 신경 쓰이면 품질 70으로 재캡처하면 됩니다
- 시안(embeds)이 갱신되면 `*-mobile.jpg` 도 함께 재캡처해야 합니다 — 지금은 수동

### 6차 구현 완료 (2026-08-20) — 메이커리 구조로 전면 재편

사용자 지시: **"메이커리 홈페이지 분석해서 구조를 전체적으로 바꿔보자. 히어로 섹션에도 이미지 사용 안 하려고."**

> 🚨 **방침 변경 — 1차의 "퍼널 구조는 그대로 유지하고 비주얼만 새로" 방침은 이 지시로 대체되었습니다.**
> 이제 banded 는 **섹션 순서 자체가 메이커리 구조**입니다. shell 은 예전 퍼널 그대로 보존됩니다.

#### 구조 분기 — `app/page.tsx`
`BandedSections` / `ShellSections` 두 배열로 나눴습니다. **shell 렌더 결과는 이전과 완전히 동일**합니다(실측 확인: 원래 8개 밴드 순서 · 통짜 TrustProof(헤딩+스탯+마키 한 섹션) · 캔버스 7 · 플로팅 3단 · 상담 바 없음 · 새 섹션 0개).

#### 밴드 시퀀스 (6차 실측 · 인접 동일색 없음)
| # | 섹션 | 밴드 | 실측 색 | 헤딩 |
|---|---|---|---|---|
| 1 | Hero | void | `rgb(20,29,51)` | (h1) 매출이 나는 홈페이지를 만듭니다 |
| 2 | Selected Work | paper | `rgb(255,255,255)` | 보이는 디자인과 작동하는 구조를… |
| 3 | 브릿지(폰 2대) | void | `rgb(20,29,51)` | 말로 설명하지 않습니다 / 만든 것으로… |
| 4 | 약속 | mist | `rgb(240,243,248)` | 스밈 스튜디오는 브랜드를 기획하고… |
| 5 | Services | paper | `rgb(255,255,255)` | 필요한 만큼 만들고, 성장에 맞춰… |
| 6 | 포지션+스탯 | void | `rgb(20,29,51)` | 거품 없는 견적, 타협 없는 퀄리티. |
| 7 | 후기 마키 | mist | `rgb(240,243,248)` | (2026-08-21 6번과 **병합** — 아래 병합 절) |
| 8 | Q&A(문제) | paper | `rgb(255,255,255)` | 혹시, 이런 상황이신가요? |
| 9 | Process | mist | `rgb(240,243,248)` | 감각에 맡기지 않고, 과정으로… |
| 10 | FAQ | paper | `rgb(255,255,255)` | 자주 묻는 질문 |
| 11 | Final CTA | void | `rgb(20,29,51)` | 브랜드의 다음 장면을… |
| 12 | 푸터 | void | `rgb(20,29,51)` | — |

인접 동일색 자동 검사 결과 **`contact → footer` 한 쌍만** 걸렸고, 이는 1차부터 이어지는 의도된 예외입니다.

#### ① Hero — 이미지 제거, 타이포 선언 복귀
- `site.ts` 에 **`hero.bandedHero.useImage`** 신설(현재 `false`). 3차 실사 히어로 코드는 **한 줄도 지우지 않았고** 이 플래그만 `true` 로 하면 즉시 복귀합니다.
- ⚠️ `hero.buildingImage.enabled` 와 **다른 스위치**입니다 — 그쪽은 shell 겸용이라 건드리지 않았습니다.
- 실측(1536×745): 사진 없음 · eyebrow top **253.4**(34%) · **CTA 하단 539.6** → 첫 화면 규칙 유지 · 스크롤 큐 복귀.

#### ② Selected Work 를 앞으로 (2번째)
메이커리처럼 **작품이 말보다 먼저** 섭니다. 3열·한 줄 관점·목업·호버 스크롤 전부 현행 유지. 헤딩도 "보이는 디자인과…" 그대로.
협력 로고(`PartnerStrip`)는 **Work 바로 뒤**로 옮겼습니다 — `partners` 가 null 이라 지금은 렌더되지 않지만, 로고가 생겨도 흰→연회청→네이비로 교차가 유지됩니다(Process 뒤에 두면 연회청이 연달아 붙습니다).

#### ③ 약속 — 독립 섹션 승격 (신규 `promise-cards.tsx`, 연회청)
- eyebrow **"OUR PROMISE"**(`site.ts` `promiseSection`).
- 헤딩은 **새로 쓰지 않고** 기존 `trustProof.heading`("스밈 스튜디오는 브랜드를 기획하고 / **매출이 성장하는 구조**를 만듭니다.")을 가져왔습니다. TrustProof 가 banded 에서 렌더되지 않으므로 **문구 중복이 없습니다**.
- 🚨 카드는 여전히 `trustProof.promises` 의 **`enabled: true` 2건만**("불만족 시 100% 전액 환불" · "예약제 소수 정예 운영"). 미확정 2건은 대기 그대로입니다.

#### ④ Services — 메인 복귀 (`service-system.tsx` banded 분기, 흰)
- 2026-08-03 에 메인에서 빠졌던 섹션을 되살렸습니다. **shell 마크업은 그대로 두고** banded 만 새 카드로 그립니다.
- 내용은 `services.ts` 의 **`serviceAxes` 4종을 그대로** 씁니다 — 번호 + 영문명 + `titleKo` + `tagline` 한 줄 + **카테고리 칩**(`includes` 앞 4개). **새 카피 0줄**.
- 🚨 메이커리에는 서비스마다 "건수"가 붙지만 **넣지 않았습니다** — 검증된 실적 수치가 없습니다.
- 배경 장식(`ChannelIconsBackdrop`)은 banded 에서 끕니다("장식 전면 off" 규칙).

#### ⑤ 포지션 선언 + 스탯 (신규 `position-band.tsx`, 네이비)
- 🚨 **헤딩은 초안(팀 검토 대기)** — `site.ts` `positionBand.heading`: **"거품 없는 견적,\n타협 없는 퀄리티."**
  메이커리 문장을 번역·복제하지 않고 **결만** 차용했고, 가격·비교 수치는 **적지 않았습니다**(표시광고 리스크).
- 오른쪽에 **검증된 스탯 2개**(8월 남은 티오 2건 / 불만족 시 환불 100%) 카운트업. 티오는 `availability` 단일 소스라 하단 상담 바와 항상 같은 값입니다. 고객만족도(`value: null`)는 여전히 빠집니다.
- 왼쪽 아래에 `trustProof.note`("높은 퀄리티를 위해 예약제로 진행됩니다") — 티오 수치와 같은 자리에서 읽히도록 여기로 왔습니다.
- `positionBand.enabled = false` 로 하면 밴드가 통째로 빠지고 Services(흰)→후기(연회청)로 이어져 교차 규칙은 유지됩니다.

#### ⑥ 후기 마키 — 독립 섹션 (신규 `testimonial-marquee.tsx`, 연회청)
- 마키 동작(rAF 구동 · 누르는 동안만 정지+드래그 · 화면 밖 정지 · reduced-motion 정지)은 **공용 훅으로 옮겼을 뿐 구현이 동일**합니다.
- 🚨 헤딩에 평점·건수(메이커리 "4.9 / 1000+")를 **쓰지 않았습니다**. → **2026-08-21 포지션 밴드와 한 섹션으로 병합** (아래 "포지션 · 후기 섹션 병합" 절).

#### ⑦ 공용 모듈 분리 (동작 변화 없음)
TrustProof 가 셋으로 쪼개지면서 안에 있던 헬퍼를 꺼냈습니다 — **구현은 복사 그대로**입니다.
- `components/ui/count-up.tsx` — `CountUp`
- `components/ui/marquee.tsx` — `useMarquee`
shell 의 `trust-proof.tsx` 도 같은 모듈을 import 합니다(그래서 두 모드가 같은 코드를 씁니다).

#### 검증
`npx tsc --noEmit` · `npx eslint src` 통과 · **콘솔 에러 0**.
캡처: `banded6-hero-1536x745.png` · `banded6-promise-*.png` · `banded6-services-*.png` · `banded6-position-*.png` · `banded6-reviews-*.png` · `banded6-full-1536x745.png`(전체 세로) · 셸 회귀 `shell-revert-6.png`.

#### 6차 이후 남은 것
- 🚨 **초안 2건 — 팀 확정 필요**: 포지션 헤딩("거품 없는 견적…" — 2026-08-21 사용자가 "이 멘트는 유지"로 확정에 가까움) · 브릿지 문구(3차부터 대기). 후기 전용 헤딩은 병합으로 없어졌습니다
- 약속 카드가 **2장뿐**이라 섹션이 다소 헐겁습니다. 미확정 2건(무제한 수정 · 월 작업량 한정)이 확정되면 `enabled` 만 켜면 3~4장으로 찹니다
- Services 카드에 **건수·기간·가격이 없습니다**(근거 없음). 확정되면 칩 옆에 붙일 자리는 있습니다
- 아래 "방향 갱신"의 **컬러 조합 확정**이 남아 있습니다 — 확정되면 `globals.css` banded 토큰만 바꾸면 이 구조 위에 그대로 얹힙니다
- ~~모바일 전면 미착수(6차 구조도 웹 우선)~~ → **2026-08-22 모바일 1차 완료**(§3)

### 브릿지 폰 화면 — 임시 데모 시안 (2026-08-21)

사용자 지시로 브릿지 밴드 폰 2대의 화면을 **가상 브랜드 데모 시안 2종**으로 교체했습니다 — 앞=`bridge-demo-a.jpg`(MOSS ATELIER 플랜테리어·라이트) / 뒤=`bridge-demo-b.jpg`(LUMEN PT 스튜디오·다크), `public/images/placeholder/`. 스위치는 `site.ts` 의 **`bridgePhoneScreens`** 하나이고 **`null` 로 두면 5차 규칙(실제 포트폴리오 `mobileImage ?? fullImage`)이 그대로 복귀**합니다. 원본 HTML 은 `public/images/placeholder/src/demo-{a,b}.html` 에 보존(재캡처 조건 390×844·DSF2 = 780×1688). **앞뒤는 지시안 그대로 유지** — 스왑 캡처를 비교했더니 다크 시안을 앞에 두면 네이비 밴드 위에서 폰 실루엣이 묻히고 밝은 시안이 뒤에서 시선을 끌어 원근이 뒤집혔습니다(현행은 뒤 폰 `brightness(.85)` 에서도 보라 오로라·CTA 가 살아 있음). banded 전용이라 shell 은 영향 없음. `tsc`·`eslint` 통과 · 콘솔 0 · 캡처 `bridge-demo-wired.png`.

### 히어로 = 초대형 워드마크 (2026-08-21) — 메이커리 실물 구성

사용자: **"히어로가 경희정원한의원이랑 너무 똑같다. 메이커리처럼 해줘."** → 메이커리 히어로 실물 캡처를 보고 **워드마크 구성으로 확정**했습니다.

- **스위치 `hero.bandedHero.variant`**(`site.ts`, 신설) — **`"wordmark"`**(현재) / `"manifesto"`(선언문 4줄 큰 타이포, 오전 중간안 · 보존) / `"declaration"`(1·2·6차 구성 · 보존). 세 구성 다 코드가 살아 있어 값만 바꾸면 즉시 되돌아갑니다. ⚠️ `useImage` 와는 **다른 축**이라 3차 실사 히어로는 `variant: "declaration"` + `useImage: true` 를 함께 켜야 복귀합니다.
- **구성**: ① 초대형 워드마크 **`SUM!M`** — "I"를 **"!"로 치환하고 그 글자만 주황**(메이커리의 `@` 자리 위트). ② "+" 마커 4개(가로 등간격, 흰 25% 12px). ③ 좌하단 선언문 3줄(15px, **마지막 줄만 굵게·흰색**) + **바로 아래 왼쪽에 메타** "© 2026 SUMIM Studio · Busan"(12px 흰 45%, 간격 10px). eyebrow·부제·**CTA 버튼**·스크롤 큐는 **없고 우하단은 비웁니다** — 2026-08-21 사용자 확정(네비 "프로젝트 문의"와 하단 상담 바가 전환 동선을 맡습니다). `wordmark.cta` 필드는 삭제하지 않고 **미사용으로 보존**(주석 표시).
- **워드마크 크기 = `min(29.9vw, 62vh)`** 🚨 실측값입니다. 잉크폭/글자크기 = **3.206** 이라 0.96/3.206 ≈ 0.299 → 1536·1920 **둘 다 뷰포트 폭의 95.9%**. `vh` 쪽은 세로가 낮은 뷰포트용 안전 상한입니다(아래 마커·선언문·CTA 가 쓰는 고정 높이 ≈311px). **글자 수·자간·치환 글자를 바꾸면 두 값 다 다시 실측**해야 합니다 — 넓은 글자 M 을 치환했더니 잉크폭이 22% 줄어든 전례가 있습니다(`accentIndex` 를 2 로 두면 "SU!IM" 이 되어 브랜드명이 깨집니다. **정답은 3**).
- 워드마크는 `Container`(max 1280px) **밖**에 두고 `leading-[0.82]`(대문자만이라 디센더 없음), 섹션에 `overflow-hidden`. 스크린리더에는 `aria-hidden` — 치환된 "!" 때문에 "SUM 느낌표 M"으로 읽힙니다(브랜드명은 네비 로고·`<title>` 이 전달). **문장 h1 은 좌하단 선언문**입니다.
- **🚨 회전 단어 제거 (2026-08-21 사용자 지시 "회전 단어는 없애줘")** — 히어로 어느 구성에서도 렌더하지 않습니다. `components/ui/rotating-word.tsx` 와 `bandedHero.rotatingWords`·`rotateMs` 는 **보존만** 하고, declaration 구성은 대신 `accentWord`("매출", 정적 주황) + `tail` 을 씁니다.
- 리빌은 전부 **CSS 전용 `.reveal-load`** 스태거(워드마크 0 → 마커 0.24 → 선언문 0.32/0.42/0.52 → 우하단 0.62s). 워드마크가 LCP 요소라 JS·IO 를 기다리지 않고, `prefers-reduced-motion` 이면 globals 가 애니메이션을 꺼 즉시 표시됩니다.
- 검증: `tsc`·`eslint` 통과 · **콘솔 에러 0**(양 해상도) · 캡처 `hero-wordmark-{1536x745,1920x1080}.png`, 중간안 `hero-manifesto-*.png`, 셸 회귀 `shell-revert-7.png`(캔버스 7 · 플로팅 3단 · 원래 히어로 문구 복귀 · 새 섹션 0).

### 팀 팔레트 적용 (2026-08-21) — 검정 · 라임 · 바이올렛

「스밈 컬러 시뮬레이터」에서 팀(김미라 안)이 고른 조합을 banded 에 얹었습니다. **shell 은 무관**합니다.

- **즉시 복귀 스위치 `bandedPalette`**(`site.ts`) — **`"team"`**(현재) / `"navy"`(3차 수능선배 세트). `layout.tsx` 가 `<html data-palette>` 를 내려보내고, `globals.css` **맨 끝**의 `html[data-visual="banded"][data-palette="team"]` 블록이 기존 banded 블록을 덮어씁니다. **기존 블록은 한 줄도 지우지 않았습니다** — `"navy"` 면 덮어쓰기가 사라져 예전 화면이 그대로 나옵니다(실측 회귀 확인: void `#141d33` · mist `#f0f3f8` · accent `#ea580c` 복귀 · 콘솔 0).
- **밴드**: void `#141d33` → **`#000000`** / mist `#f0f3f8` → **`#EAF6AD`(라임)** / paper `#ffffff` 유지. 액센트 `#EA580C` → **`#6338EE`(바이올렛)** — `--color-cta` 하나만 바꾸면 `text-cta`/`bg-cta`/`border-cta`/`bg-cta/10` 이 전부 따라옵니다(CTA 버튼·네비 문의 버튼·eyebrow 틱·워드마크 "!"·상담 바·FAQ 틱·Process 강조).
- **파생 토큰**: 검정 위 보조 글자는 네이비 계열을 버리고 **중성 회색**(`#a0a0a0` / `#7d7d7d`), 카드 면·폼 패널 `#101010`/`#181818`, 네비 dark 바 `rgba(0,0,0,.85)`. 라임 위는 **딥 올리브**(`#101208` / `#4a4f38` / `#6b6f57`) + 헤어라인 `#d3e18f`·`#bccd70`. Q&A 답변 말풍선 주황 틴트 `#ffe6d6` → **`#ece5ff`**.
- **컴포넌트에 박힌 색은 CSS 로 덮었습니다** — 버튼 호버 `#F0650F`(6곳) → `#7a52f2`, 상담 바의 `border-[#c6ccd8]`·`text-[#141a26]`, `.chat-*`·`.card-surface`·`.trade-badge`·`.sticky-consult-bar` 의 네이비 그림자. **TSX 는 한 줄도 고치지 않았습니다**(navy 로 되돌릴 때 되돌릴 것이 없게).

#### 대비 실측 (WCAG, 헤드리스 계산)
| 밴드 | 배경 | 잉크 | 보조 | muted | 액센트 |
|---|---|---|---|---|---|
| void | `#000000` | `#ffffff` **21.0** | `#a0a0a0` **8.03** | `#7d7d7d` **5.10** | `#6338EE` **3.33** |
| paper | `#ffffff` | `#101010` **19.03** | `#4d4d4d` **8.45** | `#757575` **4.61** | `#6338EE` **6.30** |
| mist | `#EAF6AD` | `#101208` **16.44** | `#4a4f38` **7.41** | `#6b6f57` **4.53** | `#6338EE` **5.49** |

바이올렛 위 흰 글자(버튼 안) **6.30:1**. 라임 위 바이올렛이 **5.49:1** 이라 지시서의 "라임 전용 딥 바이올렛(#4a28b8) 파생"은 **필요 없었습니다**(mist 의 `--color-indigo` 로만 남겨 뒀습니다).

🚨 **검정 위 바이올렛은 3.33:1** 로 본문 크기 글자에는 AA 미달입니다. 배경·보더는 그대로 두고 **`[data-band="void"] .text-cta` 만 `#9b7dff`(6.77:1)** 로 파생했습니다(라벨 "예약 없이 바로"·포지션 밴드 스탯 라벨 등). 버튼 **면**을 밝히면 흰 글자 대비가 2.9:1 로 깨지므로 `--color-cta` 자체는 건드리지 않았습니다. **예외 1건** — 워드마크 "!"(459px)는 큰 글자 기준 3:1 을 넘고 버튼과 같은 브랜드 색이어야 해서 `var(--color-cta)` 로 되돌립니다.

### 히어로 마감 (2026-08-21) — 광학 보정 · 진입 애니메이션 · 우측 정렬

**① 워드마크 광학 여백 보정** (사용자 "S와 M 모양 차이긴 한데 양옆 여백이 달라 보이니 심미적으로 비슷하게")
`margin-left: -0.015em`. 🚨 **사이드베어링 계산이 아니라 픽셀 스캔**으로 잡았습니다 — `measureText` 의 사이드베어링만 보면 **자간(−0.04em)이 이중 계산돼 부호가 뒤집힙니다**(실제로 첫 계산이 반대로 나왔습니다). 캔버스에 같은 폰트·크기·자간으로 그린 뒤 잉크 열을 세는 방식(`scratchpad/measure-ink-pixels.mjs`)이 정답입니다.
보정 전 1536: 왼 **48** / 오른 **40** → 보정 후 **41 / 47**(1920: 51 / 59). 왼쪽을 6px 더 좁힌 건 **둥근 글자 오버슛** — "S"는 곡선 접점 하나만 가장자리에 닿아 같은 여백도 더 비어 보이고, 마지막 "M"의 오른쪽은 수직 획이라 꽉 차 보입니다. 6px ≈ 캡 높이(331px)의 **1.8%** 로 활자 관습(1.5~2%)과 맞습니다.

**② 글자별 진입 애니메이션** (사용자 "각 알파벳 오른쪽 면에 벽이 있다 생각하고, 그 벽에서 스윽 나오는 느낌")
글자마다 자기 오른쪽 가장자리에 클립 경계를 두고 그 뒤(`translateX(120%)`)에서 왼쪽으로 밀어냅니다. `globals.css` `.wordmark-glyph` + `@keyframes wordmark-wall`, **0.7s · `cubic-bezier(.22,1,.36,1)` · 스태거 0.11s**(0.09s 는 이징이 앞쪽에 몰려 다섯 글자가 거의 동시로 읽혔습니다). 🚨 클립은 `overflow:hidden` 이 **아니라 `clip-path: inset(-60% -2% -60% -400%)`** 입니다 — `overflow` 를 주면 inline-block 의 베이스라인이 "마지막 줄"에서 "마진 박스 아래변"으로 바뀌어 글자가 세로로 밀립니다. `clip-path` 는 레이아웃·베이스라인·자간·광학 보정을 건드리지 않습니다(잉크 폭 94.3% 유지 · 세로 잘림 없음 — 캡처 확인). **CSS 전용**이라 LCP 규칙을 지키고 `prefers-reduced-motion` 이면 즉시 표시됩니다. 워드마크가 끝나는 **1.14s** 부터 마커 → 선언문 3줄 → 메타 스태거가 이어집니다.


**④ 선언문 크기** — 15 → 17 → **19px**(마지막 굵은 줄 → **21px**), 행간 1.85 → 1.75 → **1.7**. 메타(©)는 **12px 그대로**.

**⑤ 선언문 카피 — 2026-08-21 사용자 확정**(초안 아님): "브랜드에 **숨**을 불어넣습니다." / "보이지 않던 가치를 **더합니다**." / "**매출이 나는 홈페이지를 만듭니다.**" 워드마크 `SUM!M` 의 **숨(breath) / SUM(더하다)** 이중 의미를 선언문이 받습니다.

- 캡처: `palette-team-hero-1536x745.png` · 진입 `hero-enter-{a,b,c,done}.png`(a = 전 글자 벽 뒤 / b = S·U 완료, 마지막 M 72% / c·done = 완료).

### 브릿지 밴드 (2026-08-21) — 데모 시안 캐시 무효화 · 마우스 패럴랙스

**① `bridge-demo-a.jpg` → `bridge-demo-a2.jpg` 로 rename** 🚨 같은 경로로 시안을 교체했더니 `/_next/image` **최적화 캐시가 옛 그림을 계속 서빙**했습니다. URL 을 바꾸는 것이 가장 확실한 무효화라 파일명을 바꾸고 `site.ts` `bridgePhoneScreens.front` 를 갱신했습니다. **앞으로 시안을 갈아 끼울 때는 새 파일명을 쓰세요.** 새 캡처에서 몬스테라 실사가 정상 표시되는 것을 확인했습니다(`bridge-mouse-*.png`).

**② 마우스 3D 틸트** (사용자 "휴대폰들이 마우스 따라 조금씩, 두 개 각기 다른 방향으로" → 정정 **"위치 이동 말고 상하좌우 회전으로"**)
섹션 `pointermove` 로 커서의 **섹션 중심 대비 오프셋**(nx, ny ∈ [−1,1])을 구해 **앞 폰 `rotateY(nx·6°) rotateX(ny·−5°)` / 뒤 폰 `rotateY(nx·−8°) rotateX(ny·6°)`** 를 **반대 부호**로 겁니다. 회전은 기존 정적 기울기(`rotateY(-10°) rotateZ(2°)` 등) **앞에 붙여 가산**하고, **translate 는 쓰지 않습니다** — 폰이 제자리에서 기울기만 합니다. lerp 0.1 로 따라가 손을 멈추면 스르르 멎고, 값이 수렴하면 rAF 가 스스로 멈춥니다.
- **스크롤 패럴랙스(±28px)와 레이어가 다릅니다** — 저쪽은 바깥 그룹(`groupRef`)의 Y 이동, 이쪽은 각 폰 자신의 transform 이라 서로 덮어쓰지 않습니다. 정적 회전값은 `FRONT_ROTATE`/`BACK_ROTATE` 상수로 뽑아 JS 가 transform 을 다시 쓸 때 함께 넣습니다(클래스에도 같은 값이 있어 SSR·no-JS 에서도 기울어진 채로 보입니다).
- 원근은 **부모 그룹의 `perspective:1400px`** 을 그대로 씁니다 — 폰이 그룹의 **직계 자식**이라 래퍼를 끼우지 않았습니다(사이에 층을 하나 넣으면 perspective 가 끊겨 회전이 납작해집니다). ⚠️ `preserve-3d` 는 여전히 금지(2026-08-18 렉 전례).
- 기존 **IntersectionObserver 를 재사용**해 섹션이 화면 밖이면 리스너를 떼고 폰을 제자리로 되돌립니다. `prefers-reduced-motion` 이면 효과 전체가 걸리지 않고, 1024px 미만은 폰을 렌더하지 않아 자동 무동작입니다.
- **실측(CDP mouseMoved, 1536×745 — `matrix3d` 성분)**: 이동 성분 `tx`·`ty` 가 **전 구간 0**(translate 제거 확인). 커서 우상단 → 앞 `m13` **+0.0897** / 뒤 **−0.0236**, 앞 `m23` **+0.0402** / 뒤 **−0.0526**. 커서 좌하단 → 앞 `m13` **+0.2539** / 뒤 **−0.2521**, 앞 `m23` **−0.0762** / 뒤 **+0.0743** — **rotateY·rotateX 두 축 모두 앞뒤 부호가 반대**입니다. 회전으로 폭이 221~232px 사이에서 변하지만(원근 투영) 앞 폰 왼쪽 끝은 최소 **947px**, 텍스트 컬럼 오른쪽 끝은 **851.3px** 이라 **95.7px 여유**로 침범하지 않습니다. 콘솔 0.
- 캡처: `bridge-tilt-{rest,right,left}.png`(구 translate 안은 `bridge-mouse-*.png`).

### 🚨 스크롤바 정렬 버그 수정 (2026-08-21) — vw 상수 → 구조로

사용자 실사용 피드백: **실제 크롬에서 선언문 우측선이 M 과 어긋난다.** 헤드리스 검증은 `--hide-scrollbars` 라 맞아 보였을 뿐이고, 실제 브라우저는 **세로 스크롤바(15px) 때문에 `100vw ≠ 보이는 폭`** 이라 vw 로 잡은 상수(`3.06vw`)가 통째로 어긋났습니다.

- **상수 접근을 버리고 구조로 해결했습니다.** 워드마크 `<p>` 와 마커·선언문·메타를 **같은 `inline-block` 래퍼**에 넣고(래퍼 폭 = 워드마크 자연 폭, 부모가 `flex justify-center`), 선언문은 그 안에서 `text-align:right`. 뷰포트·스크롤바가 무엇이든 **오른쪽 끝이 자동으로 일치**합니다. `HERO_EDGE_INSET` 상수는 삭제했습니다.
- 남은 보정은 **폰트 메트릭 3개(em 단위)** 뿐이고, 워드마크 `<p>` 의 좌우 **음수 마진**으로만 들어갑니다:
  | 상수 | 값 | 뜻 |
  |---|---|---|
  | `WM_LSB_EM` | 0.0341 | "S" 좌측 사이드베어링 — 상자보다 이만큼 안쪽에서 잉크 시작 |
  | `WM_RSB_EM` | 0.0190 | 마지막 "M" 우측 여백 + **음수 자간(−0.04em)의 잔여** |
  | `WM_OPTICAL_EM` | 0.0131 | 둥근 글자(S) 오버슛 — 캡 높이의 ≈1.8% |
  `margin-right: -WM_RSB_EM` 하나가 **래퍼 오른쪽 변을 M 의 잉크 끝**에 세웁니다(이것만이 정렬을 만들고 왼쪽 값과 무관). `margin-left: -(WM_LSB_EM + WM_OPTICAL_EM)` 은 가운데 정렬된 래퍼 안에서 좌우 잉크 여백이 광학적으로 같아 보이게 미는 몫입니다.
- 🚨 **재측정은 반드시 스크롤바가 보이는 상태로**: `scratchpad/cdp-scrollbar.mjs`(cdp-lib 사본에서 `--hide-scrollbars` 제거) + `document.documentElement.style.overflowY='scroll'` 강제. 스크립트는 `scratchpad/measure-align-scrollbar.mjs`.

#### 정렬 실측 (스크롤바 15px 있는 상태, 픽셀 스캔)
| 뷰포트 | 보이는 폭 | 잉크 좌 / 우 | 선언문·메타 끝 − M 잉크 끝 | 광학 차(좌−우) | 가로 스크롤 |
|---|---|---|---|---|---|
| 1280×800 | 1265 | 34 / 1227 | **−0.6px** | −4px | 0 |
| 1536×745 | 1521 | 41 / 1475 | **−0.5px** | −5px | 0 |
| 1920×1080 | 1905 | 52 / 1847 | **−0.2px** | −6px | 0 |

세 폭 모두 **1px 미만**으로 맞고, 마커 줄 양 끝도 같은 선(46.5~1474.5 @1536)입니다. 광학 차는 의도한 방향(왼쪽이 조금 좁음 = 둥근 S 오버슛)이고 글자 크기에 비례합니다. 워드마크 크기는 `min(29.9vw, 62vh)` 그대로 두었습니다 — 스크롤바를 포함한 vw 기준이라도 잉크 폭이 보이는 폭의 94%대라 넘치지 않습니다(실측).

### 색상 조합 실험 도구 — 사이트 안 플로팅 버튼 (2026-08-21)

사용자: **"색상 조합 실험을 파일이 아니라 실제 홈페이지에서."** 좌하단 플로팅 **"색상 조합"** 버튼 → 패널에서 고르면 **지금 보고 있는 화면에 바로 적용**됩니다.

- ✅ **`site.ts` 의 `colorLab` — 사용자 결정으로 배포 후에도 유지합니다 (2026-08-21, "색 조합은 도메인 개설 후에도 유지").** 끄고 싶어지면 이 값만 `false` 로 바꾸면 버튼·패널이 통째로 사라집니다. **banded 전용**이고 shell 에서는 렌더하지 않습니다(실측 확인). 선택은 **보는 사람의 localStorage 에만** 남아 다른 방문자 화면이나 실제 팔레트에는 영향이 없습니다.
- 엔진은 「스밈 컬러 시뮬레이터」 v2 의 `<script>` 를 **그대로 TS 로 이식**했습니다 — `src/lib/palette.ts`(색 · 톤 · `shift/lum/ctr` · 명도 5~97 clamp · **버튼 전용색 자동 파생**). 값이 같아야 팀이 시뮬레이터에서 본 조합과 사이트가 일치합니다.
- UI(`components/ui/color-lab.tsx`): 색 리스트(스와치 4칸, 현재 톤이 반영됨) · 톤 칩 · **커스텀 4슬롯**(다크/라이트/미스트/포인트 컬러픽커 + hex 표시) · **대비 검수 7줄**(4.5 미만이면 ⚠ — 2026-08-22 "진한 미스트 위 포인트" 추가) · **CSS 복사** · **초기화**.
- ⚠️ 여기 적힌 **색 8종 · 톤 6종은 옛 수치**입니다 — 2026-08-21 밤에 **색 5종 · 톤 4종**으로 재큐레이션했습니다(아래 §5-B "색상 조합 재큐레이션" 절이 최신).
- **적용 방식** 🚨 `globals.css` 는 **불변**입니다. `<style id="color-lab-override">` 를 head 에 주입해 밴드 배경·밴드 토큰·액센트·버튼 호버·네비 dark 바·폼 패널·Q&A·카드·배지·상담 바를 다시 선언합니다(팀 블록이 덮는 것과 같은 범위). "초기화" = 그 태그 제거 → **팀 팔레트 즉시 복귀**. 선택값은 localStorage(`sumim-color-lab`)에 저장됩니다.
  - 선택자 접두사는 `html:root[data-visual="banded"][data-palette]` — 팀 블록(0,3,0)을 `:root` 한 겹으로 **(0,4,0)** 으로 확실히 이깁니다(dev HMR 이 스타일 순서를 흔들어도 안전).
  - 검정 위 액센트 **글자**는 시뮬레이터와 같은 방식으로 **자동 밝힘 파생**(`accentOnDark`, 4.5 확보까지 명도 +2씩)하고, 워드마크 "!"만 브랜드색으로 되돌립니다.
- 하이드레이션: 저장값 복원을 effect 안 setState 로 하면 eslint `react-hooks/set-state-in-effect` 에 걸려, `useSyncExternalStore` 로 하이드레이션 완료를 판정하고 그 전에는 렌더하지 않습니다(내부 도구라 SSR 불필요).
- **상담 바와 겹치지 않습니다** — 버튼 `bottom-[92px]`(lg 104px), 실측 상담 바가 보이는 스크롤 위치에서 버튼 하단 641 vs 바 상단 672 → **31px 여유**.

#### 검증 (CDP 실동작)
버튼 노출 → 패널 오픈 → **딥그린 × 크림** 선택 시 `override` 스타일 주입(4,674자) · void `rgb(18,41,31)` · paper `rgb(253,250,242)` · mist `rgb(238,240,228)` · `--color-cta` `#e8632b` 로 실제 변경 → **활기·발랄** 톤 적용 시 void `rgb(25,96,65)` · cta `#ff8551` 로 재계산 → **CSS 복사** 출력 확인(`--band-dark:#196041 / --band-light:#fdfbf2 / --band-mist:#f5f6ed / --accent:#ff8551 / --accent-btn:#ff8551 / --accent-ink:#171921 / --accent-on-dark:#ffbda1`) → **초기화** 시 스타일 태그 제거 + 팀 팔레트(검정·라임·바이올렛) 복귀. **콘솔 0**.
캡처: `colorlab-{button,panel,forest,forest-lively,reset}.png` · 상담 바 공존 `colorlab-with-consultbar.png` · 셸 회귀 `shell-revert-10.png`(colorLab 버튼 없음 · override 없음 · `--color-cta` `#ea580c` · 캔버스 7 · 새 섹션 0).

### 원페이지 전환 (2026-08-21) — 메인 밖으로 나가는 동선 제거

사용자: **"자사 홈피는 임팩트 있게 원페이지로. 서비스 부분 넘어가는 건 없애고 싶어. 추후 회사가 커지면 늘림."**
**banded 전용**이고 서브 페이지 라우트·컴포넌트는 **한 줄도 지우지 않았습니다**(shell 은 지금도 그쪽으로 갑니다).

- **네비 메뉴 → 섹션 앵커**: `site.ts` 에 **`navigation.bandedMenu`**·**`bandedCta`** 신설. Work→`#work` / Services→`#services` / Process→`#process` / Contact→`#contact` / "프로젝트 문의"→`#contact`. **About 은 제거**(메인에 대응 섹션 없음 — 위 §3 참고).
  - `navigation.tsx` 가 `usePathname()` 으로 갈라, **메인에서는 순수 해시**(`#work` — 라우팅 없이 그 자리에서 스크롤), `/privacy` 같은 다른 경로에서는 `/#work`(메인으로 돌아간 뒤 이동). 부드러운 스크롤은 globals 의 `scroll-behavior: smooth` + `Section` 의 `scroll-mt-20` 이 이미 깔려 있어 **새로 추가한 CSS 가 없습니다**(`prefers-reduced-motion` 이면 globals 가 `auto` 로 되돌립니다).
- **Work 카드 → 상세 페이지 링크 제거**, 대신 **프로젝트 모달**(아래 절).
- **버튼 2개 제거**: "모든 프로젝트 보기"(→`/work`) · "제작 범위 확인하기"(→`/services`). Services 카드는 정적으로 두고 호버 효과는 유지.
- **푸터**: 원래 서브 페이지 링크가 없었습니다(메일·인스타·스레드·`/privacy` 뿐) — 손대지 않았습니다.

#### 링크 검사 (banded 메인, 실측 21건)
| 종류 | 건수 | 내용 |
|---|---|---|
| 앵커(`#…`·`/`) | **15** | 네비 5 · 히어로/섹션 CTA · 상담 바 등 |
| `tel:` | 2 | 상담 바 "빠른 상담" · Final CTA "전화하기" |
| `mailto:` | 2 | 푸터 · Final CTA |
| `/privacy` | 2 | 폼 동의 · 푸터 (**법적 필요 — 유지**) |
| 외부(카톡 채널) | 2 | 🆕 2026-08-21 — 상담 바 · Final CTA (`_blank`, **의도된 외부 링크**) |
| 🚨 **내부 서브페이지** | **0** | — |

네비 앵커 실측: Services 클릭 → `scrollY 0 → 3189` · `location.hash = "#services"` · `pathname` 은 `/` 그대로 · `#services` 상단이 **80px**(고정 네비 높이 = `scroll-mt-20`)에 정확히 붙음. 캡처 `onepage-nav-anchor.png`.

### 프로젝트 모달 (2026-08-21) — 원페이지에서 상세 보기

사용자: **"프로젝트 보기를 원페이지에서 확인할 방법?"** → 카드를 누르면 **같은 페이지 위 라이트박스**가 열립니다. 신규 `components/ui/project-modal.tsx`(banded 전용).

- **구성**: 좌 55% = `fullImage` 풀페이지 캡처를 **자체 세로 스크롤**로 훑기(실측 스크롤 높이 2,439px) · 우 45% = 한 줄 관점(액센트색)·프로젝트명·업종·카테고리·**요약 전문**(카드에서는 2줄 clamp)·services 태그·(검증된 경우) result. **`projects.ts` 에 이미 있는 값만 쓰고 새 카피는 0줄**입니다.
- **데스크톱/모바일 토글** — `mobileImage` 가 있으면 칩 2개로 전환(3건 모두 보유).
- **연결 방식** 🚨 `SelectedWork`·`ProjectCard` 는 서버 컴포넌트라 상태를 들 수 없습니다. 카드를 작은 클라이언트 버튼(`ProjectCardTrigger`)으로 감싸 **커스텀 DOM 이벤트**(`sumim:open-project`)를 쏘고, 페이지에 하나만 마운트된 `ProjectModal` 이 slug 로 찾아 엽니다(Context 를 쓰면 트리 전체를 클라이언트로 내려야 합니다). **트리거 요소를 이벤트에 함께 실어** 닫을 때 그 버튼으로 포커스를 정확히 되돌립니다 — `document.activeElement` 는 프로그램 클릭에서 `body` 라 실패했습니다(실측 전례).
- **해시 딥링크** `#work-{slug}` — 열 때 `pushState`(`location.hash = …` 는 없는 id 를 찾아 상단으로 튑니다), 닫으면 해시 제거, 뒤로 가기(`popstate`)도 따라갑니다. `/#work-onseol` 로 **새로고침하면 그 모달이 바로 열립니다**(실측: `opened true` · `aria-label "온설 프로젝트"` · `scrollY 0`).
- 접근성·동작: ESC·바깥 클릭·× 버튼으로 닫기 · `body` 스크롤 잠금 · 포커스 트랩(모바일 메뉴와 같은 패턴) · 살짝 페이드+스케일 등장(`prefers-reduced-motion` 이면 즉시). **placeholder 4번째 카드는 모달도 링크도 없습니다.** 목업 호버 스크롤은 그대로 살아 있고 클릭만 모달로 갑니다.
- 하이드레이션은 `ColorLab` 과 같은 `useSyncExternalStore` 게이트를 씁니다(effect 안 setState 금지 룰).
- ⚠️ 모달 이미지는 `loading="eager"` + `sizes` 를 줍니다 — 없으면 Next 가 1440px 원본 폭으로 최적화해 dev 에서 몇 초간 **빈 칸**이 보였습니다(실측).
- 검증: 모달 열림 `onepage-modal-open.png` · 좌측 캡처 스크롤 `onepage-modal-scrolled.png`(scrollTop 854) · 딥링크 `onepage-modal-deeplink.png` · ESC 후 `dialog` 제거 + 해시 제거 + `body overflow` 복구 + **포커스 트리거 복귀 true**. 콘솔 0.

### 브릿지 폰 등장 애니메이션 (2026-08-21)

사용자: **"휴대폰 두 대도 처음 화면에 나타날 때 양쪽에서 모여 들어오든, 아래에서 올라오든."** → **양쪽에서 모이기**로 갔습니다(두 대가 이미 서로 마주 보게 기울어 있어 그 구성과 맞물립니다).

- 섹션이 처음 **25%** 보이는 순간 `.is-in` 을 붙이고 **관찰을 끊습니다(1회, 재등장 없음)**. 앞 폰은 왼쪽에서(`translateX(-90px)`), 뒤 폰은 오른쪽에서(`+110px`) + opacity 0 → 제자리. **0.9s · `cubic-bezier(.22,1,.36,1)` · 뒤 폰 0.12s 딜레이**.
- 🚨 **래퍼가 이동, 폰이 회전** — 마우스 3D 틸트가 폰의 `style.transform` 을 직접 쓰기 때문에 같은 요소에 CSS 애니메이션을 걸면 서로 덮어씁니다. 그래서 이동은 **새 부모 래퍼**(`.bridge-phone-in`)가 맡습니다. ⚠️ 래퍼가 생기면 **그룹의 `perspective` 가 폰에 닿지 않으므로** 래퍼마다 `perspective:1400px` 를 다시 줬습니다(폰은 각자 평탄 회전이라 결과가 같습니다). `preserve-3d` 는 여전히 금지.
- 등장과 틸트는 층이 달라 **합성돼도 어긋나지 않습니다**(등장 중 틸트를 막지 않았습니다).
- **실측(1536×745)**: 진입 전 `opacity 0` · 0.2s 시점 뒤 폰 `tx +110 / opacity 0`(딜레이 중) · 앞 폰 `tx −45.4 / opacity 0.50` · 0.6s 시점 둘 다 `tx 0 / opacity 1`. 캡처 `bridge-enter-{0.2s,0.6s,done}.png`. 콘솔 0.

### 포지션 · 후기 **섹션 병합** (2026-08-21) — 좌 선언 / 우 마키

사용자: **"두 섹션을 합쳐주고, 리뷰 슬라이드는 멘트 옆에 배치"**, **"'거품 없는 견적, 타협 없는 퀄리티.' 멘트는 유지"**, **"리뷰 보더 곡선 없애고 네모 유지"**, **"리뷰 배경 진하게"**.

6차의 두 섹션(포지션 밴드 = 검정 · 후기 마키 = 라임)이 **한 섹션(라임)** 이 됐습니다. 렌더는 `components/sections/testimonial-marquee.tsx` 한 곳이고, `position-band.tsx` 는 **파일만 보존**(페이지 배열에서 빠짐)합니다.

- **좌 5/12** — eyebrow(REVIEWS) → 헤딩 "거품 없는 견적, / 타협 없는 퀄리티."(기존 문구 그대로) → **검증 스탯 2개 세로 스택**(8월 남은 티오 2건 · 불만족 시 환불 100%) → 예약제 안내. 스탯을 가로로 늘어놓으면 좁은 컬럼에서 숫자가 줄바꿈돼 세로로 쌓았습니다.
- **우 7/12** — 후기 마키가 **그 컬럼 안에서만** 흐릅니다(예전처럼 화면 전체 폭이 아닙니다). 좌우 끝은 기존 `.marquee` 의 mask 페이드가 그대로 잘라 줍니다. 카드 폭 420 → **340px**. 누르면 정지·드래그 동작은 공용 훅 그대로입니다(실측 확인).
- `lg` 미만은 위(헤딩+스탯) / 아래(마키) 스택.
- **`site.ts` 필드 정리**: `positionBand` + `reviewSection` → **`reviewBand` 하나**로 합쳤습니다. 그 사이 잠깐 있었던 후기 전용 헤딩·서브("포장 없는 후기…" / "이 자리는…")는 **삭제**했습니다.
- ⚠️ `enabled: false` 로 끄면 Services(흰) → Q&A(흰)가 붙습니다 — **예전 포지션 밴드와 달라진 점**이라 끌 때 밴드 교차를 다시 확인해야 합니다.

**후기 카드 직각** — `.card-surface`(라운드 16px)를 이 카드에서만 `rounded-none!` 로 덮었습니다(globals 규칙이 (0,2,0)이라 `!` 필요). 약속 카드와 안쪽 업종 배지(999px pill)는 그대로입니다.

**카드 위 라벨** — 사용자가 "실제 업체 대표님들의 후기입니다" 류를 원했지만 **후기 5건이 전부 자리표시라 "실제" 주장은 원칙 위반**입니다. `trustProof.testimonialsArePlaceholder` 로 두 문장을 자동 전환하게 했습니다: 지금은 **"함께한 대표님들의 이야기가 담길 자리입니다."**, 실후기를 넣고 플래그를 내리면 **"함께한 대표님들이 직접 남긴 이야기입니다."** 로 알아서 바뀝니다.

#### 배경 "진하게" — 두 안 비교 후 **진한 라임** 채택
| 안 | 결과 |
|---|---|
| **검정(void)로 올리기** | 🚫 **폐기**. `.card-surface` 는 흰 면인데 카드 **안 글자가 `--color-ink`(void 에서 흰색)라 흰 위 흰 글자로 사라졌다**(캡처 `merged-void.png`). 카드 안에서만 밴드 토큰을 뒤집는 예외를 만드는 것보다 밝은 밴드 유지가 안전 |
| **라임 한 단계 진하게** ✅ | `globals.css` 에 `.band-deep` — `color-mix(in srgb, var(--color-canvas) 86%, var(--color-ink))`. 팔레트에서 **파생**시켜 navy·colorLab 조합에서도 항상 "한 단계 진한 같은 색"이 된다 |

실측 밴드 **`#cbd696`**, 카드 대비 1.35 → **1.54**(카드가 더 도드라짐). 🚨 액센트 글자가 5.49 → **4.09:1** 로 떨어져(스탯 라벨 14px, AA 미달) `.band-deep .text-cta` 를 `color-mix(… 80%, #000)` 로 눌렀습니다 → **5.55:1**. 나머지 실측: 헤딩 12.23 · 스탯 숫자 12.23 · 보조 글자 5.51 · 카드 안 인용문 18.88(흰 면 대비).

#### FAQ 아이콘 — 🚨 **회전이 실제로는 안 돌고 있었다**
사용자 "+ → ×를 45° 회전으로". 확인해 보니 아이콘은 이미 두 선 SVG 한 글리프였는데, **transition 대상이 `transform` 이라 아무것도 애니메이션되지 않았습니다** — Tailwind v4 의 `rotate-45` 는 `transform` 이 아니라 **독립 `rotate` 속성**을 씁니다(실측: 열어도 `transform: none`, 회전이 즉시 튐). `transition-[rotate,color]` 로 바꾸고 180ms → **260ms `ease-out`** + `motion-reduce:transition-none`. 실측: 클릭 120ms 시점 **35.9°** → 완료 **45deg**, 색은 액센트로 전환. 캡처 `faq-closed.png` · `faq-open.png`.

---

### 카카오톡 채널 연결 (2026-08-21) — 하단 상담 바 버튼 교체

사용자: **"헤더·아래쪽 둘 다 '프로젝트 문의하기'라 중복이니 아래쪽(하단 상담 바)은 카카오톡 채널로."**
채널 URL 확정: **`https://pf.kakao.com/_nkKSX/friend`** → `site.ts` `site.chatbotUrl` (3차부터 열려 있던 TODO 해소).

- **하단 고정 상담 바**: `stickyConsultBar.primaryCta`("프로젝트 문의하기" → `#contact`) → **`kakaoCta`("카카오톡 상담")**. 말풍선 아이콘 + 라벨, `target="_blank" rel="noopener noreferrer"`. **"빠른 상담"(tel:)은 그대로**, **네비 "프로젝트 문의"(#contact)도 그대로** — 이제 바와 네비가 **서로 다른 채널**을 엽니다.
  - `site.chatbotUrl` 이 다시 null 이 되면 예전처럼 문의 폼으로 폴백합니다(메인 `#contact` / 서브 `/contact`, `kakaoCta.fallbackHref`).
- **Final CTA "카카오톡 상담"** 버튼도 같은 URL 로 열립니다(원래 `site.chatbotUrl ?? "#contact"` 구조라 **값만 채워지면 자동**이었습니다 — 코드 변경 없이 폴백이 해제됐습니다).

#### 면색 — 카카오 옐로 vs 액센트, 캡처 비교 후 **액센트 유지**
| 안 | 실측 | 판단 |
|---|---|---|
| 카카오 옐로 `#FAE100` + 글자 `#3B1E1E` | 글자 대비 11.39:1 · **면 대 흰 바 1.33:1** | 🚫 글자는 읽히지만 **버튼 경계가 흰 바에서 안 떨어지고**, 바가 뜨는 자리가 대개 **라임 밴드 위**라 노랑·연두가 싸운다. "유채색은 액센트 하나" 규칙도 깨진다 |
| **액센트 채움 `#6338EE`** ✅ | 글자 대비 **6.30:1** · 면 대 흰 바 **6.30:1** | 채널 정체는 **말풍선 아이콘 + 라벨**이 전달한다. 팔레트를 navy 로 되돌리면 이 버튼도 자동으로 주황이 된다 |

캡처: `kakao-bar-accent.png`(채택) · `kakao-bar-yellow.png`(비교안 — CDP 임시 주입, 소스 불변).

#### 링크 검사 갱신
외부 링크가 **1종 2건 추가**됐습니다 — 상담 바 · Final CTA 의 `https://pf.kakao.com/_nkKSX/friend`.
**의도된 외부 링크**이므로 "내부 서브페이지 0" 원칙에는 영향이 없습니다(원페이지 규칙은 *메인 밖 우리 페이지로 나가지 않는다*는 뜻입니다). 둘 다 `target="_blank" rel="noopener noreferrer"` 로 새 탭에서 열립니다.

실측(1536×745): 바 `visible` · "빠른 상담" `tel:01040061002` · "카카오톡 상담" `https://pf.kakao.com/_nkKSX/friend` (`_blank`/`noopener noreferrer`) · Final CTA 카톡 버튼 동일 · 네비는 `#work #services #process #contact` 그대로. `tsc`·`eslint` 통과 · 콘솔 0.

### 흐름 중간 CTA 2개 (2026-08-21) — 팀원 클로드의 CTA 평가 수용

지적: **세로 2,071~9,384px 구간에 CTA 가 0개**이고 Work~FAQ 6개 섹션이 다음 행동을 제시하지 않는다.
→ 권고를 받아들이되 **큰 밴드를 새로 만들지 않고** 섹션 안쪽 마무리 블록으로 두 군데만 넣었습니다(밴드 리듬 불변, 버튼은 액센트 채움 1개씩·인라인 톤 — 하단 고정 상담 바가 이미 전 구간을 덮으므로 과밀하지 않게).

| # | 자리 | 문구 | 버튼 | 실측 y |
|---|---|---|---|---|
| ① | **브릿지 밴드 안**(폰 옆 좌측 텍스트 아래) | "이런 결과물이 필요하신가요?" | 우리 홈페이지 문제 진단받기 → | **2,209** |
| ② | **Process 좌측 스티키 패널**(진행 라인 아래) — *2026-08-21 저녁 재배치, 아래 절* | ~~"이 과정 그대로 진행합니다."~~ **문구 삭제** | 상담 예약하기 → | 01~08 내내 고정 |

- **①의 위치 판단**: Work 섹션 하단이 아니라 **브릿지 밴드**를 택했습니다 — 바로 옆이 폰 목업(= 결과물)이라 "이런 결과물이" 라는 지시어가 화면에서 실제로 가리키는 대상이 있고, Work 섹션 하단에 두면 카드 3열 리듬을 끊습니다.
- 🚨 **새 주장·수치 없음** — 둘 다 화면에 이미 있는 사실만 말합니다(① 옆 목업 / ② 방금 읽은 8단계).
- 문구는 `site.ts` 의 **`inlineCta`** 한 곳에 있고, 이벤트 `bridge_cta_click` · `process_cta_click` 을 `lib/analytics.ts` 에 추가해 **어느 자리에서 문의로 넘어오는지** 구분합니다.
- 실측 CTA 분포(문서 높이 11,846): **2,209 → 8,675 → 10,610(전화·카톡)**. 지적된 공백이 메워졌습니다.
- **모바일 고정 바는 모바일 착수 시** 다룹니다(지금은 1024px 이상 전용 상담 바만).
- 캡처: `cta-bridge.png` · `cta-process.png`. `tsc`·`eslint` 통과 · 콘솔 0.

### 약속 + 서비스 **섹션 병합** (2026-08-21) — 좌 프로미스 / 우 서비스 · 카드 직각 통일

사용자: **"아워 프로미스와 서비스 시스템을 한 섹션으로 묶어서. 프로미스는 왼쪽, 서비스는 오른쪽 배치, 박스들 라운드 전부 제거."**

렌더는 `components/sections/service-system.tsx` 한 곳으로 합쳤고(`#services` 앵커를 그대로 물려받습니다), `promise-cards.tsx` 는 **파일만 보존**(배열에서 빠짐)합니다.

- **상단 공통** — 헤딩 1개 = 기존 **약속 헤딩**(`trustProof.heading` + `매출이 성장하는 구조` 액센트) + 그 아래 한 줄 설명 = 기존 **`serviceSystem.description`** 을 재배치. **새 카피 0줄**입니다.
- 🚨 **상단 eyebrow 는 두지 않았습니다** — 좌우 컬럼이 이미 `OUR PROMISE` / `SERVICE SYSTEM` 라벨을 달고 있어 위에 하나 더 붙이면 같은 말이 세 번 납니다(FAQ 에서 같은 이유로 걷어낸 전례). 지시의 "공통 eyebrow+헤딩"에서 **헤딩만** 취한 판단입니다.
- **비율 5 / 7** (실측 477px / 683px) — 약속 2장은 세로 스택, 서비스 4장은 2열이라 넓은 쪽이 서비스입니다. `lg` 미만은 위(약속) / 아래(서비스) 스택.
- **Services 헤딩("필요한 만큼 만들고…")은 화면에서 빠졌습니다** — 데이터는 `services.ts` 에 그대로 있어 되살릴 수 있습니다.
- 약속 카드는 여전히 `trustProof.promises` 의 **`enabled: true` 2건만**입니다(미확정 2건이 확정되면 왼쪽 컬럼이 채워집니다 — 지금은 좌측 아래가 다소 빕니다).

#### ~~좌우 끝선 정렬 (2026-08-21 보정)~~ → **같은 날 저녁 구도 재편으로 폐기** (아래 "병합 섹션 구도 재편" 절)

사용자: **"정렬 맞추고 싶고, 내용이 비어 보이진 않게. 시스템 부분만 튀어나와 있다."**
약속 2장이 서비스 4장(2×2) 높이를 **반씩 나눠** 채우게 했습니다 — 좌측 컬럼을 flex 로 세우고(`lg:h-full`) 카드에 `lg:flex-1`, 서비스 그리드에는 `auto-rows-fr`. 늘어난 높이가 비어 보이지 않게 카드 내용은 **세로 중앙 정렬** + lg 패딩 `p-10` + 설명 행간 1.8 로 풀었습니다(**새 카피 0줄**).
실측 — **1536×745·1920×1080 둘 다 좌우 컬럼 bottom 차이 0px**, 카드 6장 전부 높이 **350px**(약속 2 · 서비스 4). `lg` 미만은 스트레치가 풀려 자연 높이로 돌아갑니다. 캡처 `merged-aligned.png`(1536) · `merged-aligned-1920.png`.

#### 카드 라운드 0 — **`.card-surface` 자체를 직각으로 승격**
- `globals.css` 의 카드 언어에서 `border-radius: 16px` → **0**. 약속·서비스·후기 카드가 한 클래스를 쓰므로 **이 한 줄로 세 곳이 동시에** 직각이 됩니다. 오전에 후기 카드에만 걸어 뒀던 `rounded-none!` 훅은 **제거**했습니다(중복).
- **직각은 카드 박스에만** — 안쪽 **pill 배지**(업종 배지 `.trade-badge` · 서비스 카테고리 칩, 실측 999px)와 **체크 원형 아이콘**은 그대로입니다.
- 📌 **카드 직각이 사이트 기조가 됐습니다.** 남은 라운드(프로젝트 모달 · 폼 패널/입력창 · Q&A 대화창 패널 · 상담 바 버튼 10px · 폰 목업 프레임)는 **팀 확인 대기**입니다 — 일괄 직각화 여부를 정하면 그때 한 번에 갑니다.

#### 밴드 시퀀스 재실측 — 인접 동일색 0 (섹션 11개 → **9개**)
| # | 섹션 | 밴드 | 실측 |
|---|---|---|---|
| 1 | Hero | void | `#000000` |
| 2 | Selected Work | paper | `#ffffff` |
| 3 | 브릿지(폰 2대) | void | `#000000` |
| 4 | **약속 + Services (병합)** | **paper** | **`#ffffff`** |
| 5 | 병합(포지션+스탯+후기) | mist·deep | `#cbd696` |
| 6 | Q&A | paper | `#ffffff` |
| 7 | Process | void | `#000000` |
| 8 | FAQ | paper | `#ffffff` |
| 9 | Final CTA(·푸터) | void | `#000000` |

병합 섹션을 **흰(paper)** 으로 둔 이유: 앞이 브릿지(검정), 뒤가 리뷰(진한 라임)라 흰이 그 사이를 끊습니다(라임으로 두면 5번과 연달아 붙습니다).
문서 세로 **11,846 → 11,177px**(섹션 하나가 줄며 −669px). 병합 섹션 높이 1,308px.

검증: `tsc`·`eslint` 통과 · **콘솔 0** · 캡처 `merged-promise-services.png`(상단) · `merged-promise-services-b.png`(하단 4장) · `full-merged2.png`(전체 세로).

### Process 밴드 검정화 + CTA 재배치 + 카카오 브랜드색 고정 (2026-08-21 저녁)

#### ① Process 밴드 라임 → **검정(void)**
사용자: **"Process 섹션을 '말로 설명하지 않습니다'(브릿지) 부분과 배경색 통일."**
`process.tsx` 의 `band` 만 바꿨습니다. 검정 위 색은 **전부 밴드 토큰이 알아서 뒤집습니다** — 단계 숫자·현재 단계 강조는 void 파생 액센트 `#9b7dff`(검정 위 **6.77:1**), 비활성 단계는 `#7d7d7d`(**5.10:1**), 진행 라인 트랙은 흰 헤어라인.

**밴드 시퀀스 재실측 — 인접 동일색 0** (`main [data-band]` 순서대로)
| # | 섹션 | 밴드 | 실측 |
|---|---|---|---|
| 1 | Hero | void | `#000000` |
| 2 | Selected Work | paper | `#ffffff` |
| 3 | 브릿지(폰 2대) | void | `#000000` |
| 4 | 약속 | mist | `#eaf6ad` |
| 5 | Services | paper | `#ffffff` |
| 6 | 병합(포지션+스탯+후기) | mist·deep | **`#cbd696`** |
| 7 | Q&A | paper | `#ffffff` |
| 8 | **Process** | **void** | **`#000000`** |
| 9 | FAQ | paper | `#ffffff` |
| 10 | Final CTA(·푸터) | void | `#000000` |

#### ② "상담 예약하기" — 섹션 끝 → **스티키 패널 안, 진행 라인 아래**
사용자(캡처 첨부): **"'이 과정 그대로 진행합니다.' 문구 삭제, 버튼만 라인 아래쪽 배치, 1번 단계부터 버튼 보이게."**
- 렌더 위치가 `process.tsx`(섹션 끝) → **`process-flow.tsx` 좌측 스티키 패널**로 옮겨졌습니다. 패널이 sticky 라 **01~08 스크롤 내내 따라옵니다**.
- 🚨 버튼은 큰 숫자 블록(`aria-hidden` · `hidden lg:block`) **밖**에 둡니다 — 안에 넣으면 보조기술에서 사라지고 lg 미만에서 같이 숨습니다. 그래서 모바일에서는 섹션 헤더 바로 아래에 섭니다.
- 문구(`inlineCta.process.note`)는 **삭제**했습니다. `site.ts` 에는 `label`·`href` 만 남고 지워진 문장은 주석에 기록해 뒀습니다.
- 🚨 **세로 예산**: 패널이 **하단 상담 바(top 672)** 에 가리면 안 됩니다. 여백을 `mt-14→mt-10`(숫자 블록) · `mt-8→mt-6`(진행 라인 행·버튼)으로 줄여 총 높이 **541 → 509px**.
  실측(1536×745): 섹션 진입 시 버튼 **624~668**(바까지 4px 여유) · 스티키 고정 후 **593~637** · 단계 08 **296~340**. 세 지점 모두 `btnFullyVisible` · `btnClearOfBar` **true**. ⚠️ 이 패널의 mt 를 다시 키우면 단계 01 에서 버튼이 바 뒤로 숨습니다.
- 캡처: `process-step01.png` · `process-step08.png`.

#### ③ 카카오톡 버튼 — **브랜드색 고정** (직전 판단을 사용자가 뒤집음)
사용자 확정: **"카카오톡 상담 버튼은 항상 카카오톡 색상으로 고정."**
- 하단 상담 바 · Final CTA 두 곳 모두 **옐로 `#FAE100` + 글자 `#3B1E1E`**(말풍선 아이콘 포함). 색은 `globals.css` 의 **`.btn-kakao`** 한 곳에 있고 **액센트 토큰(`--color-cta`)을 쓰지 않습니다** → `bandedPalette` 를 navy 로 바꾸든 colorLab 으로 실험하든 **이 버튼만은 불변**입니다(colorLab 주입 규칙이 노리는 `.bg-cta` 클래스를 아예 달지 않습니다).
- 흰 상담 바 위에서 옐로 면이 배경과 **1.33:1** 이라 경계가 흐려지는 건 **옅은 보더(검정 8%)** 한 겹으로만 잡았습니다(그림자 추가 없음).
- 선택자를 `.btn-kakao.btn-kakao`(0,2,0)로 두 번 써서 같은 자리 Tailwind 유틸리티(0,1,0)를 순서와 무관하게 이깁니다.
- 실측: 두 버튼 다 면 `#fae100` · 글자 `#3b1e1e` **11.39:1** · Final CTA 부제 `rgba(59,30,30,.75)` **11.48:1** · href `https://pf.kakao.com/_nkKSX/friend` · `target="_blank"`. 캡처 `kakao-bar-brand.png` · `kakao-cta-brand.png`.
- ⚠️ 위 "면색 — 카카오 옐로 vs 액센트" 절의 **액센트 유지 결론은 이 지시로 폐기**됐습니다(판단 근거는 기록으로 남깁니다).

`tsc`·`eslint` 통과 · 콘솔 0.

### 브릿지 폰 **V자 배치** (2026-08-21 저녁)

사용자: **"휴대폰 두 개 V자로 바꿔서 서로의 화면을 덜 가리게."**

- 이전 값(앞 `rotateZ(2deg)` / 뒤 `-1.5deg`)은 두 폰 **윗변이 서로 모이는 Λ 자**였고 앞 폰이 뒤 폰 화면을 **폭의 32%** 덮었습니다. 부호를 뒤집어 **아래는 가깝고 위는 벌어지는 V** — 앞(왼쪽) **`rotateZ(-7deg)`** / 뒤(오른쪽) **`rotateZ(7deg)`**. `rotateY`(서로 마주보기)·크기 차·`brightness(.85)` 는 그대로입니다.
- 🚨 세로로 긴 요소를 7° 돌리면 **가로 점유폭이 `h·sin7° ≈ 50px` 늘어납니다** — 그래서 그룹 폭 `max-w-350 → 430px`, 폰 높이 `94/84% → 88/80%` 로 함께 조정했습니다(실측으로 잡은 값).
- **가림 실측: 72.6px(32%) → 8px = 뒤 폰 폭의 3.4%** (목표 ≤12%). 뒤 폰 화면이 거의 다 보입니다.
- 정적 회전은 `FRONT_ROTATE`/`BACK_ROTATE` 상수와 **클래스 두 곳에 같은 값**으로 들어갑니다(SSR·no-JS 대비). 마우스 틸트가 transform 을 다시 쓸 때도 이 상수를 뒤에 붙이므로 **합성이 깨지지 않습니다** — 실측 `rotateX(1.09deg) rotateY(4.11deg) rotateY(-10deg) rotateZ(-7deg)`(앞) / `rotateX(-1.3deg) rotateY(-5.48deg) rotateY(8deg) rotateZ(7deg)`(뒤). 등장 애니(양쪽 모임)는 바깥 래퍼 담당이라 그대로입니다.
- 컬럼 침범 없음 — 폰 좌우 끝 **892~1366px**, 컬럼 **891~1368px** 안(섹션 `overflow-hidden` 유지). 캡처 `bridge-v.png`.

### 병합 섹션 **구도 재편** (2026-08-21 저녁) — 프로미스는 헤딩 옆, 시스템은 아래

사용자: **"구도가 안 예쁨. 프로미스는 텍스트(헤딩) 옆에 박스 없이, 시스템은 텍스트 밑에."**

- **상단 2단 (좌 7 / 우 5)** — 좌: 공통 헤딩 + 설명(현행 그대로) / 우: `OUR PROMISE` 라벨 + **박스 없는 약속 리스트**(체크 아이콘 + 제목 + 한 줄, 항목 사이 헤어라인, 위아래 `border-y`). **카드·배경·그림자 0** — 실측 `background rgba(0,0,0,0)` · `box-shadow none`.
  - `OUR PROMISE` 라벨은 **유지**했습니다 — 리스트가 면 없이 서 있어 라벨이 없으면 헤딩 옆 부속 문장처럼 읽힙니다.
- **하단 전체 폭** — `SERVICE SYSTEM` 라벨 + 서비스 **4열 직각 카드**. 2×2 안과 비교해 **4열 채택**: 폭 285px 에서 카테고리 칩이 한 줄에 하나씩 정확히 들어가고(칩 블록 높이 130px로 4장 동일), 카드 높이도 350px로 균일합니다. 2×2 는 카드 폭이 620px대라 칩 4개가 한 줄에 몰리고 카드가 가로로 늘어져 빈 느낌이 납니다.
- 🚨 **헤딩 줄바꿈 처리** — 원문 `trustProof.heading` 의 하드 줄바꿈(`\n`)은 전체 폭(1200px)에서는 딱 2줄이지만 **절반 폭(683px)에서는 각 줄이 한 번 더 접혀 4줄**이 되고 "기획하고" 같은 한 마디 줄이 남습니다(실측). 이 컬럼에서만 `whitespace-normal` + **`text-wrap: balance`** 로 흘려 **줄 길이가 고른 2줄**로 세웠습니다. **문구는 한 글자도 바꾸지 않았습니다.**
- 직전의 **좌우 끝선 스트레치 로직은 제거**했습니다(카드가 한쪽에만 있어 맞출 끝선이 없습니다) — 약속 리스트·서비스 카드 모두 자연 높이입니다.
- 밴드 **흰(paper) 유지**, 인접 동일색 0. 섹션 높이 1,336 → **1,054px**, 문서 세로 11,177 → **10,924px**.
- 실측(1536·1920 동일): 카드 4장 285×350px · `border-radius 0px` · 약속 항목 2건 · 라벨 2개(OUR PROMISE / SERVICE SYSTEM). 캡처 `merged-v2.png`(상단) · `merged-v2-b.png`(카드 4열) · `merged-v2-1920.png`.

### 후기 카드 확대 (2026-08-21 저녁)

사용자: **"후기 박스가 너무 작다."** 병합하며 420 → 340px 로 줄였던 카드를 되돌렸습니다.

- 카드 폭 340 → **440px**, 패딩 24 → **28px**, 별점 15 → **17px**. 인용문은 body-m(17px) 그대로이고 **최소 3줄 높이**(`min-h-[5.25em]` = 1.75 × 3, 실측 89px)를 줘서 짧은 후기가 들어와도 카드 높이가 들쭉날쭉하지 않게 했습니다.
- **좌 5 / 우 7 → 좌 4 / 우 8** 로 조정했습니다(판단): 5/7 로 두면 마키 폭 683px 에 440px 카드가 **1.55장**만 들어와 두 번째 카드가 반도 안 보입니다. 4/8(좌 368 / 우 784)이면 **1.78장**이라 다음 카드가 절반 이상 드러나 흐름이 읽힙니다. 좌측 헤딩은 368px 에서도 의도한 2줄을 유지합니다(clamp 폰트).
- 실측(1536×745): 카드 **440×272px** · 마키 뷰포트 784px · 한 화면 **1.78장** · 콘솔 0. 캡처 `reviews-bigger.png`.

### 후기 섹션 전폭 마키 + 브릿지 폰 ∧ (2026-08-21 밤)

#### ① 마키가 좁아 카드가 하나만 보이던 문제 → **전폭 블리드**
사용자: **"마키가 좁아 카드가 하나만 보인다."** 좌 4 / 우 8 나란히 구도를 **폐기**하고 셸 시절 방식으로 돌아갔습니다.
- **상단(Container 안)** — eyebrow → 선언 헤딩 → 검증 스탯 2개 → 예약제 안내 → **마키 캡션 한 줄**. 컬럼 분할 없이 한 덩어리입니다.
- **하단(Container 밖)** — 마키가 **뷰포트 전체 폭**으로 흐릅니다. 좌우 끝은 기존 `.marquee` 의 mask 페이드 그대로.
- 캡션 자리 판단: 처음엔 우측 컬럼(오른쪽 정렬)에 뒀는데 헤딩과 멀어 떠 보여서(캡처 비교) **마키 바로 위**로 내렸습니다.
- 실측(1536): 마키 폭 **0~1536px**(뷰포트 전체) · 카드 440px · **3.36장 동시 노출**(이전 1.78장). 누름 정지·놓으면 재개·드래그 전부 정상(실측 true/true/true).

#### ② 후기 카드 세로 확대
패딩 28 → **32px**(`p-8`), 인용문 최소 높이 `min-h-[5.25em]`(3줄) → **`min-h-[7em]`(4줄)**, 인용문 위 여백 `mt-4` → `mt-5`. 실측 **440×314px**(이전 272px).

#### ③ 브릿지 폰 — V자 → **∧(서로 살짝 바라보게)**
사용자: **"서로 살짝 바라보게."** 앞(왼쪽) `rotateZ(-7deg)` → **`rotateZ(4.5deg)`**, 뒤(오른쪽) `rotateZ(7deg)` → **`rotateZ(-4.5deg)`** 로 부호를 뒤집어 윗변이 서로를 향합니다.
- **3° vs 4.5° 두 안 캡처 비교 후 4.5° 채택** — 3°(`bridge-lean-3.png`)는 거의 평행으로 읽혀 "바라본다"는 인상이 안 나고, 4.5°(`bridge-lean-45.png`)는 여전히 "살짝"이면서 수렴이 눈에 잡힙니다.
- 회전이 얕아져 가로 점유폭이 줄어든 덕에 **가림 0px(0%)** 입니다(V자 때 8px/3.4%, 그 이전 Λ 때 72.6px/32%). 목표 ≤12% 충족.
- 폰 좌우 끝 **901~1358px**, 컬럼 **891~1368px** 안(침범 없음). 정적 회전은 `FRONT_ROTATE`/`BACK_ROTATE` 상수와 클래스 두 곳을 같이 고쳤고, 마우스 틸트·등장 애니(양쪽 모임)는 그대로입니다.

검증: `tsc`·`eslint` 통과 · **콘솔 0** · 캡처 `reviews-wide.png` · `bridge-lean-3.png` / `bridge-lean-45.png`.

> ⚠️ **③ 의 ∧ 방향은 바로 아래 절에서 뒤집혔습니다** — 사용자가 원한 것은 **아랫변이 서로를 향하는 V** 였습니다.

### 브릿지 폰 **V 확정** + 후기 스탯 **헤딩 옆으로** (2026-08-21 밤, 정정)

#### ① 폰 방향 정정 — ∧ → **V**, 그리고 가림을 다시 만듦
사용자 정정: **"아래쪽이 서로를 향하는 모양(아래가 모이고 위가 벌어지는 V), 가림은 0~100 기준 5~10, 각도 4 언저리."**
- 부호 반전 — 앞(왼쪽) `rotateZ(4.5deg)` → **`rotateZ(-4.5deg)`**(반시계) / 뒤(오른쪽) `-4.5deg` → **`+4.5deg`**(시계). 각도 크기는 4.5° 유지("각도 4 언저리"), `rotateY`(서로 마주보기)·크기 차·`brightness(.85)` 그대로. 실측 matrix3d 로 확인 — 앞 `sinZ −0.0785` / 뒤 `+0.0785`.
- 🚨 **4.5° 는 얕아 가로 점유폭이 거의 안 늘어나 두 폰이 전혀 안 겹칩니다**(∧ 때 가림 0%). 그래서 이번엔 각도가 아니라 **그룹 폭**으로 가림을 만들었습니다 — `max-w-[430px]` → **`405px`**. 폰 크기는 그대로입니다(높이는 컨테이너 500px 기준 %, 폭은 `aspect-ratio` 파생이라 폭만 줄이면 두 대가 가까워질 뿐입니다).
- **폭 스윕 실측**(뒤 폰 폭 213.4px 기준): 430px → 0px(0%) · **410px → 10.1px(4.7%)** · **405px → 15.1px(7.1%) ✅ 채택** · 400px → 20.1px(9.4%) · 390px → 30.1px(14.1%) · 380px → 40.1px(18.8%). 목표 5~10% 의 한가운데인 405px 로 잡았습니다.
- 침범 없음 — 폰 좌우 끝 **913.7~1345.4px**, 우측 컬럼 **891.3~1368px** 안(섹션 `overflow-hidden` 유지). 마우스 틸트·등장 애니(양쪽 모임)는 손대지 않았습니다. 정적 회전은 이전과 같이 `FRONT_ROTATE`/`BACK_ROTATE` 상수와 **클래스 두 곳**에 같은 값으로 들어갑니다(SSR·no-JS 대비).
- 캡처 `bridge-v2.png`.

#### ② 후기 섹션 스탯 — 헤딩 아래 세로 스택 → **헤딩 옆 가로 배치**
사용자: **"남은 티오·불만족(환불) 스탯을 옆으로 보내자."** 전폭 마키로 바꾸며 스탯을 헤딩 아래에 쌓았더니 상단 블록이 길어져 마키가 밀렸습니다. **1~3차 `WHY SUMIM` 2단과 같은 구도**로 되돌립니다.
- **좌**: eyebrow + 선언 헤딩(`lg:max-w-[640px]`) / **우**: 스탯 2개 **가로**(사이 세로 헤어라인 `sm:divide-x`, 좌우 패딩 32/40px) + 그 아래 예약제 안내. 두 단은 `lg:flex lg:items-end` 로 **아랫단 정렬**입니다.
- 예약제 안내(`trustProof.note`)는 **스탯 아래 작게** — `text-body-m mt-8` → **`text-caption mt-5`**. 문구는 한 글자도 안 바꿨습니다.
- `lg` 미만은 그대로 세로로 흐르고, 스탯끼리도 `sm` 미만에서는 세로 스택입니다(좁은 폭에서 숫자 줄바꿈 방지 — 숫자에 `whitespace-nowrap` 추가).
- **상단 블록 높이 590.3 → 244px**(−346). 섹션 높이 **1,115.8 → 769.6px**, 문서 세로 **11,427 → 11,081px**. 마키 시작이 섹션 상단 기준 **346px 만큼 위로** 올라와(726 → 360~380px, 리빌 진행 상태에 따라 ±20px) 첫 화면에 카드가 더 일찍 걸립니다.
- 실측(1536×745): 스탯 2개 같은 행(`top` 동일) · 헤딩 오른쪽 밖 · 스탯 박스 117×107 / 119×107px · 마키 전폭 0~1536px 유지. 캡처 `reviews-stats-side.png`.

검증: `tsc`·`eslint` 통과 · **콘솔 에러 0**(dev 의 `next/image` quality 경고 11건은 이 작업과 무관한 기존 설정 경고입니다).

### 색상 조합 **재큐레이션** — 색 8→5 · 톤 6→4 (2026-08-21 밤)

사용자: **"색상 조합이 전보다 이상해졌다."** 선택지를 늘리는 대신 **덜어 냈습니다**. `src/lib/palette.ts` 와 스탠드얼론 `스밈 컬러 시뮬레이터.html` **양쪽에 같은 값**이 들어갑니다(엔진이 같아야 팀이 시뮬레이터에서 본 조합과 사이트가 일치합니다).

#### 색 — 5종만 남김
| 키 | 이름 | dark / light / mist / accent |
|---|---|---|
| `team` | 블랙 × 라임 × 바이올렛 | `#000000` / `#ffffff` / `#eaf6ad` / `#6338ee` |
| `navy` | 네이비 × 주황 | `#141d33` / `#ffffff` / `#f0f3f8` / `#ea580c` |
| `forest` | 딥그린 × 크림 | `#12291f` / `#fdfbf4` / `#eef0e4` / `#e8632b` |
| **`terrared`** 🆕 | 테라레드 × 크림 | `#242222` / `#ffffff` / `#F9ECE5` / `#D73220` |
| **`berry`** 🆕 | 차콜 × 베리 | `#424242` / `#ffffff` / `#CCCCCC` / `#BA1650` |

- 뺀 것 5종: `violet`(team 액센트와 같은 보라라 인상이 겹침) · `ink`(navy 와 거의 같은 남색) · `terra`(신규 `terrared` 와 같은 테라코타 축, 채도가 낮아 밀림) · `mono`(미스트 위 포인트 대비 **2.8** 로 전체 최하) · `limeinv`(미라 피드백 "팀 예시와 같은 색의 재배치일 뿐, 배치가 이상함" — team 과 색 4개가 같고 light/mist 만 맞바꾼 조합).
- 🚨 `terrared`·`berry` 는 **미라 제공 팔레트**입니다. 원본은 "색 4개" 목록이라 어느 색이 어느 슬롯인지 정해져 있지 않았고, **4슬롯 매핑은 페이블 확정**입니다 — 가장 어두운 색 → dark / 흰색 → light / 남은 연한 색 → mist / 가장 채도 높은 색 → accent.
- `team` 표시명을 **"블랙 × 라임" → "블랙 × 라임 × 바이올렛"** 으로 바꿨습니다(액센트가 바이올렛인데 이름에 없어 팀이 라임만 있는 조합으로 읽었습니다). 값은 그대로입니다.

#### 톤 — 4종만 남김
`base`(기준) · `warm`(따뜻·친근) · `premium`(고급·프리미엄) · `clean`(깔끔·미니멀).
뺀 둘이 "이상해진" 주범입니다 — **`lively`**(cS **+20** / cL +12 → 액센트가 형광으로 튐) · **`calm`**(cL +9 인데 cS −13 이라 다크 밴드가 뿌옇게 뜸). 남은 넷은 채도 이동이 −16~+5 로 좁아 어느 색을 골라도 원래 인상이 유지됩니다.

#### 🚨 대비 전수 검사 (5색 × 4톤 = 20조합, colorLab 패널 실측)
- **6개 검사 항목 중 5개는 20조합 전부 통과**했습니다(다크/라이트/미스트 밴드 글자 · 포인트 버튼 글자 · 다크 위 포인트 글자). 최저값도 다크 밴드 글자 6.5(차콜×베리×따뜻·친근)입니다.
- **미달은 `미스트 위 포인트` 한 항목뿐이고 20조합 중 15개**입니다(2.2~4.2). **기존 자동 파생 규칙은 이걸 못 잡습니다** — `compose()` 가 파생시키는 것은 `accentBtn`(버튼 **면**)과 `accentOnDark`(다크 밴드 위 **글자**) 둘뿐이고, **미스트 밴드 위 액센트 글자용 파생은 아예 없습니다**.
- ⚠️ **이건 새 프리셋이 만든 문제가 아닙니다.** 재큐레이션 전 9종에서도 8종이 미달이었고(`mono` 2.8 · `forest` 2.9 · `navy` 3.2 · `ink` 3.3 · `terra` 3.6), 통과하는 건 `team`(5.5)뿐이었습니다. 신규 2종은 오히려 상위권입니다(`terrared` 4.2 · `berry` 3.9).
- 실제 영향 지점: 라임(mist) 밴드의 **스탯 라벨·별점 아이콘**(`text-cta`). → **2026-08-22 별건으로 해소**(바로 아래 절).
- 조합별 전체 수치는 scratchpad `sweep20.log`.

### 미스트 위 액센트 **자동 파생** — `accentOnMist` / `accentOnMistDeep` (2026-08-22)

위 절에서 유일하게 남아 있던 미달 항목(`미스트 위 포인트`, 20조합 중 15개)을 파생 규칙으로 닫았습니다.

#### ① `compose()` 에 파생 2종 추가 (`src/lib/palette.ts`)
```
accentOnMist      = 미스트 배경 위 4.5 확보까지 액센트 명도를 **1.5씩 내린** 값
accentOnMistDeep  = 같은 규칙을, 한 단계 진해진 배경(.band-deep) 기준으로 다시 계산한 값
```
- 방향은 **밴드 밝기로 정합니다** — 밝은 미스트(전 프리셋)면 내리고, 커스텀 4슬롯으로 어두운 미스트를 넣으면 `accentOnDark` 처럼 올립니다. 스텝 1.5 는 `accentBtn` 파생과 같은 값이라 눌린 정도가 버튼 면과 같은 결로 보입니다.
- 🚨 **`accentOnMistDeep` 이 없으면 반쪽짜리였습니다.** 실측해 보니 **사이트의 미스트 밴드 위 `.text-cta` 는 전부 `.band-deep` 안**에 있습니다(병합된 리뷰 섹션이 유일한 미스트 밴드이고 그게 `.band-deep`). 평평한 미스트 기준으로만 파생하면 **검수 패널만 20/20 이 되고 화면은 그대로 미달**인 상태가 됩니다 — 실제로 중간 검증에서 `.band-deep` 실측 대비가 **최저 2.53 · 14/20 미달**로 나왔습니다.
- 진한 배경 계산은 globals 의 `color-mix(in srgb, var(--color-canvas) 86%, var(--color-ink))` 와 **같은 식**(`mix(bandTokens(mist).ink, mist, 0.14)`)을 씁니다 — 여기서 어긋나면 화면과 검수가 갈립니다.

#### ② 적용부 3곳
| 자리 | 무엇을 |
|---|---|
| `color-lab.tsx` 주입 CSS | `[data-band="mist"] .text-cta` → `accentOnMist` / `[data-band="mist"].band-deep.band-deep .text-cta` → `accentOnMistDeep`(globals 의 `.band-deep` 규칙 0,6,1 을 0,7,1 로 이깁니다) |
| `color-lab.tsx` 검수 패널 | `미스트 위 포인트` 를 **원색이 아니라 파생색 기준**으로 재고, **`진한 미스트 위 포인트` 줄을 하나 추가**(총 7줄)했습니다 — 화면에 실제로 있는 자리를 숨기지 않기 위해서입니다. `CSS 복사` 블록에도 `--accent-on-mist` · `--accent-on-mist-deep` 이 나갑니다 |
| `globals.css` 기본 팔레트 | 기본(navy) 미스트 `.text-cta` → `#c0460c`(3.20 → **4.58**) · 기본 `.band-deep .text-cta` → `#9d370b`(3.55 → **4.75**) |

#### 🚨 team(현재 팔레트)은 **화면이 한 픽셀도 안 바뀝니다**
- 평평한 미스트: 라임 `#eaf6ad` 위 바이올렛 `#6338ee` 가 이미 **5.49:1** 이라 파생이 한 스텝도 안 내려갑니다(파생 결과 = 원색).
- 진한 미스트: 기존 `color-mix(var(--color-cta) 80%, #000)` 이 **5.55:1** 로 이미 통과라 **그 식을 그대로 뒀습니다**. 기본 규칙을 navy 값으로 바꾼 대신 `[data-palette="team"]` 한 겹을 더한 규칙으로 team 만 예전 식을 유지합니다.
- **픽셀 증명**: 리뷰 섹션 상단 블록(스탯 라벨 = 미스트 액센트 글자가 있는 자리)을 클립해 **규칙 ON / 런타임 무력화 두 장을 캡처 → SHA-256 일치**. ⚠️ 전체 세로 캡처는 마키가 **rAF 로 구동**돼 `animationPlayState` 로 못 멈춥니다 — 프레임이 달라 SHA 가 어긋나므로 **마키를 뺀 영역으로 클립**해야 합니다(첫 시도에서 헛발질).

#### 검증 — 20조합 전수 재검수
- **검수 패널 7항목 × 20조합 = 140칸 전부 4.5 이상**(최저 4.5). 항목: 다크/라이트/미스트 밴드 글자 · 포인트 버튼 글자 · 미스트 위 포인트 · **진한 미스트 위 포인트** · 다크 위 포인트 글자.
- **화면 실측**(`.band-deep` 의 렌더된 글자색 vs 렌더된 배경색): **최저 4.51 · 미달 0건**(적용 전 최저 2.53 · 미달 14건).
- 1조합 실측(네이비 × 따뜻·친근): 미스트 스탯 라벨이 주입된 파생색을 실제로 사용 — 적용 전 2.79 → **4.5**.
- 스탠드얼론 `스밈 컬러 시뮬레이터.html` 도 같은 파생 2종 + 검수 7줄 + `--accent-on-mist(-deep)` 토큰으로 맞췄습니다(미리보기 미스트 밴드의 `.accent`·스탯 라벨이 파생색을 씁니다).
- `tsc`·`eslint` 통과 · **콘솔 에러 0**. 캡처 `onmist-navy-warm.png`(패널) · `onmist-clip-{after,before}.png`(픽셀 동일 증명). 로그 `onmist-final.log` · `onmist2-final.log`.
- ⚠️ 함정 하나 — `color-lab.tsx` 의 주입 CSS 는 **템플릿 리터럴** 안이라 주석에 백틱을 쓰면 문자열이 끊깁니다(`TS1005`). 코드명은 맨글자로 적으세요.

#### localStorage 폴백
없어진 키가 저장돼 있어도 오류가 없어야 합니다. `color-lab.tsx` 의 **`readStored()` 에서 씻어 냅니다** — 렌더 쪽 `HUES.find(..) ?? HUES[0]` 폴백만으로는 저장값이 `mono` 로 남아 **패널에서 아무 항목도 선택 표시가 안 되는** 상태가 됐습니다. 실측: `{hue:'mono', tone:'lively'}` 를 심고 새로고침 → 패널 선택 표시 **"블랙 × 라임 × 바이올렛 / 기준"**, 저장값도 `{"hue":"team","tone":"base"}` 로 정정됨, 콘솔 에러 0. 스탠드얼론 HTML 은 원래 `HUES[0]/TONES[0]` 폴백이 있어 그대로입니다.

캡처: `palette-terrared.png` · `palette-berry.png`(둘 다 대비 검수 패널이 보이게 스크롤한 상태).

### 서비스 카드 **좌우 2단** · 모바일 리뷰 스탯 **헤딩 옆** · 예산 **셀렉트** (2026-08-22)

사용자 지시 3건입니다. 셋 다 **새 카피 0줄**이고 데이터·문구는 그대로입니다.

#### ① 서비스 카드 — 체크리스트를 **옆으로** (`sections/service-system.tsx`)
카드 안 구성을 상(제목·설명) / 하(체크 4) → **좌(아이콘·번호·영문명·제목·한 줄 설명) / 우(체크 4)** 로 바꿨습니다.

- 비율은 `basis-0` + **`grow-[55]` / `grow-[45]`** 입니다 — 퍼센트 `basis` 는 `gap` 을 못 빼서 카드 밖으로 넘칩니다. 두 컬럼 다 **`min-w-0`** 이라야 긴 한글 라벨이 카드를 밀지 않습니다.
- 좁은 폭에서는 패딩·간격을 한 단계 줄였습니다(`p-7 → p-6` · `gap-4`, `sm:` 부터 원래 값). 체크 라벨(`cardItems`)은 **전부 한 줄 유지**를 캡처로 확인했습니다.
- **모바일 390 실측: 카드 350×355 → 350×234px(−34%)** · 카드 그리드 1,480 → **995px** · 병합 섹션 2,299 → **1,814px**(−485) · 문서 세로 14,434 → 13,949px.
- **768(2열) 도 같은 좌우 2단**입니다 — 342×246px 4장, 라벨 한 줄. 문서 12,234 → 11,833px.

##### 🚨 데스크톱(1536)은 **현행 4열 유지** — 두 안 실측 비교 후 결정
지시대로 `2col-wide`(2열 와이드 카드 × 좌우 2단) 안을 실제로 만들어 캡처·실측했고, **섹션 총높이가 더 압축되는 쪽**을 골랐습니다.

| 안 | 카드 | 카드 그리드 | **섹션 총높이** | 문서 세로 |
|---|---|---|---|---|
| **4열 × 세로 구성 ✅ 채택** | 285×370px | 370px(1행) | **1,074px** | **11,009px** |
| 2열 와이드 × 좌우 2단 | 590×238px | 496px(2행) | 1,200px (**+126**) | 11,135px (+126) |

2열 와이드는 카드 하나는 낮아지지만 **행이 2줄이 되어 그리드가 오히려 126px 길어집니다**. 캡처(`a1-desk2w-services.png`)를 보면 590px 폭에서 좌 텍스트가 3줄 안에 끝나 **카드 오른쪽 아래가 크게 빕니다**. 285px 4열은 좌우 2단이 물리적으로 안 들어가므로(체크 라벨 한 줄 = 최소 ~110px), `lg` 이상에서만 세로 구성으로 되돌립니다.
- 스위치는 파일 상단 상수 **`DESKTOP_CARDS: "4col" | "2col-wide"`** 하나입니다. 값만 바꾸면 즉시 뒤집힙니다(lg 미만은 스위치와 무관하게 항상 좌우 2단).
- **데스크톱 회귀 실측: 문서 11,009px · 카드 285×370 · 섹션 1,074px — 작업 전과 완전히 동일**합니다.

#### ② 모바일 리뷰 스탯 — 헤딩 **옆** (`sections/testimonial-marquee.tsx`)
`lg:` 접두사를 떼어 **전 폭에서 좌 헤딩 / 우 스탯** 2단이 됐습니다(데스크톱 구도를 모바일까지 내린 것).

- 스탯끼리는 `sm` 미만에서 **세로 2개 소형** — 숫자 `clamp(2.25rem…)` → **1.75rem(28px)**, 라벨·노트 간격 한 단계 축소. `sm` 이상은 예전 가로 배치·clamp 그대로입니다.
- 🚨 **스탯 컬럼은 `sm` 미만 고정 폭 `w-[104px]`** 입니다. `shrink-0` 만 주면 컬럼 폭이 예약제 안내 문장의 max-content(≈220px)로 벌어져 **헤딩이 110px 로 짓눌립니다**. 104px 는 가장 긴 라벨("불만족 시 환불" ≈94px)이 한 줄로 들어가는 최소치이고, 남는 230px 에서 헤딩이 의도한 **2줄**로 섭니다(실측 230×80px).
- 🚨 **헤딩 아래 가로 2열 절충안은 쓰지 않았습니다** — 390px 에서도 지시한 "헤딩 옆"이 그대로 들어갔습니다(위 폭 계산). 예약제 안내(`trustProof.note`)도 104px 컬럼 안에서 2줄로 끝나 별도 처리가 필요 없었습니다.
- **실측(390×844): 리뷰 섹션 961 → 792px(−169)** · 마키 시작 6,512 → 5,858px · 430 도 793px 로 같은 구도.

##### 🚨 상단 마감 — 제목 **중앙 정렬** + 스탯 **우측 끝** (같은 날, 사용자 주석 캡처)
사용자 주석(빨간 박스 = eyebrow+헤딩 / 노란 박스 = 스탯+안내)에 따른 후속 지시이고, **"모바일 버전만이야"** 로 범위가 확정됐습니다.

| 자리 | `lg` 미만 (변경) | `lg` 이상 (불변) |
|---|---|---|
| 제목 블록 | **텍스트 중앙 정렬**(`text-center`) — eyebrow 도 `justify-center` | `lg:text-left` · `lg:justify-start` |
| 세로 정렬 | **`items-center`**(스탯 블록과 가운데 맞춤) | `lg:items-end`(아랫단 정렬) |
| 스탯 + 예약제 안내 | **`text-right`**(컬럼 안 오른쪽 변 정렬) | `sm:text-left` |
| 가로 배치 | **`justify-center`** — [헤딩+스탯]을 한 덩어리로 중앙(아래 절) | `lg:justify-between` |
| 마키 위 캡션 | **`text-center`** | `lg:text-left` |

##### 🚨 재조정 — 두 단을 **한 덩어리로 중앙**, 좌우 여백 43/43 (같은 날 2차 지시)
사용자: **"리뷰 제목, 티오·환불 양쪽 여백 똑같이 하고 중앙으로. '함께한 대표님들' 라벨도 중앙 정렬."**
- 원인은 헤딩 컬럼의 **`flex-1`** 이었습니다 — 컬럼이 남는 폭을 다 먹어 스탯이 화면 오른쪽 끝(370px)에 못 박히고, 헤딩 글줄은 그 컬럼 안에서만 가운데라 **왼쪽 여백 39px / 오른쪽 20px** 로 어긋났습니다.
- `lg` 미만에서 **`flex-1` 을 빼** 헤딩이 글줄 폭(max-content)만 차지하게 하고, `justify-center` 가 **[헤딩+스탯] 덩어리를 통째로** 가운데 세웁니다. `lg:justify-between` + `lg:flex-none` 이라 데스크톱 2단은 그대로입니다.
- **실측(390): 헤딩 43~228 · 스탯 243~347 → 덩어리 43~347, 좌우 여백 43 / 43 으로 완전 대칭.** 섹션 높이 **792px 그대로**(구도만 바뀌고 세로는 안 늘었습니다).
- 캡처 `reviews-top-center2-mob-reviews-top.png` · 데스크톱 회귀 `desk-regress2-reviews-top.png`.

##### ✅ 최종 구도 — 스탯 2개도 **가로 나란히** (사용자 스케치, 같은 날 3차 지시)
```
리뷰                 티오  환불
거품없는 -    2건   100%
```
좌 = eyebrow+헤딩 / 우 = **티오·환불 가로 2개**(사이 세로 헤어라인), 그 덩어리를 중앙. 예약제 안내는 2단 **아래 한 줄**.

- 🚨 **폭이 물리적으로 안 맞았습니다.** 헤딩 최장 글줄 188.5px + 스탯 가로 2개(라벨 원래 크기면 172px) = 360px > 컨테이너 350px. 그래서 **두 군데를 손봤습니다**:
  1. **예약제 안내를 오른쪽 컬럼 밖으로** — 이 문장의 max-content(≈220px)가 스탯 컬럼 폭을 결정하고 있었습니다. `lg` 미만에서는 **2단 아래 전체 폭 한 줄**(`order-last w-full text-center`, 부모 `flex-wrap`)로 내려갑니다. ⚠️ 노드가 둘(`lg:hidden` ↔ `hidden lg:block`)이지만 **동시에 보이는 것은 하나**라 보조기술도 한 번만 읽고, 문구는 `site.ts` 한 곳에서 옵니다.
  2. **라벨 11.5px · 스탯 노트 11px**(`max-sm:`)로 한 단계 축소 + 헤어라인 좌우 패딩 `pr-2/pl-2`. **숫자는 지시대로 28px 유지**했고 헤딩도 28px 그대로입니다 — 가장 넓은 글줄인 라벨·노트만 줄이면 들어맞습니다(24px 축소 허용은 **쓰지 않았습니다**).
- **실측(390): 헤딩 24~209(2줄) · 스탯 221~365(가로 2개) → 좌우 여백 24 / 25.** 430 도 44 / 45 로 대칭. 가로 스크롤 0.
- **섹션 높이 792 → 712px**(스탯이 가로로 서면서 −80px). 문서 세로 13,832 → **13,752px**.
- 캡처 `reviews-top-final-mob-reviews-top.png` · 데스크톱 회귀 `desk-regress3-reviews-top.png`(문서 11,009 · 섹션 770 · 헤딩 3,699/168/317×137 · 스탯 1,091·1,208 · 마키 3,926 — 불변).

##### 헤딩 미세 조정 — **문장부호 제거 + 24px** (같은 날 4차 지시)
사용자: **"'견적,' '퀄리티.' 옆의 쉼표·마침표 없애자, 폰트 사이즈 좀 줄이고."** 모바일 렌더에서만이고 데스크톱은 부호·크기 그대로입니다.

- 🚨 **`site.ts` 에 모바일용 문자열을 만들지 않았습니다.** `MaskLines`(`ui/section-header.tsx`)에 **`punctuationClassName`** 옵션을 새로 두고, 줄 끝 문장부호(`[.,!?…·]+$`)만 그 클래스를 단 `<span>` 으로 감쌉니다. 리뷰 헤딩은 `"hidden lg:inline"` 을 넘겨 **`lg` 미만에서 부호만 숨깁니다** — 데이터는 `reviewBand.heading` 한 벌이고 문구를 고칠 때 맞출 곳이 한 곳입니다.
  - 대안이던 `\n` 분리 배열은 **부호 위치가 데이터에 박혀** 폭별로 못 가르고, 두 벌 문자열은 갱신 누락이 생깁니다.
  - 옵션을 **안 주면 렌더 결과가 예전과 완전히 동일**해서 다른 `MaskLines` 호출부(섹션 헤더 전부·브릿지)는 영향이 0 입니다.
- 크기는 **`max-sm:text-[1.5rem]`(24px)**. 클램프 하한(28px)이 걸리는 구간이 곧 폰 폭이라 클램프 자체를 고치면 데스크톱 계산식까지 흔들립니다. `sm`(640px) 이상은 기존 유동값 그대로라 **태블릿 768 = 36.7px · 데스크톱 = 48px 불변**입니다. 줄간 `leading-[1.35]` 은 무단위라 **비례해서 같이 줄어듭니다**(헤딩 높이 80 → 69px, 2줄 유지).
- **여백 재실측: 390 에서 헤딩 41~193 · 스탯 205~349 → 41 / 41 완전 대칭**(직전 24/25 에서 개선). 430 도 **61 / 61**. 섹션 712 → **701px**, 문서 세로 390 **13,741px** · 430 **13,566px**.
- 캡처 `reviews-heading-mob-reviews-top.png` · 데스크톱 회귀 `desk-regress4-reviews-top.png`(**"거품 없는 견적," / "타협 없는 퀄리티." 부호 그대로** · 문서 11,009 · 섹션 770 · 헤딩 3,699/168/317×137 — 불변).

- 🚨 **eyebrow 는 부모의 `text-center` 를 안 따릅니다** — 액센트 틱 + 라벨이 `flex` 라 `justify-center` 를 따로 줘야 합니다(§5-B 2차 `SectionHeader align="center"` 와 같은 이유).
- 스탯 컬럼은 이미 컨테이너 우측 끝(266+104 = **370px** = 390−20)에 서 있었고, 왼쪽 정렬이라 숫자·문장 길이가 달라 **오른쪽 변이 들쭉날쭉**했습니다. `text-right` 한 줄로 세 줄(라벨·숫자·노트)과 안내 문장이 한 선에 섭니다.
- **참고 비교컷** `reviews-top-left-mob.png` — "좌측 정렬 유지 + 블록만 중앙 배치" 안(런타임 주입, 소스 불변). 헤딩이 230px 컬럼을 이미 꽉 채워 **블록 중앙 배치 = 좌측 정렬과 사실상 같은 그림**이고, 차이는 eyebrow 위치와 짧은 첫 줄뿐입니다. 채택은 지시대로 **중앙 정렬**(`reviews-top-center-mob-reviews-top.png`).
- **데스크톱 1536 회귀 실측: 문서 11,009px · 리뷰 섹션 770px · 헤딩 top 3,699/left 168/317×137 · 스탯 top 3,682/left 1,091·1,208 · 마키 top 3,926 — 전부 작업 전과 동일**(`desk-regress-reviews-top.png`).

##### 마키 캡션 — 실후기 문구 **사용자 확정** (2026-08-22)
`site.ts` `reviewBand.marqueeNote.real` **"함께한 대표님들이 직접 남긴 이야기입니다."(초안) → "실제 업체 대표님들의 소중한 후기입니다."(확정)**.
🚨 `placeholder`("함께한 대표님들의 이야기가 담길 자리입니다.")는 **그대로**입니다 — 후기 5건이 전부 자리표시인 지금 확정 문구를 쓰면 허위 주장이 되므로, `trustProof.testimonialsArePlaceholder` 를 `false` 로 내리기 전에는 화면에 나가지 않습니다(§6 🚨 원칙).

#### ②-B 브릿지 폰 — **모바일도 2대** (`sections/statement-band.tsx`) 🚨 모바일 1차 판단 정정

사용자: **"휴대폰이 아직도 하나야 — 두 대 원함."** 모바일 1차의 **"앞 폰 한 대만"은 오판**이었습니다(당시 근거였던 "두 대를 줄이면 각 130px 대라 안 읽힌다"는 **틀렸습니다** — 아래 실측).

##### 블록을 **하나로 합쳤습니다**
따로 두던 모바일 전용 블록(`lg:hidden` 폰 1대)과 데스크톱 블록(`hidden lg:block` 폰 2대)을 **한 트리**로 합치고 폭별 클래스만 갈랐습니다.
- 나눠 두면 ref 를 한 벌씩 더 만들어야 합니다 — 등장 애니(래퍼)·마우스 틸트(폰)는 **요소당 하나**뿐입니다. 합치니 **등장 애니가 모바일에도 그대로** 걸립니다(`.is-in` · 양쪽에서 모이기).
- 배치 규칙이 한 곳이라 "데스크톱 축소판"이 실제로 유지됩니다.
- 그리드 컬럼(`lg:col-span-5`)은 `lg` 부터만 걸리므로, 모바일에서는 텍스트 아래 일반 흐름으로 내려가 가운데 섭니다(`mt-12 lg:mt-0` · `mx-auto`).

##### 🚨 **비율 통째로 축소** — 각도를 건드리지 않고 상자만 줄임
| | 모바일(`lg` 미만) | 데스크톱(`lg`) |
|---|---|---|
| 그룹 상자 | **350 × 432px** | 405 × 500px |
| w/h | **0.810** | 0.810 (동일) |
| `perspective` | **1200px** | 1400px |
| 폰 높이 | 앞 88% / 뒤 80% (동일) | 앞 88% / 뒤 80% |

두 상자의 **w/h 가 같고** 폰 높이가 상자 높이의 %라, 길이가 전부 `350/405 = 0.864` 배로 줄어듭니다 → **가림도 같은 비율로 유지**됩니다. ⚠️ `perspective` 를 같이 줄이지 않으면(1400 그대로) 작은 상자에서 원근이 과하게 걸립니다 — `1400 × 0.864 ≈ 1200`.

##### 실측 — 겹침 **7.1%** 로 데스크톱과 동일
| 폭 | 앞 폰 | 뒤 폰 | **가림** | 가로 스크롤 |
|---|---|---|---|---|
| **390** | 175.5px(**45.0vw**) | 159.5px(40.9vw) | **13.0px = 7.1%** | 0 |
| 430 | 175.5px(46.9vw) | 159.5px | **13.0px = 7.1%** | 0 |
| 1536(회귀) | 233.4px 프레임 | 213.4px | **15.1px = 7.1%** | 0 |

- 🚨 **가림은 래퍼가 아니라 안쪽 `PhoneFrame` 의 rect 로 재야 합니다** — 회전(`rotateZ`)이 래퍼가 아니라 폰에 걸려 있어, 래퍼 rect 로 재면 **−15px(틈)** 이라는 엉뚱한 값이 나옵니다(첫 측정에서 헛발질).
- 폰 폭은 지시한 **"~46vw · 상한 190px"** 안입니다. 회전 후 바운딩(201.7 / 184.3px)은 그보다 크지만 이는 4.5° 회전이 만드는 퍼짐이고, 좌우 끝이 **8.4~381.4px**(390 뷰포트 안)이라 **가로 스크롤 0** 입니다 — 컨테이너 여백(20px)으로 살짝 흘러나오는 모습이 의도된 그림입니다.
- 각도·마감은 데스크톱 규칙 그대로 — 앞 `rotateY(-10deg) rotateZ(-4.5deg)` / 뒤 `rotateY(8deg) rotateZ(4.5deg)`(실측 `sinZ −0.0785 / +0.0785`) · 뒤 폰 `brightness(0.85)` · 화면은 데모 시안 a/b 그대로.

##### 🚨 **축소 한계 실측은 불필요했습니다** — 175px 에서 화면이 읽힙니다
캡처(`bridge-mob-2phones.png`)에서 앞 폰의 헤딩("식물이 머무는 자리를 / 설계합니다")·통계 카드("128곳" · "월 1회 정기 관리")·하단 마키, 뒤 폰의 헤딩·도넛 게이지("74%")·수치 카드("312명" · "91%")·CTA 가 전부 판독됩니다. 그리고 **더 키울 여지도 없습니다** — 그룹 폭 350px 은 390 뷰포트의 컨테이너 내폭 전체라 이미 최대치입니다.

##### 틸트·패럴랙스는 `lg` 이상 전용
같은 요소를 두 폭이 공유하게 됐으므로, 예전의 "모바일은 폰을 안 그려서 자동 무동작"이 더는 성립하지 않습니다. effect 안에서 **`matchMedia("(min-width:1024px)")` 로 매 프레임 걸러냅니다**(리스너를 붙였다 뗐다 하지 않아 창을 넓히면 즉시 살아납니다). 창을 줄여 들어온 경우 그룹에 남아 있던 이동값도 지웁니다.
- 실측 — 모바일 그룹 `transform` **(none)** · 데스크톱 `translate3d(0px, -28px, 0px)`(패럴랙스 정상).

##### 데스크톱 회귀 — 문서 **11,009px** 불변
폰 좌우 끝 **913.7~1345.4px**(우측 컬럼 891.3~1368px 안) · 가림 15.1px(7.1%) · `perspective 1400px` · 등장 `.is-in` 둘 다 true — **전부 기존 기록값과 일치**합니다(`bridge-desk-regress.png`).

#### ③ 예상 예산 범위 — 자유 입력 → **셀렉트** (`ui/contact-form.tsx` + `content/site.ts`)
`<input type="text">`(placeholder "예: 미정 / 범위가 있다면 자유롭게") → **`<select>`** 5지선다.

- 선택지는 `site.ts` 의 신설 배열 **`contactBudgetOptions`** 하나입니다: **아직 미정 / 300만원 이하 / 300~500만원 / 500~1,000만원 / 1,000만원 이상** (+ 빈 값 "선택해 주세요"). 구간을 바꾸려면 이 배열만 고치면 되고 **문구가 그대로 메일에 실립니다.**
- 🚨 **가격표가 아닙니다** — *고객이 생각하는 예산 구간*이고 스밈의 견적·단가가 아닙니다. 서비스 가격은 여전히 미확정이라 어디에도 표시하지 않습니다(`services.ts` 원칙 유지).
- 🚨 **전송 필드 키는 `budget` 그대로**라 Web3Forms 본문("예상 예산: …")도 `lib/validation.ts` 의 스키마(`max(60).optional()`)도 **한 줄도 안 고쳤습니다.** 선택 안 하면 빈 문자열이라 예전처럼 그 줄이 아예 빠집니다. 실측 — `new FormData(form).get('budget')` = **`500~1,000만원`**, 옵션 6개(빈 값 포함).
- 마크업은 바로 위 "필요한 서비스" 셀렉트와 **같은 형태**입니다(`appearance-none pr-10` + 커스텀 화살표 SVG — 다른 입력창과 글자 시작 위치를 맞추기 위한 2026-08-08 처리).
- 모바일 실측 291×51px · **글자 16.27px** 라 iOS 포커스 확대가 없습니다. `/privacy` 의 수집 항목 문구("예상 예산 범위")는 그대로 유효합니다.

#### 검증
`tsc`·`eslint` 통과 · **콘솔 에러 0**(390/430/768/1536 전부) · **가로 스크롤 0**(`scrollWidth == clientWidth`).
캡처 — 서비스 전후 `before-mob-services.png` / `after-mob-services.png` · `before-desk-services.png` / `after-desk-services.png` · 데스크톱 비교안 `a1-desk2w-services.png` · 768 `after-768-services.png` · 모바일 리뷰 `after-mob-reviews-top.png`(+ 정렬 비교 `a2-mob-reviews-top.png`) · **브릿지 폰 2대 `bridge-mob-2phones.png` · 데스크톱 회귀 `bridge-desk-regress.png`** · 폼 셀렉트 `form-desk-budget-open.png` · `form-mob-budget-open.png`.
⚠️ 네이티브 셀렉트 **드롭다운 팝업은 헤드리스 캡처에 안 잡힙니다** — 캡처는 런타임에 `select.size=6` 으로 목록을 인라인 전개한 상태입니다(소스 불변, 스크립트 `t3-form.mjs`). 측정·캡처 스크립트는 scratchpad `t3.mjs` · `t3-form.mjs` · `bridge-mob.mjs`.

### 히어로 **2줄 워드마크 + 스프레드 진입** (2026-08-22 팀·사용자 결정)

팀 피드백: **`SUM!M` 한 줄이 레퍼런스(메이커리 `m@kery`)와 너무 닮았다.** → 그날 저녁 사이 지시가 네 번 갱신됐고, 최종 확정은 아래와 같습니다.

#### 확정된 구성
| # | 결정 | 근거 |
|---|---|---|
| ① | **STUDIO 를 아래 한 줄 더** — `SUM!M` / `STUDiO` 2줄 | 메이커리 유사성 완화 |
| ② | 위 줄 "I"→**"!"**(점이 **아래**) / 아래 줄 "I"→소문자 **"i"**(점이 **위**) | 두 줄의 대비가 이 구성의 핵심. ⚠️ 소문자 i 는 **의도된 것** |
| ③ | 🚨 **"!"와 "i" 는 무조건 세로로 같은 위치**(잉크 중심 x 일치) | 사용자 필수 지시 |
| ④ | **"+" 마커 4개 삭제** | "메이커리와 똑같다" (§7 반려 목록에 등재) |
| ⑤ | **선언문 3줄 + 메타를 히어로에서 제외** | "히어로 밑의 멘트들은 따로 넣을 거니까 일단 빼놔". **문구는 `site.ts` `hero.wordmark.statement`·`meta` 에 그대로 보존** — 배치는 추후 결정 |
| ⑥ | 워드마크가 **첫 화면을 꽉 채운다** | ⑤ 로 히어로에 워드마크만 남았기 때문 |
| ⑦ | 진입 = **글자는 안 움직이고 안 일렁인다.** 배경색 덮개가 "!"·"i" 경계에서 **불규칙 지그재그로 걷힌다** | 사용자 손그림 + "글자가 일렁이면 안 됩니다" |

#### 🚨 양립 불가 — "!/i 세로 정렬" vs "두 줄 잉크 폭 동일"
- "!"는 `SUM!M` 잉크폭의 **70.2%** 지점, "i"는 `STUDiO` 잉크폭의 **76.6%** 지점입니다. **둘 다 맞추려면 세그먼트별로 자간을 조작**해야 하는데 그건 금지입니다(자간은 두 줄 모두 −0.04em 그대로).
- 사용자 지시대로 **우선순위 ① !/i 세로 정렬(필수) ② 한쪽 잉크 끝 정렬 ③ 폭 동일(포기)** 로 갔습니다.
- 초기 지시(폭 동일)의 비율 `r = 0.9252` 는 **폐기**됐고, 지금은 **액센트 잉크 중심끼리 맞추는 비율**입니다.

#### 스위치 4개 (`site.ts` 의 `hero.wordmark`)
| 스위치 | 현재 값 | 무엇을 바꾸나 |
|---|---|---|
| `layout` | **`"one-line"`(2026-08-24)** | `SUMIM` 한 줄 3칸. `"two-line"`·`"single"` 로 되돌리는 표는 §5-B "한 줄 SUMIM" 절 |
| `oneLine.accentGlyph` | **`null`** | 치환 없이 원래 `I`. `"i"` = 소문자 · `"!"` = 예전 `SUM!M`(기둥 폭은 자동 재계산) |
| `letterWave` | **`true`** | S·U·M·M 에 연한 파도 상시(액센트는 정지). `false` 면 완전 정지 |
| `enter` | **`"intro"`(2026-08-24)** | 진입 애니메이션 **5종**(`intro`/`split`/`wipe`/`spread`/`wall`). 아래 **"히어로 v9 + 인트로 시퀀스"** 절 참고 |
| `wipeKeepAccent` | **`false`** | `true` 면 `wipe` 에서 "!"·"i" 만 덮개 위에 올려 처음부터 보이게 한다 |
| `spreadSeeds` | **`2`** | 줄마다 씨앗 구멍 개수. `0` 이면 액센트 한 곳에서만 퍼진다 |
| `studioAlign` | **`"left"`** | `"right"` 면 오른쪽 잉크 끝을 맞춘다(아래 주의) |

`layout: "single"` + `enter: "wall"` 실측 회귀 — 잉크 좌/우 **41 / 1475 @1536**, 2026-08-21 기록값과 **동일**, 마커 4개 복귀(`hero-revert-single.png`). ⚠️ 선언문은 ⑤ 로 전 구성 공통 제외라 single 로 되돌려도 돌아오지 않습니다.

#### 실측 상수 (전부 **픽셀 스캔** — `measureText` 단독 금지, 2026-08-21 교훈)
측정 조건: Pretendard Variable **700** · 기준 400px · `letter-spacing −0.04em` · DOM 과 같이 **글자마다 따로**(커닝 없음) 찍어 잉크 열 스캔. 검증 — 이 표로 계산한 상자 폭 `3.20625em × 459.264px = 1472.55px` 이 실제 DOM `<p>` 폭과 **일치**.

| 줄 | 잉크폭/크기 | LSB | RSB | 액센트 잉크 중심(잉크 왼끝 기준) |
|---|---|---|---|---|
| `SUM!M` | **3.1525** | 0.0350 | 0.0187 | "!" **2.213825** |
| `STUDiO` | **3.4075** | 0.0350 | 0.0050 | "i" **2.611475** |

- **아래 줄 배율** `studioAlign: "left"` → `2.213825 / 2.611475` = **0.847733** (STUDiO 잉크폭 = SUM!M 의 **91.6%**)
  `"right"` → `0.938675 / 0.796025` = **1.179191** 🚨 **1 보다 큽니다** — 지시서 예상("STUDiO 가 왼쪽에서 들여쓰기")과 **반대로** 아래 줄이 커져 왼쪽으로 뻗습니다. 1536×745 실측에서 STUDiO 가 위 줄의 **122%** 가 되고 **왼쪽 끝이 뷰포트에 잘립니다**(섹션 `overflow-hidden`). 그래서 기본은 `"left"` 입니다.
- **광학 보정** `WM_OPTICAL_EM = 0.0131` — 2026-08-21 값 그대로(두 줄 다 "S"로 시작하므로 같은 값).
- **줄 간격** `WM_LINE2_MT_EM = −0.004em`(위 줄 em) → 실측 **캡↔캡 = 캡 높이의 11.1%**(1536) / **11.2%**(1920) / **10.3%**(390). 목표 10~12% 충족.
  ⚠️ 아래 줄 "i" 의 점은 캡보다 0.04em 위라 이 간격 안으로 들어옵니다 — **의도된 대비**입니다.
- **세로 크기(꽉 채움)** `min(29.9vw, max(40px, calc((100svh − 324px 상당) / COEF)))`
  — 실제 코드는 `WORDMARK_TWO_FIXED = 150`(= 섹션 패딩 144 + 여유 6) · `WORDMARK_TWO_COEF = 0.82 + 0.82×배율 + 줄간격` = **1.5111**.
  🚨 **단일 `N vh` 상수를 쓰지 않은 이유**: 고정 패딩이 뷰포트 높이에 비례하지 않아 낮은 뷰포트에 맞춘 값이 큰 화면에서도 그대로 걸립니다.
- **정렬 구조는 2026-08-21 그대로**(vw 상수 금지 — 스크롤바 버그). 래퍼 폭 = **위 줄 `SUM!M` 잉크 폭**.
  🚨 **아래 줄은 `width: 0` + `margin-left` 하나로 위치를 잡습니다** — `studioAlign: "right"` 처럼 아래 줄이 더 넓어져도 래퍼 폭이 흔들리지 않게 하기 위해서입니다(`white-space: nowrap` 이라 글자는 그대로 흘러 넘칩니다).

#### 진입 `enter: "spread"` — 사용자 손그림 기준
- **글자에는 어떤 필터·변형·애니메이션도 없습니다.** 처음부터 최종 위치에 최종 상태로 서 있고, **밴드 배경색 덮개**(`.wordmark-veil`)만 걷힙니다.
- **덮개 색은 반드시 `var(--color-canvas)`** — 이 변수는 밴드 배경과 **같은 값**이라 `bandedPalette` 를 navy 로 바꾸든 colorLab 을 주입하든 덮개가 배경을 따라갑니다(하드코딩 `#000` 금지). 실측 — team `rgb(0,0,0)` / navy `rgb(20,29,51)` 로 **덮개 fill 이 함께 바뀜**(`hero-spread-palette-{team,navy}.png`).
- **경계 = 매끈한 파도가 아니라 아주 불규칙한 지그재그**(사용자 손그림: 펜으로 휘갈긴 듯 고리·급격한 꺾임·길고 짧은 톱니). 레퍼런스 `홈페이지 제작/지안영어/과제-이미지/10-파도출렁임.html` 에서 가져온 것은 **"겹쳐 천천히 흐른다"는 느낌뿐**이고 그 파일의 사인 곡선은 쓰지 않았습니다.
  - 꼭짓점 **35개 + 고리 3곳(각 3점) = 44개**, 진폭 `0.028em`(캡의 **4%**, 긴 톱니는 ×3.1 = **12%**) — 지시한 3~12% 범위.
  - 좌표는 **하드코딩 의사난수**(`mulberry32`, 시드 고정 — `Math.random` 금지, SSR 불일치 방지). 줄·좌우 덮개마다 다른 시드.
  - `fill-rule="evenodd"` 라 **스스로 교차하는 고리가 구멍**으로 렌더돼 낙서 같은 결이 납니다.
  - **1px 페더** — 획을 그리지 않고 SVG 자체에 CSS `filter: blur(0.6px)`. **기기 픽셀 기준**이라 워드마크가 117px 이든 570px 이든 가장자리가 항상 ≈1px 만 풀립니다.
  - **꿈틀** — 전진과 흔들림을 **한 SMIL `<animate attributeName="d">`** 에 함께 구웠습니다(키프레임 9개마다 전선 위치 + 새 지터). CSS 로 못 하는 부분이라 SMIL 이고, **JS 로 DOM 을 재지 않습니다**(워드마크 = LCP).
- **전선 진행 곡선 = 코사인 ease-in-out** `p = (1 − cos(πt))/2`. 🚨 처음 쓴 `t^0.75`(가파른 ease-out)는 **씨앗 섬이 보이기도 전에 흡수**돼 버렸습니다(캡처로 확인).
- **타이밍** 퍼짐 **1.9s** · 아래 줄 **+0.1s** · 씨앗 **0.2s 에 시작, 4.2s 성장 곡선**.
- **씨앗 구멍** (`spreadSeeds: 2`) — 액센트 양옆 중 후보가 많은 쪽부터 번갈아 **가운데 글자**(위 줄 U·마지막 M / 아래 줄 U·O). 🚨 글자 **정중앙이 아니라 advance 의 18% 지점**(왼쪽 획 위)에 둡니다 — U·O 처럼 속이 빈 글자는 정중앙이 카운터(배경색)라 구멍을 뚫어도 **아무것도 안 보입니다**(실측 전례).
  🚨 **전선이 지나간 씨앗은 반드시 반지름 0 으로 줄여야 합니다** — `evenodd` 라 덮개 바깥에 남은 구멍 서브패스는 구멍이 아니라 **채워진 검은 얼룩**이 되어 글자 위에 남습니다(첫 캡처에서 실제로 그렇게 나왔습니다).
- `prefers-reduced-motion` 이면 `.wordmark-veil { display: none }` → **즉시 전부 표시**(실측 4개 전부 `none`).
- **클라이언트 컴포넌트 0** — 중간안이던 `burst`(글자가 미끄러져 나오고 `feTurbulence` 로 일렁임)와 그 필터 제거용 토글은 **사용자 지시로 폐기**했습니다.

#### 🚨 알아둘 함정 (같은 실수 반복 방지)
- **`<svg>` 는 대체 요소**라 `viewBox` 의 고유 비율이 있습니다. 덮개에 `top/bottom` 만 주고 `height:auto` 로 두면 상자 높이가 아니라 **고유 높이(10:1)** 로 접혀 글자 윗동만 가립니다. `height: calc(100% - 0.035em)` 로 명시했습니다(0.035em 은 아래 줄 "i" 점을 덮지 않기 위한 여유).
- **덮개 검증 캡처는 고정 UI 를 숨기고 찍어야 합니다** — 좌하단 colorLab 버튼이 아래 줄 잉크·액센트 픽셀 스캔에 섞입니다(첫 측정에서 `!↔i` 차이가 −473px 로 나왔습니다).

#### 검증 실측 (스크롤바 **보이는** 상태 · 픽셀 스캔)
| 뷰포트 | 무엇이 크기를 잡나 | F1 | 위 줄 잉크폭 / 보이는 폭 | 아래 줄 / 위 줄 | **"!"↔"i" 중심 Δ** | 왼끝 Δ | 캡↔캡 | 가로 스크롤 |
|---|---|---|---|---|---|---|---|---|
| 1536×745 | **높이** | 393.75 | 1242px = **81.7%** | 91.5% | **0.00px** ✅ | 0.00px | 11.1% | 0 |
| 1920×1080 | **폭** | 569.60 | 1795px = **94.2%** | 91.6% | **−0.50px** ✅ | 0.00px | 11.2% | 0 |
| 390×844 (mobile DSF2) | **폭** | 116.61 | 368px = **94.4%** | 91.7% | **0.25px** ✅ | −0.50px | 10.3% | 0 |
| 1536×745 `studioAlign:"right"` | 높이 | 333.73 | 1052px = 69.2% | **122.1%** | **−0.50px** ✅ | (오른끝 **0.00px**) | — | 0 |

- **첫 화면**: 세 폭 모두 워드마크 잉크가 뷰포트 안(1536 → y 117~679 / 745, 1920 → 161~972 / 1080, 390 → 357~523 / 844).
- **세로 채움**: 래퍼(두 줄 상자) **595.0px** vs 섹션 가용 **601px** — 1536 기준 **99.0%** 를 채웁니다(잉크만 보면 75.4%, 나머지는 `leading-0.82` 상자 여백).
- **전체 회귀**: 문서 높이 **11,009px — 작업 전과 동일** · 가로 스크롤 0 · **콘솔 에러 0** · `tsc`·`eslint` 통과.
- **캡처** (scratchpad) — 작업 전 `hero-before-1536.png` / 완성 `hero-two-line-left-{1536,1920,390}.png` · `hero-two-line-right-1536.png` / 진입 `hero-spread-{a,b,c,seed-island,done}-seeds2.png`(a = 0.05s **!·i 만** / b = 0.4s / c = 0.8s / seed-island = 0.6s **씨앗이 작은 섬** / done = 3.0s) / **경계 확대 2배 `hero-spread-edge-zoom-seeds2.png`** / 씨앗 없는 판 `hero-spread-*-seeds0.png` / 팔레트 추종 `hero-spread-palette-{team,navy}.png` / `hero-reduced-motion.png` / 복귀 `hero-revert-single.png`.


#### 🆕 **3칸 박스 구조 (2026-08-23)** — 두 줄이 같은 칸 골격을 쓴다

사용자: **"i랑 ! 를 제외한 좌측 우측 알파벳 각각 같은 박스 안에 들어있는 채로 자연스레 채워졌으면 좋겠고, 스튜디오의 O도 스밈의 마지막 M이랑 사이즈 같게 해줘. STUDIO의 STUD는 위로 늘리든 자간을 맞추든 해서 가로 비율 똑같게"**

```
  ┌──────────────┬───┬─────┐   ← 칸 폭·칸 사이 간격이 두 줄 **완전히 동일**
  │     SUM      │ ! │  M  │
  ├──────────────┼───┼─────┤
  │     STUD     │ i │  O  │
  └──────────────┴───┴─────┘
    좌측 박스      기둥  우측 박스
```

- **두 줄 font-size 동일**입니다 — 2026-08-22 의 `0.847733` 배율은 **폐기**했습니다. 캡 높이가 같아졌습니다.
- 골격이 같으므로 **잉크 왼끝·오른끝·`!`/`i` 중심이 전부 자동으로 맞습니다**(2026-08-22 의 "폭 동일 포기"가 이 구조로 **해소**됐습니다).
- ⚠️ **`studioAlign` 스위치는 이제 무시됩니다.** 양 끝이 함께 맞으므로 고를 것이 없습니다. 필드는 되돌릴 때를 위해 `site.ts` 에 남겨 두고 주석으로 표시했습니다.

##### 칸 골격 상수 (위 줄 `SUM!M` 의 **자연 배치**에서 뽑음 — 픽셀 스캔)
| 상수 | 값(em) | 뜻 |
|---|---|---|
| `BOX_LEFT` | **2.043496** | `SUM` 잉크 폭 |
| `BOX_GAP_L` | **0.0178125** | `SUM` 잉크 오른끝 → `!` advance 시작 |
| `BOX_SLOT` | **0.3071289** | 액센트 기둥 = max(adv `!` .307129, adv `i` .256836) |
| `BOX_GAP_R` | **0.02** | `!` advance 끝 → 마지막 `M` 잉크 왼끝 |
| `BOX_RIGHT` | **0.765** | 마지막 `M` 잉크 폭 |
| 합계 | **3.1534375** | 줄 전체 잉크 폭 |

🚨 **위 줄은 예전과 픽셀 하나도 달라지지 않습니다** — 좌·우 칸이 `scaleX = 1` · weight 700 · 자간 −0.04em 그대로라 자연 배치를 그대로 칸에 담기만 한 것입니다.
액센트는 기둥 안에서 **advance 중앙이 아니라 잉크 중앙**으로 맞춥니다 — 두 글자의 사이드베어링 차이(`!` .07/.0721 vs `i` .05/.0493)가 상쇄돼 `!`/`i` 중심이 **구조적으로** 같아집니다.

##### 아래 줄 `STUD` — 자간 −0.064em + `scaleX` 0.834942 + **weight 860**
- `STUD` 자연 잉크 폭(700·−0.04em)은 **2.4766em** 으로 목표(2.0435em)보다 **21% 넓습니다**. 지시대로 **자간을 먼저 조이고 모자란 만큼만 `scaleX`** 로 줄였습니다.
- 🚨 **자간 −0.064em 이 픽셀 충돌 한계**입니다. 글자별 마스크를 따로 그려 인접 쌍의 **실제 픽셀 여유**를 재면 S↔T **5px** · T↔U **5px** · U↔D **14px**(기준 400px)이고, 한 스텝(0.002em)만 더 조이면 **T 가로획이 U 세로획에 닿습니다**.
  ⚠️ **상자(사이드베어링) 기준으로 재면 안 됩니다** — S↔T 는 상자로는 이미 겹쳐 있지만 T 는 가로획이 위쪽에만 있어 실제로는 닿지 않습니다. 그래서 **픽셀 마스크 교차**로 재야 합니다.
- 그래서 **`scaleX` 는 0.835** 로, 지시서 권장선 **0.9 에 못 미칩니다**. 충돌 없이 0.9 를 만들 방법이 없습니다(위 한계 때문에 0.9 를 쓰면 T·U 가 겹칩니다).
- **weight 860** — `scaleX` 0.835 면 세로획이 16% 얇아집니다. Pretendard Variable(**wght 45~920**)의 가변축으로 되살렸습니다.

| weight | 최대 조임(자간) | 자간 후 잉크폭 | scaleX | U 세로획 × scaleX |
|---|---|---|---|---|
| 700 | −0.070em | 2.4137 | 0.8466 | 56.7px |
| 820 | −0.068em | 2.4251 | 0.8427 | 57.3px |
| 840 | −0.066em | 2.4375 | 0.8384 | 58.3px |
| **860 ✅** | **−0.064em** | **2.4469** | **0.834942** | **59.3px** |

기준 `SUM` 의 U 세로획 **59.0px** → **Δ 0.3px(0.5%)**.
⚠️ **가로획은 반대로 두꺼워집니다** — `scaleX` 는 세로획만 얇게 하므로 weight 를 올리면 T 크로스바·D 아래 곡선이 기준보다 굵어집니다. 인위적 압축의 불가피한 대가입니다.

##### 아래 줄 `O` — `scaleX` 1.186047 + **weight 560**
- `O` 자연 잉크 폭(700)은 0.6625em, 목표(`M` 잉크 폭)는 0.765em → **가로로 늘려야** 합니다. 한 글자라 자간이 없어 `scaleX` 뿐입니다.
- **weight 560** 에서 잉크 폭 0.645em → `scaleX = 0.765 / 0.645 =` **1.186047**.
- 굵기 — 560 의 고리(좌우) 획 **48px × 1.186 = 56.9px**, 기준 `M` 세로획 **57.0px** → **Δ 0.1px**. (weight 700 그대로면 59 × 1.155 = **68px** 로 19% 뚱뚱해집니다.)
- ⚠️ **위아래 곡선은 얇아집니다** — 560 의 O 상단획 **44px** vs 700 의 **52px**(−15%). 가로로 늘린 글자의 세로획을 맞추면 가로획이 얇아지는 것은 피할 수 없습니다.

##### 세로 — 두 줄이 같은 크기라 분모를 다시 실측
`WORDMARK_TWO_COEF = 0.82 + 0.82 + 줄간격` = **1.6287** (2026-08-22 의 1.5111 에서 갱신).
줄 간격 `WM_LINE2_MT_EM = −0.0113em` → **캡↔캡 = 캡의 11.2%**(1536) / 11.4%(1920) / 10.3%(390). 목표 10~12% 충족.
크기 식은 그대로 `min(29.9vw, max(40px, calc((100svh − 150px) / 1.6287)))`.

##### 🚨 구현에서 반드시 지켜야 하는 것
- **`<p>` 에 자간을 주면 안 됩니다** — 칸은 atomic inline 이라 칸 **사이에도** 자간이 붙어 3칸 골격이 틀어집니다. `letter-spacing: normal` 로 두고 자간은 **칸 안쪽(`.wordmark-fit`)에만** 줍니다.
- **층을 셋으로 나눕니다** — `.wordmark-glyph`(칸, 고정 폭) > `.wordmark-shift`(진입 이동) > `.wordmark-fit`(글자, `scaleX`).
  🚨 같은 요소에 `scaleX` 와 진입 `translateX` 애니메이션을 함께 걸면 **transform 이 통째로 덮여 `scaleX` 가 날아갑니다**(`animation-fill-mode: both` 라 끝나고도 안 돌아옵니다). 층을 나눠야 `enter: "wall"` 에서도 폭이 유지됩니다(실측 확인).
- **`.wordmark-line--box .wordmark-glyph { clip-path: none }`** — 칸 폭이 곧 잉크 폭이라 `wall` 용 클립이 걸리면 `M`·`O` 오른쪽 획이 잘립니다.
- `.wordmark-fit` 은 `transform-origin: left` — `scaleX` 를 걸어도 잉크 왼끝이 칸 왼쪽 변에 붙어 있고 오른끝만 목표 폭으로 움직입니다.
- ⚠️ `enter: "wall"` 은 two-line 에서 **글자 단위가 아니라 칸 단위(3칸)** 로 동작합니다. 글자별 벽은 `single` 전용입니다.

##### 검증 실측 (스크롤바 보이는 상태 · 픽셀 스캔 · 고정 UI 숨김)
| 뷰포트 | 잡는 축 | F | **좌측 박스 Δ** | **우측 박스 Δ** | **`!`↔`i` 중심 Δ** | **왼끝 Δ / 오른끝 Δ** | 잉크폭/보이는폭 | 캡↔캡 | 가로 스크롤 |
|---|---|---|---|---|---|---|---|---|---|
| 1536×745 | 높이 | 365.32 | **1.00px** | **−1.00px** | **0.00px** | **0.00 / 0.00** | 1152px = **75.7%** | 11.2% | 0 |
| 1920×1080 | 폭 | 569.60 | **1.00px** | **1.00px** | **−0.50px** | **−1.00 / 1.00** | 1795px = **94.2%** | 11.4% | 0 |
| 390×844 (DSF2) | 폭 | 116.61 | **0.50px** | **−0.50px** | **−0.25px** | **−1.00 / −0.50** | 368px = **94.4%** | 10.3% | 0 |

- **획 굵기 실측(1536, 캡 40% 지점 행)** — 좌측 칸 런: 위 줄 `[75, 53, 53, 52, 50, 49, 52]`(S · U 2 · M 4) / 아래 줄 `[96, 54, 55, 54, 54, 54]`(S · T · U 2 · D 2). **U 세로획 53 vs 54.5 → Δ 1.5px(2.8%)**.
  우측 칸: 위 줄 `M [52, 49, 49, 52]` / 아래 줄 `O [52, 52]` → **M 바깥 기둥 52 vs O 고리 52 = Δ 0px**.
- **덮개는 두 줄이 같은 좌표**입니다(기둥 x 가 같으므로) — 실측 `935.69×286.77@−0.55` / `469.44×286.77@1047.33` 이 두 줄 모두 동일. 애니메이션·타이밍·씨앗은 2026-08-22 그대로이고 씨앗 x 만 새 자간·`scaleX` 로 재계산했습니다.
- **회귀** — `layout:"single"` + `enter:"wall"` 잉크 **41 / 1475 @1536**(2026-08-21 기록값과 동일) · `layout:"two-line"` + `enter:"wall"` 도 골격 수치 동일(덮개 0개, `scaleX` 유지) · `prefers-reduced-motion` 덮개 4개 전부 `display:none` · **문서 높이 11,009px 불변** · 가로 스크롤 0 · **콘솔 에러 0** · `tsc`·`eslint` 통과.
- **캡처** — `hero-box-1536.png` · `hero-box-1920.png` · `hero-box-390.png` / 진입 `hero-box-spread-b.png`(t=0.6s) · `hero-box-spread-done.png`(t=3.0s) / 확대 `hero-box-stud-zoom.png`(S↔T·T↔U 충돌 없음 육안 확인) · `hero-box-sum-zoom.png`(비교) · `hero-box-mo-zoom.png`(M vs O) / 회귀 `hero-box-revert-single.png` · `hero-box-wall-1536.png`.


#### 🆕 **와이프 진입 (2026-08-23)** — `enter: "wipe"` 가 새 기본값

사용자: **"퍼져 나가는 모양이 너무 어색하네. 그라데이션을 주고 배경을 덮은 채로 배경이 왼→오로 사라지게 하는 건 어때?"**
→ 지그재그 `spread` 를 **기본에서 내리고** 와이프를 넣었습니다. `spread`·`wall` 코드는 **한 줄도 지우지 않았고** 값만 바꾸면 복귀합니다.

| `enter` | 무엇을 하나 | 상태 |
|---|---|---|
| **`"split"`** | 덮개를 **3분할** — 좌·우 두 장만 두고 **기둥은 안 덮는다.** "!"·"i" 에서 **양쪽으로** 갈라지고 앞단은 **그라데이션 + 지그재그 + 꿈틀** | **현재 기본 (2026-08-23)** |
| `"wipe"` | 2줄 블록 전체를 덮은 **배경색 덮개 한 장**이 **왼→오로 빠져나간다.** 앞단이 그라데이션 | 보존 (2026-08-23) |
| `"spread"` | "!"·"i" 만 보이고 덮개가 기둥 양옆에서 **불규칙 지그재그**로 걷힌다(+ 씨앗 구멍) | 보존 (2026-08-22) |
| `"wall"` | 벽에서 스윽. two-line 에서는 **칸 단위(3칸)**, 글자별 벽은 `single` 전용 | 보존 (2026-08-21) |

셋 다 **글자는 처음부터 최종 위치에 그대로**이고 변형·필터가 없습니다(2026-08-22 사용자 지시).

##### 구현
- 덮개는 **워드마크 래퍼 안의 `<span>` 한 장**입니다(두 줄을 통째로 덮음). 폭 = **블록 100% + 페더** 라 `translateX(100%)` 한 번이면 정확히 블록 밖으로 나갑니다. 시작은 `translateX(-페더)` — 페더가 블록 왼쪽 바깥에 숨고 불투명부가 블록을 전부 덮습니다.
- 🚨 **색 그라데이션이 아니라 마스크로 앞단을 풉니다.** `linear-gradient(transparent → 밴드색)` 으로 하면 투명 끝이 **투명 검정**이라 검정이 아닌 팔레트(navy 등)에서 앞단에 **어두운 띠**가 보입니다(실측으로 확인했습니다). 면은 `background-color: var(--color-canvas)` 단색으로 두고 **알파만** `mask-image` 로 그라데이션합니다 → navy 실측 **밴드 배경 대비 최대 편차 1/255**(사실상 0).
- 🚨 **마스크 스톱을 2개(선형)로 두면 안 됩니다** — 알파 기울기가 램프 끝에서 **갑자기 0** 이 되는 기울기 불연속이라 사람 눈이 그 자리를 **세로 선(마하 밴드)** 으로 읽습니다(2026-08-23 사용자 지적, `hero-wipe-edge-zoom-before.png` 에 그대로 보입니다). **smoothstep 형태의 다단 스톱**(0/.03/.16/.5/.84/.97/1 @ 0·10·25·50·75·90·100%)으로 양 끝 기울기를 눕혔습니다 — 흰 바탕 위 한 행 실측(램프 224px): 앞끝 15% 평균 기울기 **1.059 → 0.559**, 뒷끝 15% **1.088 → 0.618**(중앙은 1.114 → 1.500 으로 오히려 가팔라져 S 커브가 됩니다). 페더 총폭·시간·이징은 그대로입니다. 확인 캡처 `hero-wipe-b-smooth.png` · `hero-wipe-edge-zoom.png`.
- 색이 `var(--color-canvas)` 라 팔레트·colorLab 을 그대로 따라갑니다(navy 실측 `rgb(20,29,51)`).
- 덮개에 `font-size: var(--wm-size)` 를 다시 겁니다 — 래퍼가 크기를 정하지 않아 그냥 두면 페더 em 이 본문 16px 로 계산됩니다.
- `pointer-events: none` 상시 + 끝 키프레임에서 `visibility: hidden`(구간 내내 visible 로 보간되고 100% 에서만 hidden).
- CSS keyframes 하나뿐이라 **LCP 규칙**을 지킵니다(JS 0).
- `prefers-reduced-motion` 이면 `display: none` → 즉시 전부 표시(실측 확인).

##### 실측으로 고른 값
- **페더 폭 `0.63em` = 워드마크 잉크 폭(3.1534em)의 20%** (1536×745 에서 **230.2px**). 같은 프레임(t=0.6s) 비교 —
  `0.32em`(10%)은 경계가 칼로 자른 듯 **딱딱**하고, `0.95em`(30%)은 글자 두 개가 한꺼번에 흐려져 **와이프가 아니라 페이드**로 읽힙니다. 0.63em 이 방향감과 부드러움이 함께 남는 지점입니다(지시 18~25% 안).
- **지속 1.6s · 이징 `ease-in-out`** 🚨 지시서가 준 두 후보의 덮개 앞단 x 를 실측해 골랐습니다(블록 x 194~1342 @1536×745):

  | 이징 | 0.15s | 0.3s | 0.6s | 1.1s | 실질 종료 |
  |---|---|---|---|---|---|
  | house `cubic-bezier(.22,1,.36,1)` | **574**(이미 34% 노출) | 1004 | 1412(끝) | — | **≈0.55s** |
  | **`ease-in-out` ✅** | −8 | 79 | 435 | 1245 | 1.6s |

  house 이징은 **1.6s 중 1s 가 죽은 시간**이라 와이프가 "휙" 지나가고 뒤가 비어 어색합니다. `ease-in-out` 은 지속 시간 내내 고르게 지나갑니다.
- 🔀 **`wipeKeepAccent`(기본 `false`)** — `true` 면 "!"·"i" 칸만 `z-index: 1` 로 덮개 위에 올라가 처음부터 보입니다(`spread` 의 결을 유지하고 싶을 때). 비교 캡처 `hero-wipe-b-keep0.png` / `hero-wipe-b-keep1.png`.

##### 검증
- **3칸 골격 수치는 하나도 바뀌지 않았습니다** — 좌측 박스 Δ **1.00px** · 우측 박스 Δ **−1.00px** · `!`↔`i` 중심 Δ **0.00px** · 두 줄 왼끝/오른끝 Δ **0.00 / 0.00** · 캡↔캡 **11.2%**(전부 3칸 구조 절의 값과 동일).
- **문서 높이 11,009px 불변** · 가로 스크롤 0 · **콘솔 에러 0** · `tsc`·`eslint` 통과 · `pointer-events: none` 확인.
- **캡처** — `hero-wipe-a.png`(0.15s) · `hero-wipe-b.png`(0.6s) · `hero-wipe-c.png`(1.1s) · `hero-wipe-done.png` / 페더 비교 `hero-wipe-feather-narrow.png`(0.32em) · `hero-wipe-feather-wide.png`(0.95em) / 이징 비교 `hero-wipe-house-a.png` · `hero-wipe-ease-inout-{a,b}.png` / `wipeKeepAccent` `hero-wipe-b-keep0.png` · `hero-wipe-b-keep1.png` / 팔레트 `hero-wipe-palette-navy.png` / `hero-wipe-reduced-motion.png` / 골격 회귀 `hero-wipe-geom-1536.png`.


#### 🆕 **split 진입 (2026-08-23)** — `enter: "split"` 이 새 기본값

사용자: **"그라데이션을 3분할 해서 i랑 ! 에서 양쪽으로 퍼져나가게 해줘. 그라데이션은 지그재그 모양으로 일렁거렸으면 좋겠어."**
→ `wipe` 의 **부드러운 페더** + `spread` 의 **출발점·지그재그**를 합친 구성입니다. `wipe`·`spread`·`wall` 은 전부 보존했습니다.

- **3분할** — 덮개는 **좌측 박스용 / 우측 박스용 두 장뿐**이고 **액센트 기둥은 덮지 않습니다.** 좌측은 기둥 왼쪽 경계에서 **왼쪽으로**, 우측은 기둥 오른쪽 경계에서 **오른쪽으로** 동시에 걷힙니다. 폭이 2.7배 차이나지만 **같은 1.6s 에 끝나게**(다른 속도) 했습니다 — 이징은 `wipe` 와 같은 `ease-in-out`.
- 🚨 **액센트 칸은 `z-index: 1` 로 덮개 위 층에 올립니다.** 덮개가 기둥을 안 덮어도 **페더(blur 3σ ≈ 0.3em ≈ 110px)가 양옆에서 기둥(0.307em ≈ 112px) 안쪽까지 번져** "!"·"i" 가 흐려집니다(실측 확인). 위 층으로 올려야 t=0 부터 또렷합니다.
- **덮개 한 장이 두 줄을 함께** 덮습니다(줄마다 나누지 않음). 🚨 **판단 근거**: 페더를 `blur` 로 내는데 줄별로 나누면 덮개의 **위·아래 변에도 blur 가 먹어** 이웃 줄 잉크가 미리 드러납니다(줄 간격은 캡의 11% = 29px 인데 blur 3σ 는 110px). 블록 전체를 덮고 **위아래로 0.75em 씩 넘겨** 두면 blur 가 잉크에서 먼 자리에서만 일어납니다. 지그재그 11단계가 두 줄 높이 전체에 걸쳐 있어 줄마다 다른 구간이 보이므로 위상이 같아 보이지도 않습니다.

##### 구현 후보 비교 → **(a) 채택**
| 후보 | 방식 | 결과 |
|---|---|---|
| **(a) ✅** | SVG 지그재그 `<path>`(밴드색) + CSS **`blur()`** | 톱니·고리가 **또렷하게 남고** 경계는 부드럽다. 가우시안이라 페더가 자연히 S 커브 → **마하 밴드 없음**(2026-08-23 교훈) |
| (b) | 선형 smoothstep 그라데이션 rect + `feTurbulence`/`feDisplacementMap` | 경계가 **물결(사인)로 뭉개져** 손그림의 "톱니·고리"가 사라진다. 같은 프레임 비교 `hero-split-cand-b.png` |

- **전진은 CSS `translateX`**, **꿈틀은 SVG SMIL `d` 모핑(주기 2.4s · 키프레임 7)** 으로 **분리**했습니다. 한 애니메이션에 섞으면 키프레임 수가 전진의 부드러움과 꿈틀 주기를 동시에 결정해 버립니다(`spread` 때의 제약). 꼭짓점마다 자기 위상의 사인을 더해 한 바퀴 돌면 첫 키프레임과 정확히 다시 만나므로 이음매가 없습니다.
- 덮개 폭이 자기 자신이라 `translateX(±100%)` 한 번이면 정확히 블록 밖으로 나갑니다. 끝 키프레임에서 `visibility: hidden`, `pointer-events: none` 상시, `prefers-reduced-motion` 이면 `display: none`(실측 `['none','none']`).
- 색은 `var(--color-canvas)` 라 팔레트·colorLab 을 따라갑니다(navy 실측 덮개 `rgb(20,29,51)`).

##### 실측으로 고른 값
- **페더 = `blur(0.10em)`** (1536에서 36.5px, 전이 폭 ≈3σ = 0.30em) · **지그재그 진폭 0.30em** · **세로 11단계** · 꿈틀 진폭 0.09em.
- 🚨 **지그재그의 세로 파장이 blur 반경보다 충분히 길어야 합니다.** 처음에 `blur 0.16em`(58px) + 34단계(파장 34px)로 잡았더니 **파장 < 반경** 이라 톱니가 통째로 뭉개져 **그냥 부드러운 그라데이션**으로만 보였습니다(캡처 확인). 파장을 3배로 늘리고 반경을 줄여 **파장/반경 ≈ 3** 을 만든 것이 지금 값입니다.
- 톱니 길이 분포는 긴 톱니 ×2.2 · 짧은 톱니 ×0.25, 고리 3곳(각 3점). 좌표는 **하드코딩 의사난수**(`mulberry32`, 시드 고정 — `Math.random` 금지).

##### 검증
- **3칸 골격 수치 불변** — 좌측 박스 Δ **1.00px** · 우측 박스 Δ **−1.00px** · `!`↔`i` 중심 Δ **0.00px** · 두 줄 왼끝/오른끝 Δ **0.00 / 0.00** · 캡↔캡 **11.2%**.
- **문서 높이 11,009px 불변** · 가로 스크롤 0 · **콘솔 에러 0** · `tsc`·`eslint` 통과.
- **캡처** — `hero-split-a.png`(0.15s, **"!"·"i" 만 또렷**) · `hero-split-b.png`(0.6s) · `hero-split-c.png`(1.1s) · `hero-split-done.png` / 경계 2배 확대 `hero-split-edge-zoom.png` / 후보 비교 `hero-split-cand-a.png` · `hero-split-cand-b.png` / `hero-split-palette-navy.png` / `hero-split-reduced-motion.png` / 골격 회귀 `hero-split-geom-1536.png`.


##### 🆕 보강 2건 (2026-08-23 저녁 사용자 피드백)

**① 완전 은폐** — 사용자 "지금은 처음부터 글자가 좀 보이네."
덮개의 지그재그 **기준선을 기둥 경계에 두면 안 됩니다.** ① 톱니가 기준선보다 안쪽으로 최대 `2.2 × 진폭` 물러나고 ② 그 위에 `blur` 가 3σ 만큼 더 번져서, 기둥에 붙은 글자(위 줄 마지막 `M`, 아래 줄 `D`)의 가장자리가 t=0 부터 비쳤습니다.
→ 기준선을 **기둥 안쪽으로 `2.4 × 진폭(0.30) + 3.2 × blur 반경(0.10)` = `1.04em`** 밀어 넣었습니다(`SPLIT_HIDE_EM`). 좌·우 덮개가 기둥 위에서 서로 겹치지만 **액센트 칸이 `z-index: 1`** 이라 무해합니다.
**검증(t=0.05s 프레임 픽셀 스캔)** — 워드마크 블록 범위 x `114~1422` · y `69~724` 에서 **액센트 기둥(x 943~1055) 바깥의 밝기 > 8/255 픽셀 = 0개, 최대 밝기 0.00** ✅ (기둥 안 최대 밝기 89.6 은 팝인 중인 `!`·`i` 자신입니다).
⚠️ 계측할 때 **좌하단 Next dev 배지**는 shadow DOM 이라 `position:fixed` 숨기기가 안 먹습니다 — 검사 범위를 블록 주변으로 좁혀야 합니다(처음에 329px 로 잡혔습니다).

**② 2단계 진입 — Phase 1 팝인 → Phase 2 걷힘** — 사용자 "!·i 가 점을 기준으로 뿅 나타난 뒤 글자가 걷혔으면".

| | 시간 | 내용 |
|---|---|---|
| **Phase 1** | 0 → **0.42s** | `!`·`i` 가 **자기 점(dot) 자리를 원점으로** `scale(0) → 1`. 이징 `cubic-bezier(.34,1.56,.64,1)`(≈1.09 오버슈트). 두 줄 **동시 시작** |
| **Phase 2** | 0.42s → **2.02s** | 기존 split 걷힘(1.6s `ease-in-out` + 지그재그 꿈틀). `animation-delay: 0.42s` 라 Phase 1 동안 덮개는 **전혀 움직이지 않습니다** (SMIL 꿈틀도 `begin="0.42s"`) |

`!` 의 점은 **아래**, `i` 의 점은 **위** 라 두 글자가 블록 한가운데(두 점이 맞닿는 곳)에서 위·아래로 뿅 벌어집니다.
🚨 **원점은 실제 렌더 픽셀 스캔값**입니다(1536×745 · fs 365.32px · 줄상자 299.56px):
`!` 잉크 덩어리 21~202(막대) / **223~282(점)** → 점 중심 252.5px = **84.29%** · `i` 잉크 덩어리 **6~60(점)** / 85~279(막대) → 점 중심 33px = **11.02%**. 가로는 글리프 잉크 중심(`!` 49.89% · `i` 50.41%).
⚠️ 측정 창을 요소 상자 **밖**까지 열면 이웃 줄의 점이 섞입니다(첫 측정에서 실제로 그랬습니다).
🚨 CSS 선택자는 **`.wordmark-line--box .wordmark-fit.wordmark-pop`** 이어야 합니다 — 3칸 규칙 `.wordmark-line--box .wordmark-fit { transform-origin: left center }` 가 (0,2,0) 이라 `.wordmark-pop`(0,1,0) 만으로는 **원점이 `left center` 로 덮입니다**(실측으로 확인).

- `prefers-reduced-motion` : 덮개 `display:none` + 팝인 `animation:none` → **전부 즉시 표시**(실측 `덮개 ['none','none']` · `팝인 ['none / none','none / none']`).
- **골격 회귀** — 좌측 박스 Δ **1.00px** · 우측 박스 Δ **−1.00px** · `!`↔`i` 중심 Δ **−1.00px** · 두 줄 양 끝 Δ **0.00 / 0.00** · 캡↔캡 **11.2%**. (중심 Δ 가 0.00 → −1.00 으로 바뀐 것은 팝인이 끝난 뒤에도 `transform: scale(1)` 이 남아 래스터화가 서브픽셀만큼 달라지기 때문입니다 — 허용 범위 |Δ| ≤ 1px 안입니다.)
- **문서 높이 11,009px 불변** · 가로 스크롤 0 · **콘솔 에러 0** · `tsc`·`eslint` 통과.
- **캡처** — `hero-split2-a.png`(0.05s) · `hero-split2-b.png`(0.25s, 팝인 중) · `hero-split2-c.png`(0.5s, 팝인 완료·덮개 출발 직전) · `hero-split2-d.png`(1.1s) · `hero-split2-done.png` / 은폐 계측용 `hero-split2-a-measure.png` / 경계 확대 `hero-split2-edge-zoom.png` / `hero-split2-reduced-motion.png` / 골격 회귀 `hero-split2-geom-1536.png`.
- ⚠️ **a 프레임(0.05s)이 완전한 검정은 아닙니다** — 오버슈트 이징이 앞이 가팔라 0.05s(진행 12%)면 `!`·`i` 가 이미 **약 30% 크기**로 올라옵니다. 기둥 **바깥**은 위 검증대로 완전히 0 입니다. 순수 검정은 t ≲ 0.01s 구간입니다.


##### 🆕 보강 3·4건 (2026-08-23 밤 사용자 피드백)

**③ Phase 1 을 "뿅" → 다시 "스윽"으로** — 사용자 **"그라데이션 적용 전에 했던 스윽 나오는 느낌으로 돌리고"**

| | 시간 | 내용 |
|---|---|---|
| **Phase 1** | 0 → **0.7s** | `!`·`i` 가 각자 **자기 칸의 오른쪽 벽 뒤**(`translateX(120%)`)에서 왼쪽으로 밀려 나온다. 2026-08-21 `wall` 의 `@keyframes wordmark-wall` 을 **그대로 재사용**(0.7s · `cubic-bezier(.22,1,.36,1)`), 두 줄 동시 |
| **Phase 2** | 0.7s → **2.3s** | split 걷힘 1.6s `ease-in-out` + 지그재그 꿈틀. `animation-delay: 0.7s`(SMIL 꿈틀도 `begin="0.7s"`)라 Phase 1 동안 덮개는 정지 |

- 클립은 기존 교훈대로 **`clip-path: inset()`**(`overflow:hidden` 은 inline-block 베이스라인을 밀어 글자가 세로로 튑니다).
- 🚨 **오른쪽 인셋은 `-2%` 가 아니라 `+1%`** 입니다. 2026-08-21 값(`-2%` = 칸 밖으로 2% 여유)을 그대로 쓰면 벽 뒤에 세워 둔 글자가 그 **2.2px 틈으로 비칩니다**(t=0.05s 실측 25px 검출). 칸 폭이 액센트 advance 라 잉크가 좌우 0.07em 씩 안쪽에 있어 1% 를 잘라도 안 닿습니다.
- 🚨 `prefers-reduced-motion` 에서 **기존 `.wordmark-glyph > span { animation: none }`(0,2,1)로는 안 꺼집니다** — 슬라이드 규칙이 (0,4,0)이라 이깁니다. reduced-motion 블록에 같은 특이도의 규칙을 따로 넣었습니다(실측 `슬라이드 ['none / none','none / none']`).
- 🚫 **"뿅"(scale 팝인)은 미사용 보존** — `@keyframes wordmark-pop` 과 `.wordmark-line--box .wordmark-fit.wordmark-pop` 규칙, 그리고 실측해 둔 점(dot) 원점(`!` 84.29% / `i` 11.02%, 가로 49.89% / 50.41%)이 `globals.css` 주석에 그대로 남아 있습니다.
- **완전 은폐 재검증(t=0.05s)** — 기둥 **바깥** 밝기 > 8/255 픽셀 **0개 · 최대 0.00**, 기둥 **안**도 **0.00**. 슬라이드는 벽 뒤에서 시작하므로 **a 프레임이 완전한 검정**입니다(팝인 때 남아 있던 "0.05s 에 이미 30%" 문제가 사라졌습니다).

**④ STUD 글자 간격 균일화** — 사용자 **"스튜디오는 지금 U가 T랑 D 사이 여백이 달라서 맞춰줘."**

자간이 균일해도 글자 **모양**이 달라 실제 잉크 간격은 균일하지 않습니다. 글자별 마스크를 따로 그려 **모든 행에서의 최단 수평 거리**로 재면(상자 아님):

| | S↔T | T↔U | U↔D | 편차 |
|---|---|---|---|---|
| 커닝 전 | 5px | 5px | **14px** | 9 |
| **커닝 후 ✅** | **5px** | **5px** | **5px** | **0** |

- **`D` 만 `margin-left: −0.022em`**(`STUD_KERN = [0,0,0,-0.022]`). 후보 실측 — −0.018 → 5/5/6 · −0.020 → 5/5/6 · **−0.022 → 5/5/5** · −0.024 → 5/5/4 · −0.026 → 5/5/3.
- 커닝이 붙으면 칸 안 글자를 **하나씩 `<span>` 으로 쪼갭니다**(`CellSpec.kern`). 🚨 쪼개면 OpenType 커닝이 꺼지지만 이 조합에서는 영향이 **1px 미만**입니다(좌측 박스 폭 Δ 1.00px 로 커닝 전과 동일).
- **자연 잉크 폭 2.4475 → 2.425em** 으로 줄어든 만큼 **압축이 풀렸습니다** — `scaleX` **0.834942 → 0.842679**(지시한 "0.835보다 커지는 방향" 충족). 자간 −0.064em·weight 860 은 그대로입니다.
- **획 굵기 재확인** — 기준 400px 계산: U 세로획 71px × 0.842679 = **59.83px** vs `SUM` 의 U **59.0px** → **Δ +0.83px(+1.4%)**(커닝 전 59.3px / +0.3px). 화면 실측(1536, 캡 40% 행)은 아래 줄 U 55 vs 위 줄 53 → **Δ 2px**(안티에일리어싱 임계 차). 2% 대라 weight 는 건드리지 않았습니다.
- **좌측 박스 폭 Δ 1.00px** — 목표 `SUM` 잉크폭에 그대로 맞습니다.

##### 검증 (③④ 공통)
- **골격 수치** — 좌측 박스 Δ **1.00px** · 우측 박스 Δ **−1.00px** · `!`↔`i` 중심 Δ **0.00px**(팝인의 잔여 `scale(1)` 이 사라져 −1.00 → 0.00 으로 되돌아왔습니다) · 두 줄 양 끝 Δ **0.00 / 0.00** · 캡↔캡 **11.2%**.
- **문서 높이 11,009px 불변** · 가로 스크롤 0 · **콘솔 에러 0** · `tsc`·`eslint` 통과 · `prefers-reduced-motion` 덮개·슬라이드 전부 없음.
- **캡처** — `hero-split3-a.png`(0.05s, **완전 검정**) · `hero-split3-b.png`(0.35s, `!`·`i` 슬라이드 중) · `hero-split3-c.png`(0.75s, 슬라이드 완료·덮개 출발 직전) · `hero-split3-d.png`(1.4s) · `hero-split3-done.png` / 은폐 계측 `hero-split3-a-measure.png` / 경계 확대 `hero-split3-edge-zoom.png` / **STUD 간격 `hero-split3-stud-zoom.png`**(전 상태 `hero-box-stud-zoom.png` 와 **같은 영역**) · `hero-split3-stud-full.png`(U↔D 까지 보이는 넓은 컷) / `hero-split3-reduced-motion.png` / 골격 회귀 `hero-split3-geom-1536.png`.


##### 🆕 보강 ⑤ — 아래 줄 액센트를 **"!" 의 거울상**으로 (2026-08-23)

사용자: **"히어로의 i부분은 !를 뒤집어서 만들어줘"**
아래 줄 액센트가 소문자 `i` 가 아니라 **위 줄과 똑같은 `!` 글리프를 세로로 뒤집은 것**이 됐습니다 — 같은 글리프이므로 **획 굵기·잉크 폭이 완전히 같은 정확한 거울상**입니다(점이 **위**, 기둥이 **아래로 넓어짐**).

- **스위치** `site.ts` `hero.wordmark.lines[1].accentFlipOf` — **`"!"`(현재·기본)** / **`null` 이면 예전 소문자 `accentGlyph`("i") 로 복귀**(되돌릴 값은 같은 객체의 `accentGlyph` 에 그대로 있습니다). `null` 실측 확인 — 아래 줄 글자 `i`, 뒤집기 래퍼 없음, **액센트 칸 폭은 두 줄 다 116.91px 로 동일**.
- 🚨 **뒤집기 축은 줄 상자 중심이 아니라 `!` 잉크의 세로 중심**입니다. 그래야 뒤집은 뒤에도 세로 잉크 범위가 그대로라 **점 위끝 = 캡 높이 / 기둥 아래끝 = 베이스라인**(위 줄 `!` 와 같은 범위)이 됩니다.
  - 픽셀 스캔(기준 2000px): `!` 잉크 위끝 **−0.707em**(= `M` 캡과 동일) · 아래끝 **+0.009em**(점 오버슛) → 잉크 세로 중심 **−0.349em**.
  - `transform-origin` 은 **상자 위 변 기준**이라 "상자 위 변 → 베이스라인" 거리가 필요합니다 — `leading-[0.82]` 상자에서 폰트 메트릭상 **0.7655em**. 🚨 **크롬은 베이스라인을 정수 px 로 스냅**하므로 실제 렌더는 0.7644em(F 380.67) · 0.7632em(F 116.61) 입니다. 축이 d 만큼 어긋나면 잉크가 **2d** 밀리므로, 두 기준 뷰포트의 잔차가 같은 크기로 갈리는 **0.764em** 을 씁니다 → 축 = 0.764 − 0.349 = **`0.415em`**(`ACCENT_FLIP_ORIGIN_Y_EM`).
- 🚨 **뒤집기는 글자 바로 바깥의 전용 래퍼(`.wordmark-flip`)** 가 맡습니다 — `.wordmark-fit` 에는 칸별 `scaleX` + `transform-origin: left center`, `.wordmark-shift` 에는 진입 `translateX` 가 이미 걸려 있어 같은 요소에 얹으면 **transform 이 통째로 덮여 서로 지웁니다**(3칸 절의 같은 교훈). 층을 나눠서 `split`·`wipe`·`spread`·`wall` **네 진입 전부에서 뒤집기가 살아 있습니다**(실측).
- **액센트 칸 상수는 두 줄 다 `!` 값**입니다 — `lsb 0.07` · 잉크 0.165 · advance 0.3071289. `BOX_SLOT` 은 원래 max(adv `!`, adv `i`) 라 **값 그대로 0.3071289**(칸 폭은 두 줄 공용 한 폭 유지). `accentOffsetEm()` 이 두 줄 완전히 같은 값을 내므로 **잉크 중심 x 가 구조적으로 일치**합니다.
- ⚠️ 아래 줄 잉크 위끝이 예전 `i`(점이 캡보다 0.04em 위)보다 **0.04em 내려와 캡 높이에 정확히 맞습니다** — 줄 간격(`WM_LINE2_MT_EM`)은 캡 기준이라 **건드리지 않았습니다**.

**검증 실측** (픽셀 스캔 · 고정 UI 숨김 · 진입 완료 후, Δ 는 아래 줄 − 위 줄, **각 줄 자기 베이스라인 기준**)
| 뷰포트 | Δ 잉크 위끝 | Δ 잉크 아래끝 | **Δ 잉크 중심 x** | Δ 잉크 폭 | 가로 스크롤 | 콘솔 |
|---|---|---|---|---|---|---|
| **1440×900** | **+0.16px** | **−0.84px** | **0.00px** | **0.00px** | 0 | 0 |
| **390×844 (DSF2)** | **+0.20px**(0.40 기기px) | **+0.20px** | **0.00px** | **0.00px** | 0 | 0 |

- **진입 4종 전부 같은 수치**(Δ 위끝 0.16 · 아래끝 −0.84 · 중심 x 0.00 · 폭 0.00 @1440) — `split`(기본) · `wall` · `wipe` · `spread` 를 값만 바꿔 가며 실측했고 **콘솔 에러 0**. `prefers-reduced-motion` 에서도 뒤집기 유지(슬라이드만 꺼짐).
- **Phase 1 슬라이드와 합성 확인** — `wordmark-wall` 을 t=0.35s 에 정지시켜 찍은 프레임에서 두 액센트 모두 `translateX 5.44px` 이고 아래 줄 래퍼는 `matrix(1,0,0,-1,0,0)` 유지.
- `tsc`·`eslint` 통과(기본값·`null` 복귀값 양쪽).

#### 🆕 **한 줄 `SUMIM` + 글자 파도 (2026-08-24)** — `layout: "one-line"` 이 새 기본값

사용자 지시 3건이 같은 날 겹쳐 들어왔습니다.
① **"스튜디오는 없애고 SUMIM 만 남기는데, 느낌표도 그냥 i 로 설정"**
② **"소문자 i 말고 원래 글자 I 그대로, 색만"** ← ①의 액센트 부분을 뒤집은 최종 지시
③ **"원래 형태를 유지한 채로 연한 파도가 일렁거리게"**

##### 되돌리는 표 — `site.ts` `hero.wordmark.layout`
| 값 | 화면 | 언제 |
|---|---|---|
| **`"one-line"`** | **`SUMIM` 한 줄 · 3칸 박스 `[SUM][I][M]`** | **현재 · 2026-08-24** |
| `"two-line"` | `SUM!M` / `STUD¡O` 두 줄 · 같은 3칸 골격 | 2026-08-22~23 |
| `"single"` | `SUM!M` 한 줄 · 글자별 진입 + `markersSingle` 개 "+" 마커 | 2026-08-21 |

- `"one-line"` ↔ `"two-line"` 은 **같은 3칸 골격**이라 값만 바꾸면 됩니다. `"single"` 은 골격 자체가 다른 옛 구성입니다.
- **두 줄 코드는 한 줄도 지우지 않았습니다** — `lines[1]`(STUDIO)·`STUD_KERN`·`STUD_SCALE_X`·`O_SCALE_X`·`ACCENT_FLIP` 전부 그대로 살아 있고 `WM_CELLS_TWO_LINE` 에 담겨 있습니다.
- 🔀 액센트 글리프는 `hero.wordmark.oneLine.accentGlyph` — **`null`(현재) = 치환 없이 원래 `I`** / `"i"` = 소문자 `SUMiM` / `"!"` = 예전 `SUM!M`. **기둥 폭·잉크 상수는 `hero.tsx` 의 `ACCENT_METRICS` 가 글리프에 맞춰 자동으로** 고릅니다(세 글자 다 픽셀 스캔 실측값 보유).

##### 3칸 골격 재실측 (`SUMIM` 전부 대문자 · 픽셀 스캔 · weight 700 · 자간 −0.04em)
| 항목 | 값(em) | 두 줄(`!` 기둥)과 비교 |
|---|---|---|
| `BOX_LEFT` (`SUM` 잉크) | **2.043496** | 동일 |
| `BOX_GAP_L` | **0.0178125** | 동일 |
| `BOX_SLOT` (`I` advance) | **0.2661133** | `!` 0.3071289 · `i` 0.2568359 |
| `BOX_GAP_R` | **0.02** | 동일 |
| `BOX_RIGHT` (`M` 잉크) | **0.765** | 동일 |
| **전체 잉크 폭** | **3.1124218** | 3.1534375 |

🚨 **좌·우 칸과 두 간격은 가운데 글자가 무엇이든 안 변합니다** — `BOX_GAP_L` 은 `M` 의 rsb + 자간, `BOX_GAP_R` 은 `M` 의 lsb + 자간만으로 정해지기 때문입니다(실측 확인). 바뀌는 건 **기둥 폭 하나**뿐입니다.
글자별 실측: `I` adv **0.2661133** · lsb **0.06** · 잉크 **0.1475** · 캡 0.7075 · 오버슛 0.

##### 크기 — 한 줄이라 **세 뷰포트 전부 폭이 잡습니다**
- **폭 기준 `30.8vw`** — 잉크가 뷰포트 폭의 **96%** 가 되는 값(`96 / 3.1124218`). ⚠️ 액센트 글리프를 바꾸면 다시 잡아야 합니다: `!` → 30.4 · `i` → 30.9 · **`I` → 30.8**.
- **세로 기준** 분모가 두 줄의 1.6287 → **`0.82`**(한 줄 `leading` 그대로). 식은 그대로 `min(30.8vw, max(40px, calc((100svh − var(--hero-reserve)) / 0.82)))`.

| 뷰포트 | 폭 기준 | 높이 기준 | **잡는 축** | 실제 F | 잉크 폭 / 보이는 폭 | 가로 스크롤 |
|---|---|---|---|---|---|---|
| 1536×745 | 470.0px | 567.1px | **폭** | 468.47 | 1457px = **95.8%** | 0 |
| 1920×1080 | 588.6px | 975.6px | **폭** | 586.74 | 1825px = **95.8%** | 0 |
| 390×844 (DSF2) | 120.5px | 614.6px | **폭** | 120.12 | 375px = **96.0%** | 0 |

- 선언문 블록(`statementPlacement: "below"`, 2026-08-23 사용자 결정)은 **손대지 않았고** 세 폭 모두 첫 화면 안입니다(하단 y 665.8 / 881.8 / 626.5).
- ⚠️ **모바일은 여전히 세로가 빕니다** — 폭이 크기를 잡아 844px 화면에서 잉크 세로가 88px 뿐입니다(한 줄이라 두 줄 때 166px보다 더 낮아졌습니다). 모바일 전용 크기 규칙은 아직 없습니다.
- **문서 높이는 이 변경과 무관합니다** — `one-line` / `two-line` 둘 다 히어로 745px · 문서 **9,083px** 로 같았습니다(실측). 11,009 → 9,083 은 이 세션 밖의 다른 작업(대표 섹션 삭제·FAQ 축소·Q&A combo 등)에서 온 값입니다.

##### 진입 — `split` 한 줄판 (그대로 동작)
Phase 1 = **`I` 가 자기 칸 오른쪽 벽에서 스윽**(0.7s) → Phase 2 = 좌(SUM) 덮개는 왼쪽으로 · 우(M) 덮개는 오른쪽으로 **같은 1.6s** 에 걷힘(지그재그 + blur 페더 + 꿈틀). 값·구조는 2026-08-23 그대로이고 기둥 좌표만 `I` 기준으로 옮겨졌습니다.
**완전 은폐 재검증(t=0.05s)** — 워드마크 블록 범위에서 **액센트 기둥(x 1004~1130) 바깥 밝기 > 8/255 픽셀 0개 · 최대 밝기 0.00**, 기둥 안도 **0.00**(벽 뒤에서 시작). ✅

##### 🌊 글자 파도 (`letterWave: true`)
액센트를 뺀 좌·우 칸(`S·U·M` / `M`)에만 **아주 약한 액체 왜곡**이 상시 흐릅니다. **액센트 `I` 는 정지** — 기둥이 기준축이라 여기가 흔들리면 워드마크가 통째로 떠 보입니다(실측 `I` 칸 `filter: none`).

- **파이프라인** `feTurbulence(fractalNoise, numOctaves 1)` → `feOffset`(세로 이동) → `feColorMatrix`(R 을 0.5 로 고정) → `feDisplacementMap`.
  🚨 **R 고정 = 가로 변위 0** → **세로로만** 밀립니다 = 가로 획이 출렁이는 물결(지시한 "지그재그 아님").
- 🚨 **움직임은 `baseFrequency` 진동이 아니라 `feOffset`** 입니다. 처음엔 주파수를 흔들었는데 노이즈가 **원점 기준으로 재스케일**될 뿐이라 0.5s 간격 픽셀 변화가 **0.06%**(사실상 정지)였습니다. 노이즈 래스터를 세로로 밀면 마루가 실제로 흘러 **0.62%** 가 됩니다(10배). 필터 영역을 위아래로 열어(`y −60% / height 220%`) 밀어 낼 노이즈를 확보했습니다.
- 🚨 **진행 방향(y) 주파수가 "움직임 인지"를 지배**합니다 — 스윕 실측(0.5s 변화율): fy 0.003 → 0.18% · 0.006 → 0.05% · 0.010 → 0.51% · **(fx 0.005, fy 0.008) → 0.9%**. 마지막 조합 채택.
- `dy` 를 `0 → −220 → 0` 으로 **왕복**시킵니다 — turbulence 는 주기가 없어 한 방향으로 계속 밀면 루프가 안 맞습니다. spline 이징이라 양 끝에서 부드럽게 섭니다. **주기 6s**(지시 4~7s).
- **진폭 = `scale` 7px** — 후보 캡처 비교(`hero-sumim-wave-{1,2,3}.png`, 1536 · F 468px): **5px(1.06%)** 는 정지 프레임에서 거의 안 보이고, **9px(1.91%)** 는 `M` 꼭짓점 가장자리가 살짝 녹은 듯 보입니다. **7px = 1.49%** 가 "형태 유지 + 일렁임 인지" 균형점입니다(지시 1~2% 안).
- 🚨 `scale` 은 **px** 이라 글자 크기에 비례하지 않습니다 — 폭 구간으로 두 벌을 두고 CSS 미디어 쿼리(1024px)로 갈아 끼웁니다: **lg 7px / sm 2px**(390에서 F 120px → **1.66%**). 파장·이동량도 같은 비율로 줄였습니다(sm `0.0195 0.0312` · dy 56).
- 🚨 **`numOctaves` 는 1** 입니다 — 2 와 비교하니 ① 결이 노이지해져 "부드러운 물결"이 덜하고 ② idle 메인스레드 비용이 **8.0% → 11.5%(+30%)** 였습니다. 1 이 양쪽 다 낫습니다.

##### 성능 실측 (1536 · 헤드리스 **소프트웨어 렌더**라 실제 GPU 환경보다 비관적)
| | rAF fps | 메인스레드 Task 점유 |
|---|---|---|
| 파도 ON · idle | **60** | **10.8%** |
| 파도 ON · 스크롤 | **60** | **12.7%** |
| 파도 OFF · idle | 60 | 5.2% |
| 파도 OFF · 스크롤 | 60 | 11.1% |

→ **파도가 더 쓰는 몫은 idle +5.6%p · 스크롤 +1.7%p**, fps 는 네 조건 모두 **60 유지**(지시 임계 55fps 여유). 그래서 `letterWave` 기본값을 **`true`** 로 두었습니다.
⚠️ 완화 카드는 이미 하나 썼습니다(`numOctaves` 2 → 1, 비용 −30%). 더 필요해지면 필터 영역(`height 220%`)을 줄이는 것이 다음 순서입니다.

- **진입과의 관계** — 파도는 상시입니다(덮개 아래에서도 돌지만 안 보입니다). `prefers-reduced-motion` 이면 **파도·덮개·슬라이드 전부 없음**(실측 `파도 ['none','none'] · 덮개 ['none','none'] · 슬라이드 ['none']`).
- 필터는 **워드마크 칸에만** 걸립니다 — 히어로 밖 영향 0.

##### 검증·캡처
- `tsc`·`eslint` 통과 · **콘솔 에러 0**(세 뷰포트) · 가로 스크롤 0 · 마커 0 · `sr-only h1 "SUMIM Studio"` 유지.
- **캡처** — `hero-sumim-1536.png` · `hero-sumim-1920.png` · `hero-sumim-390.png` / 진입 `hero-sumim-{a,b,c,d,done}.png`(0.05s **완전 검정** / 0.35s `I` 슬라이드 중 / 0.75s 슬라이드 완료 / 1.4s 걷히는 중 / 2.8s 완료) · 은폐 계측 `hero-sumim-a-measure.png` / 파도 진폭 후보 `hero-sumim-wave-{1,2,3}.png`(5·7·9px) · 옥타브 비교 `hero-sumim-wave-oct{1,2}.png` · **움직임 두 시점 `hero-sumim-wave-t0.png` / `hero-sumim-wave-t1.png`**(0.5s 간격, 픽셀 0.62% 변화) / `hero-sumim-reduced-motion.png`.


#### 🆕 **히어로 최종 색·선언문 (2026-08-24 저녁)** — 흰 `I` · 보라 파도 · 큰 한 줄 선언문

사용자: **"히어로 I 흰색으로 바꾸고 파도를 기존 I 색상으로 변경해보자. 밑에 선언문은 '매출이 나는 홈페이지를 만듭니다'를 제외하고 다 지워주고, 매출이 나는 홈페이지를 만듭니다 자체를 SUMIM 밑에 크게 놔둬줘. SUMIM과 가로 넓이 같게끔"**

> 바로 앞 단계(같은 날 오후)에서 글자 파도가 `displace`(외곽선 밀기) → **`letterWave: "fill"`**(글자 안쪽에 물결 3겹, `globals.css` `.wordmark-wavefill`, `background-clip: text`)로 바뀌어 있었습니다. 이 절은 그 위에 얹은 **색·선언문** 변경입니다.

##### ① 액센트 `I` 를 흰색으로 — 스위치 보존
- `hero.tsx` 의 **`ACCENT_TINT: boolean = false`** 한 줄이 전부입니다. **`true` 로 올리면 `I` 가 다시 CTA 보라**로 돌아옵니다(그때는 아래 `WAVE_ON_ACCENT` 도 같이 꺼야 합니다 — `wordmark-wavefill` 은 글자를 `-webkit-text-fill-color: transparent` 로 비우고 배경으로 칠하므로 `.text-cta`(글자색)가 아무 효과가 없습니다).
- 액센트 구조는 **하나도 지우지 않았습니다** — `oneLine.accentIndex: 3` · `ACCENT_METRICS`(`!` .3071 / `i` .2568 / **`I` .2661**) · `accentOffsetEm()` · 기둥 폭 계산 전부 그대로입니다.

##### ② 글자 안 파도를 **CTA 보라**로 — 🚨 `background-blend-mode: color`
🚨 **data-URI SVG 안에는 CSS 변수를 못 씁니다.** 그래서 파도 SVG 는 **회색조(검정 알파)로 그대로 두고**, 배경 레이어 맨 위에 `linear-gradient(var(--color-cta), var(--color-cta))` 한 겹을 깔아 **`background-blend-mode: color`** 로 색만 입힙니다.

- `color` 블렌드 = `SetLum(소스, Lum(백드롭))` — **색상·채도는 소스(CTA), 명도는 백드롭**.
  - 물이 없는 곳(백드롭 = 잉크 `#ffffff`, Lum 1) → SetLum(보라, 1) = **정확히 흰색**(ClipColor 가 흰으로 수렴). 흰 `I`·흰 선언문과 색이 **완전히 같습니다**.
  - 물이 깊을수록 백드롭이 어두워져 **점점 진한 보라**가 됩니다.
- ⇒ SVG 는 하드코딩인데도 **색은 `--color-cta` 를 100% 따라갑니다.** 실측 — `data-palette="navy"` 로 바꾸면 `--color-cta` `#ea580c` 이고 **파도가 주황으로 함께 바뀝니다**(`hero-final-palette-navy.png`). colorLab 주입도 같은 변수를 바꾸므로 동일하게 따라갑니다.
- 🔁 **검정 물로 되돌리려면** `globals.css` `.wordmark-wavefill` 의 `background-blend-mode` 줄과 첫 번째 `linear-gradient` 층, `background-size/position/repeat` 의 **첫 항목만** 지우면 2026-08-24 오후 값으로 복귀합니다.

**알파(= 물 깊이) 후보 비교** — 겹별 비율(앞 1 : 중 0.825 : 뒤 0.65)과 수위 차(앞 25% / 중 40% / 뒤 60%)는 그대로 두고 앞 겹 최대 알파만 바꿔 같은 구도로 캡처했습니다.

| 후보 | 알파(앞/중/뒤) | 가장 깊은 곳 백드롭 명도 | 화면 |
|---|---|---|---|
| **a35 ✅ 채택** | .35 / .29 / .23 | **0.355** | 바닥이 `#6338EE` **원색 그대로**(보라 자신의 명도 0.349와 거의 같음) |
| a55 | .55 / .45 / .36 | 0.158 | 바닥이 짙은 남보라로 가라앉음 |
| a75 | .75 / .62 / .49 | 0.048 | 거의 검정 — "보라"가 아니라 **검은 얼룩**으로 읽힘 |

오후 값(2.6~4%)은 **거의 안 보였고**(사용자 "파도가 안 보여"), a35 는 정지 프레임에서 3단 층이 또렷합니다.

##### ③ `I` 에도 파도를 넣는다 — `WAVE_ON_ACCENT: boolean = true`
`hero-final-ion.png` / `hero-final-ioff.png` 를 같은 구도로 찍어 비교했습니다. **`I` 를 빼면 기둥만 순백으로 남아 `SUM`·`M` 의 물결이 거기서 뚝 끊기고 워드마크가 세 조각으로 읽힙니다.** 넣으면 `--wm-wave-org`(기둥 = `BOX_X_SLOT` 2.0613085em)로 위상이 이어져 물결이 워드마크를 **한 줄기로 가로지릅니다**. `false` 로 내리면 예전처럼 액센트 칸만 무늬 없이 남습니다.

🚨 **`slide` 칸은 파도를 한 층 안쪽(`.wordmark-shift`)에 건다** — 진입 `translateX` 애니메이션이 걸린 층이 있으면 크롬이 그 층을 따로 합성해 **조상 칸의 `background-clip: text` 가 그 글자를 못 잡습니다. `I` 가 통째로 사라집니다**(첫 캡처에서 실제로 "SUM M"으로 나왔습니다 — clip-path·z-index·blend 를 하나씩 꺼 보고 **애니메이션이 원인**임을 확인). transform 이 걸린 요소 **자신**에게 걸면 정상입니다. `.wordmark-shift` 의 왼쪽 변 = 칸의 왼쪽 변이라 `--wm-wave-org` 는 그대로 넘겨도 위상이 안 어긋납니다(실측).

##### ④ 선언문 = **마지막 한 줄만, 워드마크 폭에 맞춰 크게** (= 화면에 보이는 `h1`)
- `site.ts` 의 `hero.wordmark.statement` **3줄 문자열은 한 글자도 안 고쳤습니다.** 렌더만 `STATEMENT_LINE_INDEX = 2`(마지막 줄)로 줄였습니다. 앞 두 줄은 주석으로 "2026-08-24 히어로에서 제외"라 표시해 뒀습니다.
- 이 줄이 **`id="hero-heading"` 인 `h1`** 이 됐습니다(예전 `sr-only h1 "SUMIM Studio"` 를 대체 — 브랜드명은 네비 로고·`<title>` 담당). `statementPlacement: "none"` 으로 내리면 sr-only h1 로 자동 복귀합니다.

🚨 **폭 일치는 잉크 기준**(3칸 골격과 같은 규칙). 픽셀 스캔 실측(Pretendard Variable 700 · 자간 0 · 기준 400px, 문장을 한 번에 그려 잉크 열 스캔):

| 항목 | **마침표 포함(채택)** | 마침표 뺀 안 |
|---|---|---|
| advance | 13.074707em | 12.792480em |
| 첫 글자 "매" 사이드베어링 | 0.05em | 0.05em |
| **잉크 폭** | **12.9675em** | 12.7375em |
| 배율(= `WM_TOTAL_INK` / 잉크폭) | **0.240017** | 0.244352 |

- 문장 글자 크기 = **`calc(var(--wm-size) * 0.240017)`** → 1536 에서 F 473.09 → **113.55px**. 두 잉크 폭이 **정의상 같습니다**.
- 왼끝 맞추기 = `margin-left: calc(var(--wm-size) * -0.025101)` — 워드마크 `<p>` 의 광학 보정 `WM_OPTICAL_EM 0.0131` + 문장 사이드베어링 `0.05 × 0.240017`. ⚠️ `em` 으로 주면 **h1 자신의 글자 크기**라 틀립니다.
- **자간은 0** 입니다 — 배율만으로 |Δ| ≤ 1px 이 나와 자간 조정이 필요 없었습니다(`HEADLINE_LS_EM` 상수는 남겨 뒀습니다).
- **마침표는 포함**합니다. 뺀 안(`hero-final-nodot-1536.png`)은 문장이 0.23em 짧아진 만큼 글자가 커져 **`SUMIM` 보다 굵어 보이고**, 오른끝이 마침표 대신 "다"의 세로획이라 오히려 `M` 의 사선 끝과 붙어 보입니다. 사용자 확정 카피에도 마침표가 있습니다.
- 🚨 **`h1` 은 워드마크 래퍼 안에 두고 `w-0 min-w-full`** 입니다 — 폭 0 이라 래퍼의 shrink-to-fit 계산에 끼어들지 않고(끼면 문장이 래퍼를 넓혀 워드마크가 가운데서 밀립니다), 실제 폭은 `min-width: 100%` 로 래퍼 폭(= 워드마크 잉크 폭)이 됩니다.
- 🚨 **덮개 전용 안쪽 층을 하나 더 뒀습니다** — `.wordmark-split` 은 `height: calc(100% + 1.5em)` 이라 위치 기준 조상의 높이를 그대로 씁니다. 선언문이 래퍼 안에 들어오면서 덮개가 문장까지 덮지 않도록 **워드마크만 감싸는 `<div>`** 를 추가했습니다(선언문이 없을 때는 예전 래퍼와 높이가 같아 **동작 완전 동일**).

##### ⑤ 메타·CTA 는 그대로, 배치만
`hero.factsLine`("부산 · 대표가 처음부터 끝까지 직접 진행 · 예약제 운영")과 CTA 2개(`statementCta` "홈페이지 무료 진단 받기" / `statementLink` "작업 보기 ↓")는 **문구·링크 한 글자도 안 바꿨습니다.** 큰 문장 아래 한 행(좌 메타 / 우 CTA, `lg` 미만 세로 스택)으로 재배치했을 뿐입니다.

**진입 리빌** — 문장은 `--reveal-delay: 2.30s`(= `WORDMARK_DONE` = 슬라이드 0.7 + 걷힘 1.6), 메타·CTA 블록은 **2.42s** 로 기존 `.reveal-load` 스태거를 씁니다.

##### 🚨 세로 예산 — 문장은 **고정 px 가 아니라 계수**에 들어간다
문장 크기가 `--wm-size × 0.240017` 이라 워드마크에 **비례**합니다. 고정 px 로 `--hero-reserve` 에서 빼면 세로가 좁은 화면에서 식이 발산하므로 **분모에 더했습니다**:

```
WORDMARK_BOX_COEF = 0.82 (한 줄 leading) + 0.240017 (문장) = 1.060017
--hero-reserve : 230px (≥1024) / 300px (<1024)     ← 2026-08-23 의 280/340 은 선언문 3줄 시절 값
```
- 230px = 패딩 144(80+16+16+32) + 문장 위 여백 8 + [메타+CTA] 행 68 + 여유 10.
- ⚠️ **세 뷰포트 전부 여전히 폭이 크기를 잡습니다** → **워드마크는 한 픽셀도 안 작아졌습니다**(F 473.09 / 591.36 / 120.12 = 각각 정확히 30.8vw). 세로 타협이 **필요 없었습니다.**

##### 검증 실측 (스크롤바 보이는 상태 · 픽셀 스캔 · 고정 UI 숨김)
| 뷰포트 | F | 문장 크기 | 배율 | **Δ 왼끝 / Δ 오른끝** | 잉크폭 / 뷰포트 | 첫 화면 하단 y | 섹션 높이 | 가로 스크롤 |
|---|---|---|---|---|---|---|---|---|
| **1536×745** | 473.09 | 113.55 | 0.240017 | **−1.0 / +1.0px** | **95.8%** | 문장 615.2 · **CTA 703.2** | 745 | 0 |
| **1920×1080** | 591.36 | 141.94 | 0.240016 | **−1.0 / +1.0px** | 95.8% | 문장 845.4 · CTA 933.4 | 1080 | 0 |
| **390×844**(DSF2) | 120.12 | 28.83 | 0.240017 | **0 / +0.5px**(CSS px) | 95.8% | 문장 437.4 · CTA 593.9 | 844 | 0 |

- **1536×745 첫 화면에 워드마크 + 큰 문장 + 메타 + CTA 가 전부 들어옵니다**(CTA 하단 703.2 ≤ 745, 여유 41.8px). 섹션 높이가 세 뷰포트 다 `100svh` 그대로라 콘텐츠가 넘치지 않았습니다.
- **문서 높이 9,083px @1536 — 이 변경과 무관하게 불변**(한 줄 SUMIM 절의 기록값과 동일).
- `prefers-reduced-motion` — **파도 `animation: none`(물은 남고 흐름만 정지)** · 덮개 `display:none` · 슬라이드 `none` · 리빌 `none / opacity 1`(실측).
- 🚨 **글자 밖으로 파도가 새지 않는지 픽셀 검증**("글자 사이 여백이 연보라로 보인다"는 지적) — 파도만 끈 기준 렌더(글자 순백)와 **픽셀 단위 diff**: 기준에서 검정인 자리에 색이 있는 픽셀이 1536 **47개 / 620,544** · 390 **147개 / 179,400**, 그마저 **최대 밝기 합 36/765(≈rgb 12,12,12)** 로 전부 글리프 가장자리 안티에일리어싱입니다. 좌표 샘플링도 잉크 왼끝−20px · `M↔I` · `I↔M` · 오른끝+20px 전부 **rgb(0,0,0)**. 3~4배 확대 캡처(`hero-final-gap-zoom-1536.png` · `hero-final-gap-zoom-390.png`)에서도 칸 사이가 **딱 떨어지는 순수 검정**입니다. ⇒ `background-clip: text` 는 정상이고, 연보라로 보이는 것은 **칸 사이 위상을 이어 붙여 수위가 가로로 연속**되기 때문입니다(의도한 결과 — ③ 참고). 검증 스크립트 `f-leak.mjs` · `f-leak2.mjs`.
- `tsc`·`eslint` 통과 · **히어로발 콘솔 에러 0**(dev 의 `next/image` quality 경고 9건은 이 작업과 무관한 기존 설정 경고입니다).

##### 캡처 (scratchpad)
`hero-final-1536.png` · `hero-final-1920.png` · `hero-final-390.png` / 파도 알파 후보 `hero-final-wave-a35.png`(채택) · `hero-final-wave-a55.png` · `hero-final-wave-a75.png` / `I` 파도 `hero-final-ion.png`(채택) · `hero-final-ioff.png` / 마침표 뺀 안 `hero-final-nodot-1536.png` / 진입 `hero-final-b.png`(0.35s `I` 슬라이드 중) · `hero-final-done.png`(3.2s) / 팔레트 추종 `hero-final-palette-navy.png` / `hero-final-reduced-motion.png`.
측정·캡처 스크립트: `f-lib.mjs` · `f-scan.mjs` · `f-base.mjs`(문장 잉크 실측) · `f-geom.mjs`(3해상도 Δ) · `f-caps.mjs`.


#### 🆕 **히어로 v9 + 인트로 시퀀스 (2026-08-24)** — `enter: "intro"` 가 새 기본값

사용자 지시가 여러 차례에 걸쳐 들어왔고, 마지막 상태만 아래에 정리합니다(중간 시도는 각 항목의 "왜 이렇게 됐나"에 남깁니다).

1. "검은 화면에서 '매출이 나는 홈페이지를 만듭니다.' 이 멘트가 저 파도 치는 물결이 점점 상승하는 느낌으로 **화면 정중앙**에 나타났다가 **지금 히어로 섹션 위치로 내려가고**, 스밈의 **I 도 다른 알파벳들이랑 같이** 나타났으면"
2. "차오르는 동안 텍스트 **윤곽이 살짝 보이는데** 없앨 수 있나" / "파도는 **그라데이션** 줘서 자연스럽게" / "**색상도 좀 더 밝게**"
3. "히어로의 **파도 높이를 올려줘**. 흰 부분이 적게 나오도록"
4. "스밈 처음 생길 때도 **색이 차오르지 말고**, 글자 내에서 **파도 모양이 먼저** 보이고 비어 있는 부분이 **흰 색상으로 차오르게**"
5. "**파도치는 모양대로** 나올 순 없는걸까" → "**반이 먼저 있지 말고 파도에 따라 같이 차올랐으면**"
6. "**그라데이션은 지우고**" (2회 — 최종적으로 **전면 제거**)
7. "첫 문장 등장 화면에서는 **헤더도 가려졌으면**"
8. "**첫 인트로 등장 부분처럼 SUMIM 도 똑같이 파도 적용**해줘"
9. "**보라색 차고 잠깐 멈추고 흰색 차는 게 어색하다. 보라색이랑 같은 속도로 흰색이 차오르면**" · "**그라데이션은 아예 지우자**" · "**파도도 스밈에서 무한히 지속되는 그 파도 크기로**"
10. 히어로 구성 3건 — 메타 줄을 **영문 카피라이트**로 · 선언문을 **작게 · 스밈 하단 오른쪽**으로 · **CTA 2개 삭제**

##### v9 히어로 구성 (인트로가 끝난 최종 화면)

```
      ┌─────────────────────────────────────────┐
      │   S U M I M                             │  ← 워드마크(상시 파도, 수위 55/70/88%)
      │             매출이 나는 홈페이지를 만듭니다.│  ← h1, --wm-size × 0.075, 오른끝 정렬
      │                  © 2026 SUMIM Studio · Busan│  ← 카피라이트 12px, 같은 오른끝
      └─────────────────────────────────────────┘
```

- **CTA 2개 삭제** — `HERO_CTA_ON: false`. `site.ts` 의 `statementCta`·`statementLink`·`factsLine` 과 `HeroStatement` 컴포넌트는 **한 글자도 안 지웠고** `true` 로 올리면 그대로 돌아옵니다. 전환 동선은 네비 [무료 진단] + 하단 상담 바가 맡습니다(2026-08-21 과 같은 판단).
- **카피라이트 문구**는 `site.ts` 의 `hero.wordmark.meta`(2026-08-21 확정값) 그대로입니다.
- 🚨 **오른끝 정렬은 구조로 맞습니다** — 워드마크 래퍼(`inline-block`)의 폭이 `<p>` 의 마진 박스(= `WM_TOTAL_INK − WM_OPTICAL_EM`)라 **래퍼 오른쪽 변 = 마지막 `M` 잉크 오른끝**입니다. 그래서 `w-0 min-w-full` + `text-right` 만으로 맞고, 남는 건 문장 마지막 글자(마침표)의 **우측 사이드베어링 0.057207em** 뿐이라 그만큼만 밉니다.
- 🚨 미는 방법은 **`position: relative; right: −0.057207em`** 입니다 — `margin-right` 음수는 `width:0 + min-width:100%` 블록에서 **과잉 제약이 되어 무시**됩니다(LTR 규칙). 단위는 **h1 자신의 em** 이라 배율이 구간마다 달라져도 항상 맞습니다.

**문장 크기 후보 비교** (`hero-v9-size-{055,075,095}.png`, 1536 기준)

| 배율 | 크기 | 판단 |
|---|---|---|
| 0.055 | 26.0px | 캡션처럼 읽히고, 12px 카피라이트와 **2배 차이뿐**이라 위계가 흐리다 |
| **0.075 ✅ 채택** | **35.5px** | 선언문으로 읽히고 카피라이트와 **약 3배** 차이 · 워드마크(473px)를 압도하지 않는다 |
| 0.095 | 44.9px | 워드마크와 **경쟁하기 시작**하고 시선이 분산된다 |

🚨 **모바일은 배율이 다릅니다** — 워드마크가 30.8vw 라 390 에서 F=120px 뿐이고 같은 0.075 면 문장이 **9px** 로 안 읽힙니다(실측). `globals.css` 가 `--hero-headline-ratio` 를 **<1024 에서 0.115**(390 → 13.8px)로 올립니다. 중앙 배율 `--hero-headline-center` 도 같은 비율로 올려 **확대 배율 k = center / ratio ≈ 2.13 이 두 구간에서 같습니다.**

##### 타임라인 (총 **3.85s** — 지시 "4초 이내")

| 구간 | 시각(s) | 무엇이 | 상수(`hero.tsx`) |
|---|---|---|---|
| — | 0 → 0.15 | **완전한 검은 화면**(네비도 숨김) | `INTRO_RISE_DELAY 0.15` |
| **A 차오름** | 0.15 → **1.65** | 문장이 **화면 정중앙**에서 **보라 물결로 아래→위로**(2.13배 확대) | `INTRO_RISE_DUR 1.5` |
| **A′ 색 전환** | 1.55 → **1.95** | 보라 복제본이 꺼지고 **진짜 흰 문장**이 켜진다 | `INTRO_TINT_DELAY 1.55` · `INTRO_TINT_DUR 0.4` |
| **B 이동+축소** | 1.80 → **2.70** | 문장이 **워드마크 아래 오른쪽 제자리·제 크기**로 | `INTRO_MOVE_DELAY 1.8` · `INTRO_MOVE_DUR 0.9` |
| 네비 | 1.85 → **2.35** | 상단 고정 네비 페이드 인 | `INTRO_NAV_DELAY 1.85` · `INTRO_NAV_DUR 0.5` |
| **C 상승(한 번에)** | 2.15 → **3.45** | 전선이 글자 **바닥에서 꼭대기까지 등속**으로 한 번에 올라간다. 55% 수위 아래는 **물(보라)**, 그 위는 **흰색** — 자동으로 이어진다 | `INTRO_WM_DELAY 2.15` · `INTRO_WM_PHASE_DUR 1.3` |
| 메타 | 3.25 → **3.85** | 카피라이트 `.reveal-load` | `INTRO_META_DELAY 3.25` |

- 🚨 **Phase C 는 2막이 아니라 한 동작**입니다(2026-08-25 사용자 "보라색 차고 잠깐 멈추고 흰색 차는 게 어색하다"). 앞 판은 [물 상승] → [흰색 상승] **두 애니메이션을 딜레이로 이어 붙여** 경계에 멈춤이 보였습니다. 지금은 **전선 변수 하나(`--hero-front`)** 가 `linear` 로 한 번에 올라가고 물/흰색은 그 값에서 **파생**됩니다.
- **로드 시 1회**(CSS `fill: both`, IO 0) · **스크롤 안 잠금** · **JS 0 · 클라이언트 컴포넌트 0**.

##### 🚨 물은 **"칠"이 아니라 "마스크"** — 그리고 그라데이션은 **전부 뺐다**

`.wordmark-wavefill`(상시 파도)의 `background-clip:text` + `blend:color` 방식은 **물이 없는 곳이 순백**이라 차오르기 전에도 글자가 다 보입니다. 인트로는 반대여야 하므로 **복제 글자를 CTA 파생색으로 두고 물결 모양을 `mask-image` 로** 씌웁니다(마스크 알파 0 = 검은 밴드 그대로).

- 🆕 **그라데이션 전면 제거**(사용자 "그라데이션 줘도 여전히 어색") — 물은 **균일한 불투명 색**, 수면 경계는 **또렷한 파형**입니다. 층 구분은 **겹별 알파**만으로: **앞 겹 1.0 / 뒤 겹 0.5**. 몸통은 알파 1(원색), 두 마루 사이만 0.5 로 한 단 옅습니다.
- 계단 방지용 **`feGaussianBlur stdDeviation="0.5"`(0.01em)** 만 남겼습니다(지시 상한 0.03em 안).
- 타일 — viewBox `0 0 120 240` · **세로 4.8em** · 수면 중심선 y=120(타일 위에서 2.4em) · 진폭 앞 **0.13em** / 뒤 **0.26em** · 파장 **1.4 / 2.8em**(드리프트 키프레임 `wm-wavefill-x1`/`x3` 공용이라 **정확히 한 파장**을 움직여야 이음매가 없습니다).

##### 🚨 "차오르는 동안 윤곽이 보인다" — **원인은 마스크 타일의 위 변**이었다 (픽셀로 규명)

t=0.8s 프레임을 **행 단위로 스캔**하니 물 위쪽에 **정확히 3줄**만 밝은 행이 있었고, 그 y 가 세 겹의 `mask-position-y`(= 타일 위 변)와 **1px 오차 없이 일치**했습니다(y 337 / 356 / 364, 최대 밝기 **3 / 4 / 5** of 255). `.hero-intro-water` 만 끄면 0 이 되므로 진짜 `h1`·덮개·AA 가 아니라 **타일 위 변의 리샘플링 잔여 알파**입니다.

⇒ **타일 세로를 1.4em → 4.8em 으로 키우고 수면을 타일 위에서 2.4em** 에 뒀습니다. 수위가 가장 낮을 때도 타일 위 변이 잉크 밖(−0.85em)이라 **어떤 순간에도 잉크와 만나지 않습니다.**

**검증** — 수면의 부드러운 경계를 뺀 "완전히 물 밖" 구간에서 **밝기 > 2/255 픽셀 0개 · 최대 밝기 0**(문장 t=0.3/0.6/1.0/1.4 · 워드마크 t=2.1/2.4/2.7/3.0).

##### 🎨 인트로 물 색 — **CTA 를 밴드 잉크 쪽으로 섞어** 밝힌다 (하드코딩 없음)

`color-mix(in srgb, var(--color-cta) var(--hero-water-mix), var(--color-ink))`.

| 후보 | 결과색 | 검정 위 대비 | 판단 |
|---|---|---|---|
| 100% | `#6338ee` | 3.33:1 | 검은 화면에서 어둡다 |
| 80% | `#8260f1` | 4.89:1 | 목표(6:1) 미달 |
| **65% ✅ 채택** | **`#9a7ef4`** | **6.64:1** | 밝고 또렷하면서 CTA 보라로 읽힌다 |
| 50% | `#b19cf7` | 8.93:1 | 라벤더로 옅어져 정체성이 약해진다 |

팔레트가 어두운 면용으로 파생하는 `accentOnDark`(#9b7dff · 6.77:1)와 사실상 같은 자리입니다. ⚠️ **최종 상태와는 무관**(인트로 복제본 전용).

##### 🌊 상시 파도 **수위 상향** (사용자 "흰 부분이 적게")

`hero-wavelevel-c{0,1,2,3}.png` 비교 → **앞 55 / 중 70 / 뒤 88%** 채택(1차 25/40/60 → 흰 면이 위 20~25%만 남음). 45/60/78 은 아직 흰 면이 넓고, 65/80/95 는 `I`·`M` 이 거의 전부 보라가 되어 **그냥 보라 글자**로 읽힙니다. 겹 간격(15/18%p)은 그대로라 원근은 유지됩니다.
값 `--wm-wave-y1 −0.04473em` / `y2 −0.15085em` / `y3 −0.2782em` (식 `y = 0.7644 − p × 0.7075 − 0.42`).

##### 🚨 Phase C — **덮개 하나 · 변수 하나 · 등속 한 번** (2026-08-25 최종)

진짜 워드마크에는 **끝까지 아무것도 걸지 않습니다**(칸마다 `background-clip: text` 가 걸려 있어 `opacity` 를 얹으면 크롬이 층을 따로 합성해 **글자를 못 잡는 사고**가 있었습니다 — 2026-08-24 "SUM M" 사건). 대신 **밴드색 덮개 한 장**(`.hero-intro-crest`)의 **물결 모양 아래 변**이 올라가며 진짜 워드마크를 드러냅니다.

| 시각 | `.hero-intro-crest` 전선(`--hero-front`) | 화면 |
|---|---|---|
| ~2.15s | 0.98em (잉크 **바닥 아래**) | 워드마크 전부 검정 |
| 2.15 → 2.81s | 0.98 → 0.4554em | 드러난 자리가 **물(보라)** — 상시 파도 그대로 |
| 2.81s | **0.4554em = 상시 파도 앞 겹 수위(55%)** | 여기부터 위는 **흰색** |
| 2.81 → 3.45s | 0.4554 → −0.06em | **흰색이 이어서** 차오른다 |
| 3.45s~ | `visibility: hidden` | 최종 워드마크 |

- 🚨 **"보라 → 정지 → 흰색" 2막이 아닙니다.** 전선이 55% 수위를 지나는 순간부터 그 위가 흰색일 뿐이고, **드러나는 것이 진짜 워드마크**라 색 전환이 공짜로 따라옵니다. 55% 지점에 **정지 프레임이 없습니다**(아래 실측).
- 🚨 **파도 크기가 최종 상시 파도와 "정의상" 같습니다** — 인트로 전용 파형을 아예 없앴고(워드마크 물 복제본 삭제) 덮개의 아래 변만 `.wordmark-wavefill` **앞 겹과 같은 파장 1.4em · 진폭 0.112em · 같은 위상**(`--wm-wv1x`/`--wm-wv1y` 공용)입니다. **교대 순간에 파형이 튈 여지가 구조적으로 없습니다.**
- 🚨 **그라데이션 0개** — 마스크가 **한 겹**이고 알파는 1 뿐이며, 계단 방지 `feGaussianBlur stdDeviation="0.5"`(= 0.5/240 × 4.8em = **0.01em**)만 남았습니다. `mask-composite` 도, 세로 `linear-gradient` 앞단도 없어졌습니다.
- 위치 상수 — 세로 `var(--wm-wv1y) + var(--hero-front) − 2.4em`(2.4em = 타일 위 변에서 수면까지) · 가로 `var(--wm-wv1x) + 0.5869em`(= 덮개 왼쪽 넘침 0.6 − 광학 보정 0.0131). ⚠️ `--wm-wave-y1` 을 바꾸면 55% 수위가 움직이므로 전선 범위를 다시 볼 것.
- 전선 범위 **0.98em → −0.06em (1.04em)** — 덮개 상자 좌표에서 잉크가 0.127 ~ 0.855em 이고 물결 마루(±0.112em)·blur 여유를 뺀 값입니다.

**🔀 스위치** — `INTRO_WM_MODE: "wave"(기본) | "water" | "split"` · `INTRO_C1_MODE: "rise"(기본) | "flow" | "swell"`. `"water"`(보라 물 복제본이 차오른 뒤 교대) · `"flow"`(옆에서 밀려옴) · `"swell"`(출렁이며 안착) 은 캡처 비교까지 마친 뒤 기본에서 내려온 안이고 **코드는 그대로** 있습니다.

##### 🚨 Phase B 이동량·확대 — 닫힌 식이라 JS 측정이 없다

```
lift = −P_t/2 + C − 0.41·F        C = P_b/2 + (2W + G + 메타여백 + 메타높이)/2 − (W + G)
tx   = −(WM_TOTAL_INK − WM_OPTICAL_EM)/2 + HEADLINE_INK_EM × center / 2   (단위 --wm-size)
k    = center / ratio
```
🚨 `F` 항은 어떤 구성에서도 **정확히 −L/2 = −0.41**(워드마크 줄 높이의 절반)로 떨어집니다.
🚨 `--hero-intro-lift` 는 **`h1` 에서 선언**해야 합니다(섹션에 두면 그 자리에 `--wm-size` 가 없어 폴백으로 떨어집니다).
🚨 `tx`·`k` 는 **인라인 변수로 내려보내면 안 됩니다** — 구간별 미디어 쿼리가 인라인을 이길 수 없어 `globals.css` 가 `--hero-headline-center` / `--hero-headline-ratio` 로 계산합니다.
🚨 `transform-origin: right center` — 오른끝 정렬이라 오른쪽 변을 축으로 잡아야 배율 1(최종)에서 오른끝이 제자리에 남습니다.

**Phase A 중앙 정렬 실측**(잉크 bbox 중심 − 화면 중심) — 1536 **Δx −2.5 / Δy −2.0px** · 1920 **−3.5 / −2.5** · 390 **−0.4 / −0.9(CSS px)**. (문장 잉크의 좌우·상하 비대칭 때문이며 육안으로는 정중앙입니다.)

##### 🕶 인트로 동안 **상단 고정 네비 숨김**

`body:has([data-hero-intro="on"]) .site-nav` 한 규칙입니다.
- 🚨 **`:has()` 로 페이지 범위를 좁힙니다** — 네비는 루트 레이아웃에 있어 `/contact` 등에도 뜨는데, `data-hero-intro="on"` 은 히어로가 `enter: "intro"` 일 때만 붙습니다. **다른 `enter` 모드·다른 페이지에서는 규칙이 아예 안 걸립니다.**
- 🚨 **덮개로 가리지 않았습니다**(후보 ⓑ) — 네비가 `fixed z-50` 이라 덮으려면 층을 더 올려야 하고 그러면 **클릭 히트영역·스크롤까지** 막힙니다. 투명도만 다루고 투명한 동안은 **`pointer-events: none`** 이라 안 보이는 버튼이 클릭을 먹지 않습니다(실측 `opacity 0 / none` → `1 / auto`).
- **등장 = Phase B 시작(1.85s)** 부터 0.5s. 문장이 제자리로 내려가기 시작할 때 같이 떠서 "화면이 살아난다"는 인상이 한 번에 옵니다.
- **인트로 도중 스크롤**(t=1.2s, y=420) 하면 네비는 여전히 숨김입니다 — 다만 **1.85s 뒤 자동으로 뜨고** 그 사이 네비가 필요한 조작이 없어 그대로 뒀습니다(실측 후 판단).
- `prefers-reduced-motion` 이면 **숨기지 않습니다**(`animation: none` → opacity 1).

##### 검증 실측
| 항목 | 결과 |
|---|---|
| `npx tsc --noEmit` · `npx eslint src` | 통과(경고 0) |
| 콘솔 에러 | **0**(1536 · 1920 · 390) |
| 가로 스크롤 | **0** (`scrollWidth == clientWidth` 세 뷰포트) |
| 문서 높이 | **9,320 / 9,719 / 12,172px** — 작업 전과 **동일**(⚠️ 9,083 이 아닌 것은 같은 날 `contactForm.mode → "full"` 로 바꾼 **다른 작업** 탓입니다) |
| 히어로 섹션 높이 | 745 / 1080 / 844 — 불변 |
| 워드마크 크기 | `F` **473.088 / 591.36 / 120.12** — v9 구성 변경에도 **한 픽셀도 안 변함**(세 뷰포트 전부 폭이 크기를 잡음) |
| 문장 크기 | **35.48 / 44.35 / 13.81px** |
| **오른끝 정렬** | 워드마크 잉크 오른끝 대비 문장 **Δ +1 / +1 / +1.0(CSS px)** · 카피라이트 **0 / 0 / −0.5** → 기준(≤1px) 충족 |
| **윤곽(고스트)** | 물 밖 구간 **밝기>2 픽셀 0 · 최대 밝기 0**(문장 4시점 · 워드마크 4시점) |
| **전선 등속** | 0.1s 간격 실측(1536) — 잉크 구간(t=2.45~3.05)에서 Δy **37 / 37 / 38 / 38 / 37 / 37 / 37px**, 평균 **37.4px/0.1s**, **편차 2.7%**(기준 ±10%). 55% 수위 통과 시각 **t≈2.81s** 전후(2.75→2.85)도 Δ 38 / 37 로 **정지 구간 없음**. ⚠️ t≤2.35 와 t≥3.15 의 값이 튀는 것은 애니메이션이 아니라 측정 한계입니다(전선이 잉크 밖이라 "가장 위 잉크 행"이 포화) |
| **교대 파형 일치** | 같은 시각(3.45s)에 **덮개 있는 프레임 vs 덮개를 지운 최종 상태**를 전 픽셀 비교 — **diff 0**. 인트로가 드러내는 것이 진짜 상시 파도라 **수면 y·진폭이 정의상 동일**합니다 |
| **그라데이션 잔존** | 인트로 활성 경로에 **0개**. `globals.css` 전수 확인 — `.hero-intro-water`(문장) 타일은 `fill-opacity 1 / 0.5` 단색 · `.hero-intro-crest`(워드마크) 타일은 단색 알파 1 · 남은 `linear-gradient` 는 **미사용 보존 모드** `.hero-intro-cover--flow` 한 곳뿐이고 blur 는 두 곳 다 `stdDeviation="0.5"` = **0.01em** |
| **최종 픽셀 diff** | 같은 시각(6s) 정지 후 `enter:"split"` 빌드와 전 픽셀 비교 — **1,644 / 1,144,320 = 0.14%**. 내역: **워드마크 104px**(액센트 `I` 가장자리, 기존과 동일) + **문장 1,540px**. 🚨 문장 쪽은 **기하 차이가 아니라 글자 안티에일리어싱**입니다 — 통제 실험 결과 인트로 h1 의 애니메이션을 **꺼도**(정적 렌더) `split` 빌드와 **3,544px** 차이가 나므로, 원인은 `split` 빌드의 `.reveal-load`(opacity+transform)가 h1 을 **따로 합성**해 서브픽셀 AA 가 그레이스케일로 바뀌는 것입니다. 같은 빌드 두 번 캡처는 **diff 0**(측정 자체는 결정적). 상자 좌표·크기는 세 뷰포트 모두 소수점까지 동일합니다 |
| `prefers-reduced-motion` | 네비 `none / opacity 1` · `h1` `none / transform none` · 문장 `none / rgb(255,255,255)` · 복제본·덮개 2종 **전부 `display:none`** · 상시 파도 `animation:none` |

##### 캡처 (scratchpad)
**v9 최종** `hero-v9-{1536,1920,390}.png` · `hero-v9-reduced-motion.png` · `hero-v9-final-diff.png`
**문장 크기 후보** `hero-v9-size-{055,075,095}.png`
**인트로 프레임** `hero-v9-intro-{a,b,c,d,e,f,done}.png`
**Phase C 최종(등속 한 번 · 그라데이션 0)** `hero-intro8-{r10,r30,r55,r75,r95,done}.png`(전선 진행률 10/30/55/75/95%) · **교대 전후 `hero-intro8-handoff-{before,after}.png`(같은 시각 · diff 0)** / 이전 판 `hero-intro7-{c0,c1,c2,c3,done}.png` · `hero-intro7-same-{stmt,wm}.png` · `hero-intro6-{c0,c1,c2,c3,done}.png`
**네비 숨김** `hero-intro5-nav-{a,b,c,done}.png` · `hero-intro5-nav-390.png` · `hero-intro5-nav-scroll.png`
**중간 후보(보존)** C-1 방식 `hero-intro4-c1-{a1,a2,a3}.png`(flow) · `{b1,b2,b3}`(swell) · `hero-intro4-c1-nograd-{1,2,3}.png` / 물 색 `hero-intro2-color-mix{100,80,65,50}.png` / 상시 파도 수위 `hero-wavelevel-c{0,1,2,3}.png` / 그라데이션 전·후 `hero-intro2-grad-{before,after}.png` / 등장 방식 `hero-intro-cand-{water,split}-*.png`
측정·캡처 스크립트 — `i-base.mjs` · `i-ink2/3.mjs` · `i2-ghost*.mjs`·`i3-ghost.mjs`·`i8-final.mjs`(윤곽) · `i2-wavelevel.mjs` · `i3-color.mjs` · `i4-cand.mjs` · `i6-meas.mjs`·`i6-size.mjs`·`i6-intro.mjs`(v9 기하) · `i7-caps.mjs` · `i7-mtx.mjs`·`i7-diffregion.mjs`(diff 원인 분해) · `i-final.mjs`+`i-diff.mjs`.


### 🆕 **Work 4분할 박스 (2026-08-25)** — 전폭 4칸 격자 · 기울기 없는 정면 목업

사용자: **"한 줄에 4칸으로 가로 확장 · 폭은 화면 전폭 · 칸 사이 여백은 최소로 하고 연한 회색 경계선 ·
박스 안에 모니터 화면과 작은 모바일 화면이 둘 다 네모 박스로 · 박스 밖 돌출 없음 · 3D 기울기 제거 ·
호버 동작(어두워짐 + 로고 + 자동 스크롤)은 유지 · 설명글은 박스 안쪽 아래에 항상 · 4번째는 준비 중 자리"**

#### 🔀 스위치 — `selected-work.tsx` 상단 `WORK_LAYOUT`
| 값 | 화면 |
|---|---|
| **`"quad"`** | **현재** — 화면 전폭 **4분할 네모 박스**(모바일·768 은 2×2) |
| `"cards3"` | 2026-08-19~ 의 **3열 기울기 목업 카드**(모니터 + 박스 밖으로 돌출된 폰, 설명글은 카드 아래) |

🚨 **`cards3` 쪽 부품은 한 줄도 안 지웠습니다** — `project-card.tsx` · `device-mockup.tsx` · `TILTS` · `compact` 그대로입니다.
**복귀 실측**: `"cards3"` 로 되돌리면 1536 문서 높이 **9,320px** · Work 섹션 **1,055.08px** 로 **작업 전 baseline 과 완전히 동일**합니다(`work-quad-revert-cards3.png`).

##### 새 파일 · 고친 파일
| 파일 | 무엇 |
|---|---|
| `components/ui/project-quad-card.tsx` | **신설** — 정면 배치 전용 칸(`ProjectQuadCard`) + 준비 중 칸(`WorkQuadPlaceholder`) |
| `components/sections/selected-work.tsx` | `WORK_LAYOUT` 분기 · 격자를 `Container` **밖**에 두어 전폭 |
| `content/projects.ts` | `selectedWork.placeholderCell` 추가(`{ title: "다음 프로젝트", note: "준비 중입니다." }`) |

🚨 `DeviceMockup` 을 재사용하지 않은 이유 — 그 컴포넌트는 **3D 기울기 + 폰 돌출**이 존재 이유라 이번 지시(둘 다 제거)와 정반대입니다. 플래그로 갈랐다면 한 컴포넌트가 상반된 두 형태를 들게 됩니다. 호버 규칙(50% 오버레이 · 로고 · **모니터 9s / 폰 11s** · 해제 **1.2s**)은 **같은 값 그대로** 옮겼습니다.

#### 전폭·경계선 — `gap-px` + 그리드 배경
- 격자는 `Section` 의 직계 자식(섹션에 좌우 패딩이 없어 `w-full` 이 곧 전폭). **섹션 헤더는 지금처럼 `Container` 안 중앙**입니다.
- 🚨 **칸에 보더를 주지 않았습니다** — 인접 변이 겹쳐 2px 로 보입니다. 대신 `grid ... gap-px bg-line` 이라 **틈 자체가 정확히 1px**이고, 칸은 `bg-canvas`(흰 면)로 그 틈만 남깁니다. **구조적으로 2px 이 될 수 없습니다.** 바깥 좌우 변은 뷰포트 끝이라 선을 두지 않고 위아래만 `border-y border-line`.
- 색은 밴드 토큰 `--color-line`(paper 밴드 실측 `rgb(229,229,229)`) — 하드코딩 0, 팔레트·colorLab 을 따라갑니다.

#### 정한 값과 근거
| 항목 | 값 | 근거 |
|---|---|---|
| 칸 비율 | 1536 에서 **383.25 × 403.13px (0.951)** · 1920 **479.25 × 469.86** · 768 **383.5 × 397.88** · 390 **194.5 × 280.55** | 높이를 고정하지 않고 **내용이 정한다**(그리드 행이 자동으로 4칸을 같은 높이로 맞춤). 모니터 16:10 + 폰 여유 + 설명글 3줄이 눌리지 않는 자연 높이 |
| 칸 패딩 | `p-4 sm:p-5 lg:p-6` | 1536 에서 24px → 화면 폭 335px 확보 |
| 모바일 화면 **위치** | **모니터 우측 하단**(`right-[5%]`), 아래로 **미디어 폭의 7%** 만큼 내려옴 | 좌·우 두 안을 같은 구도로 캡처 비교(`work-quad-phone-right.png` / `work-quad-phone-left.png`). **왼쪽 안은 세 사이트의 좌측 정렬 헤드라인을 폰이 그대로 가립니다**("삶의 '결'을 찾아드립니다" · 온설 로고 · "내신과 정시는…"). 오른쪽이면 헤드라인이 살아 있습니다 |
| 모바일 화면 **크기** | 미디어 폭의 **21%**(1536 에서 70px) | 3열 카드가 쓰던 비율 그대로 — 모니터↔폰 크기 관계가 **확정된 값에서 안 바뀝니다**. 캡처에서 폰 화면의 레이아웃이 판독됩니다 |
| 폰 프레임 | `deviceFrame` 스위치 그대로(**현재 `"png"` = 실사 프레임**) | 2026-08-23 사용자 확정. `"css"` 로 내리면 이 칸도 납작한 사각 폰으로 함께 바뀝니다 |
| 설명글 구성 | **관점 한 줄(액센트) + 프로젝트명 + 업종** | 이름은 세 건 다 **한 줄 유지**(383px 폭). `summary` 는 **제외**(`QUAD_SHOW_SUMMARY = false`) — 넣으면 칸이 **405.13 → 454.72px(+49.6)** 로 길어지고 4번째 "준비 중" 칸 아랫부분이 그만큼 비어 격자가 "글 상자"로 읽힙니다(`work-quad-summary-on.png`). 요약 전문은 **모달에 그대로** 있습니다 |
| 호버 오버레이 범위 | **화면 영역만**(모니터·폰) | 설명글은 항상 보여야 하므로. 실측 — 호버 3초 시점 오버레이 아래변 **306px** vs 프로젝트명 위변 **384px** → **겹치지 않습니다** |
| 쉬는 모습 | `coverImage` 그대로(아래에 `fullImage` 를 깔고 커버만 페이드아웃) | "커버는 사용자가 확정한 정지 화면"이라는 `device-mockup.tsx` 규칙을 그대로 지킵니다 |

#### 4번째 칸 = 준비 중
`projects.ts` 에 **4번째 프로젝트가 없습니다**(placeholder 항목도 없음). 그래서 화면 이미지 없이 **점선 상자 + "다음 프로젝트 / 준비 중입니다."** 만 둡니다 — **호버 오버레이도 모달도 링크도 없습니다**(실측 `placeholderIsButton: false` · 클릭 가능한 칸 3개). 🚨 없는 실적을 그림으로 만들지 않습니다(§6 원칙).
🚨 점선 상자는 **프로젝트 칸의 상자 골격을 그대로** 씁니다(테두리 + `p-2` + 크롬 줄 높이 14px + 화면 16:10). 화면만 16:10 으로 두면 크롬 줄만큼 짧아져 **네 칸의 상자 밑변이 어긋납니다**(첫 캡처에서 실제로 그랬습니다).
실제 4번째 프로젝트가 생기면 `projects` 배열에 항목을 더하면 자리표시가 자동으로 밀려납니다.

#### 검증 실측
| 뷰포트 | 열 | **칸 폭(4칸)** | 폭 편차 | 칸 사이 틈 | 칸 높이 | 문서 높이 | Work 섹션 | 가로 스크롤 |
|---|---|---|---|---|---|---|---|---|
| **1536×745** | 4 | 383.25 × 4 | **0.000px** | **1 / 1 / 1px** | 403.125 | 9,320 → **9,204**(−116) | 1,055.08 → **938.88**(−116.20) | **0** |
| **1920×1080** | 4 | 479.25 × 4 | **0.000px** | 1 / 1 / 1px | 469.859 | 9,719 → **9,669**(−50) | 1,055.08 → **1,005.61**(−49.47) | **0** |
| **768×1024** | 2×2 | 383.5 × 4 | **0.000px** | 1 / 1px | 397.875 | 10,134 → **9,877**(−257) | 1,468.50 → **1,212.06**(−256.44) | **0** |
| **390×844**(DSF2) | 2×2 | 194.5 × 4 | **0.000px** | 1 / 1px | 280.547 | 12,172 → **11,114**(−1,058) | 1,995.27 → **937.72**(−1,057.55) | **0** |

🚨 **다른 섹션 영향 0** — 네 뷰포트 모두 **문서 높이 변화량 = Work 섹션 높이 변화량**(오차 ≤ 0.6px, 반올림 몫)입니다. 히어로는 손대지 않았습니다.
- **호버 실측**(1536, 2번째 칸 온설, 3초 시점): 모니터 `object-position` **50% 0% → 50% 34.9987%**(9s linear) · 폰 **→ 50% 28.6353%**(11s linear) · 오버레이 `rgba(0,0,0,.5)` · `opacity 1` · **로고 opacity 1** — 전부 정상.
- **모달**: 카드 클릭 → `aria-label "문결 필라테스 프로젝트"` · 해시 `#work-moongyul-pilates` · `body overflow hidden` / **ESC** → 닫힘 + 해시 제거 + 스크롤 복구 / **딥링크** `/#work-onseol` 로 새로고침 → `"온설 프로젝트"` 모달이 열린 채 시작.
- **콘솔**: dev 의 `next/image` quality 경고 **9건뿐**(작업 전 baseline 과 같은 건수) — **에러 0**.
- `npx tsc --noEmit` · `npx eslint src` 통과.

#### 캡처 (세션 scratchpad)
`work-quad-1536.png` · `work-quad-1920.png` · `work-quad-768.png` · `work-quad-390.png` /
격자만 `work-quad-grid-1536.png` · 한 칸 확대 `work-quad-cell-zoom.png` / **경계선 6배 확대 `work-quad-border-zoom.png`** /
호버 3초 `work-quad-hover.png` / 모달 `work-quad-modal.png` /
후보 비교 **`work-quad-phone-right.png`(채택) · `work-quad-phone-left.png`** · 요약 포함안 `work-quad-summary-on.png` /
복귀 `work-quad-revert-cards3.png` / 작업 전 `work-before-{1536,1920,768,390}.png`.
측정·캡처 스크립트 — `w-lib.mjs` · `w-base.mjs`(baseline) · `w-verify.mjs`(4뷰포트 기하) · `w-zoom.mjs`(격자·경계 확대) · `w-hover.mjs` · `w-modal.mjs` · `w-cand.mjs`(폰 위치 후보) · `w-revert.mjs`.

#### 남은 것
- **`/work` 서브 페이지는 예전 3열 카드 그대로**입니다(`ProjectCard` 사용). 원페이지 전환 이후 그 페이지는 네비에서 빠져 있어 이번 지시 범위 밖으로 뒀습니다.
- 모바일(390)에서 폰 목업 폭이 **41px** 이라 화면 안 글자는 형태만 읽힙니다 — 지시한 "축소판 유지·판독 가능"은 모니터 화면 기준으로 충족(캡처 확인).

#### 🆕 **프레임 제거 · 호버 정보 (2026-08-26)** — 이미지가 칸을 꽉 채우고, 설명은 호버로

사용자: **"셀렉트 워크 부분의 목업들 휴대폰 PNG·모니터 그림 다 지우고 네모 박스에 차도록 만들어 줬으면 좋겠어. 호버했을 때 로고와, 홈페이지명, 설명문이 나오면 좋을 것 같아."**

##### 🔀 스위치 — `project-quad-card.tsx` 상단
| 상수 | 현재 | 무엇 |
|---|---|---|
| **`QUAD_FRAME`** | **`"none"`** | `"device"` 로 바꾸면 **2026-08-25 구성이 통째로 복귀**(모니터 크롬 + 우측 하단 폰 + 칸 안 아래 상시 설명글). **기존 코드는 한 줄도 안 지우고 `DeviceCell()` 로 옮겼을 뿐**입니다 |
| `QUAD_ASPECT` | `"4 / 3"` | 칸 비율(모바일 2×2 도 같은 값). 후보 비교는 아래 표 |
| `QUAD_HOVER_TEXT` | `"perspective"` | `"summary"` 면 요약 2줄(clamp). **새 카피 0줄** — 둘 다 `projects.ts` 기존 값 |
| `QUAD_HOVER_ALIGN` | `"center"` | `"bottom-left"` 면 좌하단 스택 |
| `QUAD_HOVER_SHADE` | `"60"` | 오버레이 농도 50 / 60 / 70 |
| `QUAD_HOVER_TRADE` | `true` | 호버에 업종(`industry`) 한 줄 추가 |
| `QUAD_PLACEHOLDER` | `"plain"` | `"dashed"` 면 준비 중 칸 안에 점선 상자 |

🚨 **`phone-frame.tsx` · `device-mockup.tsx` · `deviceFrame` 스위치는 그대로입니다** — 브릿지 밴드 폰 2대가 계속 씁니다. Work 칸이 `"none"` 에서 안 쓸 뿐입니다.
🚨 `selected-work.tsx` 의 **`WORK_LAYOUT` 은 손대지 않았습니다**(`"cards3"` 복귀 경로 그대로).

##### 무엇이 바뀌었나
- 칸 = **이미지 한 장**. 브라우저 크롬 줄(점 3개·주소 바)·폰 프레임·칸 안쪽 패딩이 전부 빠지고 `object-cover` 가 **가장자리까지** 채웁니다.
- **평상시 텍스트 0.** 칸 아래 있던 관점·프로젝트명·업종은 **호버 오버레이로 이동**했습니다.
- 칸 사이 **1px 회색 경계선(`gap-px` + `bg-line`)과 전폭 4분할 격자는 그대로**입니다(실측 아래).
- 클릭 → **기존 프로젝트 모달** 그대로(딥링크 포함). **모달 안은 한 줄도 안 건드렸습니다.**

##### 정한 값과 근거
| 항목 | 값 | 근거 |
|---|---|---|
| **칸 비율** | **4 : 3** (1536 에서 383.25 × **287.44px**) | 후보 3안 같은 구도 캡처(`work-flat-ratio-{43,11,1610}.png`). **1:1**(383px)은 커버가 화면 비율(1.33~1.6)이라 **좌우를 17~33% 잘라** 온설 로고·지안 네비가 통째로 날아갑니다. **16:10**(239.5px)은 커버 두 장과 정확히 맞지만 **띠처럼 납작**해지고(모바일 390 에서 **121.6px**) 호버 4줄(로고+이름+관점+업종 ≈142px)이 칸의 59% 를 먹습니다. 4:3 은 문결 커버(1200×900)와 **정확히 일치**하고 나머지 둘은 좌우 **8.3%씩**만 잘립니다 |
| **이미지 조합** | 쉬는 모습 **`coverImage`** / 호버 스크롤 **`fullImage`** | 2026-08-25 규칙 그대로 — "커버는 사용자가 확정한 정지 화면". 아래에 풀페이지를 깔고 커버만 페이드아웃하므로 전환이 끊기지 않습니다(실측 호버 3초 시점 커버 `opacity 0`) |
| **호버 구성** | 가운데 정렬 · **로고(칸 높이 13%) → 프로젝트명 → 관점 한 줄 → 업종** | 후보 2안 비교(`work-flat-hover-center.png` 채택 / `work-flat-hover-bottomleft.png`). 좌하단 안은 **로고가 흐르는 캡처의 글자 위에 겹쳐** 읽히고(온설 "온설 이야기" 위), 네 칸이 같은 모서리에 몰려 격자 리듬이 한쪽으로 쏠립니다. 가운데 안은 네 칸이 대칭이고 밑에 깔린 사이트의 좌측 정렬 헤드라인과 안 부딪힙니다 |
| **오버레이 농도** | **60%** (`rgba(0,0,0,.6)`) | 🚨 **흐르는 캡처의 최악 프레임 = 순백**입니다. 흰 글자 대비를 계산하면 50% → **3.98:1(AA 미달)** · **60% → 5.74:1** · 70% → 8.52:1. 60% 가 **AA 를 넘는 최소치**이고, 70%(`work-flat-shade-70.png`)는 사이트 화면이 거의 안 보여 "작업물을 보여주는" 목적을 해칩니다(50% 비교컷 `work-flat-shade-50.png`) |
| **업종 포함** | 포함(한 줄, `text-caption` · 흰 65%) | 캡처에서 네 줄이 287px 칸 안에 여유롭게 서고(블록 142px = 칸의 49%), 관점 문장만으로는 업종이 안 읽힙니다 |
| **호버 자동 스크롤** | **유지** — `fullImage` 9초 linear, 해제 1.2초 복귀 | 기존 값 그대로(폰이 없어져 11초 트랙은 이 칸에서 안 씁니다) |
| **준비 중 칸** | **빈 면 + `+` + 문구**(`"plain"`) | 프레임이 사라져 점선으로 흉내 낼 상자가 없습니다. 세 칸이 **가장자리까지 찬 면**이라 네 번째도 면 하나로 두는 편이 격자와 맞습니다(`work-flat-placeholder.png`). 🚨 호버·모달·링크 없음(실측 `placeholderIsButton: false` · 클릭 가능한 칸 3개) — 없는 실적을 그림으로 만들지 않습니다 |

🚨 **흰 글자에 `text-white` 를 쓸 수 없습니다** — `globals.css` 1366~1369 행이 **밝은 밴드(paper·mist)에서 `.text-white` 를 잉크로 되돌립니다**(다크 전용 흰 글자 보호 규칙). Work 는 흰 밴드라 글자가 **`rgb(16,16,16)`** 으로 나왔습니다(실측). 어두운 스크림 위 전용이므로 **`text-[#ffffff]` / `/85` / `/65`** 로 값을 직접 줬습니다. globals 는 안 건드렸습니다.
🚨 **`cn()` 은 단순 join(tailwind-merge 아님)** 이라 같은 속성 유틸을 겹치면 순서 운에 맡기게 됩니다 — `RISE(delay)` 를 인자 받는 함수로 만들어 `delay-*` 를 한 번만 붙입니다.

##### 검증 실측
| 뷰포트 | 열 | 칸 폭 × 4 | 폭 편차 | 칸 높이 | 틈 | 문서 높이 | Work 섹션 | 가로 스크롤 |
|---|---|---|---|---|---|---|---|---|
| **1536×745** | 4 | 383.25 | **0.000px** | **287.44** | 1/1/1px | 9,316 → **9,201**(−115) | 938.88 → **823.19**(−115.69) | **0** |
| **1920×1080** | 4 | 479.25 | **0.000px** | **359.44** | 1/1/1px | 9,782 → **9,672**(−110) | 1,005.61 → **895.19**(−110.42) | **0** |
| **768×1024** | 2×2 | 383.5 | **0.000px** | **287.63** | 1/1px | 9,984 → **9,764**(−220) | 1,212.06 → **991.56**(−220.50) | **0** |
| **390×844**(DSF2) | 2×2 | 194.5 | **0.000px** | **145.88** | 1/1px | 11,236 → **10,966**(−270) | 937.72 → **668.38**(−269.34) | **0** |

🚨 **다른 섹션 영향 0** — 네 뷰포트 모두 **문서 높이 변화량 = Work 섹션 변화량**(오차 ≤ 0.7px, 반올림 몫). 경계선 색은 밴드 토큰 그대로 `rgb(229,229,229)` · 그리드 `gap: 1px` · `border-y 1px`.
- **호버 실측**(1536, 2번째 칸 온설, 3초): `object-position` **50% 0% → 50% 35.7425%**(9s linear) · 커버 `opacity 0` · 오버레이 `rgba(0,0,0,.6)` `opacity 1` · **로고·프로젝트명·관점·업종 전부 `opacity 1`** · 프로젝트명 색 **`rgb(255,255,255)`**.
- **모달**: 첫 칸 클릭 → `aria-label "문결 필라테스 프로젝트"` · 해시 `#work-moongyul-pilates` · `body overflow hidden` / **ESC** → 닫힘 + 해시 제거 + 스크롤 복구(`visible`) / **딥링크** `/#work-onseol` 새로고침 → `"온설 프로젝트"` 모달이 열린 채 시작.
- **복귀 실측**: `QUAD_FRAME = "device"` 로 되돌리면 **9,316 / 9,782 / 9,984 / 11,236** · Work **938.88 / 1,005.61 / 1,212.06 / 937.72** · 칸 높이 **403.13 / 469.86 / 397.88 / 280.55** 로 **2026-08-25 값과 완전히 동일**합니다(`work-flat-revert-device.png`).
- **콘솔**: dev 의 `next/image` quality 경고 8건뿐(baseline 9건 — 폰 목업이 빠져 `mobileImage` 요청이 하나 줄었습니다) — **에러 0**. `npx tsc --noEmit` · `npx eslint src` 통과.

##### 캡처 (세션 scratchpad)
`work-flat-1536.png` · `work-flat-1920.png` · `work-flat-768.png` · `work-flat-390.png` /
호버 3초 **`work-flat-hover.png`** /
호버 후보 비교 **`work-flat-hover-center.png`(채택)** · `work-flat-hover-bottomleft.png` /
농도 비교 `work-flat-shade-50.png` · `work-flat-shade-70.png`(60% 는 `work-flat-hover.png` 와 동일 조건) /
비율 후보 **`work-flat-ratio-43.png`(채택)** · `work-flat-ratio-11.png` · `work-flat-ratio-1610.png` /
준비 중 칸 `work-flat-placeholder.png` · 모달 `work-flat-modal.png` · 복귀 `work-flat-revert-device.png`.
스크립트 — `wf-lib.mjs` · `wf-verify.mjs`(4뷰포트) · `wf-hover.mjs` · `wf-ratio.mjs` · `wf-cell.mjs` · `wf-modal.mjs` · `wf-deep.mjs` · `wf-set.mjs`(상수 토글) · `wf-shot1.mjs`.

🚨 **헤드리스 캡처 함정 2가지**(이번에 크게 헤맸습니다)
1. **`Page.captureScreenshot` 에 `clip` 을 주면 이 Edge(151)는 흰 판을 돌려줍니다** — 뷰포트 좌표·문서 좌표 둘 다 실패했습니다. **뷰포트 전체를 찍고 `sharp` 로 잘라야** 합니다(`shotCrop`).
2. **세션의 첫 캡처도 흰 판**입니다(headless 창이 화면에 없어 컴포지터가 프레임을 새로 안 그림). `shotCrop` 이 찍은 뒤 **채널 stdev < 1.5 면 균일한 판으로 보고 최대 6회 재촬영**합니다.

##### 남은 것
- **`/work` 서브 페이지는 여전히 예전 3열 카드**입니다(`ProjectCard`). 네비에서 빠진 라우트라 이번에도 범위 밖입니다.
- 모바일(390)에서 칸이 **194.5 × 145.9px** 이라 사이트 화면은 "무슨 색·무슨 구성"까지만 읽힙니다. 호버가 없는 터치에서는 **탭 → 모달**이 그대로 동선입니다.



### 온설 모바일 캡처 **재촬영** (2026-08-25) — `onseol-v3-mobile.jpg`

사용자: **"온설 모바일 목업 두 군데 다 이상해."** 맞았습니다. `onseol-v2-mobile.jpg`(08-21 밤 캡처)가 **잘못 찍혀 있었습니다.**

#### 무엇이 잘못돼 있었나
- **고정 헤더가 화면 한가운데 떠 있었습니다.** 온설 로고 + `메뉴` 밴드가 **최상단이 아니라 CSS y≈456px** 에 흰 배경(`.os-scrolled` 상태)으로 그려져, 히어로의 `온설 증류소 · 강화` 아이브로우 줄을 덮고 있었습니다. 폰 목업 안에서는 위 1/3 이 "글자 없는 올리브 배경"으로만 보였습니다.
- **히어로 자체는 멀쩡했습니다** — 픽셀로 재면 히어로 높이 844 CSS px, 카피 위치 727 CSS px 로 새 캡처와 **동일**합니다. 어긋난 것은 **헤더 한 덩어리뿐**입니다.
- 🚨 **`projects.ts` 치수가 실제 파일과 2배 어긋나 있었습니다** — 세 장 다 실제 **1560×8400** 인데 `width: 780, height: 4200` 로 적혀 있었습니다.

#### 원인 (실측으로 좁힘)
`.os-header{position:fixed;inset:0 0 auto}` 입니다. **`captureBeyondViewport` 는 문서 좌표로 찍지만 `position:fixed` 요소는 그 순간의 스크롤 오프셋 자리에 칠해집니다.** 리빌 강제 스크롤로 문서 끝까지 내려갔다가 **상단 복귀 900ms** 로는 스크롤이 실제로 0 이 되기 전에 셔터가 내려갔고, 그 결과 헤더가 `scrollY≈456` 자리에 박제됐습니다. 흰 배경인 것도 같은 증거입니다(스크롤 상태에서만 붙는 `.os-scrolled`).

#### 고친 캡처 조건 (스크립트: scratchpad `cdp-shot.mjs`)
390×844 · DSF2 · `mobile:true` + iPhone Safari UA · 원본 `http://localhost:3000/embeds/onseol.html` · 상단 2100 CSS px · JPEG 80 — **여기까지는 §5-B 기존 조건 그대로**입니다. 바꾼 것은 **상단 복귀 절차 하나**입니다.

1. `document.documentElement.style.scrollBehavior='auto'` 를 **먼저** 박고(스무스 스크롤 잔여 제거), 스크롤은 전부 `behavior:'instant'`.
2. 리빌 강제: `document.fonts.ready` → 300px 씩 문서 끝까지 → 맨 아래 600ms.
3. **상단 복귀 = `scrollTo(0)` 를 150ms 간격으로 8회 반복 + 1600ms 추가 대기** (기존 900ms 1회 → 강화).
4. **캡처 직전 `window.scrollY === 0` 을 확인하고, 0 이 아니면 즉시 실패**시킵니다(스크립트가 throw). 이 가드가 이번 사고의 재발 방지 장치입니다.
5. 캡처 후 **파일을 열어 육안 확인** — 헤더가 최상단인지, 로고 흰 픽셀의 y 좌표를 스캔해 수치로도 확인했습니다.

🚨 **좌표 함정 (같은 착각 반복 방지)** — clip 에 `scale: 2` 를 주면 `deviceScaleFactor: 2` 와 **곱해져 4배**가 됩니다. 그래서 "상단 2100 CSS px" 이 **1560×8400 픽셀** 파일로 나옵니다(문서 §5-B 의 "780×4200 실픽셀" 은 `scale` 을 뺀 값이라 실제 파일과 다릅니다). **이미지 y ÷ 4 = CSS px** 입니다. 이걸 ÷2 로 잘못 재면 "히어로가 2배로 늘어났다"는 없는 병을 진단하게 됩니다(실제로 한 번 그랬습니다).

#### 세 장 전수 점검 — 문결·지안은 **정상, 재캡처 안 함**
| 파일 | 헤더 위치 | 리빌 | 판정 |
|---|---|---|---|
| `moongyul-mobile.jpg` | 최상단(y=0) | 전 구간 채워짐 | ✅ 그대로 |
| `jian-mobile.jpg` | 최상단(y=0) | 성적 카드 3장 포함 전부 채워짐 | ✅ 그대로 |
| `onseol-v2-mobile.jpg` | **CSS y≈456** | 채워짐 | 🚨 재캡처 |

문결·지안이 멀쩡한 이유도 확인했습니다 — **두 사이트는 헤더가 `fixed` 가 아니라 문서 흐름 안에 있습니다.** 같은 절차로 찍어도 온설만 병이 났던 게 이 때문입니다.

#### `-v3` 파일명 (캐시 무효화) + `projects.ts` 치수 정정
`-v2` 때와 같은 이유(`/_next/image` 최적화 캐시)로 덮어쓰지 않고 **`onseol-v3-mobile.jpg`**(1560×8400 · 726KB) 로 새로 만들었습니다. **`onseol-v2-mobile.jpg` 는 지우지 않고 남겨 뒀습니다.**

`src/content/projects.ts` 의 `mobileImage` **세 줄 전부** 실제 파일 픽셀로 맞췄습니다.

| 프로젝트 | 전 | 후 |
|---|---|---|
| 문결 | `moongyul-mobile.jpg` · 780×4200 | `moongyul-mobile.jpg` · **1560×8400** |
| 온설 | `onseol-v2-mobile.jpg` · 780×4200 | **`onseol-v3-mobile.jpg`** · **1560×8400** |
| 지안 | `jian-mobile.jpg` · 780×4200 | `jian-mobile.jpg` · **1560×8400** |

⚠️ 780:4200 과 1560:8400 은 **비율이 같아서**(1:5.3846) Next Image 의 비율 계산 결과가 바뀌지 않습니다 — 그래서 **레이아웃은 한 픽셀도 안 움직였습니다**(아래 실측). 그래도 "선언값 = 실제 픽셀" 이 맞는 상태로 고쳐 뒀습니다.

#### 반영 확인 (실측 · 1440×900)
사용처 **세 곳 전부 새 파일**을 씁니다. 폰 화면 최상단에 온설 로고 + `메뉴` 가 보이는 것을 캡처로 육안 확인했습니다.
- **Work 4분할 칸 폰** — `/_next/image?url=/images/projects/onseol-v3-mobile.jpg&w=128&q=75` (60×130 @ x617,y1404)
- **브릿지 밴드 뒤쪽 폰** — 같은 파일 `w=256` (196×380 @ x1093,y1889). `site.ts` 의 `bridgePhoneScreens` 가 **`null`** 이라 프로젝트 `mobileImage` 를 그대로 씁니다(08-21 기록의 "브릿지는 온설을 안 쓴다"는 그때 데모가 켜져 있던 상태 기준이고, 지금은 해당 없음).
- **프로젝트 모달 "모바일" 탭** — 같은 파일 `w=640`.

**다른 섹션 영향 0** — 문서 높이 **1440에서 9,362px**(직전 기록값과 동일) · 가로 스크롤 0(`scrollWidth 1440 = clientWidth 1440`) · 콘솔 에러 0 · `npx tsc --noEmit` 통과 · `npx eslint src` 통과. 이미지 교체뿐이라 레이아웃 불변이 정상이고, 실제로 불변입니다.

캡처: `onseol-mobile-before.png`(잘못된 v2 상단) · `onseol-mobile-after.png`(새 v3 상단) · `work-quad-onseol-cell.png` · `bridge-onseol-phone.png` · `modal-onseol-mobile.png`. 헤드리스는 작업 후 종료했고 **dev :3000 은 그대로 살려 뒀습니다**.

### 온설 목업 **최신화** (2026-08-21 밤)

사용자: **"온설 목업 사진 최신화가 안 됐네."** 임베드가 **2026-08-12** 판이었습니다.

#### 무엇이 구버전이었나
`public/embeds/onseol.html`(3.14MB, 08-12) → **`온설\전달용\온설_시안.html`**(6.81MB, 08-16)로 교체했습니다. 온설 폴더의 구조는 인수인계 문서 §1 그대로입니다 — **소스는 `code.html` 하나**, `build-single.js` 가 만드는 `전달용\온설_시안.html` 이 **단일 파일 완성본**, `이관용\` 은 아임웹 붙여넣기 **조각**이라 페이지가 아닙니다.

| | 구 임베드 | 신 시안 |
|---|---|---|
| 히어로·title | 따뜻한 이야기를 한 병에 담았습니다 | **온기가 쌓인 곳에, 온설이 깃듭니다** |
| 라우트 | 4개 (home·philosophy·product-onseol25·contact) | **7개** (+ magazine-list·magazine-yeosu·magazine-cocktail) |
| 매거진 | 3건 전부 `[실제 글로 교체 예정]` / `2026. 00. 00` | 2건 실제 글로 교체(여수 섬의 날 · 시그니처 두 잔) |
| 인라인 이미지 | 11개 | 20개 (히어로 사진 전환·워드마크·스튜디오 컷 추가) |
| 색·폰트 | — | **차이 없음**(`:root` 변수·`Noto Sans KR` 동일). 리디자인이 아니라 **콘텐츠 확장**이 차이의 전부 |

- 자기완결성 확인 — `build-single.js` 가 모든 이미지를 `OS_ASSETS` 맵에 base64 로 인라인하고 HTML 은 `data-os-src="assets/…"` 를 **맵의 키**로 씁니다. 실제 상대경로 요청 **0개**라 그대로 복사해도 안 깨집니다(외부 의존은 Google Fonts 1건뿐이고 구 임베드도 동일).
- ⚠️ **남은 것 2가지** — ① 매거진 "재료" 편 1건은 아직 `[실제 글로 교체 예정] / 2026. 00. 00` 입니다(온설 원본 파일 상태 그대로. 우리가 지어내면 안 되는 자리라 손대지 않았습니다). ② 2026-08-17 대표님 요청 "스마트스토어 → 상품 상세 링크"는 `이관용\` 조각에만 반영됐고 `code.html`·`온설_시안.html` 에는 **미반영**이라 임베드에도 구 링크가 3곳 따라옵니다. 온설 쪽 원본을 고쳐야 하는 일이라 여기서 손대지 않았습니다.

#### 재캡처 — 지안 전례 절차 그대로 (스크립트: scratchpad `onseol-shots.mjs`)
1. **리빌 강제**(3종 공통): `document.fonts.ready` → 300px 씩 문서 끝까지 스크롤 → 맨 아래 600ms → 상단 복귀 900ms → `captureBeyondViewport`. 실측 로드 실패 이미지 **0개**.
2. **커버** `onseol-v2.jpg` — 1440×900 · JPEG 82 · 134KB
3. **풀페이지** `onseol-v2-full.jpg` — **1440×5771** · JPEG 80 · 563KB (이전 5850 → `projects.ts` 높이 갱신)
4. **모바일** `onseol-v2-mobile.jpg` — 390×844 · DSF2 · `mobile:true` + iPhone UA · 상단 2100 CSS px = **780×4200** · JPEG 80 · 700KB (이전 1.07MB 보다 가벼워졌습니다)
- **빈 섹션 육안 확인 완료** — 브랜드 철학 3카드(Nature/Warmth/Honesty) · 사진 · 제품 2종 · 매거진 3카드 전부 채워짐.

#### 🚨 `-v2` 파일명 (캐시 무효화)
같은 경로로 덮어쓰지 않고 **파일명을 바꿨습니다**. 브릿지 데모(`bridge-demo-a2`) 때와 같은 이유로 `/_next/image` 최적화 캐시가 옛 그림을 계속 서빙합니다. 구 파일 3종(`onseol.jpg`·`onseol-full.jpg`·`onseol-mobile.jpg`)은 지우지 않고 남겨 뒀습니다.

#### 같이 고친 문구 (임베드와 어긋나서)
- `summary` **"4페이지" → "7페이지"** (매거진 3면이 늘었습니다)
- `overview` 인용 슬로건 **"따뜻한 이야기를 한 병에 담았습니다" → "온기가 쌓인 곳에, 온설이 깃듭니다"** — 바로 옆 임베드에 새 문구가 떠 있어 옛 문구를 인용하면 화면과 어긋납니다.

#### 반영 확인 (실측)
목업·모달·상세 페이지 전부 새 파일을 씁니다 — `/_next/image?url=/images/projects/onseol-v2.jpg` · `onseol-v2-full.jpg` · `onseol-v2-mobile.jpg`. `/work/onseol` 의 iframe `src=/embeds/onseol.html` **내부 문서 title = "온설 ONSEOL — 온기가 쌓인 곳에, 온설이 깃듭니다"**(새 판 확인). 콘솔 에러 0.
- **브릿지 폰은 온설을 쓰지 않습니다** — `site.ts` 의 `bridgePhoneScreens` 가 데모 2종(`bridge-demo-a2`/`-b`)으로 고정돼 있어 이번 교체와 무관합니다(`null` 로 되돌리면 그때 `onseol-v2-mobile.jpg` 가 들어갑니다).
- 캡처: `onseol-card.png`(목업 카드) · `onseol-modal.png`(모달) · `onseol-embed-page.png`(상세 페이지 iframe).

### 단일 HTML **v3 재추출** (2026-08-21 밤)

위 4건(브릿지 V · 후기 스탯 가로 · 색상 재큐레이션 · 온설 최신화)을 담아 팀 공유용 단일 파일을 다시 뽑았습니다. 절차는 §0 의 검증된 것 그대로입니다.

```bash
cd sumim-studio
NEXT_DIST_DIR=.next-prod npx next build          # dev :3000 · .next 불가침
NEXT_DIST_DIR=.next-prod npx next start -p 3101
node <scratchpad>/export-2026-08-21-v3.mjs \
  "…/스밈스튜디오 시안 2026-08-21 v3.html" ".next-prod/static/chunks" "http://localhost:3101"
# 끝나면 3101 종료
```

- 산출물: `스밈 스튜디오\스밈스튜디오 시안 2026-08-21 v3.html` — **11.53MB**(v2 12.97MB 보다 가벼움: 온설 캡처 3종이 작아졌습니다). **v2 는 지우지 않고 보존**했습니다.
- 스크립트는 `export-2026-08-21-v2.mjs` 사본이고 **배너 문구만** 갱신했습니다(v2 대비 변경점 4줄 · "색 5종 × 톤 4종"으로 수정).
- 추출 로그: 이미지·폰트 27건 6.60MB → CSS 10.48MB → JS 9 + 지연 7 = 11.50MB.
- ⚠️ 추출 중 `! 등록 패턴 못 찾음 0cz1d0mv5g_q7.js` 경고가 1건 났지만 **file:// 실측 콘솔 에러 0 · 외부 요청 0** 이라 동작에 영향이 없습니다(지연 청크 중 registerChunk 패턴이 없는 것 하나).
- file:// 검증 수치는 위 §0 표의 v3 줄에 있습니다. 캡처 `v3-file-hero.png`(히어로) · `v3-file-work.png`(포트폴리오 3장 — **온설 카드가 새 히어로 + "7페이지"** 로 바뀐 것이 보입니다).

### 서비스 카드 정돈 (2026-08-21) — pill 칩 → 체크 리스트 + 축 아이콘

사용자: **"밑에 작은 버튼들(칩)이 난잡 — 아이콘 활용해 더 짧게/정돈되게."**

- **pill 칩 폐기** → **체크 리스트**: 테두리·배경 있는 알약 4개가 카드 폭에 따라 제멋대로 접히던 자리를, **14px 체크(원 배경 없이 획만, 액센트색) + 한 줄 라벨** 세로 리스트로 바꿨습니다. 항목 간 9px, 테두리·배경 0. 4카드 모두 4항목이라 **높이가 자연히 통일**됩니다(실측 370px 4장 동일).
- **축 아이콘 추가** — 카드 맨 위에 얇은 선 아이콘 하나: 나침반(전략) · 레이아웃 격자(웹) · 돋보기(검색) · 연필(콘텐츠). **인라인 SVG**(라이브러리 금지) · 22px · 1.5px 스트로크 · `currentColor` 라 밴드 토큰을 따릅니다.
  - **유/무 두 안을 캡처로 비교**(`services-tidy.png` / `services-tidy-noicon.png`) → **유 채택**. 네 카드를 한눈에 구분해 주고, 사용자 요청("아이콘 활용해")에 직접 대응합니다. 빼고 싶으면 카드의 `.service-axis-icon` 블록만 지우면 되고 그 경우 카드 높이가 370 → 336px 로 줄어듭니다.

#### 라벨 압축표 (페이블 확정 — 뜻은 그대로, 수식어만 덜어냄)
| 축 | 카드 라벨 (`cardItems`) | 원문 (`includes`, /services 에서 계속 사용) |
|---|---|---|
| 브랜드 전략 | 목표 진단 · 시장·경쟁사 조사 · 포지셔닝 설정 · 핵심 메시지 정리 | 현재 상황과 목표 진단 · 고객·시장·경쟁사 조사 · 브랜드 포지셔닝 방향 설정 · 핵심 메시지와 문장 정리 |
| 웹 경험 설계 | 사용자 흐름 설계 · 콘텐츠 구조 설계 · UI·디자인 시스템 · 반응형 개발 | 사이트맵과 사용자 흐름 설계 · 화면별 콘텐츠 구조 정리 · UI 디자인과 디자인 시스템 · 반응형 웹 개발 |
| 검색 기반 | 키워드 구조 · 메타데이터 정리 · 구조화 데이터 · 플레이스 정비 | 검색 키워드 구조 설계 · 페이지별 메타데이터 정리 · 구조화 데이터와 사이트맵 · 네이버 플레이스 정보 구조 |
| 콘텐츠 운영 | 주제·키워드 설계 · 캘린더 설계 · 블로그 제작 · 발행 기준 정리 | 콘텐츠 주제와 키워드 정리 · 콘텐츠 캘린더 설계 · 블로그 콘텐츠 제작 · 채널별 발행 기준 정리 |

🚨 **`includes` 를 덮어쓰지 않고 새 필드 `cardItems` 를 만들었습니다.** 지시는 "`includes` 교체(원문은 주석 보존)"였지만, `includes` 는 **`/services` 목록·상세 페이지가 전체 목록(5~6개)으로 렌더**하고 있어 4개짜리 압축본으로 갈아 끼우면 그 페이지에서 항목이 **사라집니다**(예: "브랜드 톤 가이드" · "접근성과 성능 점검" · "측정 환경 설정"). 지금 구조는 메인 카드 = 압축본, 서브 페이지 = 원문으로 **둘 다 살아 있습니다**(원문은 주석이 아니라 실제 데이터라 되돌릴 필요도 없습니다).

카드 패딩·직각·4열은 그대로. 실측 285×370px 4장 동일 · 라벨 전부 **한 줄**(높이 20~22px) · pill 잔여 0 · 콘솔 0 · `tsc`·`eslint` 통과.

### 🚨 대형 정리 패스 (2026-08-21) — 파티클 코드 아카이브 + banded 전용 정리

사용자: **"파티클 사용했던 코드들은 따로 분리해 저장해놓고, 지금 하고 있는 코드만 정리."**

#### 아카이브 — `스밈 스튜디오/_archive/particle-shell-2026-08-21/`
`src` **밖**이라 빌드·타입 검사에 안 들어갑니다. 목록과 복원 절차는 그 폴더의 **`RESTORE.md`**, §4-B 원문은 **`SHELL_NOTES.md`** 에 있습니다.
- **삭제한 부품 7종**: `particle-backdrop` · `dna-helix` · `city-skyline` · `section-blobs` · `rotating-word` · `growth-particles` · `stat-counter`
- **삭제한 섹션 7종**: `trust-proof`(약속/포지션/후기로 쪼개진 원본) · `proof-strip` · `brand-statement` · `sumim-difference` · `evidence` · `team` · `hero-visual-parallax`
- **삭제한 라우트**: `/onepage`
- **분기 제거 전 스냅샷**: hero · page · layout · navigation · client-problems · project-card · faq-section · scroll-progress · section-header · process-flow · selected-work · service-system · final-cta · statement-band · position-band · site.ts
- **CSS**: `globals.css` 150개 top-level 블록 중 **셸 스코프 57개**(`html[data-shell]` · `main[data-snap]`)를 통째로 `shell-styles.css` 로 옮기고, 죽은 클래스 14블록(`.hero-nowrap` · `.scroll-cue` · `.floating-action-stack` · `.cta-scrim` · `.work-grid`/`.work-grid-mask` · `#work .eyebrow-pill` · /onepage 스냅)도 함께 옮겼습니다. **2013줄 → 1226줄**.

#### src 정리 (파일별)
| 파일 | 무엇을 했나 |
|---|---|
| `content/site.ts` | 스위치 7개 삭제 · 히어로를 `hero.wordmark` 하나로 · `navigation.bandedMenu/bandedCta` → `menu/cta` 로 승격(셸 서브페이지 메뉴 삭제) |
| `app/layout.tsx` | `data-shell` 제거, `data-visual="banded"` **고정**(CSS 선택자를 건드리지 않으려고 속성은 유지) |
| `app/page.tsx` | `ShellSections` 배열·모드 분기 삭제 → 원페이지 배열 하나 |
| `sections/hero.tsx` | 605줄 → **190줄**. wordmark 만 남기고 사진·manifesto·declaration·파티클 분기 삭제 |
| `sections/client-problems.tsx` | 카톡 패널 고정(헤어라인 말풍선 모드 삭제) + 배경 장식 제거 |
| `sections/selected-work.tsx` | 격자·블롭·파티클·"모든 프로젝트 보기" 삭제, 모달은 항상 마운트 |
| `sections/service-system.tsx` | 4종 카드 고정(3패키지 마크업 삭제 — 데이터 `servicePackages` 는 /services 가 계속 사용) |
| `sections/final-cta.tsx` | 건물 캔버스·스크림 삭제, 빠른 상담 2버튼·폼 안내 상시 노출 |
| `sections/faq-section.tsx` · `process.tsx` · `process-flow.tsx` · `statement-band.tsx` | 모드 분기·장식·인디고 강조 삭제 |
| `ui/accordion.tsx` · `ui/section-header.tsx` | 셸 모습(인디고 호버 · pill 배지) 삭제 |
| `ui/scroll-progress.tsx` | 우하단 플로팅 3단 삭제 → **상단 진행 바만** (▲TOP 재도입 여부는 팀 확인 대기) |
| `ui/project-card.tsx` | 평면 커버 모드·서브페이지 링크 분기 삭제 → 목업 + 모달 트리거 |
| `ui/navigation.tsx` | `overPhoto` prop 삭제 — 사진 히어로가 사라진 뒤로 **항상 true** 였어서 `!solid`(스크롤 전)로 바꿔 **화면은 그대로** 두고 축만 줄였습니다 |
| `ui/sticky-consult-bar.tsx` · `ui/color-lab.tsx` | banded 조건 삭제(항상 켬) |

#### 검증
- **픽셀 비교**: 정리 전/후 **1536 폭 전체 세로 캡처(11,633px)** 를 같은 조건(마키 정지·리빌 전부 발동)으로 찍어 **SHA-256 이 완전히 동일**했습니다 — 화면 불변 증명.
- 동작 각 1회 실측: 마키 구동 ✓ / 누르는 동안 정지 ✓ / 상담 바 등장(top 672) ✓ / 컬러 랩 버튼 ✓ / FAQ 회전 45deg + 액센트 ✓ / 프로젝트 모달 열림(`#work-moongyul-pilates`) → ESC 로 닫힘·스크롤 복구 ✓ / 브릿지 폰 마우스 틸트 ✓ / 밴드 순서 인접 동일색 없음 ✓ / **콘솔 에러 0**.
- `npx tsc --noEmit` · `npx eslint src` 통과, **`NEXT_DIST_DIR=.next-prod npm run build` 성공**(19 페이지 — 서브 페이지 라우트 전부 정상 생성).
- ⚠️ 정리 중 dev 서버가 Turbopack 내부 오류로 죽어 **한 번 재시작**했습니다(캐시 삭제 경고 후 정상).

### 방향 갱신 (2026-08-20 저녁, 팀)
- **메이커리 스타일 확정** — 이미지·영상 소재가 없으므로 **텍스트 배치(타이포 플레이) + 컬러 조합**으로 임팩트. 레퍼런스 중 메이커리가 최종 선택
- 컬러는 미정 — `스밈 스튜디오스밈 컬러 시뮬레이터.html`(단일 파일, 퍼널 요약에 팔레트 실시간 적용, 팀 예시 #000000/#EAF6AD/#6338EE 포함 프리셋 7개)로 팀이 조합 선택 예정. 조합 확정 시 banded 토큰(globals `html[data-visual="banded"]`)에 반영
- ⚠️ 3차의 실사 히어로(사옥 사진)는 이 방향과 상충 가능 — 컬러 확정 시 사진 유지/타이포 히어로 복귀 여부 함께 결정

### 🧪 진단 시안 3종 — 스위치로 켜져 있음, **사용자 확정 대기** (2026-08-23 오후)

페이블 진단("첫 화면 가치제안 0 · 신뢰 요소 전부 자리표시 · 오퍼 4개 이름")에 대한 시안입니다. 코드는 오푸스 서브에이전트, 캡처 검증은 페이블. 전부 `site.ts` 스위치이며 **기본값이 시안 쪽**입니다 — 마음에 안 들면 아래 값으로 즉시 복귀.

| 스위치 | 시안 값(현재) | 복귀 값 | 무엇 |
|---|---|---|---|
| `hero.statementPlacement` | "below" | "none" | 워드마크 아래 확정 선언문 3줄 + `hero.factsLine` + CTA. 워드마크 예산은 `--hero-reserve`(150→280/340px)로만 조정, 글자 기하 불변. 1536×745·1440×900·390×844 첫 화면 안에 들어감(실측) |
| `reviewBand.hideMarqueeWhilePlaceholder` | true | false | 후기가 자리표시인 동안 마키·캡션 숨김, 약속 2건을 스탯 아래 한 줄로 |
| `bridgePhoneScreens` | null | 데모 객체(주석에 보존) | 브릿지 폰 = 실제 문결·온설 모바일 캡처 |
| `founder.enabled` (+ 네비 About) | true | false | FAQ↔CTA 사이 라임 밴드 대표 섹션(모노그램 "한", `photo: null`). **소개 2문장은 페이블 초안 — 대표 확인 필요** |
| `offer.unified` | true | false | CTA 전부 "홈페이지 무료 진단" 계열로 통일(원문은 `*Original` 필드) |
| `contactForm.mode` | "compact" | "full" | 이름·연락처·업체명/주소·고민 한 가지(+동의). 서버 액션 키 집합 동일, compact 는 이메일 선택 |
| `copyMode` | "plain" | "original" | 서비스 4카드 태그라인·Q&A 답변을 사장님 언어로(`taglinePlain`, 원문 보존). /services 서브페이지는 원문 그대로 |
| `typeScale` | "large" | "default" | `<html data-type>` — 본문 17px·캡션 13px·서비스 항목 15px 고정. 리뷰 밴드 스탯 라벨 11~11.5px 은 모바일 레이아웃 실측값이라 남김 |

**중복 제거 (2026-08-23, 사용자 지시 "섹션별 중복은 한군데만")** — 각 사실은 담당 섹션 한 곳에만. 지운 문자열은 소스에 `// 2026-08-23 중복 제거:` 주석으로 보존.
- 환불 100%·예약제 → **라임 밴드만**(자리표시 동안 아이브로우 `reviewBand.eyebrowWhilePlaceholder`="OUR PROMISE"). 서비스 밴드의 OUR PROMISE 열 제거(설명문을 우측 5/12 열로), 대표 섹션 `founder.facts` 제거, `hero.factsLine`="부산 · 홈페이지 제작 + 블로그 마케팅"
- "대표가 처음부터 끝까지" → **대표 섹션만**. Process 부제(`processSection.description`=null)·FAQ "담당자가 바뀌지 않나요" 제거
- FAQ 10→7: "기획안이나 자료가 없어도", "상담 후 바로 계약", "담당자" 삭제(CTA 안내·다른 문항과 중복)
- Process 01 note = `offer.unified` ? "무료 진단" : "무료 상담"
- **CTA 섹션은 사용자 지시로 그대로**(안내 3줄·이메일 유지)
- 대표 섹션: 좌측 모노그램(398px)보다 우측 글이 짧아 라임 여백이 남음 — `lg:items-center` 한 줄이면 해결, 사용자 확인 대기

**2026-08-23 저녁 추가**
- 히어로 아랫줄 `i` = 윗줄 `!` 를 잉크 중심 기준 세로 반전(`hero.wordmark.lines[1].accentFlipOf: "!"`, 복귀 `null` → `accentGlyph: "i"`). 실측 Δ중심x 0.00px, Δ상단 ≤0.2px. 상세는 §5-B "보강 ⑤"
- **대표(한병선) 섹션 삭제**(사용자 지시): `founder.enabled: false` + 네비 About 주석 처리. 코드·카피는 보존
- **Q&A 대안 3종** `clientProblems.style: "chat"(현재) | "sheet"(진단표: 검정 증상/라임 처방 2열) | "cards"(검정 카드 2×2, 큰 보라 숫자) | "list"(큰 타이포 리스트 40px + 우측 답)` — 사용자 선택 대기. 캡처는 세션 scratchpad `qna/`. 새 카피는 `sheetLabels`(증상/처방)뿐
- Q&A 1차 3안(sheet/cards/list) 사용자 반려("여전히 별로") → **2차 3안 추가**: `clientProblems.style` 에 `"check"`(셀프 체크리스트 — 클라이언트 컴포넌트 `client-problems-check.tsx`, 누르면 답 펼침 + 검정 요약 바 "{n}가지가 해당되시네요" + CTA) · `"ticker"`(전폭 2줄 마키, 2행은 연한 회색 — 한글 text-stroke 는 지저분해서 폐기) · `"root"`(증상 칩 + "원인은 대개 하나" 48px + 증상→처방 + CTA). 새 카피 필드 10개(checkEyebrow…rootBody)는 site.ts. 기본은 여전히 "chat", 사용자 선택 대기
- 사용자 "체크와 티커를 위아래로, 한 화면에" → `clientProblems.style: "combo"` 추가(티커 60px 2줄 + 셀프 체크 2×2 + 검정 요약 바). 섹션 높이 실측 624px(1440·1536, 초기 상태) / 804px(390). `Section` 에 `paddingY="none"` 옵션 추가(다른 섹션 영향 없음). **2026-08-23 사용자 확정("지금 좋다") → 기본값 "combo"**. 복귀는 "chat"
- 티커 보정(사용자: 속도 절반 이하 + 내용 다양화): `clientProblems.tickerPhrases` 12문장(기존 4 + 새 8, 페이블 초안) — 1행 짝수/2행 홀수 인덱스로 6문장씩. 속도 `--ticker-duration: 140s` → combo 86→31.5px/s, ticker 108→39px/s(≈36%). 문장 추가/삭제 시 px/s 재측정. ⚠️ 서브에이전트가 작업 후 기본값을 "chat" 으로 되돌리는 버릇이 있음 — 확정값은 **"combo"**, 작업 후 490행 확인할 것
- **휴대폰 목업 = 실사 PNG 프레임**(사용자: CSS로 그린 폰이 애매하다 → 1번안 채택, 페이블이 라이선스 확인 후 다운로드). `public/images/devices/iphone-17-pro-max.png`(389×800, 화면 영역 투명, Mobile FIRST — 상업 이용·수정 허용, 출처 표기 불필요, 파일 단독 재판매 금지 · LICENSE.txt 동봉). 새 `components/ui/phone-frame.tsx`(화면 구멍 실측 16.93/13.73/355.91/772.94, 스퀘어클 반경 52px→백분율), 스위치 `deviceFrame: "png" | "css"`(복귀 "css"). 브릿지 2대 + 포트폴리오 카드 폰 적용, 브라우저 목업은 CSS 유지. 폰 비율 차이(389/800 vs 9/19.5)는 **폭 고정·높이 5% 축소**로 흡수해 V자 겹침 6.3% 유지
- 온설 여수 글 첫 화면 사진: 사용자가 보낸 컷은 보유 파일(photo-yeosu-booth)과 **다른 컷** — 파일 수령 대기(온설 프로젝트 쪽 일)

#### 🆕 combo — 헤딩 `text-h1` 통일 **(확정)** + 티커 띠 후보 3안 **(🧪 사용자 선택 대기)** (2026-08-25)

**① 헤딩 크기 — 확정 적용.** 팀: "헤딩 폰트 크기가 다른 섹션과 다르다."
`ComboHeader()` 만 `text-h2 mt-4` 였고 **다른 섹션(chat·check 패널 포함)은 전부 `text-h1 mt-6`** 이었습니다 → **`text-h1 mt-6` 으로 통일**했습니다(원래 `text-h2` 는 "1440×900 한 화면 예산" 때문에 눌러 둔 값).

- 🚨 **늘어난 세로를 다른 데서 흡수할 필요가 없었습니다.** 실측 헤딩 **36px/lh 45 → 48px/lh 56.64** · `mt` **16 → 24px** → 섹션 **623.50 → 643.13px(+19.63)**.
  **1440×900 예산은 820px**(900 − 고정 네비 80)이므로 643.13 은 **176.87px 여유**로 들어갑니다. 섹션 패딩(56/40) · 티커 `my-7` · 체크 그리드 간격은 **한 값도 안 건드렸습니다**.
- 다른 폭: 768 **761.92 → 777.69**(+15.77) · 390 **804 → 816**(+12).
- **다른 섹션 영향 0** — 문서 높이 증가분이 섹션 증가분과 같습니다(1440 9,342→**9,362** · 1536 9,204→**9,223** · 768 9,877→**9,893** · 390 11,114→**11,126**).

**② 티커 띠 후보 3안 — 🧪 확정 아님, 기본값은 현재 화면 그대로.**
팀: **"티커 띠가 너무 혼자 넓은 느낌"** — 가로(전폭이라 혼자 튄다) / 세로(띠가 두껍다) 두 갈래로 읽혀 **양쪽을 실제 스위치**로 만들었습니다(런타임 주입 아님). `client-problems.tsx` 상단:

```ts
const TICKER_WIDTH: "bleed" | "contained" = "bleed";  // 현재
const TICKER_SIZE:  "base"  | "small"     = "base";   // 현재
```

| 후보 | 스위치 | 1536 티커 행 | 행 폭(x) | 글자 | 위아래 여백 | **섹션 높이**(1440·1536) | 문서 높이(1536) |
|---|---|---|---|---|---|---|---|
| **기준(현재)** | `bleed` + `base` | **60px** | 1536 (x=0) | 44px | `my-7`(28) | **643.13** | 9,223 |
| **A 가로** | `contained` + `base` | 60px | **1200 (x=168)** | 44px | `my-7` | **643.13**(불변) | 9,223 |
| **B 세로/크기** | `bleed` + `small` | **44px** | 1536 (x=0) | **34px** | **`my-5`**(20) | **595.13**(−48) | 9,175 |
| **C = A+B** | `contained` + `small` | 44px | 1200 (x=168) | 34px | `my-5` | **595.13**(−48) | 9,175 |

- A 는 **세로가 한 픽셀도 안 변합니다**(폭만 Container 로 들어옴). B·C 는 **−48px**.
- `contained` 여도 마키는 그대로 흐릅니다 — `.ticker-row { overflow: hidden }` 이 이미 있어 트랙만 그 안에서 잘립니다. `--ticker-duration` 은 **안 건드렸으므로** 트랙 길이가 같아 **초당 픽셀도 그대로**입니다.
- ⚠️ `"ticker"` 안(전폭 대형 티커)은 이 스위치를 **타지 않습니다** — combo 전용(`compact` 일 때만 분기).
- **캡처(같은 구도 1536, 섹션 전체)**: `qna-ticker-base.png` · `qna-ticker-a.png` · `qna-ticker-b.png` · `qna-ticker-c.png` · 헤딩 변경 전 `qna-heading-h2-before.png`. 측정 스크립트 `q-lib.mjs` · `q-run.mjs` · `q-all.mjs`.
- 검증: `tsc`·`eslint` 통과 · **콘솔 에러 0**(dev `next/image` quality 경고 9건은 무관) · **가로 스크롤 0**(390·768·1536·1440) · 네 후보 전부 동일.

#### 🆕 combo — **티커 순서 뒤집기 (2026-08-25 · 확정)** + 중복 제거 **(🧪 후보)**

팀: **"셀프 체크 띠 부분이 생뚱맞다."** 진단해 보니 원인이 둘이었습니다.

1. **티커가 헤딩과 체크 사이를 가로막고 있었습니다.** 헤딩("혹시, 이런 상황이신가요?")이 무엇을 소개하는지 알기 전에 12문장이 먼저 흘러, 헤딩과 체크가 서로 떨어져 읽힙니다.
2. 🚨 **`tickerPhrases` 12문장 중 앞 4개가 `cards[].title` 4문항과 글자 그대로 같습니다** — 문자열 비교로 확인했습니다(4/12 완전 일치). 체크에서 방금 읽은 문장이 바로 위 띠에서도 흐릅니다.

⚠️ 지시서에 있던 "헤딩 아래 `description`(…네 가지 문제입니다)이 12문장과 어긋난다"는 **combo 에서는 해당이 없습니다** — `ComboHeader()` 는 `checkEyebrow` + `heading` 만 렌더하고 `description` 은 쓰지 않습니다(chat·sheet 등 다른 안 전용). 실제 중복은 위 ②뿐입니다.

##### 사용자 선택 = 순서 뒤집기 → 스위치 `TICKER_ORDER` (`client-problems.tsx` 상단)
| 값 | 순서 |
|---|---|
| **`"after-summary"`(새 기본)** | 헤딩 → **안내** → 체크 2×2 → **검정 요약 바 + CTA** → **전폭 티커** |
| `"before-summary"` | 헤딩 → 안내 → 체크 2×2 → **전폭 티커** → 검정 요약 바 — 후보 B |
| `"top"` | **2026-08-25 이전 그대로**(헤딩 → 티커 → 체크 → 요약 바). 복귀용 |

##### 🚨 배치 후보 A vs B — 캡처 비교 후 **A(요약 바 다음 티커) 채택**
| | A `after-summary` ✅ | B `before-summary` |
|---|---|---|
| 섹션 높이(1536) | 736.17px | 716.17px(−20) |
| 티커 폭 | **전폭 0~1536px** | **1200px(x=168)** — Container 안으로 갇힘 |
| 읽는 흐름 | 체크 → **결과(요약 바) → CTA** 로 끝나고, 그 뒤 티커가 여운 | 체크 → 티커 → **뒤늦게 결과** |

**B 를 안 쓴 결정적 이유는 두 가지**입니다. ① **요약 바는 체크 블록의 마무리**입니다(`{n}가지가 해당되시네요` + CTA). 그 사이에 12문장이 끼면 "체크 → 결과 → 행동" 사슬이 끊기고 CTA 가 섹션 맨 아래 외딴 블록이 됩니다. ② B 는 티커가 **`SelfCheck` 안 `Container` 속으로 들어가 전폭이 깨집니다** — 팀이 지적한 "혼자 넓은 느낌"과 별개로, `TICKER_WIDTH: "bleed"` 라는 **현재 확정값과 화면이 어긋납니다**(캡처 `qna-order-candidate-b.png` 참고 — 좌우 168px 여백이 생겨 띠가 중간에 낀 상자처럼 보입니다).

구현 — `SelfCheck` 에 **`beforeSummary?: ReactNode` 슬롯**을 새로 뒀습니다(체크 4행과 요약 바 **사이**). 안 넘기면 **예전과 완전히 같은 마크업**이 나오므로 `style: "check"` 안(`ProblemChecklist`)에는 영향이 0 입니다.

##### 티커 라벨 — 🧪 **새 카피를 짓지 않고 기존 문장을 옮겼습니다(초안 · 사용자 확정 대기)**
티커가 아래로 내려가면 "이 띠가 왜 여기 있는지"를 받아 줄 줄이 필요합니다. **`site.ts` 에 이미 있는 문장 두 개를 재배치**했고 **새 문장은 한 줄도 짓지 않았습니다.**

| 자리 | 문구 | 출처 | 상태 |
|---|---|---|---|
| 헤딩 아래 안내 | **"해당되는 항목을 눌러 보세요."** | `clientProblems.checkHint` — `style:"check"` 안이 원래 쓰던 줄 | 티커가 위에 있던 시절엔 세로 예산 때문에 뺐던 것이라, 이제 **제자리를 찾은 복원**입니다 |
| 티커 위 라벨 | **`전부 "홈페이지가 말을 안 해서" 생기는 일입니다.`** | `clientProblems.tickerStatement` — `style:"ticker"` 안이 **티커 아래**에 쓰던 문장 | 🧪 **초안** — 사실을 더하지도 빼지도 않았지만 자리가 바뀌었으므로 확정은 사용자 몫 |

- `TICKER_ORDER: "top"` 에서는 둘 다 렌더하지 않습니다 — 예전 화면을 픽셀 그대로 되돌리기 위해서입니다.
- 라벨 문구를 바꾸고 싶으면 `site.ts` 의 `tickerStatement` 한 곳만 고치면 됩니다(`"ticker"` 안과 공용이라 그쪽도 같이 바뀝니다).

##### 🧪 중복 제거 — `TICKER_DEDUPE` (기본 **`false`**, 사용자 선택 대기)
`true` 면 `tickerPhrases` 에서 **`cards[].title` 과 문자열이 같은 4문장을 렌더 시점에 걸러** 8문장만 흐릅니다.
🚨 **`site.ts` 의 12줄 배열은 한 줄도 지우지 않았습니다**(비교·복귀용).
🚨 **파리티 분배는 필터 뒤에 다시 계산합니다** — 원본 인덱스로 가르면 앞 4개가 빠진 뒤 6+2 로 기울어집니다. 걸러낸 배열에서 다시 갈라야 **4+4** 가 됩니다(실측 `items` 두 줄 다 8 = 4문장 × 2벌).

**⚠️ 속도 — 줄마다 duration 이 다릅니다.** `px/s = 트랙폭 ÷ 2 ÷ duration` 인데 문장이 줄면 트랙이 짧아져 **같은 140s 로는 느려집니다**(실측 30.2 / 27.9). 걸러지는 4문장이 두 줄에 고르게 갈리지 않아 축소율도 다릅니다.

| 줄 | 트랙폭 12문장 → 8문장 | 비율 | duration | **px/s (원판 → dedupe)** |
|---|---|---|---|---|
| 윗줄 | 8,833 → **5,610px** | 0.635 | 140s → **89s** | 31.5 → **31.5** |
| 아랫줄 | 8,877 → **5,185px** | 0.584 | 140s → **82s** | 31.7 → **31.6** |

**08-23 에 확정한 속도(≈31.5~31.7px/s)를 그대로 유지**하며 **넘지 않습니다**(지시 조건). 값은 `TICKER_DURATION_DEDUPE = { top: "89s", bottom: "82s" }` 한 곳에 있고 `--ticker-duration` 을 인라인으로 내려보냅니다 — `globals.css` 는 **안 건드렸습니다**.

##### 세로 실측 — **+93px, 다른 섹션 영향 0**
늘어난 93px 은 **되살린 안내 한 줄(49px) + 티커 라벨 한 줄(44px)** 이 전부입니다(티커 행·체크·요약 바·섹션 패딩은 **한 값도 안 건드렸습니다**).

| 뷰포트 | 섹션 전 → 후 | 문서 전 → 후 | 증가분 일치 | 가로 스크롤 |
|---|---|---|---|---|
| **1440×900** | 643.13 → **736.17**(+93.04) | 9,362 → **9,455**(+93) | ✅ | 0 |
| 1536×960 | 643.13 → **736.17**(+93.04) | 9,438 → **9,531**(+93) | ✅ | 0 |
| 768×1024 | 777.69 → **869.28**(+91.59) | 9,893 → **9,984**(+91) | ✅ | 0 |
| 390×844 | 816 → **906.23**(+90.23) | 11,126 → **11,216**(+90) | ✅ | 0 |

🚨 **1440×900 한 화면 규칙 유지** — 예산 **820px**(900 − 고정 네비 80) 대비 736.17px 이라 **83.83px 여유**가 남습니다(직전 176.87px 에서 줄었지만 여전히 들어갑니다). 그래서 이번에도 **다른 요소를 줄여 흡수할 필요가 없었습니다.**
- `TICKER_DEDUPE: true` 여도 **섹션 높이는 736.17px 로 같습니다**(티커 행 높이가 고정이라 문장 수와 무관).
- 🚨 직전 작업의 **`TICKER_WIDTH`/`TICKER_SIZE` 후보 스위치는 기본값 그대로**(`"bleed"` / `"base"`) 뒀습니다 — 사용자 선택 대기 중입니다.

##### 검증·캡처
`npx tsc --noEmit` · `npx eslint src` 통과 · **콘솔 에러 0**(390·768·1440·1536) · **가로 스크롤 0**(네 폭 전부) · `ProblemChecklist`(`style:"check"`) 마크업 불변.
캡처(같은 구도, 섹션 전체) — `qna-order-before.png`(현재=`top`) · **`qna-order-after.png`(채택 A)** · `qna-order-candidate-b.png`(후보 B) · `qna-order-after-dedupe.png`(+중복 제거) · 모바일 `qna-order-390.png` · 참고 `qna-order-768.png` · `qna-order-1440.png`. 측정 스크립트 `qna-shot.mjs`.


남은 결정: 대표 사진·소개 문장 확정 / "무료 진단" 오퍼 이름 확정(코치 전략의 "원페이지 무료"와 맞출지) / 대표 섹션 하단 여백 / **🧪 티커 띠 후보 A·B·C 중 선택(또는 현재 유지)** / **🧪 티커 위 라벨 문구 확정**(현재 `tickerStatement` 재배치 초안) / **🧪 `TICKER_DEDUPE` 켤지**(중복 4문장 제거).

## 6. 지금 남은 작업 (사용자 확인 대기)

1. **Process 02 착수금 조건**, **06 수정 횟수 정책** — `process.ts`에 `note: null`로 비워둔 자리
2. **고객만족도 수치** — TrustProof의 `stats`에 `value: null`이라 화면에 안 나옴
3. **실제 고객 후기** — `trustProof.testimonials` 5건 전부 자리표시 (`testimonialsArePlaceholder: true`). 실후기가 들어오면 **그 플래그만 `false`** 로 내리면 마키 위 캡션이 사용자 확정 문구 **"실제 업체 대표님들의 소중한 후기입니다."**(2026-08-22 확정, `reviewBand.marqueeNote.real`)로 자동 전환됩니다 — 문구는 이미 넣어 뒀으니 그때 고칠 것이 없습니다. **후기 본문이 자리표시인 채로 플래그만 내리면 안 됩니다**(허위 주장)
4. ✅ **실제 포트폴리오 4건 완성 (2026-08-26)** — 문결·온설·지안 3건(2026-08-12 교체) + **경희정원한의원**이 4번째로 추가됐다(사용자 지시). `projects.ts` 에 `kyunghee-garden` 항목 신설 — 캡처 3종(`kyunghee.jpg` 1440×900 · `kyunghee-full.jpg` 1440×8662 · `kyunghee-mobile.jpg` 1560×8400)과 임베드 `public/embeds/kyunghee-garden.html`(= 카카오톡 수신 "경희정원한의원_6차시안_v6 (1).html" 과 **바이트 동일**)은 같은 날 준비돼 있던 것을 연결만 했다. `selected-work.tsx` 는 `projects.slice(0, quad ? 4 : 3)` + **placeholder 는 4건 미만일 때만** 렌더하도록 수정 → "준비 중" 칸이 자동으로 밀려났다. 소개 문구(관점 "암 회복 진료가 신뢰로 읽히도록" 등)는 시안 v6 의 실제 내용(7가지 회복 설계 영역·최정원 원장·암 회복 진료)에서만 뽑은 **초안 — 팀 검토 대기**. 로고 파일은 없어 호버 오버레이가 이름 텍스트로 폴백. Vercel 미리보기에서 4칸 격자·호버 오버레이·모달·딥링크(`#work-kyunghee-garden`)·ESC 닫기 실측 확인, 콘솔 에러 0 · 가로 스크롤 0 · `tsc`·`eslint` 통과
5. ✅ **도메인 확정 (2026-08-26)** — `sumimstudio.co.kr` (가비아 구매). `src/lib/metadata.ts` 의 `siteUrl` **기본값을 이 도메인으로 교체**했다(플레이스홀더 `sumim-studio.example.com` 삭제) → robots·sitemap·OG 가 실제 도메인으로 나간다(실측 `Sitemap: https://sumimstudio.co.kr/sitemap.xml`). `NEXT_PUBLIC_SITE_URL` 을 주면 그 값이 이긴다(미리보기 배포에서 그 주소로 덮고 싶을 때).
   ✅ **사업자등록번호 확정** — `799-09-03319`(상호 "스밈 스튜디오") → `site.ts` `business.registrationNumber`, 푸터에 "사업자등록번호 799-09-03319" 렌더 확인.
   ⚠️ **아직 없는 것**: 대표자명(`business.representative`) · 사업장 주소(`business.address`) · **`/privacy` 의 개인정보 보호책임자 이름·연락처**(폼으로 개인정보를 받으므로 **공개 전 필수**) · 팀 소개 · SVG 로고 · GA4(`NEXT_PUBLIC_GA_ID`) · Clarity
6. **배포 (Vercel) — 🆕 2026-08-26 미리보기 배포까지 완료**
   - ✅ **로그인 완료** — 계정 `camel7116`, 프로젝트 **`marketing-rap/sumim-studio`** 에 연결(`vercel link --yes`). 🚨 **CLI 로그인이 `TypeError: Cannot convert argument to a ByteString`(51116 = '재')로 실패한다** — **컴퓨터 이름이 "재훈"(한글)**이라 기기명 헤더 변환에서 죽는 것. 해결 = `NODE_OPTIONS="--require <scratchpad>/hostname-fix.js"`(내용: `require('os').hostname = () => 'sumim-pc'`)를 붙여 실행. 로그인은 사용자 크롬(Vercel 로그인 상태)에서 device 승인으로 통과했다
   - ✅ **환경변수 등록 완료** — `NEXT_PUBLIC_WEB3FORMS_ACCESS_KEY` 를 Production·Preview 에 등록. 🚨 CLI 59.5 는 기본이 sensitive 라 `NEXT_PUBLIC_` 접두사와 충돌한다 — **`--no-sensitive` 플래그 필수**(`printf '%s' "$KEY" | npx vercel env add NEXT_PUBLIC_WEB3FORMS_ACCESS_KEY production --no-sensitive`)
   - ✅ **미리보기 배포 2회 성공**(CLI 방식·`npx vercel deploy --yes`) — 최신: `https://sumim-studio-a9udbgcpc-marketing-rap.vercel.app`(경희정원 포함). 빌드 19페이지 정상, 콘솔 에러 0
   - ✅ **문의 메일 수신처 = sumimstudio@naver.com 으로 교체 완료(2026-08-26 저녁)** — 사용자가 web3forms.com 에서 네이버 주소로 새 Access Key 를 발급받아 전달(구 키는 지메일 수신이었음). `.env.local` + Vercel env(Production·Preview, `--no-sensitive`) 교체 → 재배포 → **라이브(sumimstudio.co.kr)에서 실제 제출 → `/thanks` 리다이렉트 확인**. ⚠️ `vercel env ls` 의 값 표시가 `eyJ2IjoidjIi…` 로 보이는 것은 **Vercel 의 암호화 봉투**다 — 실값 확인은 `vercel env pull` 로 할 것(실측으로 새 키 반영 확인함). 네이버 수신함 최종 확인은 사용자 몫(에이전트는 naver 메일 접근 불가)
   - ⚠️ **Deployment Protection 이 켜져 있다** — 미리보기 URL 이 Vercel 로그인 없이는 302 → SSO 로 튕긴다(팀원 공유 불가). 끄려면 프로젝트 Settings → Deployment Protection (사용자 결정 대기)
   - **상태**: Vercel CLI **59.5.0** `npx` 로 실행 가능 · 프로덕션 빌드 통과 · **git 저장소가 아님**(`sumim-studio` 도 아님)
   - 🚨 **로그인은 사용자가 직접** — `npx vercel login`(계정 인증은 에이전트가 대신할 수 없다). 로그인 후 `npx vercel`(미리보기) → `npx vercel --prod`(정식)
   - 🚨 **환경변수** Settings → Environment Variables 에 **`NEXT_PUBLIC_WEB3FORMS_ACCESS_KEY`**(값은 로컬 `.env.local`) — **없으면 문의 폼 메일이 안 간다**. `NEXT_PUBLIC_*` 는 빌드 시점에 박히므로 **등록 후 재배포** 필요. ⚠️ API 키 입력은 **사용자 몫**(에이전트가 키를 입력하지 않는다)
   - **미리보기 vs 정식**: 코드·빌드는 같고 붙는 주소만 다르다. 미리보기는 배포마다 고유 URL + **Vercel 이 자동 noindex**, 정식은 대표 주소 + 검색 노출 + 대시보드에서 즉시 롤백. 미리보기 배포를 그대로 **프로덕션으로 승격** 가능
   - **도메인 연결 순서**: Vercel 프로젝트 생성 → Settings → Domains 에 `sumimstudio.co.kr` 추가 → **거기 표시되는 레코드 값을** 가비아 DNS 관리에 입력(네임서버 변경 또는 A·CNAME). 값은 바뀔 수 있으니 **화면에 뜨는 것을 그대로** 쓸 것. `www` 도 함께 추가 권장. HTTPS 인증서는 자동
   - ⚠️ **Hobby 플랜은 약관상 비상업적 용도** — 자사 홈페이지 정식 운영은 **Pro(월 $20)** 가 맞다(팀 확인용 미리보기 단계는 무관)
   - 🧭 **사용자 미결정(질문 닫음)**: ① CLI 로 올릴지 GitHub 연결로 갈지 ② 미리보기부터 vs 바로 정식 ③ 검색 노출 차단 여부 ④ 좌하단 colorLab 패널을 공개 사이트에도 남길지(2026-08-21 "유지" 결정은 있으나 배포 시점 재확인 필요)
7. **히어로 선언문 3줄 + 메타의 새 자리** — 2026-08-22 히어로에서 뺐고 문구는 `site.ts` `hero.wordmark.statement`·`meta` 에 보존 중입니다. **선언문을 대문(인트로 화면)으로 빼는 안 — 미라 재검토 중, 미확정**

### 🆕 2026-08-23 — 라임 → **라벤더** 기본 전환

**2026-08-23 라임 → 라벤더 `#ECE8FB` 기본**(`bandedPalette = "lavender"`, 팀 팔레트 `"team"` 보존), **colorLab 6색**.

- 사용자 결정: 라임 밴드 `#EAF6AD` 가 겉돈다 → **액센트 바이올렛 `#6338EE` 의 옅은 틴트**인 라벤더로 교체. 유채색 축이 바이올렛 하나로 모입니다. **검정·흰·액센트·카카오 색은 한 값도 안 바뀌었습니다.**
- `globals.css` 에 `html[data-visual="banded"][data-palette="lavender"]` 블록 신설 — **team 블록은 한 줄도 안 지웠습니다.** `site.ts` 의 `bandedPalette` 를 `"team"` 으로 되돌리면 라임이 그대로 복귀합니다(실측 회귀 확인: 캔버스 `#eaf6ad` · 진한 밴드 `#cbd696` · 잉크 `#101208` · 진한 밴드 액센트 `#4f2dbe`).
- 🚨 **라벤더에서는 `.band-deep` 이 무효입니다** — 리뷰/약속 밴드를 한 단계 누르지 않고 **평평한 `#ECE8FB` 그대로** 칠합니다(팀이 고른 시안이 그 모습이었습니다). 규칙은 `background-color: var(--color-canvas)` 라 ⓐ 캔버스 값 한 곳만 고치면 따라오고 ⓑ colorLab 실험 조합에서도 그 조합의 미스트를 그대로 따라갑니다. 클래스는 지우지 않아서 `"team"` 으로 돌리면 진한 라임이 살아납니다. **되돌릴 값**: 한 단계 진한 라벤더가 다시 필요하면 `#D9D2F7`.
- 미스트 위 파생 토큰(딥 바이올렛 그레이) — 잉크 `#0c0915` **16.43** / 보조 `#4a4168` **7.79** / muted `#5c5575` **5.81** / 액센트 `#6338ee` **5.25**. **밴드 위 실측 최저 5.25 · AA 미달 0건.** muted 는 team 이 라임에서 못 넘던 진한 배경 기준(3.37)도 여유로 넘습니다.
- `colorLab` **색 5종 → 6종** — "블랙 × 라벤더 × 바이올렛"을 **목록 맨 앞**에 넣었습니다(`HUES[0]` 폴백 = 사이트 기본과 일치). 톤 4종은 기존 규칙으로 자동 파생(기준 `#ece8fb` · 따뜻 `#f3f0fd` · 프리미엄 `#e5e0f9` · 클린 `#f4f2fc`). **검수 7항목 × 4톤 = 28칸 전부 4.5 이상.** `초기화` 는 라벤더 기본으로 돌아옵니다(`DEFAULT_STATE.hue = "lavender"`). 스탠드얼론 `스밈 컬러 시뮬레이터.html` 도 같은 1종을 추가해 값을 맞췄습니다.
- ⚠️ **알아 둘 것** — 라벤더가 활성인 동안은 colorLab 에서 **라임을 골라도 평평한 라임**(`#eaf6ad`)으로 보입니다. `.band-deep` 무효화 규칙이 팔레트 단위라 그렇습니다. 실제 team 사이트의 진한 라임(`#cbd696`)을 보려면 `bandedPalette` 를 `"team"` 으로 바꿔야 합니다.
- 검증: `tsc`·`eslint` 통과 · 콘솔 에러 0 · 1536 가로 스크롤 0 · 리뷰 밴드 실측 `rgb(236,232,251)` · 다른 섹션(검정·흰) 색 변화 0.

### 🚨 절대 하지 말 것
- **후기·만족도·실적 수치를 지어내지 마세요.** 표시광고법 위반이며, 사용자와 "검증된 수치만 쓴다"고 합의한 원칙입니다. 근거가 없으면 그 항목을 **비워두거나 빼는 것**이 맞습니다.

---

## 7. 반려 목록 (다시 제안하지 마세요)

이미 만들어 보여드렸다가 사용자가 되돌린 것들입니다.

| 시도 | 결과 |
|---|---|
| 히어로 중앙정렬 | "별루다" → 좌측 정렬 유지 **(2026-08-18 셸 모드에서 사용자가 중앙정렬로 재지정 — 셸 켜진 동안은 중앙)** |
| 사이트 전체 중앙정렬 | 비추천 결론 (선언=중앙, 정보=좌측) |
| Process 원형 플로우 | "우리 원래 모양 유지" → 스티키 스크롤 유지 |
| 문제 섹션 RESULT 다크 카드 | "이상해" → 제거 |
| 섹션 16:9 풀프레임 | "너무 어색해" |
| 히어로 흰색 워시 30% 오버레이 | 다크 스크림이 낫다 |
| 히어로 사진을 하단 배치 | 배경으로 확정 |
| 주황 CTA 흰색 반전 호버 | 밝아지는 호버로 변경 |
| 카드 3D 플립 | 디자인 문서 §11.5 금지 |
| 폰트 preload | FCP 악화로 롤백 |
| ★4.89 등 미검증 수치 | 법적 리스크로 보류 |
| **전역 고정 캔버스 모핑** (§4-B 승격) | 스크롤 따라 형태가 바뀌던 구조 — 폐기 |
| **휠 스냅(모든 변형)** (§4-B 승격) | "휠 액션 어색" · "섹션 이동 시 파티클이 따라오지 말라" |
| **코인 플립(Y축) · 도트 폰트 QNA 재배열 · 원 안에 글자 · 원→삼각형 글리프 전환** (§4-B 승격) | 전부 반려 |
| **Q&A 뒤 큰 압출 Q·A 글자 · FAQ 째깍 회전(3초에 한 번)** (§4-B 승격) | 반려 |
| **캔버스가 배경 밴드까지 칠하기** (§4-B 승격) | 캔버스가 안 그려지는 순간 글자가 사라진다 — 배경은 CSS가 칠한다 |
| **스크롤 위치에 묶인 모핑** (§4-B 승격) | 멈추면 반쯤 변한 채 서 있다 |
| 후기 헤딩 "고객의 말로 남는 자리입니다." | "별로" → 2026-08-21 병합 섹션에서 포지션 문구로 통합 |
| **히어로 `+` 마커 4개** | 메이커리와 똑같다는 팀 지적으로 **제거(2026-08-22)**. `site.ts` 의 `markers: 0` — 필드만 보존 |
| **워드마크 글자가 움직이거나 일렁이는 진입**(중간안 `burst` — 글자가 액센트 자리에서 좌우로 미끄러져 나오고 `feTurbulence` 물감 필터로 흔들림) | 사용자 "글자가 일렁이면 안 됩니다" → **폐기**, 배경색 덮개가 걷히는 `spread` 로 대체(2026-08-22) |
| **두 줄 워드마크를 `font-size` 배율 하나로 맞추는 안**(2026-08-22 의 r = 0.9252 → 0.8478) | "!"/"i" 세로 정렬과 폭 동일이 양립하지 않아 폭을 포기했던 안 → **2026-08-23 3칸 박스 구조로 대체**(칸별 자간·scaleX·weight 로 둘 다 충족). 배율 접근은 다시 쓰지 않습니다 |

**예외**: 무한 마키·숫자 카운트업은 원래 금지 항목이었으나, 사용자가 명시적으로 요청해 도입했습니다(지금은 병합 섹션 §6).

> 🚨 위 **§4-B 승격** 항목들은 파티클 셸 시절 반려 목록입니다. 파티클 코드는 아카이브로 갔지만
> **반려 결정은 그대로 유효**하므로 여기로 올렸습니다. 배경 장식을 다시 제안하기 전에 이 표를 보세요.

---

## 8. 사용자와 일하는 방식

- **묻지 않은 변경에 민감합니다.** 지목한 부분만 고치고, 곁다리 개선은 먼저 물어보세요
- 시안은 **말로 설명하지 말고 화면으로** 보여주세요 (스크린샷). 마음에 안 들면 즉시 되돌릴 수 있게 스위치를 두는 방식을 선호합니다
- 메인의 다크 CTA 섹션과 `/contact` 페이지는 **헤딩이 같아** 혼동한 적이 있습니다. 스크린샷으로 지시받으면 어느 페이지인지 먼저 확인하세요
- 수정할 때마다 `tsc` + `eslint` 통과 + 스크린샷 검증을 하는 흐름으로 작업해 왔습니다

---

## 9. 참고한 레퍼런스

- **siterium.co.kr** — 흑백+블루, 문제를 STEP→RESULT로 심화, 디바이스 목업 포트폴리오. 프로세스 내용과 신뢰 섹션 구조를 여기서 차용
- **hothaan.co.kr** — 밝은 3D 톤. 스밈의 차분한 브랜드와 안 맞아 톤은 채택하지 않음

---

**마지막 정리: 2026-08-26** (⓪-AL 🆕 **Work 칸 프레임 제거 · 호버 정보**(2026-08-26) — 사용자 "목업들 휴대폰 PNG·모니터 그림 다 지우고 네모 박스에 차도록. 호버했을 때 로고와, 홈페이지명, 설명문이 나오면". **스위치 `QUAD_FRAME: "none"(새 기본) | "device"`**(`project-quad-card.tsx` 상단) — `"device"` 면 2026-08-25 모니터+폰 목업이 통째로 복귀하고 **기존 코드는 한 줄도 안 지우고 `DeviceCell()` 로 옮겼을 뿐**. 🚨 `phone-frame.tsx`·`device-mockup.tsx`·`deviceFrame` 스위치·`WORK_LAYOUT` 은 **미변경**(브릿지 폰 2대가 계속 씀) · 칸 = **이미지 한 장**(크롬 줄·폰 프레임·칸 패딩 전부 제거, `object-cover` 로 가장자리까지) · **평상시 텍스트 0**, 관점·이름·업종은 **호버 오버레이로 이동** · **칸 비율 4:3**(후보 3안 캡처 비교 — **1:1** 은 커버가 화면 비율(1.33~1.6)이라 좌우를 17~33% 잘라 온설 로고·지안 네비가 날아가고, **16:10** 은 커버 두 장과 정확히 맞지만 띠처럼 납작(390 에서 **121.6px**)해 호버 4줄이 칸의 59% 를 먹음. 4:3 은 문결 커버 1200×900 과 **정확히 일치**, 나머지 둘은 좌우 8.3%씩만 잘림) · 이미지 조합은 **2026-08-25 규칙 그대로**(쉬는 모습 `coverImage` / 호버 스크롤 `fullImage` 9초 linear · 해제 1.2초) · **호버 구성 = 가운데 정렬 · 로고(칸 높이 13%) → 이름 → 관점 → 업종**(후보 2안 비교 — 좌하단 안은 **로고가 흐르는 캡처의 글자 위에 겹치고** 네 칸이 한쪽 모서리로 쏠림) · 🚨 **오버레이 60%** — 흐르는 캡처의 최악 프레임이 **순백**이라 흰 글자 대비를 계산하면 50% **3.98:1(AA 미달)** · **60% 5.74:1** · 70% 8.52:1 → 60% 가 **AA 를 넘는 최소치**이고 70% 는 사이트 화면이 거의 안 보임 · 🚨 **`text-white` 를 못 씀** — `globals.css` 1366~1369 가 밝은 밴드에서 `.text-white` 를 잉크로 되돌려 글자가 **`rgb(16,16,16)`** 으로 나옴(실측) → 어두운 스크림 전용이라 `text-[#ffffff]`/`/85`/`/65` 로 직접 지정(globals 미변경) · 🚨 **`cn()` 은 단순 join** 이라 `RISE(delay)` 를 함수로 만들어 `delay-*` 를 한 번만 붙임 · **준비 중 칸 = 빈 면 + `+` + 문구**(`QUAD_PLACEHOLDER: "plain"` — 프레임이 없어 흉내 낼 상자가 없음. 호버·모달·링크 없음, 실측 `placeholderIsButton:false` · 클릭 가능한 칸 3개) · **검증** 칸 폭 편차 **0.000px** · 틈 **1px** · 경계선 `rgb(229,229,229)` · **가로 스크롤 0**(390·768·1536·1920) · 🚨 **다른 섹션 영향 0**(문서 변화량 = Work 변화량: 1536 9,316→**9,201** / 938.88→**823.19** · 1920 9,782→**9,672** / 1,005.61→**895.19** · 768 9,984→**9,764** / 1,212.06→**991.56** · 390 11,236→**10,966** / 937.72→**668.38**) · 호버 3초 실측 `object-position` **50% 0% → 50% 35.7425%**(9s linear) · 커버 `opacity 0` · 오버레이 `rgba(0,0,0,.6)` · 로고·이름·관점·업종 전부 `opacity 1` · 이름 색 `rgb(255,255,255)` · 모달 클릭/ESC/딥링크 `/#work-onseol` 전부 정상 · **복귀 실측** `"device"` 면 9,316 / 9,782 / 9,984 / 11,236 · 칸 높이 403.13 / 469.86 / 397.88 / 280.55 로 **08-25 값과 완전 동일** · `tsc`·`eslint` 통과 · 콘솔 **에러 0**(dev `next/image` quality 경고 8건 — 폰 목업이 빠져 baseline 9건에서 하나 줄어듦) · 캡처 `work-flat-{1536,1920,768,390}.png` · `work-flat-hover.png` · **`work-flat-hover-center.png`(채택)** · `work-flat-hover-bottomleft.png` · `work-flat-shade-{50,70}.png` · **`work-flat-ratio-43.png`(채택)** · `work-flat-ratio-{11,1610}.png` · `work-flat-placeholder.png` · `work-flat-modal.png` · `work-flat-revert-device.png` · 스크립트 `wf-lib.mjs` · `wf-verify.mjs` · `wf-hover.mjs` · `wf-ratio.mjs` · `wf-cell.mjs` · `wf-modal.mjs` · `wf-deep.mjs` · `wf-set.mjs` · `wf-shot1.mjs` · 🚨 **헤드리스 캡처 함정 2가지** — ① 이 Edge(151)는 `Page.captureScreenshot` 에 **`clip` 을 주면 흰 판**을 돌려준다(뷰포트·문서 좌표 둘 다) → **뷰포트 전체를 찍고 sharp 로 잘라야** 함 ② **세션 첫 캡처도 흰 판**(컴포지터가 프레임을 새로 안 그림) → 채널 **stdev < 1.5 면 균일한 판으로 보고 최대 6회 재촬영** · ⓪-AK 🆕 **Q&A(combo) 티커 순서 뒤집기**(2026-08-25 · **확정**) — 팀 "셀프 체크 띠 부분이 생뚱맞다". 원인 둘: ① 티커가 **헤딩과 체크 사이를 가로막아** 헤딩이 뭘 소개하는지 알기 전에 12문장이 먼저 흐름 ② 🚨 `tickerPhrases` 12문장 중 **앞 4개가 `cards[].title` 과 글자 그대로 동일**(문자열 비교 4/12 완전 일치). ⚠️ 지시서의 "`description`(…네 가지)이 12문장과 어긋난다"는 **combo 에 해당 없음** — `ComboHeader` 는 `checkEyebrow`+`heading` 만 렌더하고 `description` 을 안 씀. **스위치 `TICKER_ORDER: "after-summary"(새 기본) | "before-summary" | "top"(이전 그대로, 복귀용)`** — 새 순서는 **헤딩 → 안내 → 체크 2×2 → 검정 요약 바 + CTA → 전폭 티커**. 🚨 **배치 후보 A vs B 캡처 비교 → A 채택**: ⓐ **요약 바가 체크 블록의 마무리**(`{n}가지가 해당되시네요` + CTA)라 그 사이에 12문장이 끼면 "체크 → 결과 → 행동" 사슬이 끊기고 CTA 가 섹션 맨 아래 외딴 블록이 됨 ⓑ B 는 티커가 `SelfCheck` 안 `Container` 로 들어가 **전폭이 깨짐**(1536→1200, x=168) — 확정값 `TICKER_WIDTH:"bleed"` 와 화면이 어긋남. 구현은 `SelfCheck` 에 **`beforeSummary?: ReactNode` 슬롯** 신설(안 넘기면 마크업 완전 동일 → `style:"check"` 영향 0). **라벨은 새 카피 0줄** — `site.ts` 기존 문장 재배치: 헤딩 아래 안내 = `checkHint`("해당되는 항목을 눌러 보세요.", 티커가 위에 있던 시절 세로 예산 때문에 뺐던 줄의 복원) · 티커 위 라벨 = **`tickerStatement`**(`"ticker"` 안이 티커 **아래**에 쓰던 문장, 🧪 **자리가 바뀌었으므로 초안 — 사용자 확정 대기**). `"top"` 에서는 둘 다 렌더 안 함(예전 화면 픽셀 그대로). 🧪 **중복 제거는 후보 — `TICKER_DEDUPE` 기본 `false`**: `true` 면 `cards[].title` 과 같은 4문장을 **렌더 시점에만** 걸러 8문장(🚨 `site.ts` 12줄 배열은 한 줄도 안 지움). 🚨 **파리티는 필터 뒤 재계산**해야 4+4(원본 인덱스로 가르면 6+2 로 기울어짐). ⚠️ **속도 — 줄마다 duration 이 다름**: 걸러지는 4문장이 두 줄에 고르게 안 갈려 트랙 축소율이 다름(윗줄 8,833→**5,610**(0.635) · 아랫줄 8,877→**5,185**(0.584)) → `TICKER_DURATION_DEDUPE = { top: "89s", bottom: "82s" }` 로 **px/s 를 원판과 동일**하게(31.5→31.5 · 31.7→31.6). 08-23 확정 속도를 **넘지 않음**. `globals.css` 미변경(인라인 `--ticker-duration`). **세로 +93px 이 전부**(되살린 안내 49 + 티커 라벨 44 — 티커 행·체크·요약 바·섹션 패딩 **한 값도 안 건드림**) · **다른 섹션 영향 0**(문서 증가분 = 섹션 증가분: **1440 643.13→736.17 / 9,362→9,455** · 1536 동일 / 9,438→9,531 · 768 777.69→869.28 / 9,893→9,984 · 390 816→906.23 / 11,126→11,216) · 🚨 **1440×900 한 화면 유지**(예산 820px 대비 **83.83px 여유**, 직전 176.87 에서 줄었지만 들어감) · dedupe 여도 섹션 높이 동일(티커 행 고정) · 🚨 직전 작업의 **`TICKER_WIDTH`/`TICKER_SIZE` 는 기본값 그대로**(`bleed`/`base`, 사용자 선택 대기) · 검증 `tsc`·`eslint` 통과 · 콘솔 에러 0 · 가로 스크롤 0(390·768·1440·1536) · 캡처 `qna-order-before.png`·**`qna-order-after.png`(채택)**·`qna-order-candidate-b.png`·`qna-order-after-dedupe.png`·`qna-order-390.png`(+`-768`·`-1440`) · 스크립트 scratchpad `qna-shot.mjs` · ⓪-AJ 🚨 **온설 모바일 캡처 재촬영**(2026-08-25) — 사용자 "온설 모바일 목업 두 군데 다 이상해". `onseol-v2-mobile.jpg` 는 **고정 헤더(`.os-header{position:fixed}`)가 CSS y≈456px 에 흰 배경으로 박제**돼 있었습니다(위 1/3 이 글자 없는 올리브 배경). 히어로 자체는 정상(높이 844 · 카피 727, 새 캡처와 동일)이고 **어긋난 것은 헤더 한 덩어리뿐**. 원인 = `captureBeyondViewport` 는 문서 좌표로 찍지만 `position:fixed` 요소는 **그 순간 스크롤 오프셋** 자리에 칠해지는데, **상단 복귀 900ms** 로는 스크롤이 0 이 되기 전에 셔터가 내려갔습니다(흰 배경인 것도 스크롤 상태에서만 붙는 `.os-scrolled` 증거). 고친 조건 = `scrollBehavior='auto'` 선박제 + `scrollTo(0)` **150ms×8회 + 1600ms** + **캡처 직전 `window.scrollY===0` 가드(아니면 throw)**. 나머지(390×844 · DSF2 · `mobile:true` · iPhone UA · 상단 2100 CSS px · JPEG 80)는 §5-B 그대로. 🚨 **좌표 함정** — clip `scale:2` × `deviceScaleFactor:2` = **4배**라 "상단 2100 CSS px" 이 **1560×8400** 파일로 나옵니다(**이미지 y÷4 = CSS px**. ÷2 로 재면 "히어로가 2배 늘어났다"는 없는 병을 진단합니다 — 실제로 한 번 그랬습니다). 새 파일 **`onseol-v3-mobile.jpg`**(1560×8400 · 726KB. `-v2` 는 캐시 무효화 전례대로 지우지 않고 남김) · **문결·지안은 전수 확인 후 정상 판정, 재캡처 안 함**(두 사이트는 헤더가 `fixed` 가 아니라 문서 흐름 안에 있어 같은 병이 안 남) · `projects.ts` 의 `mobileImage` **세 줄 전부 780×4200 → 1560×8400**(실제 파일 픽셀. 비율 동일 1:5.3846 이라 **레이아웃 불변**) · 사용처 3곳 전부 새 파일 확인(Work 4분할 폰 `w=128` · 브릿지 뒤 폰 `w=256`, `bridgePhoneScreens`=`null` 이라 프로젝트 `mobileImage` 사용 · 모달 "모바일" 탭 `w=640`) · **다른 섹션 영향 0**(1440 문서 **9,362px** 직전 기록값과 동일 · 가로 스크롤 0 · 콘솔 에러 0 · `npx tsc --noEmit`·`npx eslint src` 통과) · 캡처 `onseol-mobile-{before,after}.png`·`work-quad-onseol-cell.png`·`bridge-onseol-phone.png`·`modal-onseol-mobile.png` · 스크립트 scratchpad `cdp-shot.mjs`·`verify-shots.mjs` · ⓪-AI **Q&A(combo) 헤딩 통일 + 티커 후보 3안**(2026-08-25) — ① **확정**: `ComboHeader` 만 `text-h2 mt-4` 였던 것을 다른 섹션과 같은 **`text-h1 mt-6`** 으로 통일(헤딩 36px/lh45 → **48px/lh56.64** · mt 16 → 24px). 🚨 **다른 요소를 줄여 흡수할 필요가 없었다** — 섹션 **623.50 → 643.13px(+19.63)** 인데 **1440×900 예산 820px**(900 − 고정 네비 80) 대비 **176.87px 여유**라 섹션 패딩(56/40)·티커 `my-7`·체크 그리드 간격을 **한 값도 안 건드렸다**(768 761.92→777.69 · 390 804→816). **다른 섹션 영향 0**(문서 증가분 = 섹션 증가분: 1440 9,342→9,362 · 1536 9,204→9,223 · 768 9,877→9,893 · 390 11,114→11,126) ② 🧪 **확정 아님 — 사용자 선택 대기**: 팀 "티커 띠가 너무 혼자 넓은 느낌"이 가로/세로 두 갈래로 읽혀 **실제 스위치 2개**(`client-problems.tsx` 상단 **`TICKER_WIDTH: "bleed"(현재) | "contained"`** · **`TICKER_SIZE: "base"(현재) | "small"`**)로 후보를 만들고 **기본값은 현재 화면 그대로** 뒀다. **A(가로 · contained)** 행 폭 1536→**1200(x=168)**, 세로는 **한 픽셀도 안 변함**(643.13) / **B(세로 · small)** 행 **60→44px** · 글자 **44→34px** · 여백 `my-7→my-5`, 섹션 **643.13→595.13(−48)** / **C = A+B**(595.13). `contained` 여도 마키는 그대로 흐른다(`.ticker-row { overflow:hidden }` 이 이미 있고 `--ticker-duration` 미변경이라 초당 픽셀도 동일) · ⚠️ `"ticker"` 안은 이 스위치를 타지 않음(combo 전용) · 캡처 `qna-ticker-{base,a,b,c}.png` · 변경 전 `qna-heading-h2-before.png` · 검증 `tsc`·`eslint` 통과 · 콘솔 에러 0 · 가로 스크롤 0(390·768·1440·1536, 네 후보 전부) · ⓪-AH 🚨 **Work 4분할 박스**(2026-08-25) — 사용자 "한 줄에 4칸으로 가로 확장 · 화면 전폭 · 칸 사이 여백 최소 + 연한 회색 경계선 · 박스 안에 모니터와 작은 모바일이 둘 다 네모 박스로 · 박스 밖 돌출 없음 · 3D 기울기 제거 · 호버는 유지 · 설명글은 박스 안쪽 아래에 항상 · 4번째는 준비 중". **스위치 `WORK_LAYOUT: "quad"(기본) | "cards3"`**(`selected-work.tsx` 상단) — `cards3` 면 3열 기울기 목업 카드가 그대로 복귀하고 **기존 부품(`project-card.tsx`·`device-mockup.tsx`·`TILTS`·`compact`)은 한 줄도 안 지웠다**(복귀 실측 1536 문서 **9,320px** · 섹션 **1,055.08px** = 작업 전 baseline 과 동일) · 신설 `components/ui/project-quad-card.tsx`(정면 배치 전용 칸 + 준비 중 칸) — 🚨 `DeviceMockup` 재사용 안 함(그 컴포넌트는 **3D 기울기 + 폰 돌출이 존재 이유**라 이번 지시와 정반대. 호버 규칙 50% 오버레이·로고·**모니터 9s / 폰 11s**·해제 **1.2s** 는 같은 값 그대로 이식) · 🚨 **경계선은 `gap-px` + 그리드 배경 `bg-line`**(칸에 보더를 주면 인접 변이 겹쳐 2px 이 된다 — 이 방식은 **틈 자체가 정확히 1px** 이라 구조적으로 겹칠 수 없음. 실측 틈 **1/1/1px**, 색 `--color-line` = paper 밴드 `rgb(229,229,229)`, 하드코딩 0) · 격자는 `Container` **밖**(섹션에 좌우 패딩이 없어 `w-full` = 전폭), **섹션 헤더는 Container 안 중앙 유지** · **칸 폭 편차 0.000px**(1536 **383.25×4** · 1920 **479.25×4** · 768·390 은 2×2 로 **383.5×4 / 194.5×4**) · 칸 높이 **403.13 / 469.86 / 397.88 / 280.55px**(고정하지 않고 내용이 정함 — 그리드 행이 4칸을 같은 높이로 맞춤) · 🔀 **폰 위치 후보 2안 캡처 비교 → 우측 하단 채택**(좌측 안은 세 사이트의 **좌측 정렬 헤드라인을 폰이 그대로 가림**) · 폰 크기 **미디어 폭의 21%**(3열 카드가 쓰던 비율 그대로 = 확정된 모니터↔폰 관계 불변), 아래로 미디어 폭의 **7%** 만 내려와 **칸 밖 돌출 0** · 설명글 = **관점 한 줄(액센트) + 프로젝트명 + 업종**, `summary` 제외(`QUAD_SHOW_SUMMARY: false` — 넣으면 칸이 **405.13 → 454.72px(+49.6)** 로 길어지고 4번째 칸 아래가 그만큼 비어 격자가 "글 상자"로 읽힘. 요약 전문은 모달에 그대로) · 🚨 **호버 오버레이는 화면 영역만** 덮는다(실측 오버레이 아래변 **306px** vs 프로젝트명 위변 **384px** — 설명글은 항상 보임) · 쉬는 모습은 **`coverImage` 그대로**(아래 `fullImage` 를 깔고 커버만 페이드아웃 — `device-mockup.tsx` 의 "커버는 사용자가 확정한 정지 화면" 규칙 유지) · **4번째 칸 = 준비 중**(`projects.ts` 에 4번째 항목이 아예 없어 `selectedWork.placeholderCell` 신설 "다음 프로젝트 / 준비 중입니다." — **화면 이미지·호버·모달·링크 전부 없음**, 실측 클릭 가능한 칸 3개. 🚨 점선 상자는 **프로젝트 칸의 상자 골격 그대로**(테두리 + p-2 + 크롬 줄 14px + 16:10) — 화면만 16:10 으로 두면 크롬 줄만큼 짧아져 네 칸 밑변이 어긋난다) · **호버 3초 실측** 모니터 `object-position` **50% 0% → 50% 34.9987%**(9s linear) · 폰 **→ 50% 28.6353%**(11s linear) · 오버레이 `rgba(0,0,0,.5)` opacity 1 · 로고 opacity 1 · **모달** 클릭 → "문결 필라테스 프로젝트" + 해시 `#work-moongyul-pilates` + body 잠금 / ESC → 닫힘·해시 제거·스크롤 복구 / 딥링크 `/#work-onseol` 새로고침 → 그 모달로 시작 · **검증** `tsc`·`eslint` 통과 · **가로 스크롤 0**(390/768/1536/1920) · 콘솔 **에러 0**(dev `next/image` quality 경고 9건은 baseline 과 같은 건수) · 🚨 **다른 섹션 영향 0** — 네 뷰포트 전부 **문서 높이 변화량 = Work 섹션 높이 변화량**(1536 −116 / −116.20 · 1920 −50 / −49.47 · 768 −257 / −256.44 · 390 −1,058 / −1,057.55), 히어로 미변경 · 캡처 `work-quad-{1536,1920,768,390}.png` · `work-quad-border-zoom.png`(6배) · `work-quad-hover.png` · `work-quad-modal.png` · 후보 `work-quad-phone-{right,left}.png` · `work-quad-summary-on.png` · 복귀 `work-quad-revert-cards3.png` · 작업 전 `work-before-*.png` · ⚠️ **`/work` 서브 페이지는 예전 3열 카드 그대로**(네비에서 빠진 라우트라 범위 밖) · ⓪-AG 🚨 **Phase C 마감 3건**(2026-08-25) — ① **"보라 차고 잠깐 멈추고 흰색 차는 게 어색하다"** → 두 애니메이션(물 상승 / 흰색 상승)을 딜레이로 이어 붙이던 구조를 버리고 **전선 변수 하나(`--hero-front`)가 `linear` 로 잉크 바닥→꼭대기를 한 번에** 올라가게 했다. 드러나는 것이 **진짜 워드마크**라 전선이 55% 수위를 지나는 순간부터 그 위가 자동으로 흰색이 된다 = **2막이 아니라 하나의 상승** · 실측 **Δy 37/37/38/38/37/37/37px per 0.1s · 평균 37.4 · 편차 2.7%**(기준 ±10%), 55% 통과 시각 t≈2.81s 전후도 38/37 로 **정지 프레임 0** ② **"그라데이션은 아예 지우자"** → 인트로 활성 경로에 **그라데이션 0개**(워드마크 덮개는 `mask-composite`·세로 `linear-gradient` 앞단까지 전부 제거하고 **마스크 한 겹·알파 1**, 문장 타일은 단색 `fill-opacity 1/0.5`, blur 는 두 곳 다 `stdDeviation="0.5"` = **0.01em**. 남은 `linear-gradient` 는 미사용 보존 모드 `.hero-intro-cover--flow` 한 곳뿐) ③ **"파도도 스밈에서 무한히 지속되는 그 파도 크기로"** → 인트로 전용 파형을 **아예 없앴다**(워드마크 물 복제본 `.hero-intro-water--wm` 렌더 삭제, `water` 모드에만 보존). 덮개 아래 변만 `.wordmark-wavefill` **앞 겹과 같은 파장 1.4em · 진폭 0.112em · 같은 위상**(`--wm-wv1x`/`--wm-wv1y` 공용)이라 **교대에 파형이 튈 여지가 구조적으로 없다** — 검증 **같은 시각(3.45s) 덮개 있는 프레임 vs 덮개 지운 최종 상태 diff 0** · 앞서 보고한 "SUMIM 물이 문장보다 옅어 보임"도 **해소**(이제 진짜 상시 파도를 그대로 보여줌) · 부수 효과로 평면 덮개(`.hero-intro-cover`)도 `wave` 모드에서 불필요해져 **덮개 1장 · 변수 1개 · 애니메이션 1개**로 단순해졌다 · 검증 `tsc`·`eslint` 경고 0 · 콘솔 **0** · 가로 스크롤 **0** · 문서 높이 **9,320/9,719/12,172px 불변** · 최종 픽셀 diff **1,644/1,144,320 = 0.14%**(내역·원인 앞 절과 동일) · reduced-motion 이면 덮개 `display:none`·`transform:none`·네비 표시 · ⓪-AF 🚨 **히어로 v9 + 인트로 완성** — 사용자 지시 9건을 한 구성으로 수렴시켰습니다. **① v9 레이아웃**: 메타 줄 → **영문 카피라이트**(`hero.wordmark.meta` "© 2026 SUMIM Studio · Busan", 2026-08-21 확정값 재사용 · `factsLine` 보존) · 선언문을 **작게(`--wm-size × 0.075` = 1536 에서 35.5px) · 워드마크 아래 오른끝 정렬**(후보 0.055/0.075/0.095 캡처 비교 — 0.055 는 캡션처럼 읽히고 12px 카피라이트와 2배 차이뿐, 0.095 는 워드마크와 경쟁) · **CTA 2개 삭제**(`HERO_CTA_ON: false`, 필드·컴포넌트 전부 보존) · 🚨 **오른끝은 구조로 맞는다**(래퍼 폭 = `WM_TOTAL_INK − WM_OPTICAL_EM` 이라 **래퍼 오른쪽 변 = 마지막 M 잉크 오른끝**, `w-0 min-w-full` + `text-right` 만으로 정렬되고 마침표 우측 사이드베어링 **0.057207em** 만 밈) · 🚨 미는 건 **`position:relative; right`** — `margin-right` 음수는 `width:0+min-width:100%` 에서 **과잉 제약으로 무시**됨 · 🚨 **모바일 배율 별도**(390 은 F=120px 뿐이라 0.075 면 문장이 **9px** → <1024 에서 `--hero-headline-ratio` **0.115**, 중앙 배율도 같은 비율로 올려 확대 k≈2.13 을 두 구간에서 동일하게) · `--hero-reserve` 230/300 → **200/190px** · 실측 **오른끝 Δ 문장 +1/+1/+1.0px · 카피라이트 0/0/−0.5px**(1536/1920/390), 워드마크 F **473.088/591.36/120.12 불변** **② 인트로**(총 **3.85s**): A 문장이 **화면 정중앙에서 2.13배로 크게** 물결로 차오름(0.15→1.65) → A′ 보라→흰색 교대(1.55→1.95) → B **이동+축소**로 제자리(1.80→2.70, `transform-origin: right center`) → **C-1 워드마크에 문장과 똑같은 물이 바닥→55% 수위로 상승**(2.15→2.75) → **C-2 흰색이 그 위로 상승**(2.75→3.45) → 카피라이트(3.25→3.85) · 🚨 **Phase A/C 가 같은 물을 쓴다**(사용자 "첫 인트로 등장 부분처럼 SUMIM 도 똑같이") — 타일·겹별 알파·색·드리프트·이징은 `.hero-intro-water` **한 곳**에만 있고 `--text`/`--wm` 규칙은 **시간·도달 수위 변수 네 개**만 다름 **③ 윤곽(고스트) 원인 규명** — t=0.8s 를 행 단위 스캔하니 물 위에 **정확히 3줄**만 밝고 그 y 가 세 겹의 `mask-position-y`(= **마스크 타일 위 변**)와 **1px 오차 없이 일치**(최대 밝기 3/4/5 of 255), `.hero-intro-water` 만 끄면 0 → **타일 위 변의 리샘플링 잔여 알파**였다. **해결 = 타일 세로 1.4→4.8em · 수면을 타일 위에서 2.4em** 에 두어 위·아래 변이 **어떤 순간에도 잉크와 안 만나게** → 검증 **물 밖 구간 밝기>2 픽셀 0 · 최대 0**(문장 4시점 · 워드마크 4시점) **④ 그라데이션 전면 제거**(사용자 2회 지적) — 물은 **균일한 불투명 색**, 층 구분은 **겹별 알파(앞 1.0 / 뒤 0.5)** 만, 계단 방지 **blur 0.01em** 만 남김 **⑤ 색 밝게** — `color-mix(in srgb, var(--color-cta) 65%, var(--color-ink))` = **#9a7ef4 · 검정 대비 6.64:1**(원색 3.33 · 80% 4.89 · 50% 8.93 은 라벤더로 옅어짐), 하드코딩 없이 팔레트 추종 **⑥ 상시 파도 수위 상향** 25/40/60 → **55/70/88%**(후보 4장 비교, 65/80/95 는 "그냥 보라 글자"로 읽힘) **⑦ 네비 숨김**(사용자 "첫 문장 등장 화면에서는 헤더도") — `body:has([data-hero-intro="on"]) .site-nav` 한 규칙, 🚨 **`:has()` 로 페이지 범위를 좁혀** 다른 `enter` 모드·다른 페이지에는 안 걸림 · 덮개(ⓑ)가 아니라 **투명도**(네비가 `fixed z-50` 이라 덮으면 클릭·스크롤까지 막힘) + 투명한 동안 `pointer-events:none` · **1.85s 부터 0.5s 페이드 인** · reduced-motion 이면 숨기지 않음 **검증** `tsc`·`eslint` **경고 0** · 콘솔 **0** · 가로 스크롤 **0** · 문서 높이 **9,320/9,719/12,172px 불변** · 히어로 섹션 높이 불변 · **최종 픽셀 diff 1,644/1,144,320 = 0.14%** — 내역 **워드마크 104px**(액센트 `I` 가장자리) + **문장 1,540px**, 🚨 문장 쪽은 **기하가 아니라 안티에일리어싱**(통제 실험: 인트로 h1 애니를 꺼도 `split` 빌드와 3,544px 차이 → 원인은 `split` 의 `.reveal-load` 가 h1 을 따로 합성하는 것. 같은 빌드 두 번 캡처는 **diff 0** 으로 측정은 결정적) · reduced-motion 이면 복제본·덮개 2종 `display:none` + `transform:none` + 네비 표시 · ⓪-AC 🚨 **히어로 인트로 보강 4건**(사용자 피드백) — ① **차오르는 중 글자 윤곽이 살짝 보임** → 🚨 **픽셀로 원인 규명**: t=0.8s 를 행 단위 스캔하니 물 위에 **정확히 3줄**만 밝은 행이 있었고 y 가 세 겹의 `mask-position-y`(= **마스크 타일 위 변**)와 **1px 오차 없이 일치**(y 337/356/364 · 최대 밝기 **3/4/5** of 255), `.hero-intro-water` 만 끄면 0 → 진짜 `h1`·덮개·AA 가 아니라 **타일 위 변의 리샘플링 잔여 알파**. **해결 = 타일 세로 1.4em → 4.8em · 수면을 타일 위에서 2.4em 에** 두어 타일 위·아래 변이 **어떤 순간에도 잉크와 안 만나게** 함 → 검증 **램프 13px 를 뺀 구간에서 밝기>2 픽셀 0 · 최대 밝기 0**(문장 t=0.3/0.5/0.8/1.1 · 워드마크 t=2.2/2.5/2.62/2.9) ② **"선들이 있어서 눈에 안 들어온다"** → 겹 **3→2** + 각 겹을 **세로 그라데이션**(수면 알파 0 → 0.8em 아래 알파 1) + 🚨 **`feGaussianBlur stdDeviation=4`**(세로 0.08em) — 그라데이션만으로는 **물결 마루에서 알파가 툭 올라가** 선이 남는다(그라데이션은 수평·경계는 물결), blur 는 경계를 따라가므로 해결. 진폭이 blur 의 1.6~3.3배라 **물결 형태는 유지** · 파장은 1.4/2.8em 그대로(드리프트 키프레임이 `.wordmark-wavefill` 과 공용이라 **정확히 한 파장** 이동해야 이음매가 없음) ③ **색 더 밝게** → 하드코딩 없이 `color-mix(in srgb, var(--color-cta) 65%, var(--color-ink))` = **#9a7ef4 · 검정 대비 6.64:1**(원색 100% 3.33 · 80% 4.89 · 50% 8.93 은 라벤더로 옅어져 CTA 정체성 약화) — 팔레트 추종 유지, **최종 상태와는 무관**(인트로 복제본 전용) ④ **상시 파도 수위 상향**("흰 부분이 적게") → 후보 4장 비교 후 **앞 55 / 중 70 / 뒤 88%** 채택(25/40/60 → 흰 면 위 20~25%만 남음. 65/80/95 는 `I`·`M` 이 거의 전부 보라가 되어 **그냥 보라 글자**로 읽힘) · 겹 간격 15/18%p 유지로 원근 보존 · ⓪-AD 🚨 **Phase C 재설계 — 파도가 먼저, 흰색이 나중에 차오름**(사용자 "스밈 처음 생길 때도 색이 차오르지 말고, 글자 내에서 파도 모양이 먼저 보이고 비어 있는 부분이 흰 색상으로 차오르게") — **진짜 워드마크엔 아무것도 안 걸고** 밴드색 덮개 **두 장**의 걷히는 순서로만 만듦: 평면 `.hero-intro-cover` 가 **2.30→2.65s 페이드 아웃**해 **물 부분만** 드러나고, 수면 모양 `.hero-intro-crest` 가 **2.60→3.35s 아래→위로 물러나며** 빈 자리를 **흰색으로** 채움 → 1차의 보라 복제본이 필요 없어져 **페인트 비용까지 감소**하고 **최종 픽셀 동일이 구조적으로 보장** · `.hero-intro-crest` 마스크는 `mask-composite: intersect` 2겹 — ⓐ 물결 타일의 **상하 반전판**(`.wordmark-wavefill` **앞 겹과 같은 파형·같은 위상**, `--wm-wv1x/y` 공용 + blur 0.03em)로 실제 수면과 겹쳐 이음매 제거, 위치 상수 세로 `var(--wm-wv1y) − 1.94473em`·가로 `var(--wm-wv1x) + 0.5869em`(⚠️ `--wm-wave-y1` 을 바꾸면 같이 고칠 것) ⓑ 세로 `linear-gradient` 앞단 `--hero-white` **0.78em → −0.02em** + **0.10em 램프**(딱딱한 직선 금지) · 스위치 **`INTRO_WM_MODE: "wave"(기본) | "water"(1차) | "split"`** · 타임라인 **총 3.80s**(A 0.15→1.65 · A′ 1.55→1.95 · B 1.80→2.70 · **C-1 2.30→2.65** · **C-2 2.60→3.35** · 메타 3.20→3.80) · 검증 `tsc`·`eslint` 통과 · 콘솔 **0** · 가로 스크롤 **0** · 문서 높이 **9,320 / 9,719 / 12,172px 불변** · 중앙 정렬 Δ **−0.01 / 0.00 / −0.01px** · **최종 픽셀 diff 104 / 1,144,320 = 0.0091%**(최대 33/765, 전부 액센트 `I` 가장자리 — 1차 157px 보다 감소) · reduced-motion 이면 덮개 2종·복제본 `display:none` + `transform:none` · ⓪-AB 🚨 **히어로 인트로 시퀀스** — 사용자 "홈페이지 입장할 때 검은 화면에서 '매출이 나는 홈페이지를 만듭니다.' 이 멘트가 저 파도 치는 물결이 점점 상승하는 느낌으로 화면 정중앙에 글씨가 나타났다가 지금 히어로 섹션 위치로 내려가고, 스밈의 I 도 다른 알파벳들이랑 같이 나타났으면 좋겠어." → **`enter: "intro"` 가 새 기본값**(모드 5종 `intro`/`split`/`wipe`/`spread`/`wall` — 기존 코드 한 줄도 안 지움). **A 차오름 0.15→1.65s**(문장이 **화면 정중앙**에서 보라 물결로 아래→위) · **A′ 색 전환 1.55→1.95s**(보라 복제본 페이드아웃 + 진짜 흰 문장 페이드인 = 교차 페이드 채택 — 최종에 진짜 `h1` 하나만 남아 **픽셀 동일이 구조적으로 보장**) · **B 이동 1.80→2.70s**(`translateY` 하나, 최종 `none`) · **C 워드마크 2.30→3.10s**(다섯 글자 **동시** 차오름) · **C′ 교대 3.00→3.40s** · 메타·CTA 3.25→3.85s = **총 3.85s**(지시 4s 이내), A↔B·B↔C 가 겹쳐 "도착과 맞물림"을 만듦 · 🚨 **물을 칠이 아니라 마스크로**(`.wordmark-wavefill` 의 `background-clip:text`+`blend:color` 방식은 물 없는 곳이 **순백**이라 차오르기 전에도 글자가 다 보임 → 복제 글자를 `color: var(--color-cta)` 로 두고 **같은 파형 타일 3겹을 `mask-image`** 로 씌움, `mask-composite` 기본 `add` 로 합성 알파 최대 **0.886**, 알파 .62/.50/.40 · 수위 차 0/−0.11/−0.25em, SVG 안엔 흰 알파뿐이라 **팔레트 추종**) · 🚨 **진짜 워드마크엔 손 안 댐**(칸마다 `background-clip:text` 라 opacity 애니를 얹으면 크롬이 층을 따로 합성해 글자를 못 잡는 "SUM M" 함정 재발 — 은폐는 위에 얹는 **밴드색 판** `.hero-intro-cover` 가 맡고 층 순서 [진짜]<[덮개]<[물 복제본]) · 🚨 **Phase B 이동량은 닫힌 식**(`lift = −P_t/2 + 22px + S/2 − 0.41·F`, `F` 항이 정확히 −L/2 로 떨어짐 — JS 측정 0) → **식 vs 실측 Δ −0.01 / 0.00 / −0.01px** @1536·1920·390(390 은 이동량이 **1px** — 모바일은 문장이 **이미 화면 중앙**, 기하 그대로) · `--hero-intro-lift` 는 **`h1` 에서 선언**해야 함(섹션에 두면 `--wm-size` 가 없어 폴백) · `--hero-pad-top` 68/**80** · `--hero-intro-block`(=S) **120.5/52px** 실측 · 🚨 **수위 범위는 기준선이 아니라 마루/골로**(첫 시도에서 t=0 에 워드마크 아래가 비침 — 골 = 기준선 −0.492em · 마루 = +0.142em, 잉크 세로 문장 0.047~0.950em · 워드마크 0.047~0.775em → rise text **1.5→−0.12em** / wm **1.32→−0.12em**) · 🚨 **`@property` `initial-value` 는 계산 독립 단위**여야 함(`2em` 으로 뒀더니 규칙이 통째로 무효 → 보간 대신 **50% 지점에서 툭 튀는 계단**, `0px` 로 수정) · 이징은 **`cubic-bezier(.4,0,.6,1)`**(앞이 빠른 `(.33,0,.25,1)` 은 43% 지점에 이미 진행률 0.72 라 캡처가 만수위) · 드리프트·bob 6종은 `.wordmark-wavefill` 키프레임 **그대로 재사용**(50ms 스텝 · iteration 1) · 🔀 **등장 방식 후보 비교 → `INTRO_WM_MODE: "water"` 채택**(`"split"` 은 같은 프레임에서 **기둥 `I`·이웃 `M` 이 먼저 드러나고 `S`·`U` 가 마지막**이라 "다섯 글자 동시"가 아니고 가로 방향이라 문장의 세로 차오름과 리듬이 끊김) · **로드 시 1회**(CSS `fill: both` 뿐, IO 0) · **스크롤 안 잠금**(t=1.2s 에 420px 스크롤해도 정상) · **JS 0 · 클라이언트 컴포넌트 0** · 검증 — `tsc`·`eslint` 통과 · **콘솔 0**(3해상도) · 가로 스크롤 **0** · 문서 높이 **9,320px @1536 불변**(1920 9,719 · 390 12,172 도 불변. 9,083 이 아닌 것은 같은 날 `contactForm.mode`→`"full"` 로 바꾼 **다른 작업** 탓이고 baseline 도 9,320) · 3칸 골격·F·문장 크기 전부 불변 · **최종 픽셀 diff `enter:"split"` 빌드 대비 157 / 1,144,320 = 0.0137%, 최대 차 30/765(≈rgb 10,10,10), 위치는 액센트 `I` 글리프 가장자리뿐**(split 은 `I` 칸에 `translateX` 층이 있어 따로 합성돼 AA 가 다름 — 실질 픽셀 동일) · `prefers-reduced-motion` 이면 `h1` `animation:none`·`transform:none` + 복제본·덮개 `display:none` → **최종 상태 즉시** · ⓪-AA 🚨 **히어로 최종 색·선언문** — 사용자 "히어로 I 흰색으로 바꾸고 파도를 기존 I 색상으로 변경해보자. 밑에 선언문은 '매출이 나는 홈페이지를 만듭니다'를 제외하고 다 지워주고, 매출이 나는 홈페이지를 만듭니다 자체를 SUMIM 밑에 크게 놔둬줘. SUMIM과 가로 넓이 같게끔". ① 액센트 `I` **흰색**(`ACCENT_TINT: false` 한 줄 — `true` 로 올리면 보라 복귀, `accentIndex`·`ACCENT_METRICS`·기둥 계산은 **하나도 안 지움**) ② 🚨 **글자 안 파도를 CTA 보라로 — `background-blend-mode: color`**(data-URI SVG 안에는 CSS 변수를 못 쓰므로 파도 SVG 는 **회색조 그대로** 두고 맨 위에 `linear-gradient(var(--color-cta),...)` 한 겹을 `color` 블렌드 = `SetLum(소스, Lum(백드롭))` → 물 없는 곳은 백드롭이 순백이라 **정확히 흰색**, 깊을수록 진한 보라) — **팔레트 추종 실측 확인**(`data-palette="navy"` → cta `#ea580c`, 파도가 주황으로 함께 바뀜) · 알파 후보 **a35(.35/.29/.23) 채택**(가장 깊은 곳 백드롭 명도 **0.355** ≈ `#6338EE` 자신의 0.349 라 **바닥이 CTA 원색**, a55 는 0.158·a75 는 0.048 로 검게 죽음) · 겹별 비율(1:0.825:0.65)과 수위 차(25/40/60%)는 유지 ③ **`I` 에도 파도 넣음**(`WAVE_ON_ACCENT: true` — 빼면 기둥만 순백으로 남아 물결이 **뚝 끊기고 워드마크가 세 조각**으로 읽힘, 비교 캡처) · 🚨 **`slide` 칸은 파도를 `.wordmark-shift` 에 건다**(진입 `translateX` 가 걸린 층이 따로 합성돼 **조상 칸의 `background-clip:text` 가 글자를 못 잡아 `I` 가 통째로 사라짐** — 첫 캡처가 "SUM M", clip-path·z-index·blend 를 하나씩 꺼 보고 원인 확정) ④ **선언문 = 마지막 한 줄만 크게 = 화면에 보이는 `h1`**(`id="hero-heading"`, 예전 sr-only h1 대체 · `site.ts` 3줄 문자열은 **한 글자도 안 고치고** 렌더만 `STATEMENT_LINE_INDEX 2`) — 잉크 폭 일치는 픽셀 스캔 실측 **문장 잉크 12.9675em** → 배율 `WM_TOTAL_INK / 12.9675` = **0.240017**(`calc(var(--wm-size) * 0.240017)`), 왼끝 밀기 **−0.025101 × --wm-size**(광학 0.0131 + 사이드베어링 0.05×배율), **자간 0**(불필요) · **마침표 포함 채택**(뺀 안은 0.23em 짧아진 만큼 글자가 커져 SUMIM 보다 굵어 보임) · 🚨 `h1` 은 워드마크 래퍼 안 **`w-0 min-w-full`**(폭 0 이라 래퍼 shrink-to-fit 에 안 끼어듦) · 🚨 **덮개 전용 안쪽 층 추가**(`.wordmark-split` 이 `height: calc(100% + 1.5em)` 이라 문장까지 덮는 것 방지, 선언문 없으면 동작 동일) ⑤ 메타·CTA 2개는 **문구·링크 그대로** 큰 문장 아래 한 행으로 재배치, 리빌은 기존 `.reveal-load`(문장 2.30s · 메타/CTA 2.42s) · 🚨 **세로 예산은 계수에**(문장이 워드마크에 비례 → `WORDMARK_BOX_COEF 0.82 → 1.060017`, `--hero-reserve` 280/340 → **230/300px**) — **세 뷰포트 전부 여전히 폭이 크기를 잡아 워드마크는 한 픽셀도 안 작아짐**(F 473.09/591.36/120.12 = 정확히 30.8vw) · 실측 **Δ 왼끝/오른끝 −1.0/+1.0px @1536·1920, 0/+0.5px @390** · 잉크폭 **95.8%** 유지 · **1536×745 첫 화면에 워드마크+문장+메타+CTA 전부**(CTA 하단 **703.2 ≤ 745**, 여유 41.8px) · 문서 높이 **9,083px 불변** · 가로 스크롤 0 · reduced-motion 파도 정지(물은 유지)·덮개·슬라이드·리빌 전부 없음 · 콘솔 0 · ⓪-Z 🚨 **히어로 한 줄 `SUMIM` + 글자 파도** — 사용자 ① "스튜디오는 없애고 SUMIM 만" ② "소문자 i 말고 원래 글자 I 그대로, 색만" ③ "원래 형태를 유지한 채로 연한 파도가 일렁거리게". **`layout: "one-line"` 이 새 기본값**(3칸 `[SUM][I][M]`), 두 줄 코드·상수는 **한 줄도 안 지우고** `WM_CELLS_TWO_LINE` 에 보존 — 값만 바꾸면 복귀 · 액센트는 `oneLine.accentGlyph: null` 로 **치환 없이 대문자 I**(기둥 폭은 `ACCENT_METRICS` 가 글리프별 실측값에서 자동 선택: `!` .3071 / `i` .2568 / **`I` .2661**) · `SUMIM` 전부 대문자 재실측 — 전체 잉크 **3.1124em**, 좌·우 칸과 두 간격은 **가운데 글자와 무관하게 불변**(GAP 은 `M` 의 사이드베어링 + 자간만으로 정해짐) · 크기 **30.8vw**(잉크 96%) · 세로 분모 1.6287 → **0.82**, **세 뷰포트 전부 폭이 잡음**(1536 95.8% · 1920 95.8% · 390 96.0%) · 진입 `split` 그대로(Phase 1 `I` 벽 슬라이드 0.7s → Phase 2 좌우 걷힘 1.6s), **완전 은폐 재검증 기둥 밖 밝은 픽셀 0 · 최대 밝기 0.00** · 🌊 **글자 파도**(`letterWave: true`) — 액센트 뺀 칸에만 `feTurbulence → feOffset → feColorMatrix(R 고정) → feDisplacementMap`, **세로 변위만**이라 가로 획이 출렁임 · 🚨 움직임은 `baseFrequency` 진동이 아니라 **`feOffset` 세로 이동**(주파수 진동은 0.5s 픽셀 변화 **0.06% = 사실상 정지**, feOffset 은 **0.62%**) · fy 가 인지를 지배(0.003→0.18% · 0.006→0.05% · 0.010→0.51% · **fx .005/fy .008 → 0.9%**) · 진폭 **7px = 글자의 1.49%**(5px 은 안 보이고 9px 은 M 꼭짓점이 녹음) · `numOctaves` **1**(2 대비 비용 −30% + 결이 더 부드러움) · scale 이 px 이라 **1024px 기준 두 벌**(lg 7 / sm 2 = 1.66%) · 성능 실측 1536 **fps 60 유지**, 메인스레드 Task idle 5.2%→**10.8%**(+5.6%p) · 스크롤 11.1%→**12.7%**(+1.7%p) · `prefers-reduced-motion` 이면 파도·덮개·슬라이드 전부 없음 · 문서 높이는 이 변경과 **무관**(one-line/two-line 둘 다 히어로 745 · 문서 9,083px) · 콘솔 0 · ⓪-Y 🚨 **split 보강 2건** — ③ **Phase 1 을 "뿅" → 다시 "스윽"으로**(사용자 "그라데이션 적용 전에 했던 스윽 나오는 느낌으로 돌리고"): `!`·`i` 가 각자 **자기 칸 오른쪽 벽 뒤**(`translateX(120%)`)에서 밀려 나옴 — 2026-08-21 `@keyframes wordmark-wall` 재사용(0.7s), Phase 2 는 `animation-delay: .7s`(SMIL `begin=.7s`) · 🚨 벽 클립 오른쪽 인셋은 `-2%` 가 아니라 **`+1%`**(원래 값이면 벽 뒤 글자가 2.2px 틈으로 비침 — t=0.05s 실측 25px) · 🚨 reduced-motion 은 슬라이드 규칙이 (0,4,0)이라 기존 `.wordmark-glyph > span`(0,2,1)으로 안 꺼져 **같은 특이도 규칙을 따로** 넣음 · `wordmark-pop` 은 keyframes·규칙·점 원점 실측값까지 **미사용 보존** · **a 프레임(0.05s)이 완전 검정**(기둥 안팎 최대 밝기 0.00) ④ **STUD 간격 균일화**(사용자 "U가 T랑 D 사이 여백이 달라서 맞춰줘"): `D` 만 `margin-left −0.022em` 커닝 → 최단 잉크 간격 **5/5/14 → 5/5/5(편차 0)** · 자연 폭 2.4475→**2.425em** 으로 줄어 **scaleX 0.834942 → 0.842679**(압축 완화) · weight 860 유지, U 세로획 71×0.8427 = **59.83px** vs 기준 **59.0px**(Δ +0.83px, 1.4%) · 커닝이 붙은 칸은 글자를 span 으로 쪼개지만 OpenType 커닝 손실은 **1px 미만** · 골격 불변(좌 박스 Δ 1.0 · 우 박스 Δ −1.0 · !/i 중심 Δ **0.00** · 양 끝 Δ 0.0) · 문서 높이 **11,009px 불변** · 콘솔 0 · ⓪-X 🚨 **split 보강 2건** — ① **완전 은폐**(사용자 "처음부터 글자가 좀 보이네"): 덮개 지그재그 기준선을 기둥 **안쪽으로 1.04em**(= 2.4×진폭 + 3.2×blur반경) 밀어 넣음 → t=0.05s 프레임 픽셀 스캔에서 **기둥 바깥 밝기>8/255 픽셀 0개 · 최대 밝기 0.00** ② **2단계 진입**(사용자 "!·i 가 점을 기준으로 뿅 나타난 뒤 글자가 걷혔으면"): **Phase 1 팝인 0~0.42s** — 두 글자가 **자기 점 자리를 원점**으로 scale(0)→1, 오버슈트 이징 `cubic-bezier(.34,1.56,.64,1)`, 원점은 실제 렌더 픽셀 스캔값(`!` 84.29% · `i` 11.02%) / **Phase 2 걷힘 0.42~2.02s** — 덮개에 `animation-delay: .42s`(SMIL 꿈틀도 `begin=.42s`)라 팝인 동안 정지 · 🚨 CSS 는 `.wordmark-line--box .wordmark-fit.wordmark-pop`(0,3,0)로 잡아야 3칸 규칙의 `transform-origin: left center`(0,2,0)를 이김 · reduced-motion 이면 덮개·팝인 전부 없음 · 골격 수치 불변(양 끝 Δ 0.0 · !/i 중심 Δ −1.0) · 문서 높이 **11,009px 불변** · 콘솔 0 · ⓪-W 🚨 **히어로 진입 `split` 추가·기본값 승격** — 사용자 "그라데이션을 3분할 해서 i랑 ! 에서 양쪽으로 퍼져나가게 해줘. 그라데이션은 지그재그 모양으로 일렁거렸으면 좋겠어." → `wipe` 의 **부드러운 페더** + `spread` 의 **출발점·지그재그** 결합. 덮개 **좌·우 두 장**만 두고 **기둥은 안 덮어** "!"·"i" 가 t=0 부터 보이고 거기서 **양쪽으로** 갈라짐(폭이 2.7배 차이나지만 **같은 1.6s `ease-in-out`**) · 🚨 액센트 칸은 **`z-index:1` 로 덮개 위**에 올림(페더 blur 3σ 110px 이 기둥 112px 안까지 번져 흐려짐 — 실측) · 🚨 덮개 **한 장이 두 줄을 함께** 덮음(줄별로 나누면 blur 가 위·아래 변에도 먹어 이웃 줄이 미리 드러남, 줄 간격 29px vs blur 3σ 110px) · 구현은 후보 **(a) SVG 지그재그 path + CSS `blur()`** 채택 — (b) 그라데이션 rect + `feTurbulence`/`feDisplacementMap` 은 경계가 **물결로 뭉개져** 톱니·고리가 사라짐(비교 캡처) · 🚨 **지그재그 세로 파장 > blur 반경** 이어야 함(첫 시도 blur 0.16em + 34단계는 파장 34px < 반경 58px 이라 톱니가 통째로 사라짐 → **blur 0.10em + 11단계**로 파장/반경 ≈ 3) · 전진은 **CSS `translateX`**, 꿈틀은 **SMIL `d` 모핑(2.4s)** 로 분리 · `wipe`·`spread`·`wall` 전부 보존 · **3칸 골격 수치 불변**(좌 박스 Δ 1.0 · 우 박스 Δ −1.0 · !/i 중심 Δ 0.0 · 양 끝 Δ 0.0) · 문서 높이 **11,009px 불변** · 가로 스크롤 0 · 콘솔 0 · ⓪-V 🚨 **히어로 진입을 와이프로 교체** — 사용자 "퍼져 나가는 모양이 너무 어색하네. 그라데이션을 주고 배경을 덮은 채로 배경이 왼→오로 사라지게 하는 건 어때?" → **`enter: "wipe"` 가 새 기본값**(지그재그 `spread` · 벽 `wall` 은 코드 보존, 값만 바꾸면 복귀). 2줄 블록 전체를 덮는 **배경색 덮개 한 장**이 `translateX(100%)` 로 빠져나가고 **앞단만 그라데이션** · 🚨 색 그라데이션이 아니라 **마스크로 알파만** 푼다(`transparent → 밴드색` 은 투명 끝이 **투명 검정**이라 navy 팔레트에서 어두운 띠가 생김 — 실측 후 교체, 마스크 방식은 밴드 배경 대비 **최대 편차 1/255**) · 마스크 스톱은 **smoothstep 다단**(2-stop 선형은 램프 끝 기울기 불연속 → 세로 선으로 읽힘, 뒷끝 기울기 1.088→**0.618**) · 페더 **0.63em = 잉크폭의 20%**(0.32em 은 딱딱 · 0.95em 은 페이드로 읽힘) · **1.6s `ease-in-out`** 🚨 house 이징 `cubic-bezier(.22,1,.36,1)` 은 **0.55s 만에 끝나** 1.6s 중 1s 가 죽은 시간이라 탈락(앞단 x 실측 0.15s 574 vs ease-in-out −8) · 스위치 `wipeKeepAccent`(기본 false) 신설 · `prefers-reduced-motion` 이면 덮개 `display:none` · **3칸 골격 수치 전부 불변**(좌 박스 Δ 1.0 · 우 박스 Δ −1.0 · !/i 중심 Δ 0.0 · 양 끝 Δ 0.0) · 문서 높이 **11,009px 불변** · 가로 스크롤 0 · 콘솔 0 · ⓪-U 🚨 **히어로 워드마크 3칸 박스 구조** — 사용자 지시로 각 줄을 `[좌측 박스][액센트 기둥][우측 박스]` 로 재편. **두 줄 font-size 동일**(2026-08-22 의 0.8478 배율 폐기)이라 캡 높이가 같고, 칸 골격이 같아 **잉크 왼끝·오른끝·!/i 중심이 전부 자동 정렬**(2026-08-22 의 "폭 동일 포기"가 해소) · 좌측 박스 = `SUM` 잉크폭 **2.043496em** 에 `STUD` 를 **자간 −0.064em(픽셀 충돌 한계, S↔T·T↔U 여유 5px) + scaleX 0.834942 + weight 860**(가변축 보정 — U 세로획 71×0.835 = **59.3px** vs 기준 **59.0px**)으로 채움 · 우측 박스 = `M` 잉크폭 **0.765em** 에 `O` 를 **scaleX 1.186047 + weight 560**(고리획 48×1.186 = **56.9px** vs M 기둥 **57.0px**) · 액센트 기둥 = `!` advance 0.3071289em, 두 글자를 **잉크 중심 기준** 중앙 정렬 · 세로 계수 1.5111 → **1.6287**, 줄간격 −0.0113em(캡의 **11.2%**) · ⚠️ `studioAlign` 스위치는 **무시됨**(필드 보존) · ⚠️ scaleX 0.9 권장선은 **충돌 없이 달성 불가**라 0.835 채택, 압축한 STUD 는 가로획이·늘린 O 는 세로 곡선이 기준보다 각각 두껍/얇아짐(불가피) · 실측 3해상도 전부 **좌측 박스 Δ ≤ 1.0px · 우측 박스 Δ ≤ 1.0px · !/i 중심 Δ ≤ 0.5px · 양 끝 Δ ≤ 1.0px** · 잉크폭 1536 **75.7%** / 1920 **94.2%** / 390 **94.4%** · 문서 높이 **11,009px 불변** · 가로 스크롤 0 · 콘솔 0 · ⓪-T 🚨 **히어로 2줄 워드마크 + 스프레드 진입** — 메이커리 유사성 지적으로 `SUM!M` 한 줄 → **`SUM!M` / `STUDiO` 2줄**(위 "!" 점 아래 / 아래 소문자 "i" 점 위) · **"+" 마커 4개 삭제**(§7 반려) · 🚨 **"!"와 "i" 잉크 중심 x 일치가 필수**라 "두 줄 폭 동일"은 **포기**(양립 불가 — 아래 줄 배율 = 액센트 중심비 **0.847733**, 실측 중심 Δ **0.00 / −0.50 / 0.25px** @1536·1920·390) · **선언문 3줄 + 메타는 히어로에서 제외**(문구는 `site.ts` 보존, 배치 미확정 · `sr-only h1` 로 접근성 유지)해 워드마크가 **첫 화면을 꽉 채움**(래퍼 595 / 가용 601px = 99%, 1536 은 높이·1920/390 은 폭이 크기를 잡음 — 잉크폭 81.7 / 94.2 / 94.4%) · 진입은 **글자 불변**, **밴드 배경색 덮개**가 "!"·"i" 경계에서 **불규칙 지그재그(고리 3 + 톱니, 하드코딩 의사난수, 1px 페더 `blur(0.6px)`)** 로 걷히고 **씨앗 구멍 2개**가 아주 천천히(0.2s 시작·4.2s 곡선) 먼저 열림 · 덮개 색은 `var(--color-canvas)` 라 팔레트/colorLab 을 따라감(navy 실측 확인) · `prefers-reduced-motion` 이면 덮개 없음 · 스위치 4개 `layout`/`enter`/`spreadSeeds`/`studioAlign` · 문서 높이 **11,009px 불변** · 가로 스크롤 0 · 콘솔 0 · ⓪-S **공간 효율화 3건** — ① 서비스 카드 안 **상/하 → 좌우 2단**(좌 텍스트 / 우 체크 4): 모바일 카드 355→**234px** · 병합 섹션 2,299→**1,814px**, 데스크톱은 두 안 실측 비교 후 **현행 4열 유지**(4열 섹션 1,074 vs 2열 와이드 1,200px — 스위치 `DESKTOP_CARDS`) ② 모바일 리뷰 **스탯을 헤딩 옆**(스탯 컬럼 `w-[104px]` 고정 · 숫자 28px) — 리뷰 섹션 961→**792px**, 이어서 **모바일 전용 상단 마감**(제목 블록 **텍스트 중앙 정렬** + eyebrow `justify-center` · **`items-center`** 세로 중앙 · 스탯·안내 `text-right` · 재조정으로 헤딩 `flex-1` 제거 + **`justify-center`** 하여 [헤딩+스탯]을 **한 덩어리로 중앙** · 최종 구도는 사용자 스케치대로 **스탯 2개도 가로 나란히**(예약제 안내를 2단 아래 한 줄로 빼고 라벨 11.5px·노트 11px 로 축소해 390 폭 확보 — 숫자 28px 유지) · 헤딩은 **문장부호 제거 + 24px**(`MaskLines` 신설 옵션 `punctuationClassName` 으로 줄 끝 부호만 `hidden lg:inline` — `site.ts` 문자열 한 벌 유지) → 좌우 여백 **41/41 대칭** · 섹션 792→**701px** · 마키 위 캡션도 중앙 / 데스크톱 `lg:` 는 전부 예전 값이라 **1536 픽셀 불변**) + 마키 캡션 **실후기 문구 사용자 확정**("실제 업체 대표님들의 소중한 후기입니다." — placeholder 모드는 유지) ③ 예상 예산 범위 **자유 입력 → 셀렉트 5지선다**(`site.ts` `contactBudgetOptions`, 전송 키 `budget` 불변) ④ 🚨 **브릿지 폰 모바일도 2대 V자**(모바일 1차 "1대 채택" 오판 정정) — 모바일·데스크톱 **블록 통합** 후 상자만 축소(350×432 ↔ 405×500, w/h 0.810 동일 · perspective 1200 ↔ 1400)해 **가림 7.1% 동일**, 앞 폰 175.5px(45vw)에서 화면 판독 확인 · 틸트·패럴랙스는 `matchMedia` 로 `lg` 이상 전용 / 문서 세로 390 14,434→**13,832px** · 768 12,234→**11,885px** · 데스크톱 1536 **11,009px 불변** · 가로 스크롤 0 · 콘솔 0 · ⓪-R **모바일 1차** — 기준 390×844: 브릿지 **앞 폰 1대** 모바일 전용 배치 · 하단 고정 바를 **[빠른 상담]+[카카오톡] 2버튼**으로(등장 임계 600px → 뷰포트 1개) · **colorLab 모바일 숨김** · 모달 **세로 스택 55vh + 모바일 캡처 기본** · 히어로 블록 간격 확대 / 390·430·768 **가로 스크롤 0** · 데스크톱 1536 문서 높이 **11,009px 불변** · ⓪-Q **미스트 액센트 자동 파생**(`accentOnMist` + `accentOnMistDeep`) — 검수 7항목 × 20조합 **140칸 전부 4.5 이상**, `.band-deep` 화면 실측 최저 **2.53 → 4.51**(미달 14건 → 0건), team 팔레트는 **픽셀 동일** · 이하 2026-08-21: ⓪-P **단일 HTML v3 재추출** — `스밈스튜디오 시안 2026-08-21 v3.html` 11.53MB, file:// 콘솔 에러 0 · 외부 요청 0 · v2 보존 · ⓪-O **색상 조합 재큐레이션**(색 8→5 · 톤 6→4, 테라레드 × 크림 · 차콜 × 베리 신규 / violet·ink·terra·mono·limeinv·calm·lively 제거) + **온설 목업 최신화**(임베드 08-12 → 08-16 시안, 캡처 3종 `-v2` 재촬영, summary "4페이지"→"7페이지"·overview 슬로건 갱신) · ⓪-N **정정 패스** — 브릿지 폰 **V 확정**(앞 −4.5° / 뒤 +4.5°, 그룹 폭 430→405px 로 조여 **가림 15.1px = 뒤 폰 폭의 7.1%**) + 후기 스탯 **헤딩 옆 가로 배치**(상단 블록 590→244px, 섹션 1,116→770px) · ⓪ 후기 마키 **전폭 블리드**(카드 3.36장 노출) + 카드 세로 확대(440×314) + 브릿지 폰 ∧ 4.5°(**아래 절에서 V 로 뒤집힘**) · ⓪-D 후기 카드 **440px 로 확대**(좌 4 / 우 8 재배분) · ⓪-C 서비스 카드 **칩 → 체크 리스트 + 축 아이콘**(라벨 압축) · ⓪-B 브릿지 폰 **V자 배치**(가림 32%→3.4%) + 병합 섹션 **구도 재편**(좌 헤딩 / 우 박스 없는 약속 리스트, 하단 서비스 4열 카드) · ⓪-A 약속 + Services **한 섹션 병합**(좌 프로미스 5 / 우 서비스 7, 공통 헤딩 1개) + **카드 라운드 전부 제거**(.card-surface 직각 승격) · ⓪-0 카카오톡 채널 확정·연결(하단 상담 바 채움 버튼을 "프로젝트 문의하기"→"카카오톡 상담"으로 교체, 네비와 중복 해소) · ⓪-2 흐름 중간 CTA 2개(브릿지 밴드·Process 끝) 추가 — 팀원 클로드 평가 수용 · ① 포지션 밴드 + 후기 마키를 **한 섹션으로 병합** — 좌 선언·스탯 / 우 컬럼 마키, 라임 한 단계 진하게, 후기 카드 직각, placeholder 자동 전환 라벨 · ② FAQ 아이콘 회전을 **실제로 동작하게** 수정(Tailwind v4 는 `rotate` 를 독립 속성으로 쓴다) · ③ **대형 정리 패스** — 파티클 셸 코드·CSS·§4-B 기록을 `_archive/particle-shell-2026-08-21/` 로 이관하고 `src` 를 원페이지 한 벌로 정리(스위치 7개 삭제 · `globals.css` 2013→1226줄 · 전체 세로 캡처 **픽셀 동일** 확인))

이전 정리: 2026-08-20 (리디자인 6차 — **메이커리 구조 전면 재편**: 모드별 섹션 배열 분기 · 히어로 이미지 제거 · Work 를 2번째로 · 약속/후기 독립 섹션 승격 · Services 메인 복귀(4종 카드) · 포지션 선언 밴드 신설 · CountUp·useMarquee 공용 모듈화)

이전 정리: 2026-08-20 (리디자인 5차 — 포트폴리오 **실제 모바일 뷰 캡처** 3종(`*-mobile.jpg`, 390×844·DSF2·mobile:true, 리빌 강제) · `projects.ts` `mobileImage` 신설 · 브릿지 폰·목업 폰이 `mobileImage ?? fullImage` 사용(목업은 두 모드 공통))

이전 정리: 2026-08-20 (리디자인 4차 — 브릿지 밴드 오른쪽에 **자작 폰 프레임 2대**(아이폰형 다이내믹 아일랜드 + 갤럭시형 펀치홀, 외부 PNG 미사용) · 화면은 실제 포트폴리오 `fullImage` · 겹침 32% · transform 패럴랙스 ±28px · 1024px 미만 폰 숨김)

이전 정리: 2026-08-20 (리디자인 3차 — 수능선배 스타일: 딥 네이비 `#141d33` + 연회청 `#f0f3f8` 밴드 · 하단 고정 상담 바(수치는 `availability` 단일 소스) · 히어로 실사 복귀(좌하단) · 확신 화법 브릿지 밴드(초안) · 카드 언어 통일 16px + 업종 주황 배지 · banded 플로팅 3단 off)

이전 정리: 2026-08-20 (리디자인 1차 + 2차 — visualMode 스위치 · 무채색 밴드 재배정 · 선언 히어로 + 회전 단어 · 약속 카드 · CTA 빠른 상담 2버튼 · 장식 전면 off / 주황 틱 eyebrow · 헤딩 굵기 통일 · Q&A 패널 대비 · Work 한 줄 관점 · Process 주황 · FAQ 중앙 단일 컬럼 + 문항 2건)

이전 정리: 2026-08-19 (파티클 셸 · 섹션별 캔버스 · 라인 모프 · 목업 호버 로고 · 지안 갱신 · rise 재조정 · Q&A 상하 정적 페이드 · 포트폴리오 배경 격자 · DNA 선 리본 · CTA 블루프린트 v4 · WHY SUMIM 스탯 2단)
